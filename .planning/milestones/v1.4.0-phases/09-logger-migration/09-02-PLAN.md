---
phase: 09-logger-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/ManualControlView.swift
  - ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift
  - ValetudoApp/ValetudoApp/Views/TimersView.swift
autonomous: true
requirements:
  - LOG-01
  - LOG-03

must_haves:
  truths:
    - "ManualControlView, RoomsManagementView und TimersView enthalten kein einziges print()-Statement mehr"
    - "Alle drei Views haben eine private let logger = Logger(...) Property mit korrektem subsystem und category"
    - "Alle 17 ursprünglichen print()-Aufrufe sind durch logger.error() mit privacy: .public ersetzt"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/ManualControlView.swift"
      provides: "os.Logger-Integration"
      contains: "private let logger = Logger"
    - path: "ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift"
      provides: "os.Logger-Integration"
      contains: "private let logger = Logger"
    - path: "ValetudoApp/ValetudoApp/Views/TimersView.swift"
      provides: "os.Logger-Integration"
      contains: "private let logger = Logger"
  key_links:
    - from: "RoomsManagementView.swift"
      to: "os.Logger"
      via: "import os + private let logger"
      pattern: "logger\\.error\\("
    - from: "TimersView.swift"
      to: "os.Logger"
      via: "import os + private let logger"
      pattern: "logger\\.error\\("
---

<objective>
print()-Aufrufe in ManualControlView (5), RoomsManagementView (8) und TimersView (4) durch os.Logger ersetzen.

Purpose: Strukturiertes Logging in den verbleibenden View-Dateien; nach diesem Plan sind alle Views vollständig migriert.
Output: 3 View-Dateien mit logger.error()-Calls statt print(), je eine private Logger-Instanz pro View.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Etabliertes Logger-Pattern aus bestehenden ViewModels -->
```swift
import os
private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "TypeName")
```

Privacy-Konventionen:
- Fehlermeldungen: `logger.error("Beschreibung: \(error.localizedDescription, privacy: .public)")`
- Sensible Daten (IPs, Hostnamen): `privacy: .private`
- Debug-Info: `logger.debug("...")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logger-Migration ManualControlView</name>
  <files>
    ValetudoApp/ValetudoApp/Views/ManualControlView.swift
  </files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/ManualControlView.swift (vollständige Datei lesen, dann bearbeiten)
  </read_first>
  <action>
**ManualControlView.swift** (5 print()-Stellen: Zeilen 152, 164, 176, 200, 214):
1. `import os` zur Import-Sektion hinzufügen (nach `import SwiftUI`)
2. Am Anfang der `ManualControlView`-Struct eine Logger-Property einfügen:
   `private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "ManualControlView")`
3. Zeile 152: `print("Failed to check capabilities: \(error)")` → `logger.error("Failed to check capabilities: \(error.localizedDescription, privacy: .public)")`
4. Zeile 164: `print("Failed to enable manual control: \(error)")` → `logger.error("Failed to enable manual control: \(error.localizedDescription, privacy: .public)")`
5. Zeile 176: `print("Failed to disable manual control: \(error)")` → `logger.error("Failed to disable manual control: \(error.localizedDescription, privacy: .public)")`
6. Zeile 200: `print("Movement failed: \(error)")` → `logger.error("Movement failed: \(error.localizedDescription, privacy: .public)")`
7. Zeile 214: `print("Stop failed: \(error)")` → `logger.error("Stop failed: \(error.localizedDescription, privacy: .public)")`
  </action>
  <verify>
    <automated>grep "print(" /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Views/ManualControlView.swift && echo "FAIL: print() still present" || echo "PASS: no print() found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "print(" ValetudoApp/ValetudoApp/Views/ManualControlView.swift` liefert keinen Treffer
    - `grep "private let logger = Logger" ValetudoApp/ValetudoApp/Views/ManualControlView.swift` liefert 1 Treffer mit category: "ManualControlView"
    - `grep "import os" ValetudoApp/ValetudoApp/Views/ManualControlView.swift` liefert 1 Treffer
    - `grep -c "logger.error" ValetudoApp/ValetudoApp/Views/ManualControlView.swift` liefert 5
  </acceptance_criteria>
  <done>ManualControlView: kein print() mehr, 1 Logger-Property, 5 Fehler via logger.error()</done>
</task>

<task type="auto">
  <name>Task 2: Logger-Migration RoomsManagementView und TimersView</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift
    ValetudoApp/ValetudoApp/Views/TimersView.swift
  </files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift (vollständige Datei lesen, dann bearbeiten)
    - ValetudoApp/ValetudoApp/Views/TimersView.swift (vollständige Datei lesen, dann bearbeiten)
  </read_first>
  <action>
**RoomsManagementView.swift** (8 print()-Stellen: Zeilen 185, 189, 203, 215, 228, 247, 261, 498):
1. `import os` zur Import-Sektion hinzufügen
2. Am Anfang der `RoomsManagementView`-Struct eine Logger-Property einfügen:
   `private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "RoomsManagementView")`
3. Zeile 185: `print("Failed to load material properties: \(error)")` → `logger.error("Failed to load material properties: \(error.localizedDescription, privacy: .public)")`
4. Zeile 189: `print("Failed to load capabilities: \(error)")` → `logger.error("Failed to load capabilities: \(error.localizedDescription, privacy: .public)")`
5. Zeile 203: `print("Failed to set segment material: \(error)")` → `logger.error("Failed to set segment material: \(error.localizedDescription, privacy: .public)")`
6. Zeile 215: `print("Failed to load segments: \(error)")` → `logger.error("Failed to load segments: \(error.localizedDescription, privacy: .public)")`
7. Zeile 228: `print("Failed to rename segment: \(error)")` → `logger.error("Failed to rename segment: \(error.localizedDescription, privacy: .public)")`
8. Zeile 247: `print("Failed to join segments: \(error)")` → `logger.error("Failed to join segments: \(error.localizedDescription, privacy: .public)")`
9. Zeile 261: `print("Failed to split segment: \(error)")` → `logger.error("Failed to split segment: \(error.localizedDescription, privacy: .public)")`
10. Zeile 498: `print("Failed to load map: \(error)")` → `logger.error("Failed to load map: \(error.localizedDescription, privacy: .public)")`

**TimersView.swift** (4 print()-Stellen: Zeilen 77, 90, 102, 324):
1. `import os` zur Import-Sektion hinzufügen
2. Am Anfang der `TimersView`-Struct eine Logger-Property einfügen:
   `private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "TimersView")`
3. Zeile 77: `print("Failed to load timers: \(error)")` → `logger.error("Failed to load timers: \(error.localizedDescription, privacy: .public)")`
4. Zeile 90: `print("Failed to toggle timer: \(error)")` → `logger.error("Failed to toggle timer: \(error.localizedDescription, privacy: .public)")`
5. Zeile 102: `print("Failed to delete timer: \(error)")` → `logger.error("Failed to delete timer: \(error.localizedDescription, privacy: .public)")`
6. Zeile 324: `print("Failed to save timer: \(error)")` → `logger.error("Failed to save timer: \(error.localizedDescription, privacy: .public)")`
  </action>
  <verify>
    <automated>grep "print(" /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Views/TimersView.swift && echo "FAIL: print() still present" || echo "PASS: no print() found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "print(" ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift` liefert keinen Treffer
    - `grep "print(" ValetudoApp/ValetudoApp/Views/TimersView.swift` liefert keinen Treffer
    - `grep "private let logger = Logger" ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift` liefert 1 Treffer mit category: "RoomsManagementView"
    - `grep "private let logger = Logger" ValetudoApp/ValetudoApp/Views/TimersView.swift` liefert 1 Treffer mit category: "TimersView"
    - `grep -c "logger.error" ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift` liefert 8
    - `grep -c "logger.error" ValetudoApp/ValetudoApp/Views/TimersView.swift` liefert 4
  </acceptance_criteria>
  <done>RoomsManagementView und TimersView: kein print() mehr, je 1 Logger-Property, alle Fehler via logger.error()</done>
</task>

</tasks>

<verification>
```bash
grep -r "print(" \
  ValetudoApp/ValetudoApp/Views/ManualControlView.swift \
  ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift \
  ValetudoApp/ValetudoApp/Views/TimersView.swift
# Erwartet: keine Ausgabe (0 Treffer)
```
</verification>

<success_criteria>
- Keine print()-Aufrufe in ManualControlView, RoomsManagementView, TimersView
- Jede der 3 Dateien hat `import os` und eine `private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "TypeName")`-Property
- Alle 17 print()-Aufrufe sind durch logger.error(..., privacy: .public) ersetzt
- Projekt kompiliert ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-logger-migration/09-02-SUMMARY.md` erstellen.
</output>
