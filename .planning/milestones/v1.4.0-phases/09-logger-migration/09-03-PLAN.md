---
phase: 09-logger-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/SupportManager.swift
  - ValetudoApp/ValetudoApp/Views/SupportReminderView.swift
autonomous: true
requirements:
  - LOG-02
  - SAFE-03

must_haves:
  truths:
    - "SupportManager.swift enthält kein einziges print()-Statement mehr"
    - "SupportManager hat eine private let logger = Logger(...) Property mit category: SupportManager"
    - "SupportReminderView nutzt Task { try await Task.sleep(for: .seconds(2)) } statt DispatchQueue.main.asyncAfter"
    - "SupportReminderView importiert kein Foundation-DispatchQueue-Dependency mehr als alleinige Notwendigkeit"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/SupportManager.swift"
      provides: "os.Logger-Integration"
      contains: "private let logger = Logger"
    - path: "ValetudoApp/ValetudoApp/Views/SupportReminderView.swift"
      provides: "Structured Concurrency Migration"
      contains: "Task.sleep"
  key_links:
    - from: "SupportManager.swift"
      to: "os.Logger"
      via: "import os + private let logger"
      pattern: "logger\\.error\\("
    - from: "SupportReminderView.swift"
      to: "Task.sleep"
      via: "Task { try await Task.sleep(for:) }"
      pattern: "Task\\.sleep"
---

<objective>
SupportManager.swift: letzten print()-Aufruf durch os.Logger ersetzen. SupportReminderView.swift: DispatchQueue.main.asyncAfter durch Task.sleep (Swift Structured Concurrency) ersetzen.

Purpose: Vollständige Logger-Migration und Concurrency-Bereinigung; nach diesem Plan sind alle Requirements LOG-01, LOG-02, LOG-03, SAFE-03 erfüllt.
Output: SupportManager mit logger.error(), SupportReminderView mit Task.sleep statt DispatchQueue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Etabliertes Logger-Pattern -->
```swift
import os
private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "TypeName")
```

<!-- Task.sleep Replacement Pattern -->
// VORHER (DispatchQueue):
DispatchQueue.main.asyncAfter(deadline: .now() + 2) {
    withAnimation(.spring(duration: 0.4)) {
        showReminder = true
    }
}

// NACHHER (Task.sleep):
Task {
    try await Task.sleep(for: .seconds(2))
    await MainActor.run {
        withAnimation(.spring(duration: 0.4)) {
            showReminder = true
        }
    }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logger-Migration SupportManager</name>
  <files>
    ValetudoApp/ValetudoApp/Services/SupportManager.swift
  </files>
  <read_first>
    - ValetudoApp/ValetudoApp/Services/SupportManager.swift (vollständige Datei lesen, dann bearbeiten)
  </read_first>
  <action>
**SupportManager.swift** (1 print()-Stelle: Zeile 37):
1. `import os` zur Import-Sektion hinzufügen (nach `import StoreKit` oder den vorhandenen Imports)
2. In der `SupportManager`-Klasse (nicht in einer Methode) eine Logger-Property einfügen:
   `private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "SupportManager")`
3. Zeile 37: `print("Failed to load products: \(error)")` ersetzen durch:
   `logger.error("Failed to load products: \(error.localizedDescription, privacy: .public)")`
  </action>
  <verify>
    <automated>grep "print(" /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Services/SupportManager.swift && echo "FAIL: print() still present" || echo "PASS: no print() found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "print(" ValetudoApp/ValetudoApp/Services/SupportManager.swift` liefert keinen Treffer
    - `grep "private let logger = Logger" ValetudoApp/ValetudoApp/Services/SupportManager.swift` liefert 1 Treffer mit category: "SupportManager"
    - `grep "import os" ValetudoApp/ValetudoApp/Services/SupportManager.swift` liefert 1 Treffer
    - `grep "logger.error" ValetudoApp/ValetudoApp/Services/SupportManager.swift` liefert 1 Treffer
  </acceptance_criteria>
  <done>SupportManager: kein print() mehr, 1 Logger-Property, der Fehlerfall via logger.error()</done>
</task>

<task type="auto">
  <name>Task 2: DispatchQueue → Task.sleep in SupportReminderView</name>
  <files>
    ValetudoApp/ValetudoApp/Views/SupportReminderView.swift
  </files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/SupportReminderView.swift (vollständige Datei lesen, dann bearbeiten)
  </read_first>
  <action>
**SupportReminderView.swift** (Zeile 93 — DispatchQueue.main.asyncAfter):

Ersetze den gesamten DispatchQueue-Block (Zeilen 93-97):
```swift
DispatchQueue.main.asyncAfter(deadline: .now() + 2) {
    withAnimation(.spring(duration: 0.4)) {
        showReminder = true
    }
}
```

Durch:
```swift
Task {
    try? await Task.sleep(for: .seconds(2))
    await MainActor.run {
        withAnimation(.spring(duration: 0.4)) {
            showReminder = true
        }
    }
}
```

Hinweis: `try?` (nicht `try`) verwenden, damit ein CancellationError die Animation nicht verhindert. Der `SupportReminderOverlay`-ViewModifier liegt in derselben Datei; sicherstellen dass die Änderung im `onAppear`-Block des Modifiers liegt (nicht im inneren SupportReminderView struct).
  </action>
  <verify>
    <automated>grep "DispatchQueue" /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Views/SupportReminderView.swift && echo "FAIL: DispatchQueue still present" || echo "PASS: no DispatchQueue found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DispatchQueue" ValetudoApp/ValetudoApp/Views/SupportReminderView.swift` liefert keinen Treffer
    - `grep "asyncAfter" ValetudoApp/ValetudoApp/Views/SupportReminderView.swift` liefert keinen Treffer
    - `grep "Task.sleep" ValetudoApp/ValetudoApp/Views/SupportReminderView.swift` liefert 1 Treffer
    - `grep "Task {" ValetudoApp/ValetudoApp/Views/SupportReminderView.swift` liefert 1 Treffer
    - `grep "MainActor.run" ValetudoApp/ValetudoApp/Views/SupportReminderView.swift` liefert 1 Treffer
  </acceptance_criteria>
  <done>SupportReminderView: kein DispatchQueue mehr, Delay via Task.sleep(for: .seconds(2)) mit MainActor.run für UI-Update</done>
</task>

</tasks>

<verification>
```bash
# Kein print() in SupportManager
grep "print(" ValetudoApp/ValetudoApp/Services/SupportManager.swift
# Erwartet: keine Ausgabe

# Kein DispatchQueue in SupportReminderView
grep "DispatchQueue\|asyncAfter" ValetudoApp/ValetudoApp/Views/SupportReminderView.swift
# Erwartet: keine Ausgabe

# Gesamt-Check: kein print() mehr in Views + Services
find ValetudoApp/ValetudoApp/Views ValetudoApp/ValetudoApp/Services -name "*.swift" | xargs grep "print("
# Erwartet: keine Ausgabe
```
</verification>

<success_criteria>
- SupportManager.swift: kein print(), 1 Logger-Property mit category "SupportManager"
- SupportReminderView.swift: kein DispatchQueue/asyncAfter, stattdessen Task { try? await Task.sleep(for: .seconds(2)) }
- find ValetudoApp -name "*.swift" | xargs grep "print(" liefert null Treffer in Views und Services
- Projekt kompiliert ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-logger-migration/09-03-SUMMARY.md` erstellen.
</output>
