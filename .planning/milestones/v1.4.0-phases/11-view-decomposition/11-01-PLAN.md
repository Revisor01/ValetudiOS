---
phase: 11-view-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/MapView.swift
  - ValetudoApp/ValetudoApp/Views/MapMiniMapView.swift
  - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  - ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
  - ValetudoApp/ValetudoApp/Views/MapSheetsView.swift
  - ValetudoApp/project.yml
autonomous: true
requirements:
  - ORG-02

must_haves:
  truths:
    - "MapView.swift kompiliert fehlerfrei nach dem Refactoring"
    - "MiniMapView ist in MapMiniMapView.swift als eigenständige Datei definiert"
    - "InteractiveMapView und MapDrawingHelpers sind in MapInteractiveView.swift"
    - "Control-Bars (Bottom, RoomEdit, GoTo, SavePreset, SplitRoom) sind in MapControlBarsView.swift"
    - "Hilfs-Sheets (MapRenameSheet, SaveGoToPresetSheet, GoToPresetsSheet) sind in MapSheetsView.swift"
    - "MapView.swift bleibt als schlanker Container/Coordinator mit MapTabView, MapPreviewView, MapView, MapContentView"
    - "xcodegen generate registriert alle neuen Dateien im Xcode-Target"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/MapMiniMapView.swift"
      provides: "MiniMapView struct"
      contains: "struct MiniMapView"
    - path: "ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift"
      provides: "InteractiveMapView struct + drawing extension"
      contains: "struct InteractiveMapView"
    - path: "ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift"
      provides: "Alle fünf Control-Bar-Structs und MapControlButton"
      contains: "struct MapControlButton"
    - path: "ValetudoApp/ValetudoApp/Views/MapSheetsView.swift"
      provides: "MapRenameSheet, SaveGoToPresetSheet, GoToPresetsSheet, RoomEditButton"
      contains: "struct MapRenameSheet"
  key_links:
    - from: "MapView.swift MapContentView"
      to: "MapMiniMapView.swift MiniMapView"
      via: "Direct struct reference"
    - from: "MapView.swift MapContentView"
      to: "MapControlBarsView.swift BottomControlBar"
      via: "Direct struct reference"
    - from: "MapView.swift MapContentView"
      to: "MapInteractiveView.swift InteractiveMapView"
      via: "Direct struct reference"
---

<objective>
MapView.swift (2532 Zeilen) in logische Sub-View-Dateien aufteilen.

Purpose: Die Datei ist zu groß für zuverlässige Xcode-Performance und schwer zu navigieren. Die MARK-Kommentare definieren bereits klare Grenzen.
Output: 4 neue Dateien + schlankes MapView.swift (ca. 700 Zeilen als Coordinator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-view-decomposition/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MapView.swift vollständig lesen und Sub-View-Dateien erstellen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapMiniMapView.swift
    ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
    ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
    ValetudoApp/ValetudoApp/Views/MapSheetsView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/MapView.swift (ALLE 2532 Zeilen lesen — zwingend, bevor irgendwas erstellt wird)
  </read_first>
  <action>
    Lies MapView.swift vollständig. Extrahiere dann folgende Blöcke in neue Dateien:

    **MapMiniMapView.swift** — Zeilen 179–465 (MARK: Mini Map View bis kurz vor MARK: Map Content View):
    - `struct MiniMapView` komplett
    - Benötigt: `import SwiftUI`, `import os` (für Logger wenn vorhanden), die Typen `RobotMap`, `VirtualRestrictions`, `Segment`, `MapParams`
    - KEIN `import` dieser Typen nötig — Swift Module teilen sich automatisch

    **MapInteractiveView.swift** — Zeilen 1663–2300 (MARK: Interactive Map View bis kurz vor MARK: Map Control Button):
    - `struct InteractiveMapView` komplett
    - Alle zugehörigen Extensions auf `InteractiveMapView` (MARK: Tap Targets, Segment Info, Map Calculations, Drawing Functions, Zone Drawing)
    - Benötigt: `import SwiftUI`, `import os`

    **MapControlBarsView.swift** — Zeilen 725–1244 (MARK: Bottom Control Bar bis kurz vor MARK: Coordinate Transformation) + Zeilen 2301–2344 (MARK: Map Control Button):
    - `struct BottomControlBar` (MARK: Bottom Control Bar, ~725)
    - `struct RoomEditBar` (MARK: Room Edit Bar, ~932)
    - `struct GoToConfirmBar` (MARK: GoTo Confirm Bar, ~1004)
    - `struct SavePresetConfirmBar` (MARK: Save Preset Confirm Bar, ~1069)
    - `struct SplitRoomBar` (MARK: Split Room Bar, ~1114)
    - `struct MapControlButton` (MARK: Map Control Button, ~2301)
    - Diese Structs referenzieren `MapEditMode`, `MapViewModel` — bleiben in MapView.swift definiert, sind im gleichen Module sichtbar

    **MapSheetsView.swift** — Zeilen 2345–2531:
    - `struct MapRenameSheet` (MARK: Map Rename Sheet, ~2345)
    - `struct RoomEditButton` (MARK: Room Edit Button, ~2390)
    - `struct SaveGoToPresetSheet` (MARK: Save GoTo Preset Sheet, ~2417)
    - `struct GoToPresetsSheet` (MARK: GoTo Presets Sheet, ~2463)
    - Benötigt: `import SwiftUI`, `@Environment(\.dismiss)` etc. — alle bereits vorhanden

    Jede neue Datei beginnt mit `import SwiftUI` (+ `import os` wenn Logger verwendet).
    Keine Datei braucht explizite Imports für andere App-Typen — gleiche Swift-Module.
  </action>
  <verify>
    <automated>
      grep -l "struct MiniMapView" ValetudoApp/ValetudoApp/Views/MapMiniMapView.swift
      grep -l "struct InteractiveMapView" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
      grep -l "struct MapControlButton" ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
      grep -l "struct MapRenameSheet" ValetudoApp/ValetudoApp/Views/MapSheetsView.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - MapMiniMapView.swift existiert und enthält `struct MiniMapView`
    - MapInteractiveView.swift existiert und enthält `struct InteractiveMapView`
    - MapControlBarsView.swift existiert und enthält `struct MapControlButton`, `struct BottomControlBar`
    - MapSheetsView.swift existiert und enthält `struct MapRenameSheet`, `struct GoToPresetsSheet`
    - Jede Datei beginnt mit `import SwiftUI`
  </acceptance_criteria>
  <done>4 neue Dateien erstellt, jede mit den korrekt extrahierten Structs</done>
</task>

<task type="auto">
  <name>Task 2: MapView.swift bereinigen, xcodegen ausführen, Build prüfen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapView.swift
    ValetudoApp/project.yml
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/MapView.swift (erneut lesen — aktuelle Version nach Task 1)
    ValetudoApp/ValetudoApp/Views/MapMiniMapView.swift
    ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
    ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
    ValetudoApp/ValetudoApp/Views/MapSheetsView.swift
    ValetudoApp/project.yml
  </read_first>
  <action>
    1. Aus MapView.swift die extrahierten Blöcke entfernen:
       - MiniMapView (Zeilen ~179–465) entfernen — ist jetzt in MapMiniMapView.swift
       - InteractiveMapView + alle zugehörigen Extensions (~1663–2300) entfernen — ist jetzt in MapInteractiveView.swift
       - BottomControlBar, RoomEditBar, GoToConfirmBar, SavePresetConfirmBar, SplitRoomBar, MapControlButton (~725–1244 + ~2301–2344) entfernen — ist jetzt in MapControlBarsView.swift
       - MapRenameSheet, RoomEditButton, SaveGoToPresetSheet, GoToPresetsSheet (~2345–2531) entfernen — ist jetzt in MapSheetsView.swift

    2. MapView.swift soll danach enthalten:
       - Alle Enums und Structs (MapEditMode, RestrictionType, RestrictionIdentifier, MapParams) — Zeilen 1–37
       - MapTabView (Zeilen ~39–56)
       - MapPreviewView (Zeilen ~59–178)
       - MapContentView (Zeilen ~466–724 + MARK: Coordinate Transformation ~1245–1269 + MARK: Drawing Overlay ~1270–1296 + MARK: Split Line Handles ~1297–1387 + MARK: Restriction Delete Overlay ~1388–1612 + MARK: Gestures ~1613–1637)
       - MapView (Sheet/Modal, Zeilen ~1638–1661)

    3. xcodegen ausführen: `cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodegen generate`

    4. Build prüfen: `cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`

    Falls Build-Fehler auftreten:
    - "use of undeclared type X" → X wurde versehentlich gelöscht; prüfen ob X in korrekter Datei vorhanden ist
    - "cannot find 'BottomControlBar'" → sicherstellen dass in MapControlBarsView.swift die Deklaration korrekt ist (kein `private`)
    - Modifier-Fehler → Extension-Methoden die auf `MapContentView` liegen müssen ggf. in MapView.swift verbleiben
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... build` endet mit `BUILD SUCCEEDED`
    - MapView.swift hat weniger als 800 Zeilen (war 2532)
    - `wc -l ValetudoApp/ValetudoApp/Views/MapView.swift` gibt <= 800 aus
    - Keine Datei im Views-Ordner ist als Duplikat vorhanden (kein Struct doppelt definiert)
  </acceptance_criteria>
  <done>BUILD SUCCEEDED, MapView.swift schlanker Container, alle 4 Sub-View-Dateien kompilieren sauber</done>
</task>

</tasks>

<verification>
- `xcodebuild ... build` → BUILD SUCCEEDED
- `wc -l ValetudoApp/ValetudoApp/Views/MapView.swift` → max. 800 Zeilen
- `grep -rn "struct MiniMapView" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in MapMiniMapView.swift)
- `grep -rn "struct InteractiveMapView" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in MapInteractiveView.swift)
- `grep -rn "struct MapControlButton" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in MapControlBarsView.swift)
</verification>

<success_criteria>
MapView.swift ist ein schlanker Coordinator (<=800 Zeilen). Die vier neuen Dateien enthalten die extrahierten Structs. `xcodebuild` meldet BUILD SUCCEEDED.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/11-view-decomposition/11-01-SUMMARY.md` erstellen.
</output>
