---
phase: 03-api-completeness
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Services/NotificationService.swift
  - ValetudoApp/ValetudoApp/ValetudoApp.swift
autonomous: true
requirements: [API-01, API-02, UX-03]

must_haves:
  truths:
    - "Benutzer sieht Liste vorhandener Map-Snapshots in RobotSettingsView und kann einen wiederherstellen"
    - "Benutzer sieht ausstehende Kartenänderung und kann sie akzeptieren oder ablehnen"
    - "Notification-Actions GO_HOME und LOCATE fuehren die jeweilige Roboter-Aktion aus"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "Map Snapshot Section und Pending Map Change Section"
      contains: "MapSnapshotCapability"
    - path: "ValetudoApp/ValetudoApp/Services/NotificationService.swift"
      provides: "handleNotificationResponse Methode"
      contains: "handleNotificationResponse"
    - path: "ValetudoApp/ValetudoApp/ValetudoApp.swift"
      provides: "AppDelegate mit UNUserNotificationCenterDelegate"
      contains: "UIApplicationDelegateAdaptor"
  key_links:
    - from: "ValetudoApp.swift AppDelegate"
      to: "NotificationService.handleNotificationResponse"
      via: "userNotificationCenter didReceive"
      pattern: "handleNotificationResponse"
    - from: "NotificationService.handleNotificationResponse"
      to: "ValetudoAPI.basicControl / locate"
      via: "RobotManager.getAPI(for:)"
      pattern: "basicControl.*home|locate"
    - from: "RobotSettingsView Map Snapshot Section"
      to: "ValetudoAPI.getMapSnapshots / restoreMapSnapshot"
      via: "Task closure"
      pattern: "getMapSnapshots|restoreMapSnapshot"
---

<objective>
Map-Snapshot-Management und Pending-Map-Change UI in RobotSettingsView einbauen, plus Notification-Actions GO_HOME und LOCATE verdrahten.

Purpose: Benutzer kann Karten verwalten (Snapshots wiederherstellen, aenderungen akzeptieren/ablehnen) und direkt aus Notifications den Roboter steuern.
Output: Neue Sections in RobotSettingsView, funktionierender UNUserNotificationCenterDelegate, NotificationService-Handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-completeness/03-RESEARCH.md
@.planning/phases/03-api-completeness/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — new API methods available: -->
```swift
// ValetudoAPI.swift — new methods
func getMapSnapshots() async throws -> [MapSnapshot]
func restoreMapSnapshot(id: String) async throws
func getPendingMapChange() async throws -> PendingMapChangeState
func handlePendingMapChange(action: String) async throws  // "accept" or "reject"
```

<!-- Model structs from RobotState.swift: -->
```swift
struct MapSnapshot: Codable, Identifiable { let id: String }
struct PendingMapChangeState: Codable { let enabled: Bool }
```

<!-- Existing patterns in RobotSettingsView (capability gating): -->
```swift
// Capability check pattern (line ~449):
let capabilities = try await api.getCapabilities()
hasMappingPass = DebugConfig.showAllCapabilities || capabilities.contains("MappingPassCapability")
// ... (16 capability checks follow this pattern)
```

<!-- RobotSettingsView structure: -->
```swift
// @State vars at top for capability flags (hasMapReset, hasMappingPass, etc.)
// .task {} in body calls loadCapabilities() and loadSettings()
// Sections gated by: if hasXYZ { Section { ... } }
```

<!-- NotificationService existing: -->
```swift
@MainActor class NotificationService: ObservableObject {
    static let shared = NotificationService()
    func setupCategories()  // already registers GO_HOME, LOCATE actions
    // MISSING: handleNotificationResponse — needs to be added
}
```

<!-- RobotManager access pattern: -->
```swift
@MainActor class RobotManager: ObservableObject {
    func getAPI(for id: UUID) -> ValetudoAPI?
    var robots: [RobotConfig]  // first robot fallback per D-07
}
```

<!-- ValetudoApp.swift current: -->
```swift
@main struct ValetudoApp: App {
    @StateObject private var robotManager = RobotManager()
    @StateObject private var errorRouter = ErrorRouter()
    // NO AppDelegate currently
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification-Actions via AppDelegate und NotificationService verdrahten</name>
  <files>ValetudoApp/ValetudoApp/ValetudoApp.swift, ValetudoApp/ValetudoApp/Services/NotificationService.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/ValetudoApp.swift (kompletter Inhalt — @main struct, bestehende StateObjects)
    - ValetudoApp/ValetudoApp/Services/NotificationService.swift (komplett — setupCategories, bestehende Methoden)
    - ValetudoApp/ValetudoApp/Services/RobotManager.swift (getAPI, robots Property, init)
    - .planning/phases/03-api-completeness/03-RESEARCH.md (Pattern 3: UNUserNotificationCenterDelegate, Pitfall 1+2+6)
  </read_first>
  <action>
    **A) ValetudoApp.swift erweitern (per D-05, UX-03):**

    Import `UserNotifications` hinzufuegen.

    Neue `AppDelegate` Klasse VOR dem `@main struct` einfuegen:
    ```swift
    class AppDelegate: NSObject, UIApplicationDelegate, UNUserNotificationCenterDelegate {
        func application(
            _ application: UIApplication,
            didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]? = nil
        ) -> Bool {
            UNUserNotificationCenter.current().delegate = self
            return true
        }

        func userNotificationCenter(
            _ center: UNUserNotificationCenter,
            didReceive response: UNNotificationResponse,
            withCompletionHandler completionHandler: @escaping () -> Void
        ) {
            let actionIdentifier = response.actionIdentifier
            Task { @MainActor in
                await NotificationService.shared.handleNotificationResponse(actionIdentifier: actionIdentifier)
            }
            // WICHTIG: completionHandler sofort aufrufen (Pitfall 6) — Task laeuft im Hintergrund
            completionHandler()
        }
    }
    ```

    Im `@main struct ValetudoApp` das `@UIApplicationDelegateAdaptor` hinzufuegen:
    ```swift
    @UIApplicationDelegateAdaptor(AppDelegate.self) var appDelegate
    ```
    Platzierung: direkt nach der Zeile `@StateObject private var robotManager = RobotManager()`.

    **B) NotificationService.swift erweitern (per D-05, D-06, D-07):**

    Neue MARK-Section `// MARK: - Notification Response Handler` am Ende der Klasse (vor der schliessenden Klammer) hinzufuegen:

    ```swift
    // MARK: - Notification Response Handler
    func handleNotificationResponse(actionIdentifier: String) async {
        // Roboter ermitteln: RobotManager ist @MainActor singleton via .shared pattern
        // NotificationService braucht Zugriff auf RobotManager — da beide @MainActor sind,
        // greifen wir ueber die App-Instanz zu. Pragmatischer Ansatz: RobotManager als
        // static weak reference.
        guard let robotManager = NotificationService.robotManagerRef else {
            logger.warning("No RobotManager reference available for notification action")
            return
        }

        // Per D-07: selectedRobotId oder ersten verfuegbaren Roboter
        let robotId = robotManager.robots.first?.id
        guard let id = robotId, let api = robotManager.getAPI(for: id) else {
            logger.warning("No robot available for notification action \(actionIdentifier, privacy: .public)")
            return
        }

        do {
            switch actionIdentifier {
            case "GO_HOME":
                logger.info("Notification action: sending robot home")
                try await api.basicControl(action: .home)
            case "LOCATE":
                logger.info("Notification action: locating robot")
                try await api.locate()
            default:
                break
            }
        } catch {
            logger.error("Notification action \(actionIdentifier, privacy: .public) failed: \(error.localizedDescription, privacy: .public)")
        }
    }

    // Static weak reference to RobotManager — set from ValetudoApp.swift init
    static weak var robotManagerRef: RobotManager?
    ```

    **C) ValetudoApp.swift — RobotManager-Referenz setzen:**

    Im `ValetudoApp` body-Property (oder in einem `init()`) die Referenz setzen. Da `@StateObject` erst nach init verfuegbar ist, verwende `.onAppear` auf dem aeussersten View:

    Aendere den `WindowGroup`-Block so dass nach dem bestehenden `.withErrorAlert(router: errorRouter)` ein `.onAppear` hinzukommt:
    ```swift
    .onAppear {
        NotificationService.robotManagerRef = robotManager
    }
    ```
    NUR am `if hasCompletedOnboarding` Block (ContentView) — nicht am OnboardingView, da dort noch kein Roboter konfiguriert ist. Aber sicherheitshalber auch am OnboardingView-Block, falls eine Notification waehrend Onboarding eintrifft.

    Pragmatischer: Am besten in BEIDEN Zweigen `.onAppear { NotificationService.robotManagerRef = robotManager }` hinzufuegen.
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "UIApplicationDelegateAdaptor" ValetudoApp/ValetudoApp/ValetudoApp.swift
    - grep -q "UNUserNotificationCenterDelegate" ValetudoApp/ValetudoApp/ValetudoApp.swift
    - grep -q "completionHandler()" ValetudoApp/ValetudoApp/ValetudoApp.swift
    - grep -q "handleNotificationResponse" ValetudoApp/ValetudoApp/Services/NotificationService.swift
    - grep -q "GO_HOME" ValetudoApp/ValetudoApp/Services/NotificationService.swift
    - grep -q "LOCATE" ValetudoApp/ValetudoApp/Services/NotificationService.swift
    - grep -q "robotManagerRef" ValetudoApp/ValetudoApp/Services/NotificationService.swift
    - grep -q "robotManagerRef = robotManager" ValetudoApp/ValetudoApp/ValetudoApp.swift
  </acceptance_criteria>
  <done>AppDelegate registriert sich als UNUserNotificationCenterDelegate, completionHandler wird sofort aufgerufen, NotificationService.handleNotificationResponse dispatcht GO_HOME und LOCATE an ValetudoAPI, RobotManager-Referenz wird bei App-Start gesetzt</done>
</task>

<task type="auto">
  <name>Task 2: Map-Snapshot und Pending-Map-Change Sections in RobotSettingsView</name>
  <files>ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift (komplett lesen — Capability-Check-Pattern in loadCapabilities(), bestehende Sections, @State vars)
    - .planning/phases/03-api-completeness/03-RESEARCH.md (Pitfall 3: kein Snapshot-erstellen Button!)
    - .planning/phases/03-api-completeness/03-01-SUMMARY.md (neue API-Methoden verifizieren)
  </read_first>
  <action>
    Per D-01: Map-Snapshot und Map-Reset Aktionen in RobotSettingsView platzieren.
    Per D-08: UI-Elemente nur anzeigen wenn Capability vorhanden.

    **A) Neue @State Properties hinzufuegen** (bei den anderen has*-Properties oben):
    ```swift
    @State private var hasMapSnapshot = false
    @State private var hasPendingMapChange = false
    @State private var mapSnapshots: [MapSnapshot] = []
    @State private var pendingMapChangeEnabled = false
    @State private var isRestoringSnapshot = false
    @State private var isHandlingMapChange = false
    ```

    **B) Capability-Checks in loadCapabilities()** erweitern (nach den bestehenden capabilities.contains-Zeilen):
    ```swift
    hasMapSnapshot = DebugConfig.showAllCapabilities || capabilities.contains("MapSnapshotCapability")
    hasPendingMapChange = DebugConfig.showAllCapabilities || capabilities.contains("PendingMapChangeHandlingCapability")
    ```

    **C) Neue Section "Map Snapshots"** einfuegen — nach der bestehenden Map-Section (die hasMapReset enthaelt). Platzierung: NACH der "Map" Section, VOR der naechsten Section.

    ```swift
    // MARK: - Map Snapshots
    if hasMapSnapshot {
        Section(header: Text("settings.map_snapshots")) {
            if mapSnapshots.isEmpty {
                Text("settings.no_snapshots")
                    .foregroundStyle(.secondary)
            } else {
                ForEach(mapSnapshots) { snapshot in
                    HStack {
                        VStack(alignment: .leading) {
                            Text(snapshot.id)
                                .font(.footnote)
                                .lineLimit(1)
                        }
                        Spacer()
                        Button {
                            Task {
                                guard let api = robotManager.getAPI(for: robot.id) else { return }
                                isRestoringSnapshot = true
                                do {
                                    try await api.restoreMapSnapshot(id: snapshot.id)
                                } catch {
                                    errorRouter.show(error)
                                }
                                isRestoringSnapshot = false
                            }
                        } label: {
                            if isRestoringSnapshot {
                                ProgressView()
                            } else {
                                Text("settings.restore")
                            }
                        }
                        .disabled(isRestoringSnapshot)
                    }
                }
            }
        }
    }
    ```

    WICHTIG per Pitfall 3: KEIN "Snapshot erstellen" Button — nur Liste + Wiederherstellen. Snapshots werden automatisch von der Firmware erstellt.

    **D) Neue Section "Pending Map Change"** direkt nach Map Snapshots:
    ```swift
    // MARK: - Pending Map Change
    if hasPendingMapChange && pendingMapChangeEnabled {
        Section(header: Text("settings.pending_map_change")) {
            Text("settings.pending_map_change.description")
                .font(.footnote)
                .foregroundStyle(.secondary)
            HStack {
                Button {
                    Task {
                        guard let api = robotManager.getAPI(for: robot.id) else { return }
                        isHandlingMapChange = true
                        do {
                            try await api.handlePendingMapChange(action: "accept")
                            pendingMapChangeEnabled = false
                        } catch {
                            errorRouter.show(error)
                        }
                        isHandlingMapChange = false
                    }
                } label: {
                    Label(String(localized: "settings.accept"), systemImage: "checkmark.circle.fill")
                }
                .disabled(isHandlingMapChange)

                Spacer()

                Button(role: .destructive) {
                    Task {
                        guard let api = robotManager.getAPI(for: robot.id) else { return }
                        isHandlingMapChange = true
                        do {
                            try await api.handlePendingMapChange(action: "reject")
                            pendingMapChangeEnabled = false
                        } catch {
                            errorRouter.show(error)
                        }
                        isHandlingMapChange = false
                    }
                } label: {
                    Label(String(localized: "settings.reject"), systemImage: "xmark.circle.fill")
                }
                .disabled(isHandlingMapChange)
            }
        }
    }
    ```

    **E) Daten laden** — in der bestehenden loadSettings() oder loadCapabilities() Methode, NACH den Capability-Checks:
    ```swift
    // Load map snapshots if capability exists
    if hasMapSnapshot {
        do {
            mapSnapshots = try await api.getMapSnapshots()
        } catch {
            // Silently ignore — not critical
        }
    }

    // Check pending map change
    if hasPendingMapChange {
        do {
            let state = try await api.getPendingMapChange()
            pendingMapChangeEnabled = state.enabled
        } catch {
            // Silently ignore
        }
    }
    ```
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "hasMapSnapshot" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "hasPendingMapChange" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "MapSnapshotCapability" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "PendingMapChangeHandlingCapability" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "restoreMapSnapshot" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "handlePendingMapChange" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "settings.map_snapshots" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    - grep -q "settings.pending_map_change" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  </acceptance_criteria>
  <done>Map-Snapshot-Section zeigt Liste mit Restore-Button (kein Erstellen-Button), Pending-Map-Change-Section zeigt Accept/Reject wenn enabled, beide Sections sind Capability-gated, Build erfolgreich</done>
</task>

</tasks>

<verification>
- Build kompiliert fehlerfrei
- AppDelegate ist als UNUserNotificationCenterDelegate registriert
- NotificationService kann GO_HOME und LOCATE an ValetudoAPI weiterleiten
- RobotSettingsView hat zwei neue Sections fuer Map-Snapshots und Pending Map Change
</verification>

<success_criteria>
- Notification-Actions GO_HOME/LOCATE dispatchen an ValetudoAPI (basicControl(.home) und locate())
- Map-Snapshot Section in RobotSettingsView zeigt Snapshots mit Restore-Button
- Pending Map Change Section zeigt Accept/Reject Buttons
- Alle neuen UI-Elemente sind Capability-gated per D-08
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-completeness/03-02-SUMMARY.md`
</output>
