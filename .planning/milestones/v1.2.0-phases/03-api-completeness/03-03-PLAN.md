---
phase: 03-api-completeness
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
autonomous: true
requirements: [API-03, API-04, UX-04]

must_haves:
  truths:
    - "Benutzer sieht Valetudo Events (DustBinFull, MopReminder, Errors) in einer chronologischen Liste in RobotDetailView"
    - "Benutzer kann die Reinigungsroute (Standard, Schnell, Intensiv, Tief) per Picker auswaehlen"
    - "Benutzer kann Fotos erkannter Hindernisse in einer dedizierten ObstaclePhotoView ansehen"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "Events Section und CleanRoute Picker"
      contains: "CleanRouteControlCapability"
    - path: "ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift"
      provides: "Obstacle-Foto Ansicht mit Lazy-Loading"
      contains: "getObstacleImage"
  key_links:
    - from: "RobotDetailView Events Section"
      to: "ValetudoAPI.getEvents()"
      via: "Task in .task{}"
      pattern: "getEvents"
    - from: "RobotDetailView CleanRoute Picker"
      to: "ValetudoAPI.setCleanRoute(route:)"
      via: "onChange handler"
      pattern: "setCleanRoute"
    - from: "ObstaclePhotoView"
      to: "ValetudoAPI.getObstacleImage(id:)"
      via: "task(id:) modifier"
      pattern: "getObstacleImage"
---

<objective>
Events-Ansicht, Reinigungsrouten-Picker und Obstacle-Foto-View in die App einbauen.

Purpose: Benutzer erhaelt Einblick in Roboter-Events, kann Reinigungsmodus waehlen und Hindernisfotos ansehen — die letzten drei API-Capabilities fuer Phase 3.
Output: Neue Sections in RobotDetailView (Events + CleanRoute), neue ObstaclePhotoView.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-completeness/03-RESEARCH.md
@.planning/phases/03-api-completeness/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — new API methods: -->
```swift
// ValetudoAPI.swift
func getEvents() async throws -> [ValetudoEvent]
func getCleanRoute() async throws -> CleanRouteState
func setCleanRoute(route: String) async throws
func getObstacleImage(id: String) async throws -> Data
```

<!-- Model structs from RobotState.swift: -->
```swift
struct ValetudoEvent: Codable, Identifiable {
    let __class: String
    let id: String
    let timestamp: String
    let processed: Bool
    let type: String?
    let subType: String?
    let message: String?
    var displayName: String { ... }  // computed, localized
    var iconName: String { ... }     // computed, SF Symbol
}

struct CleanRouteState: Codable {
    let route: String  // "normal", "quick", "intensive", "deep"
}
```

<!-- From RobotMap.swift — EntityMetaData (extended in Plan 01): -->
```swift
struct EntityMetaData: Codable {
    let angle: Int?
    let id: String?       // UUID fuer ObstacleImagesCapability/img/{id}
    let label: String?    // z.B. "Pedestal (89%)"
}

struct MapEntity: Codable {
    let __class: String?
    let type: String?
    let points: [Int]?
    let metaData: EntityMetaData?
}
```

<!-- Existing RobotDetailView patterns: -->
```swift
// Capability check (line ~1222):
let capabilities = try await api.getCapabilities()
hasManualControl = DebugConfig.showAllCapabilities || capabilities.contains("...")

// Intensity Picker pattern (line ~502):
// Picker("label", selection: $binding) { ForEach(presets, id: \.self) { ... } }
// onChange calls async API method

// @State properties at top for capability flags and data
// .task {} calls multiple load functions
// api obtained via: robotManager.getAPI(for: robot.id)
```

<!-- Capabilities type: -->
```swift
typealias Capabilities = [String]
// "CleanRouteControlCapability", "ObstacleImagesCapability"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Events-Section und CleanRoute-Picker in RobotDetailView</name>
  <files>ValetudoApp/ValetudoApp/Views/RobotDetailView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (Zeilen 1-30 fuer @State vars, Zeilen 500-600 fuer Intensity Picker Pattern, Zeilen 1100-1230 fuer loadData/loadCapabilities)
    - .planning/phases/03-api-completeness/03-RESEARCH.md (Pattern 2: CleanRoute Display-Namen, Event-Typen Tabelle)
    - .planning/phases/03-api-completeness/03-01-SUMMARY.md
  </read_first>
  <action>
    Per D-02 (CleanRoute Picker), D-04 (Events Section), D-08 (Capability-gating), D-10 (Graceful Degradation).

    **A) Neue @State Properties** bei den bestehenden @State vars oben hinzufuegen:
    ```swift
    // Clean Route
    @State private var hasCleanRoute = false
    @State private var currentCleanRoute = ""
    @State private var cleanRouteOptions: [String] = ["normal", "quick", "intensive", "deep"]

    // Events
    @State private var events: [ValetudoEvent] = []
    @State private var hasObstacleImages = false
    ```

    **B) Capability-Checks erweitern** in der bestehenden loadCapabilities()-Methode (nach den vorhandenen capabilities.contains-Zeilen):
    ```swift
    hasCleanRoute = DebugConfig.showAllCapabilities || capabilities.contains("CleanRouteControlCapability")
    hasObstacleImages = DebugConfig.showAllCapabilities || capabilities.contains("ObstacleImagesCapability")
    ```

    **C) CleanRoute Picker** — per D-02 analog zum bestehenden Fan-Speed/Water-Usage Picker einfuegen. Platzierung: NACH den Intensity Controls Section, VOR der naechsten Section.

    ```swift
    // MARK: - Clean Route
    if hasCleanRoute {
        Section {
            Picker(String(localized: "detail.clean_route"), selection: Binding(
                get: { currentCleanRoute },
                set: { newValue in
                    currentCleanRoute = newValue
                    Task {
                        guard let api = robotManager.getAPI(for: robot.id) else { return }
                        do {
                            try await api.setCleanRoute(route: newValue)
                        } catch {
                            errorRouter.show(error)
                        }
                    }
                }
            )) {
                Text(String(localized: "cleanroute.normal")).tag("normal")
                Text(String(localized: "cleanroute.quick")).tag("quick")
                Text(String(localized: "cleanroute.intensive")).tag("intensive")
                Text(String(localized: "cleanroute.deep")).tag("deep")
            }
        }
    }
    ```

    **D) Events Section** — per D-04 als neue Section in RobotDetailView. Platzierung: nach den Controls, idealerweise als eine der letzten Sections vor den Info/Update Sections.

    ```swift
    // MARK: - Events
    if !events.isEmpty {
        Section(header: Text("detail.events")) {
            ForEach(events.prefix(10)) { event in
                HStack {
                    Image(systemName: event.iconName)
                        .foregroundStyle(event.__class.contains("Error") ? .red : .secondary)
                        .frame(width: 24)
                    VStack(alignment: .leading, spacing: 2) {
                        Text(event.displayName)
                            .font(.subheadline)
                        if let message = event.message {
                            Text(message)
                                .font(.caption)
                                .foregroundStyle(.secondary)
                        }
                        Text(event.timestamp)
                            .font(.caption2)
                            .foregroundStyle(.tertiary)
                    }
                    Spacer()
                    if !event.processed {
                        Circle()
                            .fill(.blue)
                            .frame(width: 8, height: 8)
                    }
                }
            }
        }
    }
    ```

    HINWEIS per D-10: Obstacle-Foto-Links werden hier NICHT eingebaut — die ObstaclePhotoView (Task 2) wird separat via Map-Entities erreicht, nicht via Events-Liste. Events zeigen nur Text + Icon.

    **E) Daten laden** — in der bestehenden Load-Kette (z.B. loadData oder als eigene Funktion):

    CleanRoute laden (in loadCapabilities oder separater Funktion NACH Capability-Check):
    ```swift
    if hasCleanRoute {
        do {
            let routeState = try await api.getCleanRoute()
            currentCleanRoute = routeState.route
        } catch {
            // Silently ignore — robot may not support this
        }
    }
    ```

    Events laden (als eigene `loadEvents()` Funktion, aufgerufen in .task{}):
    ```swift
    private func loadEvents() async {
        guard let api = robotManager.getAPI(for: robot.id) else { return }
        do {
            events = try await api.getEvents()
        } catch {
            // Silently ignore — events endpoint may not be available
        }
    }
    ```
    Diese Funktion in der .task{} Kette aufrufen (parallel zu den anderen Loads).
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "hasCleanRoute" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "CleanRouteControlCapability" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "setCleanRoute" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "detail.clean_route" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "detail.events" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "getEvents" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "event.displayName" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "event.iconName" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    - grep -q "hasObstacleImages" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  </acceptance_criteria>
  <done>CleanRoute-Picker mit 4 Optionen (normal/quick/intensive/deep) capability-gated angezeigt, Events-Section zeigt bis zu 10 Events chronologisch mit Icon und displayName, unprocessed Events haben blauen Dot, Build erfolgreich</done>
</task>

<task type="auto">
  <name>Task 2: ObstaclePhotoView als neue View-Datei</name>
  <files>ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Models/RobotMap.swift (MapEntity, EntityMetaData mit id/label nach Plan-01 Erweiterung)
    - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift (getObstacleImage Methode aus Plan 01)
    - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (Zeilen 1-5 fuer Import-Pattern, @EnvironmentObject Pattern)
    - .planning/phases/03-api-completeness/03-RESEARCH.md (Pattern 4: Obstacle-Bild binary fetch, Pitfall 5: Rate-Limit)
  </read_first>
  <action>
    Per D-03: Neue ObstaclePhotoView.swift erstellen. Accessible via NavigationLink (z.B. aus MapView oder als Sheet).

    Neue Datei `ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift` erstellen:

    ```swift
    import SwiftUI
    import os

    struct ObstaclePhotoView: View {
        let obstacleId: String
        let label: String?
        let api: ValetudoAPI

        @State private var imageData: Data?
        @State private var isLoading = true
        @State private var loadError = false

        private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "com.valetudio", category: "ObstaclePhoto")

        var body: some View {
            VStack {
                if isLoading {
                    ProgressView()
                        .frame(maxWidth: .infinity, maxHeight: .infinity)
                } else if let imageData, let uiImage = UIImage(data: imageData) {
                    Image(uiImage: uiImage)
                        .resizable()
                        .aspectRatio(contentMode: .fit)
                        .clipShape(RoundedRectangle(cornerRadius: 12))
                        .padding()
                } else {
                    ContentUnavailableView(
                        String(localized: "obstacle.no_image"),
                        systemImage: "photo.badge.exclamationmark",
                        description: Text("obstacle.no_image.description")
                    )
                }
            }
            .navigationTitle(label ?? String(localized: "obstacle.photo"))
            .navigationBarTitleDisplayMode(.inline)
            .task(id: obstacleId) {
                // Lazy-Loading per D-03 — nur laden wenn View erscheint (Pitfall 5: Rate-Limit)
                isLoading = true
                loadError = false
                do {
                    imageData = try await api.getObstacleImage(id: obstacleId)
                } catch {
                    logger.error("Failed to load obstacle image \(obstacleId, privacy: .public): \(error.localizedDescription, privacy: .public)")
                    loadError = true
                }
                isLoading = false
            }
        }
    }
    ```

    WICHTIG: Diese View wird als NavigationLink-Destination verwendet. Der Aufrufer (z.B. eine zukuenftige Obstacles-Liste in MapView oder Events) uebergibt `obstacleId`, `label` und `api`.

    Fuer die Xcode-Projektdatei: Die Datei muss manuell zum Xcode-Projekt hinzugefuegt werden. Da das Projekt kein `project.yml` (XcodeGen) verwendet sondern eine `.xcodeproj` mit `project.pbxproj`, muss der Executor die Datei im Xcode-Projekt registrieren.

    Pragmatischer Ansatz: Die Datei physisch erstellen und dann `xcodebuild` testen — bei modernen Xcode-Versionen mit dem richtigen Target-Membership wird die Datei automatisch erkannt wenn sie im richtigen Verzeichnis liegt. Falls nicht: pbxproj manuell aktualisieren oder dem User mitteilen.

    ZUSAETZLICH: In RobotDetailView eine NavigationLink-Integration fuer Obstacles einbauen. In der Events-Section oder als eigene Section:

    Wenn `hasObstacleImages` true ist und Map-Entities vom Typ "obstacle" existieren, zeige eine "Hindernisse" Section. Da die Map-Daten nicht in RobotDetailView geladen werden (nur in MapView), ist der pragmatischste Ansatz: Obstacle-Fotos sind via MapView erreichbar (Map-Entities mit type "obstacle" anklicken). Fuer RobotDetailView reicht es, in der Events-Liste auf Events zu reagieren die obstacle-bezogen sind.

    Minimale Integration in RobotDetailView: KEINE extra Obstacles-Section noetig. Die ObstaclePhotoView wird primaer aus MapView heraus aufgerufen (dort sind die Obstacle-Entities mit Koordinaten verfuegbar). Eine volle MapView-Integration ist fuer Phase 3 nicht erforderlich — die View existiert und ist nutzbar.
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test -f ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
    - grep -q "struct ObstaclePhotoView: View" ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
    - grep -q "getObstacleImage" ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
    - grep -q "obstacleId" ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
    - grep -q "UIImage(data:" ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
    - grep -q 'task(id: obstacleId)' ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
    - grep -q "obstacle.no_image" ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
  </acceptance_criteria>
  <done>ObstaclePhotoView.swift existiert als eigenstaendige View mit Lazy-Loading (task(id:) statt sofortigem Laden), zeigt UIImage aus binary Data, hat Loading- und Error-States, nutzt os.Logger, Build erfolgreich</done>
</task>

</tasks>

<verification>
- Build kompiliert fehlerfrei
- RobotDetailView hat Events Section und CleanRoute Picker
- ObstaclePhotoView.swift existiert mit Lazy-Loading
- Alle neuen UI-Elemente sind Capability-gated
</verification>

<success_criteria>
- Events-Section in RobotDetailView zeigt bis zu 10 Events mit Icons und Timestamps
- CleanRoute-Picker mit 4 Optionen (normal/quick/intensive/deep) funktioniert
- ObstaclePhotoView laedt Bilder lazy per task(id:) Modifier
- Alle neuen Features per Capability-Check ein-/ausgeblendet
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-completeness/03-03-SUMMARY.md`
</output>
