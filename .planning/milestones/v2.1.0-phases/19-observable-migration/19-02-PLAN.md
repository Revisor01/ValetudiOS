---
phase: 19-observable-migration
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - ValetudoApp/ValetudoApp/ValetudoApp.swift
  - ValetudoApp/ValetudoApp/ContentView.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
  - ValetudoApp/ValetudoApp/Views/MapView.swift
  - ValetudoApp/ValetudoApp/Views/MapSheetsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  - ValetudoApp/ValetudoApp/Views/SettingsView.swift
  - ValetudoApp/ValetudoApp/Views/SupportView.swift
  - ValetudoApp/ValetudoApp/Views/AddRobotView.swift
  - ValetudoApp/ValetudoApp/Views/RobotListView.swift
  - ValetudoApp/ValetudoApp/Views/DoNotDisturbView.swift
  - ValetudoApp/ValetudoApp/Views/StatisticsView.swift
  - ValetudoApp/ValetudoApp/Views/IntensityControlView.swift
  - ValetudoApp/ValetudoApp/Views/ManualControlView.swift
  - ValetudoApp/ValetudoApp/Views/ConsumablesView.swift
  - ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift
  - ValetudoApp/ValetudoApp/Views/TimersView.swift
autonomous: true
requirements: [OBS-04]

must_haves:
  truths:
    - "Alle @StateObject sind durch @State ersetzt"
    - "Alle @ObservedObject sind durch plain var ersetzt"
    - "Alle @EnvironmentObject sind durch @Environment(Type.self) ersetzt"
    - "Alle .environmentObject() sind durch .environment() ersetzt"
    - "Das Projekt kompiliert fehlerfrei"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ValetudoApp.swift"
      provides: "@State + .environment() Injection"
      contains: ".environment(robotManager)"
    - path: "ValetudoApp/ValetudoApp/ContentView.swift"
      provides: "@Environment consumption"
      contains: "@Environment(RobotManager.self)"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "@State viewModel"
      contains: "@State private var viewModel"
  key_links:
    - from: "ValetudoApp/ValetudoApp/ValetudoApp.swift"
      to: "ContentView"
      via: ".environment() injection"
      pattern: "\\.environment\\(robotManager\\)"
    - from: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      to: "RobotDetailViewModel"
      via: "@State + State(initialValue:)"
      pattern: "State\\(initialValue:"
    - from: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      to: "MapViewModel"
      via: "@State + State(initialValue:)"
      pattern: "State\\(initialValue:"
---

<objective>
Alle View-Call-Sites migrieren: @StateObject zu @State, @ObservedObject zu plain var, @EnvironmentObject zu @Environment, .environmentObject() zu .environment(). Build-Verifikation.

Purpose: Abschluss der @Observable-Migration — Views nutzen die neuen Property Wrappers passend zum @Observable Macro.
Output: Alle 19 View-Dateien mit aktualisierten Property Wrappers, kompilierbares Projekt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-observable-migration/19-RESEARCH.md
@.planning/phases/19-observable-migration/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-Einstiegspunkt und Kern-Views migrieren (ValetudoApp, ContentView, RobotDetailView, MapView, RobotSettingsView)</name>
  <files>
    ValetudoApp/ValetudoApp/ValetudoApp.swift,
    ValetudoApp/ValetudoApp/ContentView.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift,
    ValetudoApp/ValetudoApp/Views/MapSheetsView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/ValetudoApp.swift,
    ValetudoApp/ValetudoApp/ContentView.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift,
    ValetudoApp/ValetudoApp/Views/MapSheetsView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  </read_first>
  <action>
    Migriere die Kern-View-Dateien mit den kritischen Injection- und Consumption-Sites:

    **ValetudoApp.swift** (Injection-Root):
    - Zeile 44: `@StateObject private var robotManager = RobotManager()` aendern zu `@State private var robotManager = RobotManager()`
    - Zeile 45: `@StateObject private var errorRouter = ErrorRouter()` aendern zu `@State private var errorRouter = ErrorRouter()`
    - Zeile 52: `.environmentObject(robotManager)` aendern zu `.environment(robotManager)`
    - Zeile 53: `.environmentObject(errorRouter)` aendern zu `.environment(errorRouter)`
    - Zeile 60: `.environmentObject(robotManager)` aendern zu `.environment(robotManager)`
    - Zeile 61: `.environmentObject(errorRouter)` aendern zu `.environment(errorRouter)`

    **ContentView.swift**:
    - Zeile 4: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - Zeile 5: `@EnvironmentObject var errorRouter: ErrorRouter` aendern zu `@Environment(ErrorRouter.self) var errorRouter`
    - #Preview (Zeile ~52-53): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())` und `.environmentObject(ErrorRouter())` aendern zu `.environment(ErrorRouter())`

    **RobotDetailView.swift**:
    - Zeile 4: `@StateObject private var viewModel: RobotDetailViewModel` aendern zu `@State private var viewModel: RobotDetailViewModel`
    - Zeile 20: `_viewModel = StateObject(wrappedValue: RobotDetailViewModel(...))` aendern zu `_viewModel = State(initialValue: RobotDetailViewModel(...))`

    **RobotDetailSections.swift**:
    - Zeile 105: `@ObservedObject var viewModel: RobotDetailViewModel` aendern zu `var viewModel: RobotDetailViewModel`

    **MapView.swift**:
    - Zeile 40: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (MiniMapView)
    - Zeile 60: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (InteractiveMapView)
    - Zeile 181: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (MapContentView)
    - Zeile 185: `@StateObject var viewModel: MapViewModel` aendern zu `@State var viewModel: MapViewModel`
    - Zeile 205: `_viewModel = StateObject(wrappedValue: MapViewModel(...))` aendern zu `_viewModel = State(initialValue: MapViewModel(...))`
    - Zeile 853: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (MapControlBarsView)
    - Zeile ~877: `.environmentObject(RobotManager())` in #Preview aendern zu `.environment(RobotManager())`

    **MapSheetsView.swift**:
    - Zeile 97: `@ObservedObject var presetStore: GoToPresetStore` aendern zu `var presetStore: GoToPresetStore` (GoToPresetsSheet)

    **RobotSettingsView.swift**:
    - Zeile 9: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - Zeile 10: `@StateObject private var viewModel: RobotSettingsViewModel` aendern zu `@State private var viewModel: RobotSettingsViewModel`
    - Init: `_viewModel = StateObject(wrappedValue: RobotSettingsViewModel(...))` aendern zu `_viewModel = State(initialValue: RobotSettingsViewModel(...))`
    - #Preview (Zeile ~492): `.environmentObject(robotManager)` aendern zu `.environment(robotManager)`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "@StateObject\|@ObservedObject\|@EnvironmentObject\|\.environmentObject(" ValetudoApp/ValetudoApp/ValetudoApp.swift ValetudoApp/ValetudoApp/ContentView.swift ValetudoApp/ValetudoApp/Views/RobotDetailView.swift ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift ValetudoApp/ValetudoApp/Views/MapView.swift ValetudoApp/ValetudoApp/Views/MapSheetsView.swift ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep "@StateObject" ValetudoApp.swift liefert 0 Treffer
    - grep ".environmentObject(" ValetudoApp.swift liefert 0 Treffer
    - grep ".environment(robotManager)" ValetudoApp.swift liefert mindestens 2 Treffer
    - grep "@Environment(RobotManager.self)" ContentView.swift liefert 1 Treffer
    - grep "@Environment(ErrorRouter.self)" ContentView.swift liefert 1 Treffer
    - grep "@State private var viewModel" RobotDetailView.swift liefert 1 Treffer
    - grep "State(initialValue:" RobotDetailView.swift liefert 1 Treffer
    - grep "@ObservedObject" RobotDetailSections.swift liefert 0 Treffer
    - grep "@State var viewModel" MapView.swift liefert 1 Treffer
    - grep "State(initialValue:" MapView.swift liefert 1 Treffer
    - grep "@EnvironmentObject" MapView.swift liefert 0 Treffer
    - grep "@ObservedObject" MapSheetsView.swift liefert 0 Treffer
    - grep "@State private var viewModel" RobotSettingsView.swift liefert 1 Treffer
  </acceptance_criteria>
  <done>7 Kern-View-Dateien migriert: @StateObject zu @State, @ObservedObject zu plain var, @EnvironmentObject zu @Environment, .environmentObject() zu .environment()</done>
</task>

<task type="auto">
  <name>Task 2: Restliche Views migrieren und Build verifizieren</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift,
    ValetudoApp/ValetudoApp/Views/SettingsView.swift,
    ValetudoApp/ValetudoApp/Views/SupportView.swift,
    ValetudoApp/ValetudoApp/Views/AddRobotView.swift,
    ValetudoApp/ValetudoApp/Views/RobotListView.swift,
    ValetudoApp/ValetudoApp/Views/DoNotDisturbView.swift,
    ValetudoApp/ValetudoApp/Views/StatisticsView.swift,
    ValetudoApp/ValetudoApp/Views/IntensityControlView.swift,
    ValetudoApp/ValetudoApp/Views/ManualControlView.swift,
    ValetudoApp/ValetudoApp/Views/ConsumablesView.swift,
    ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift,
    ValetudoApp/ValetudoApp/Views/TimersView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift,
    ValetudoApp/ValetudoApp/Views/SettingsView.swift,
    ValetudoApp/ValetudoApp/Views/SupportView.swift,
    ValetudoApp/ValetudoApp/Views/AddRobotView.swift,
    ValetudoApp/ValetudoApp/Views/RobotListView.swift,
    ValetudoApp/ValetudoApp/Views/DoNotDisturbView.swift,
    ValetudoApp/ValetudoApp/Views/StatisticsView.swift,
    ValetudoApp/ValetudoApp/Views/IntensityControlView.swift,
    ValetudoApp/ValetudoApp/Views/ManualControlView.swift,
    ValetudoApp/ValetudoApp/Views/ConsumablesView.swift,
    ValetudoApp/ValetudoApp/Views/RoomsManagementView.swift,
    ValetudoApp/ValetudoApp/Views/TimersView.swift
  </read_first>
  <action>
    Migriere alle verbleibenden View-Dateien und fuehre einen Build durch:

    **RobotSettingsSections.swift** (6 @EnvironmentObject-Stellen):
    - Zeile 9: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (VolumeSection)
    - Zeile 86: dasselbe (CarpetModeSection)
    - Zeile 197: dasselbe (ObstacleAvoidanceSection)
    - Zeile 413: dasselbe (QuirksSection)
    - Zeile 589: dasselbe (WifiSection)
    - Zeile 776: dasselbe (MapSnapshotSection)

    **SettingsView.swift**:
    - Zeile 4: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - Zeile 5: `@ObservedObject var notificationService = NotificationService.shared` aendern zu `var notificationService = NotificationService.shared`
    - Zeile 177: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (CreditsView)
    - #Preview (Zeile ~393): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **SupportView.swift**:
    - Zeile 5: `@ObservedObject private var supportManager = SupportManager.shared` aendern zu `private var supportManager = SupportManager.shared`

    **AddRobotView.swift**:
    - Zeile 4: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - Zeile 6: `@StateObject private var scanner = NetworkScanner()` aendern zu `@State private var scanner = NetworkScanner()`
    - Zeile 160: `@ObservedObject var scanner: NetworkScanner` aendern zu `var scanner: NetworkScanner` (ScanResultsList)
    - #Preview (Zeile ~301): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **RobotListView.swift**:
    - Zeile 4: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - #Preview (Zeile ~195): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **DoNotDisturbView.swift**:
    - Zeile 7: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - #Preview (Zeile ~160): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **StatisticsView.swift**:
    - Zeile 7: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - #Preview (Zeile ~147): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **IntensityControlView.swift**:
    - Zeile 7: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - #Preview (Zeile ~225): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **ManualControlView.swift**:
    - Zeile 7: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - #Preview (Zeile ~224): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **ConsumablesView.swift**:
    - Zeile 7: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - #Preview (Zeile ~157): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **RoomsManagementView.swift**:
    - Zeile 7: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - Zeile 387: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (RoomDetailView)
    - #Preview (Zeile ~723): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **TimersView.swift**:
    - Zeile 6: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager`
    - Zeile 169: `@EnvironmentObject var robotManager: RobotManager` aendern zu `@Environment(RobotManager.self) var robotManager` (AddTimerView)
    - #Preview (Zeile ~336): `.environmentObject(RobotManager())` aendern zu `.environment(RobotManager())`

    **Build-Verifikation:**
    Nach allen Aenderungen: `xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5`
    Falls Build-Fehler: beheben (haeufigste Ursache: vergessene .environmentObject-Stelle oder StateObject-Init).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -rn "@StateObject\|@ObservedObject\|@EnvironmentObject\|\.environmentObject(" --include="*.swift" ValetudoApp/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "@StateObject" --include="*.swift" ValetudoApp/ liefert 0 Treffer
    - grep -rn "@ObservedObject" --include="*.swift" ValetudoApp/ liefert 0 Treffer
    - grep -rn "@EnvironmentObject" --include="*.swift" ValetudoApp/ liefert 0 Treffer
    - grep -rn "\.environmentObject(" --include="*.swift" ValetudoApp/ liefert 0 Treffer
    - grep -rn "@Environment(RobotManager.self)" --include="*.swift" ValetudoApp/ liefert mindestens 15 Treffer
    - grep -rn "\.environment(robotManager)" --include="*.swift" ValetudoApp/ liefert mindestens 2 Treffer (ValetudoApp.swift)
    - xcodebuild build kompiliert fehlerfrei (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>Alle View-Dateien migriert, kein @StateObject/@ObservedObject/@EnvironmentObject/.environmentObject() verbleibt im gesamten Projekt, Build erfolgreich</done>
</task>

</tasks>

<verification>
Vollstaendige Codebase-Verifikation nach beiden Plans:
- `grep -rn "ObservableObject" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@Published" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@StateObject" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@ObservedObject" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@EnvironmentObject" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "\.environmentObject(" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@Observable" --include="*.swift" ValetudoApp/` liefert 11 Treffer
- Xcode Build erfolgreich
</verification>

<success_criteria>
Kein Legacy-Observable-Pattern verbleibt im Projekt. Alle Views nutzen @State/@Environment/plain var. Build kompiliert. App funktioniert identisch.
</success_criteria>

<output>
After completion, create `.planning/phases/19-observable-migration/19-02-SUMMARY.md`
</output>
