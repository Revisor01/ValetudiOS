---
phase: 17-background-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Info.plist
  - ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift
autonomous: true
requirements: [BG-01, BG-02, BG-03]
must_haves:
  truths:
    - "BackgroundMonitorService existiert als Singleton mit handleBackgroundRefresh() und scheduleBackgroundRefresh()"
    - "Info.plist enthaelt BGTaskSchedulerPermittedIdentifiers mit dem korrekten Task-Identifier"
    - "State-Vergleich erkennt Reinigungsende (cleaning->docked/idle), Stuck-Zustand und Error-Zustand"
    - "Letzter Status pro Roboter wird in UserDefaults persistiert und beim naechsten Check verglichen"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift"
      provides: "BGTask-Handler, State-Persistenz, Notification-Dispatch"
      exports: ["BackgroundMonitorService"]
      min_lines: 80
    - path: "ValetudoApp/ValetudoApp/Info.plist"
      provides: "BGTaskSchedulerPermittedIdentifiers Eintrag"
      contains: "BGTaskSchedulerPermittedIdentifiers"
  key_links:
    - from: "BackgroundMonitorService"
      to: "ValetudoAPI"
      via: "ValetudoAPI(config:) Instanziierung pro Roboter"
      pattern: "ValetudoAPI\\(config:"
    - from: "BackgroundMonitorService"
      to: "NotificationService"
      via: "Task { @MainActor in NotificationService.shared.notify* }"
      pattern: "NotificationService\\.shared\\.notify"
    - from: "BackgroundMonitorService"
      to: "UserDefaults"
      via: "bg_last_status_ Key pro Roboter-ID"
      pattern: "bg_last_status_"
---

<objective>
BackgroundMonitorService erstellen und Info.plist fuer BGAppRefreshTask vorbereiten.

Purpose: Die Kern-Infrastruktur fuer Hintergrund-Status-Pruefung und Notification-Versand bei geschlossener App schaffen.
Output: BackgroundMonitorService.swift (Singleton mit BGTask-Handler, State-Persistenz, Notification-Dispatch) + Info.plist mit BGTaskSchedulerPermittedIdentifiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-background-monitoring/17-RESEARCH.md

<interfaces>
<!-- Bestehende Interfaces die der Executor braucht -->

From ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift:
```swift
actor ValetudoAPI {
    init(config: RobotConfig)
    func getAttributes() async throws -> [RobotAttribute]
}
```

From ValetudoApp/ValetudoApp/Models/RobotConfig.swift:
```swift
struct RobotConfig: Codable, Identifiable, Equatable, Hashable {
    let id: UUID
    var name: String
    var host: String
    var username: String?
    var password: String?  // nil — Keychain-basiert, ValetudoAPI liest intern
}
```

From ValetudoApp/ValetudoApp/Models/RobotState.swift:
```swift
struct RobotAttribute: Codable {
    let __class: String
    let type: String?
    let subType: String?
    let value: String?
    let level: Int?
    let flag: String?
}
```

From ValetudoApp/ValetudoApp/Services/RobotManager.swift:
```swift
// Storage key fuer RobotConfigs in UserDefaults:
private let storageKey = "valetudo_robots"

// State-Vergleichslogik (analog portieren):
private func checkStateChanges(robotName: String, previous: RobotStatus?, current: RobotStatus) {
    guard let prevStatus = previous?.statusValue else { return }
    let currentStatus = current.statusValue ?? ""
    if prevStatus == "cleaning" && (currentStatus == "docked" || currentStatus == "idle") {
        notificationService.notifyCleaningComplete(robotName: robotName, area: current.cleanedArea)
    }
    if current.statusFlag == "stuck" && previous?.statusFlag != "stuck" {
        notificationService.notifyRobotStuck(robotName: robotName)
    }
    if currentStatus == "error" && prevStatus != "error" {
        let errorMsg = current.statusFlag ?? String(localized: "status.error")
        notificationService.notifyRobotError(robotName: robotName, error: errorMsg)
    }
}
```

From ValetudoApp/ValetudoApp/Services/NotificationService.swift:
```swift
@MainActor
class NotificationService: ObservableObject {
    static let shared = NotificationService()
    func notifyCleaningComplete(robotName: String, area: Int?)
    func notifyRobotError(robotName: String, error: String)
    func notifyRobotStuck(robotName: String)
}
```

From ValetudoApp/ValetudoApp/ValetudoApp.swift:
```swift
class AppDelegate: NSObject, UIApplicationDelegate, UNUserNotificationCenterDelegate {
    static let backgroundRefreshTaskIdentifier = "de.simonluthe.ValetudiOS.backgroundRefresh"
    // ^ wird in Plan 02 hinzugefuegt, hier als Referenz
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Info.plist BGTaskSchedulerPermittedIdentifiers eintragen</name>
  <files>ValetudoApp/ValetudoApp/Info.plist</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Info.plist
  </read_first>
  <action>
    BGTaskSchedulerPermittedIdentifiers Array in Info.plist einfuegen — direkt vor dem schliessenden `</dict>` Tag.

    Exakter XML-Block:
    ```xml
    <key>BGTaskSchedulerPermittedIdentifiers</key>
    <array>
        <string>de.simonluthe.ValetudiOS.backgroundRefresh</string>
    </array>
    ```

    KEIN UIBackgroundModes mit "fetch" eintragen — das ist die alte API (iOS < 13). BGAppRefreshTask benoetigt NUR BGTaskSchedulerPermittedIdentifiers.

    Task-Identifier-Format: Bundle-ID + ".backgroundRefresh" = `de.simonluthe.ValetudiOS.backgroundRefresh`
  </action>
  <verify>
    <automated>grep -c "BGTaskSchedulerPermittedIdentifiers" ValetudoApp/ValetudoApp/Info.plist && grep -c "de.simonluthe.ValetudiOS.backgroundRefresh" ValetudoApp/ValetudoApp/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BGTaskSchedulerPermittedIdentifiers" ValetudoApp/ValetudoApp/Info.plist` liefert 1 Treffer
    - `grep "de.simonluthe.ValetudiOS.backgroundRefresh" ValetudoApp/ValetudoApp/Info.plist` liefert 1 Treffer
    - `grep "UIBackgroundModes" ValetudoApp/ValetudoApp/Info.plist` liefert 0 Treffer (NICHT hinzufuegen)
    - Info.plist ist valides XML (xmllint --noout)
  </acceptance_criteria>
  <done>Info.plist enthaelt BGTaskSchedulerPermittedIdentifiers mit dem exakten Task-Identifier, kein UIBackgroundModes</done>
</task>

<task type="auto">
  <name>Task 2: BackgroundMonitorService implementieren</name>
  <files>ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Services/RobotManager.swift (checkStateChanges-Logik Zeile 202-221, loadRobots Zeile 253-281, storageKey Zeile 16)
    - ValetudoApp/ValetudoApp/Services/NotificationService.swift (Zeile 6-9 fuer @MainActor-Annotation, Zeile 43-109 fuer notify*-Methoden)
    - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift (Zeile 22-48 fuer actor + init, Zeile 173-175 fuer getAttributes)
    - ValetudoApp/ValetudoApp/Models/RobotState.swift (Zeile 17-29 fuer RobotAttribute)
    - ValetudoApp/ValetudoApp/Models/RobotConfig.swift
    - .planning/phases/17-background-monitoring/17-RESEARCH.md (Pattern 3 + Pattern 4 + Pattern 5 + Anti-Patterns)
  </read_first>
  <action>
    Neue Datei `BackgroundMonitorService.swift` in Services/ erstellen.

    **Klasse:** `final class BackgroundMonitorService` — KEIN actor, KEIN @MainActor (laeuft im BGTask-Thread).
    **Singleton:** `static let shared = BackgroundMonitorService()`
    **Logger:** `private let logger = Logger(subsystem: "de.simonluthe.ValetudiOS", category: "BackgroundMonitor")`

    **Imports:**
    ```swift
    import BackgroundTasks
    import Foundation
    import os
    ```

    **Oeffentliche Methoden:**

    1. `func scheduleBackgroundRefresh()` — Erstellt BGAppRefreshTaskRequest mit identifier `AppDelegate.backgroundRefreshTaskIdentifier`, setzt earliestBeginDate auf 15 Minuten in der Zukunft, submitted via BGTaskScheduler.shared.submit(). Fehler werden geloggt, nicht geworfen.

    2. `func handleBackgroundRefresh(task: BGAppRefreshTask)` — Der BGTask-Handler. Ablauf:
       a. Sofort `scheduleBackgroundRefresh()` aufrufen (garantiert naechsten Zyklus auch wenn dieser Task abbricht)
       b. Swift `Task` fuer async Arbeit erstellen: `let workTask = Task { await checkAllRobots() }`
       c. `task.expirationHandler` setzen: cancelt workTask UND ruft `task.setTaskCompleted(success: false)` auf
       d. Separater `Task` wartet auf workTask.result und ruft `task.setTaskCompleted(success: true)` auf
       KRITISCH: `task.setTaskCompleted(success:)` MUSS in JEDEM Codepfad aufgerufen werden.

    **Private Methoden:**

    3. `private func checkAllRobots() async` — Laedt RobotConfigs aus UserDefaults (Key: "valetudo_robots"), iteriert ueber alle Configs. Fuer jeden Roboter: `guard !Task.isCancelled else { break }` + `await checkRobot(config:)`.

    4. `private func checkRobot(config: RobotConfig) async` — Instanziiert `ValetudoAPI(config: config)`, ruft `try await api.getAttributes()` auf. Extrahiert StatusStateAttribute (value + flag) und erstellt PersistedRobotStatus. Laedt vorherigen Status via `loadPersistedStatus(for:)`, vergleicht mit `checkStateChanges()`, speichert neuen Status. Bei Fehler: nur loggen, NICHT werfen.

    5. `private func checkStateChanges(robotName: String, previous: PersistedRobotStatus?, current: PersistedRobotStatus)` — Analog zu RobotManager.checkStateChanges():
       - `cleaning` -> `docked`/`idle`: `Task { @MainActor in NotificationService.shared.notifyCleaningComplete(robotName: robotName, area: nil) }`
       - `statusFlag == "stuck"` und vorher nicht stuck: `Task { @MainActor in NotificationService.shared.notifyRobotStuck(robotName:) }`
       - `statusValue == "error"` und vorher nicht error: `Task { @MainActor in NotificationService.shared.notifyRobotError(robotName:, error:) }`
       WICHTIG: NotificationService ist @MainActor — alle Aufrufe via `Task { @MainActor in ... }` wrappen.

    **Persistenz (private):**

    6. `private struct PersistedRobotStatus: Codable` mit:
       - `let statusValue: String?` (aus StatusStateAttribute.value)
       - `let statusFlag: String?` (aus StatusStateAttribute.flag)
       - `let timestamp: Date`
       - `init(attributes: [RobotAttribute])` — extrahiert StatusStateAttribute

    7. `private func userDefaultsKey(for robotId: UUID) -> String` — gibt `"bg_last_status_\(robotId.uuidString)"` zurueck

    8. `private func loadRobotConfigs() -> [RobotConfig]` — liest UserDefaults.standard.data(forKey: "valetudo_robots"), decodiert als [RobotConfig]. Bei Fehler: leeres Array.

    9. `private func loadPersistedStatus(for robotId: UUID) -> PersistedRobotStatus?` — liest aus UserDefaults mit Key von userDefaultsKey(for:)

    10. `private func saveStatus(_ status: PersistedRobotStatus, for robotId: UUID)` — encodiert und speichert in UserDefaults

    **Referenz:** Der Task-Identifier wird ueber `AppDelegate.backgroundRefreshTaskIdentifier` referenziert (wird in Plan 02 als static let in AppDelegate ergaenzt). Fuer die Kompilierbarkeit in Plan 01: den Identifier als private Konstante definieren:
    ```swift
    static let taskIdentifier = "de.simonluthe.ValetudiOS.backgroundRefresh"
    ```
    In Plan 02 wird AppDelegate diesen ueber `BackgroundMonitorService.taskIdentifier` referenzieren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -project ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert: `test -f ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift`
    - `grep "final class BackgroundMonitorService" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "static let shared" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "import BackgroundTasks" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "handleBackgroundRefresh" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "scheduleBackgroundRefresh" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "setTaskCompleted" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert mindestens 2 Treffer (success + expiration)
    - `grep "checkStateChanges" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "NotificationService.shared" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert mindestens 3 Treffer
    - `grep "@MainActor in" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "PersistedRobotStatus" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - `grep "bg_last_status_" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` liefert Treffer
    - Projekt kompiliert fehlerfrei (xcodebuild build)
  </acceptance_criteria>
  <done>BackgroundMonitorService.swift existiert mit vollstaendiger BGTask-Handler-Logik, State-Persistenz in UserDefaults, State-Vergleich analog zu RobotManager, und Notification-Dispatch via NotificationService. Projekt kompiliert.</done>
</task>

</tasks>

<verification>
- `grep -c "BGTaskSchedulerPermittedIdentifiers" ValetudoApp/ValetudoApp/Info.plist` = 1
- `grep -c "handleBackgroundRefresh" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` >= 1
- `grep -c "setTaskCompleted" ValetudoApp/ValetudoApp/Services/BackgroundMonitorService.swift` >= 2
- Xcode Build erfolgreich
</verification>

<success_criteria>
BackgroundMonitorService ist vollstaendig implementiert und kompiliert. Info.plist ist fuer BGAppRefreshTask vorbereitet. Der Service kann (noch nicht verdrahtet) Roboter-Status pruefen, mit UserDefaults-Persistenz vergleichen, und Notifications ueber NotificationService versenden.
</success_criteria>

<output>
After completion, create `.planning/phases/17-background-monitoring/17-01-SUMMARY.md`
</output>
