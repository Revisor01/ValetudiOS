---
phase: 12-state-machine-foundation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
autonomous: true
requirements:
  - STATE-04

must_haves:
  truths:
    - "RobotDetailViewModel haelt eine UpdateService-Instanz und delegiert checkForUpdate() und startUpdate() dorthin"
    - "ValetudoInfoView liest updaterState aus dem uebergebenen UpdateService statt eigener @State-Kopie"
    - "Bestehende Update-Properties in RobotDetailViewModel sind Proxy-Computed-Properties auf updateService.phase"
    - "Beide Stellen (ViewModel + ValetudoInfoView) lesen denselben UpdateService"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "UpdateService-Delegation und Proxy-Properties"
      contains: "updateService"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift"
      provides: "ValetudoInfoView mit UpdateService-Parameter"
      contains: "let updateService: UpdateService"
  key_links:
    - from: "RobotDetailViewModel"
      to: "UpdateService"
      via: "property"
      pattern: "private\\(set\\) var updateService"
    - from: "ValetudoInfoView"
      to: "UpdateService"
      via: "init parameter"
      pattern: "let updateService: UpdateService"
    - from: "RobotDetailView"
      to: "ValetudoInfoView"
      via: "UpdateService durchreichen"
      pattern: "viewModel\\.updateService"
---

<objective>
UpdateService in RobotDetailViewModel und ValetudoInfoView verdrahten — beide lesen denselben phase-Wert.

Purpose: STATE-04 erfuellen — ein zentraler UpdateService als Single Source of Truth statt doppelter State.
Output: Geaenderte ViewModel- und View-Dateien mit UpdateService-Delegation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-state-machine-foundation/12-CONTEXT.md
@.planning/phases/12-state-machine-foundation/12-RESEARCH.md
@.planning/phases/12-state-machine-foundation/12-01-SUMMARY.md

<interfaces>
<!-- Von Plan 01 erstellte Interfaces -->

From ValetudoApp/ValetudoApp/Services/UpdateService.swift (erstellt in Plan 01):
```swift
enum UpdatePhase: Equatable {
    case idle, checking, updateAvailable, downloading
    case readyToApply, applying, rebooting, error(String)
}

@MainActor
class UpdateService: ObservableObject {
    @Published private(set) var phase: UpdatePhase = .idle
    private let api: ValetudoAPI

    init(api: ValetudoAPI)
    func checkForUpdates() async
    func startDownload() async
    func startApply() async
    func reset()
}
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift (aktueller Stand):
```swift
@MainActor
final class RobotDetailViewModel: ObservableObject {
    let robot: RobotConfig
    let robotManager: RobotManager

    // Update state (Zeile 34-41) — wird zu Proxy
    @Published var currentVersion: String?
    @Published var latestVersion: String?
    @Published var updateUrl: String?
    @Published var updaterState: UpdaterState?
    @Published var isUpdating: Bool       // = false
    @Published var showUpdateWarning: Bool // = false
    @Published var updateInProgress: Bool  // = false

    var api: ValetudoAPI? {
        robotManager.getAPI(for: robot.id)
    }
}
```

From ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift (aktueller Stand):
```swift
struct ValetudoInfoView: View {
    let robot: RobotConfig
    @EnvironmentObject var robotManager: RobotManager
    @State private var updaterState: UpdaterState?  // Eigene Kopie — wird entfernt
}
```

From ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift (Zeile 408):
```swift
ValetudoInfoView(robot: robot)
```

From ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (Zeile 180):
```swift
RobotSettingsView(robot: viewModel.robot, robotManager: viewModel.robotManager)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RobotDetailViewModel — UpdateService-Delegation und Proxy-Properties</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift (gesamte Datei — aktueller Stand aller Update-Properties und Methoden)
    - ValetudoApp/ValetudoApp/Services/UpdateService.swift (von Plan 01 erstellt — verfuegbare API)
  </read_first>
  <action>
**1. Neues Property hinzufuegen** (nach den bestehenden Update-Properties, ca. Zeile 41):
```swift
private(set) var updateService: UpdateService?
```

**2. UpdateService-Initialisierung** — neue private Methode:
```swift
private func setupUpdateService() {
    guard let api = api else { return }
    if updateService == nil {
        updateService = UpdateService(api: api)
    }
}
```
Diese Methode in `loadInitialData()` oder der zentralen Lade-Methode aufrufen, BEVOR `checkForUpdate()` aufgerufen wird. Suche nach der Stelle wo `checkForUpdate()` in der bestehenden Datei aufgerufen wird und stelle `setupUpdateService()` davor.

**3. checkForUpdate() aendern** (Zeile 265-283):
Die bestehende `checkForUpdate()` Methode BEHAELT die GitHub-Release-Logik (currentVersion, latestVersion, updateUrl) — diese gehoert nicht zu UpdateService.
Nur den Valetudo-Update-Check an UpdateService delegieren:
```swift
private func checkForUpdate() async {
    setupUpdateService()
    
    guard let api = api else { return }
    do {
        if let version = try? await api.getValetudoVersion() {
            currentVersion = version.release
        }
        
        // Valetudo Update-Check via UpdateService (per STATE-04)
        await updateService?.checkForUpdates()
        
        // GitHub Release Check (bleibt im ViewModel — nicht Update-Service-Scope)
        guard let url = URL(string: Constants.githubApiLatestReleaseUrl) else { return }
        let (data, _) = try await URLSession.shared.data(from: url)
        let release = try JSONDecoder().decode(GitHubRelease.self, from: data)
        latestVersion = release.tag_name
        updateUrl = release.html_url
    } catch {
        logger.error("Failed to check for updates: \(error, privacy: .public)")
    }
}
```

**4. startUpdate() aendern** (Zeile 450-490):
Komplette bestehende Methode durch Delegation ersetzen:
```swift
func startUpdate() async {
    guard let svc = updateService else { return }
    if case .updateAvailable = svc.phase {
        await svc.startDownload()
        // Nach Download, wenn ready: Apply starten
        if case .readyToApply = svc.phase {
            await svc.startApply()
        }
    } else if case .readyToApply = svc.phase {
        await svc.startApply()
    }
}
```

**5. Proxy-Computed-Properties** — bestehende `@Published` Properties zu Computed Properties aendern:

`updateInProgress` von `@Published var updateInProgress = false` aendern zu:
```swift
var updateInProgress: Bool {
    guard let svc = updateService else { return false }
    switch svc.phase {
    case .downloading, .applying, .rebooting, .checking:
        return true
    default:
        return false
    }
}
```
WICHTIG: Die `@Published`-Zeile `@Published var updateInProgress = false` ENTFERNEN und durch die computed property ersetzen.

`updaterState` bleibt als `@Published var updaterState: UpdaterState?` — wird in Phase 13 entfernt. In Phase 12 wird es nicht mehr aktiv gesetzt (die Stellen in checkForUpdate und startUpdate die es setzten sind jetzt delegiert), aber Views die es noch lesen bekommen `nil`. Das ist akzeptabel da ValetudoInfoView in Task 2 auf UpdateService umgestellt wird.

`isUpdating` und `showUpdateWarning` bleiben unveraendert — werden in Phase 13 entfernt.

**NICHT aendern:** `currentVersion`, `latestVersion`, `updateUrl` — diese sind GitHub-Release-bezogen, nicht Update-Service-Scope.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "private(set) var updateService: UpdateService?" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift gibt 1
    - grep -c "setupUpdateService()" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift gibt mindestens 2 (Definition + Aufruf)
    - grep "await updateService?.checkForUpdates()" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift findet 1 Treffer
    - grep "svc.startDownload\|svc.startApply\|updateService.*startDownload\|updateService.*startApply" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift findet mindestens 1 Treffer
    - grep "var updateInProgress: Bool {" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift findet 1 Treffer (computed, nicht @Published)
    - grep "@Published var updateInProgress" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift findet 0 Treffer
    - Projekt kompiliert ohne Fehler
  </acceptance_criteria>
  <done>RobotDetailViewModel delegiert checkForUpdate() und startUpdate() an UpdateService. updateInProgress ist Computed Property auf updateService.phase. UpdateService wird lazy bei erstem Gebrauch initialisiert.</done>
</task>

<task type="auto">
  <name>Task 2: ValetudoInfoView — UpdateService injizieren und eigene State-Kopie entfernen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  </files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift (Zeile 773-973 — gesamte ValetudoInfoView)
    - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift (Zeile 405-415 — NavigationLink zu ValetudoInfoView)
    - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (Zeile 175-185 — NavigationLink zu RobotSettingsView)
    - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift (nach Task 1 — updateService Property)
  </read_first>
  <action>
**1. ValetudoInfoView (RobotSettingsSections.swift, ab Zeile 773):**

Init-Parameter hinzufuegen:
```swift
struct ValetudoInfoView: View {
    let robot: RobotConfig
    let updateService: UpdateService?   // NEU — per STATE-04
    @EnvironmentObject var robotManager: RobotManager
```

`@State private var updaterState: UpdaterState?` ENTFERNEN — wird durch `updateService.phase` ersetzt.

`hasUpdate` Computed Property aendern:
```swift
private var hasUpdate: Bool {
    if case .updateAvailable = updateService?.phase {
        return true
    }
    // Fallback: GitHub-Release-Vergleich fuer Version-Badge
    guard let current = version?.release,
          let latest = latestRelease?.tag_name else { return false }
    return current != latest
}
```

In der `body` View: Alle Stellen die `updaterState?.currentVersion` lesen durch `version?.release` ersetzen (das `version`-Property bleibt erhalten und wird weiter in `loadInfo()` geladen).

`loadInfo()` Methode aendern — den `api.getUpdaterState()` Aufruf ENTFERNEN (UpdateService macht das bereits):
```swift
private func loadInfo() async {
    guard let api = api else { return }
    isLoading = true
    defer { isLoading = false }

    do {
        version = try await api.getValetudoVersion()
        hostInfo = try await api.getSystemHostInfo()
        // updaterState wird nicht mehr hier geladen — kommt von UpdateService
    } catch {
        sectionsLogger.error("Failed to load Valetudo info: \(error, privacy: .public)")
    }

    await checkForUpdate()
}
```

`checkForUpdate()` Methode bleibt — laedt nur noch GitHub-Release (kein Valetudo-State mehr).

**2. RobotSettingsView.swift (Zeile 408):**

Die Call-Site aendern. Erst muss RobotSettingsView Zugriff auf UpdateService bekommen.

In `RobotSettingsView` neuen Init-Parameter hinzufuegen:
```swift
let updateService: UpdateService?
```

Bestehenden `init(robot:robotManager:)` erweitern:
```swift
init(robot: RobotConfig, robotManager: RobotManager, updateService: UpdateService? = nil) {
    self.robot = robot
    self.updateService = updateService
    _viewModel = StateObject(wrappedValue: RobotSettingsViewModel(robot: robot, robotManager: robotManager))
}
```

NavigationLink zu ValetudoInfoView aendern (ca. Zeile 408):
```swift
ValetudoInfoView(robot: robot, updateService: updateService)
```

Preview am Ende der Datei anpassen falls noetig.

**3. RobotDetailView.swift (Zeile 180):**

NavigationLink zu RobotSettingsView aendern:
```swift
RobotSettingsView(robot: viewModel.robot, robotManager: viewModel.robotManager, updateService: viewModel.updateService)
```

Danach `xcodegen generate` ausfuehren und Build pruefen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodegen generate 2>&1 && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "let updateService: UpdateService?" ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift findet 1 Treffer
    - grep "@State private var updaterState: UpdaterState?" ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift findet 0 Treffer
    - grep "let updateService: UpdateService?" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift findet 1 Treffer
    - grep "ValetudoInfoView(robot: robot, updateService: updateService)" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift findet 1 Treffer
    - grep "updateService: viewModel.updateService" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift findet 1 Treffer
    - grep "api.getUpdaterState()" ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift findet 0 Treffer (entfernt aus loadInfo)
    - Projekt kompiliert ohne Fehler
  </acceptance_criteria>
  <done>ValetudoInfoView erhaelt UpdateService als Init-Parameter. Eigene @State updaterState-Kopie ist entfernt. RobotSettingsView und RobotDetailView reichen UpdateService durch. Beide Stellen (ViewModel + ValetudoInfoView) lesen denselben UpdateService (STATE-04).</done>
</task>

</tasks>

<verification>
- `RobotDetailViewModel.updateService` und `ValetudoInfoView.updateService` referenzieren dieselbe Instanz
- `@State private var updaterState` existiert nicht mehr in ValetudoInfoView
- `updateInProgress` ist computed property (nicht @Published) in RobotDetailViewModel
- Alle Update-Aufrufe gehen durch UpdateService
- Projekt kompiliert fehlerfrei
- Bestehende Tests laufen weiterhin (kein Breaking Change an oeffentlicher API)
</verification>

<success_criteria>
- STATE-04 erfuellt: Ein zentraler UpdateService ist Source of Truth, gelesen von ViewModel und ValetudoInfoView
- Keine eigene updaterState-Kopie mehr in ValetudoInfoView
- RobotDetailViewModel delegiert an UpdateService
- Proxy-Properties erhalten Abwaertskompatibilitaet fuer Phase 13
- Projekt kompiliert
</success_criteria>

<output>
After completion, create `.planning/phases/12-state-machine-foundation/12-02-SUMMARY.md`
</output>
