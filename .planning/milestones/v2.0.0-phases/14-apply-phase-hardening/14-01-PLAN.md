---
phase: 14-apply-phase-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/UpdateService.swift
autonomous: true
requirements: [APPLY-02, APPLY-03, APPLY-04]
must_haves:
  truths:
    - "Display bleibt waehrend Download/Apply/Rebooting an — kein Auto-Lock"
    - "Nach Apply wird der Roboter-Neustart erkannt und nicht als Fehler gewertet"
    - "Wenn die App in den Hintergrund geht waehrend Apply laeuft, wird der API-Call nicht abgebrochen"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      provides: "Idle Timer, Reboot-Polling, Background Task"
      contains: "isIdleTimerDisabled"
  key_links:
    - from: "UpdateService.phase didSet"
      to: "UIApplication.shared.isIdleTimerDisabled"
      via: "property observer or explicit call"
      pattern: "isIdleTimerDisabled"
    - from: "UpdateService.startApply()"
      to: "UIApplication.shared.beginBackgroundTask"
      via: "background task wrapping apply + reboot polling"
      pattern: "beginBackgroundTask"
    - from: "UpdateService.startApply()"
      to: "pollUntilReboot()"
      via: "sequential call after applyUpdate()"
      pattern: "pollUntilReboot"
---

<objective>
UpdateService mit Idle Timer Deaktivierung, Reboot-Fenster-Erkennung und Background Task Protection haerten.

Purpose: Den kritischen Apply-Moment absichern — kein Display-Sleep, keine falschen Fehler bei Reboot, kein iOS-Abbruch im Hintergrund.
Output: UpdateService.swift mit drei neuen Schutzschichten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-apply-phase-hardening/14-CONTEXT.md
@.planning/phases/13-state-consolidation/13-01-SUMMARY.md

<interfaces>
<!-- UpdateService current state (from Phase 12+13) -->

From ValetudoApp/ValetudoApp/Services/UpdateService.swift:
```swift
enum UpdatePhase: Equatable {
    case idle
    case checking
    case updateAvailable
    case downloading
    case readyToApply
    case applying
    case rebooting
    case error(String)
}

@MainActor
class UpdateService: ObservableObject {
    @Published private(set) var phase: UpdatePhase = .idle
    @Published private(set) var currentVersion: String?
    @Published private(set) var latestVersion: String?
    @Published private(set) var updateUrl: String?

    private let api: ValetudoAPI
    private var pollingTask: Task<Void, Never>?

    func loadVersionInfo() async
    func checkForUpdates() async
    func startDownload() async
    func startApply() async
    func reset()
}
```

From ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift:
```swift
func getUpdaterState() async throws -> UpdaterState   // GET /updater/state
func applyUpdate() async throws                        // PUT /updater { action: "apply" }
func getRobotInfo() async throws -> RobotInfo          // GET /robot
```

From ValetudoApp/ValetudoApp/Models/RobotState.swift:
```swift
struct UpdaterState: Codable {
    let __class: String?
    let busy: Bool?
    var stateType: String { __class ?? "unknown" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Idle Timer + Background Task + Reboot-Polling in UpdateService</name>
  <files>ValetudoApp/ValetudoApp/Services/UpdateService.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Services/UpdateService.swift
    - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift (lines 590-615, updater methods)
  </read_first>
  <action>
Drei Aenderungen an UpdateService.swift:

**1. Idle Timer (APPLY-02):**
- Neue private Methode `updateIdleTimer()` die `UIApplication.shared.isIdleTimerDisabled` setzt.
- `true` wenn `phase` ist `.downloading`, `.applying` oder `.rebooting`.
- `false` fuer alle anderen Phasen (`.idle`, `.error`, `.updateAvailable`, `.checking`, `.readyToApply`).
- `import UIKit` am Datei-Anfang hinzufuegen.
- `updateIdleTimer()` aufrufen in: `checkForUpdates()` nach Phase-Wechsel, `startDownload()` nach Phase-Wechsel, `startApply()` nach Phase-Wechsel, `pollUntilReboot()` bei Abschluss, `reset()`, und in `pollUntilReadyToApply()` bei Phase-Wechsel.
- WICHTIG: Statt an vielen Stellen einzeln aufrufen, eine zentrale Stelle nutzen. Da `phase` eine `@Published` Property ist, kann ein `didSet` nicht direkt verwendet werden (Property Wrapper). Stattdessen: eine private Methode `setPhase(_ newPhase: UpdatePhase)` erstellen, die `phase = newPhase` setzt UND `updateIdleTimer()` aufruft. Alle direkten `phase = ...` Zuweisungen durch `setPhase(...)` ersetzen.

**2. Reboot-Polling (APPLY-03):**
- Aktuelle `startApply()` Methode aendern: nach `api.applyUpdate()` statt direkt `phase = .rebooting` zu setzen, `setPhase(.rebooting)` aufrufen und dann `pollUntilReboot()` aufrufen.
- Neue private Methode `pollUntilReboot()`:
  ```swift
  private func pollUntilReboot() async {
      pollingTask = Task {
          // Warte 10 Sekunden bevor erstes Poll — Roboter braucht Zeit zum Herunterfahren
          try? await Task.sleep(for: .seconds(10))
          
          for _ in 0..<22 {  // 22 * 5s = 110s + 10s initial = 120s total
              guard !Task.isCancelled else { return }
              try? await Task.sleep(for: .seconds(5))
              guard !Task.isCancelled else { return }
              
              do {
                  let _ = try await api.getValetudoVersion()
                  // Roboter antwortet wieder — Erfolg
                  setPhase(.idle)
                  return
              } catch {
                  // Netzwerkfehler waehrend Reboot ist ERWARTET — weiter pollen
                  logger.info("Reboot-Polling: Roboter noch nicht erreichbar (\(error.localizedDescription, privacy: .public))")
                  continue
              }
          }
          
          // Timeout nach 120 Sekunden
          setPhase(.error("Roboter nicht erreichbar nach Update"))
      }
      await pollingTask?.value
  }
  ```
- `startApply()` wird zu:
  ```swift
  func startApply() async {
      guard case .readyToApply = phase else {
          logger.warning("startApply called in non-readyToApply phase")
          return
      }
      setPhase(.applying)
      do {
          try await api.applyUpdate()
          setPhase(.rebooting)
          await pollUntilReboot()
      } catch {
          logger.error("startApply failed: \(error.localizedDescription, privacy: .public)")
          setPhase(.error(error.localizedDescription))
      }
  }
  ```

**3. Background Task (APPLY-04):**
- Private Property: `private var backgroundTaskID: UIBackgroundTaskIdentifier = .invalid`
- In `startApply()`: VOR dem `api.applyUpdate()` Call `beginBackgroundTask` starten:
  ```swift
  backgroundTaskID = UIApplication.shared.beginBackgroundTask(withName: "ValetudoApplyUpdate") { [weak self] in
      // Expiry handler — iOS gibt uns letzte Chance aufzuraeumen
      self?.logger.warning("Background task expiring")
      if let id = self?.backgroundTaskID, id != .invalid {
          UIApplication.shared.endBackgroundTask(id)
          self?.backgroundTaskID = .invalid
      }
  }
  ```
- Nach `pollUntilReboot()` (egal ob Erfolg oder Timeout): `endBackgroundTask` aufrufen:
  ```swift
  private func endBackgroundTaskIfNeeded() {
      guard backgroundTaskID != .invalid else { return }
      UIApplication.shared.endBackgroundTask(backgroundTaskID)
      backgroundTaskID = .invalid
  }
  ```
- `endBackgroundTaskIfNeeded()` aufrufen am Ende von `startApply()` (nach await pollUntilReboot) UND im catch-Block von `startApply()`.
- Auch in `reset()` aufrufen fuer Safety.

**Zusammenfassung `setPhase` Methode:**
```swift
private func setPhase(_ newPhase: UpdatePhase) {
    phase = newPhase
    updateIdleTimer()
}

private func updateIdleTimer() {
    switch phase {
    case .downloading, .applying, .rebooting:
        UIApplication.shared.isIdleTimerDisabled = true
    default:
        UIApplication.shared.isIdleTimerDisabled = false
    }
}
```

ACHTUNG: `pollUntilReadyToApply()` existiert bereits und pollt waehrend Download. Dort die direkten `phase = ...` Zuweisungen ebenfalls durch `setPhase(...)` ersetzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "isIdleTimerDisabled" UpdateService.swift liefert mindestens 2 Treffer (true + false)
    - grep "beginBackgroundTask" UpdateService.swift liefert mindestens 1 Treffer
    - grep "endBackgroundTask" UpdateService.swift liefert mindestens 2 Treffer (end + expiry)
    - grep "pollUntilReboot" UpdateService.swift liefert mindestens 2 Treffer (definition + call)
    - grep "import UIKit" UpdateService.swift liefert 1 Treffer
    - grep "setPhase" UpdateService.swift liefert mindestens 10 Treffer (alle bisherigen phase-Zuweisungen)
    - xcodebuild BUILD SUCCEEDED
  </acceptance_criteria>
  <done>UpdateService hat Idle Timer Deaktivierung bei Download/Apply/Rebooting, Reboot-Polling mit 120s Timeout das Netzwerkfehler ignoriert, und Background Task Protection fuer den Apply-Call. Alle Phase-Zuweisungen gehen durch setPhase() fuer konsistente Idle-Timer-Steuerung.</done>
</task>

</tasks>

<verification>
- `grep -c "isIdleTimerDisabled" ValetudoApp/ValetudoApp/Services/UpdateService.swift` >= 2
- `grep -c "beginBackgroundTask" ValetudoApp/ValetudoApp/Services/UpdateService.swift` >= 1
- `grep -c "pollUntilReboot" ValetudoApp/ValetudoApp/Services/UpdateService.swift` >= 2
- `grep -c "setPhase" ValetudoApp/ValetudoApp/Services/UpdateService.swift` >= 10
- Xcode Build SUCCEEDED
</verification>

<success_criteria>
UpdateService schuetzt den Apply-Moment: Display bleibt an, Reboot wird erkannt, Background Task verhindert iOS-Abbruch. Kein falscher Error-State waehrend des Roboter-Neustarts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-apply-phase-hardening/14-01-SUMMARY.md`
</output>
