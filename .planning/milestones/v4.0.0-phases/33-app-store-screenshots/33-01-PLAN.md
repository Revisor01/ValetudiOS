---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/AppStore/screenshots/package.json
  - ValetudoApp/AppStore/screenshots/src/app/page.tsx
  - ValetudoApp/AppStore/screenshots/src/app/layout.tsx
  - ValetudoApp/AppStore/screenshots/public/screenshots/de/01-live-map.png
  - ValetudoApp/AppStore/screenshots/public/screenshots/en/01-live-map.png
  - ValetudoApp/AppStore/screenshots/public/mockup.png
  - ValetudoApp/AppStore/screenshots/export/de/01-live-map.png
  - ValetudoApp/AppStore/screenshots/export/en/01-live-map.png
  - ValetudoApp/AppStore/screenshots/PILOT_DECISION.md
autonomous: false
requirements:
  - SCREENSHOTS-01
  - SCREENSHOTS-02
user_setup: []

must_haves:
  truths:
    - "Kit-Projekt ist in ValetudoApp/AppStore/screenshots/ scaffolded und startet via bun dev (oder npm run dev)"
    - "Pilot-Screen 1 (Live-Karte) ist in DE+EN als 1320×2868 PNG exportiert"
    - "Tooling-Entscheidung ist in PILOT_DECISION.md dokumentiert (Kit weiterführen ODER SwiftUI-Fallback)"
    - "Source-Screenshot für Live-Karte aus iPhone-16-Simulator (6.1\"-Klasse, ~1125×2436 oder vergleichbar) liegt in public/screenshots/{de,en}/01-live-map.png"
  artifacts:
    - path: "ValetudoApp/AppStore/screenshots/package.json"
      provides: "Next.js + html-to-image Dependency-Definition"
    - path: "ValetudoApp/AppStore/screenshots/src/app/page.tsx"
      provides: "Screenshot-Generator mit Pilot-Screen"
    - path: "ValetudoApp/AppStore/screenshots/export/de/01-live-map.png"
      provides: "Finaler Pilot-Screen DE (1320×2868)"
    - path: "ValetudoApp/AppStore/screenshots/export/en/01-live-map.png"
      provides: "Finaler Pilot-Screen EN (1320×2868)"
    - path: "ValetudoApp/AppStore/screenshots/PILOT_DECISION.md"
      provides: "Go/No-Go-Entscheidung für Plans 02-05"
  key_links:
    - from: "Simulator iPhone 16 (6.1\")"
      to: "public/screenshots/{locale}/01-live-map.png"
      via: "xcrun simctl io ... screenshot (Source-Shot 1125×2436-Klasse)"
      pattern: "xcrun simctl io.*screenshot"
    - from: "page.tsx (Container 1320×2868)"
      to: "export/{locale}/01-live-map.png"
      via: "html-to-image Browser-Click → PNG-Download"
      pattern: "htmlToImage|toPng"
---

<objective>
Kit-Scaffolding und kompletter Pilot-Durchlauf für Screen 1 (Live-Karte) in DE+EN. Diese Phase hat ein **hartes Gate**: Nach dem Pilot wird entschieden, ob das ParthJadhav-Kit tauglich ist oder auf SwiftUI/ImageRenderer umgeschwenkt wird. Erst nach GO für das Kit werden Plans 02-05 gestartet.

Purpose: Risiko früh abfangen — Kit-Kompatibilität (SF-Pro-Rendering, Gradient-Qualität, Dynamic Island, 1320×2868-Export, korrekte Source-Shot-Skalierung von 6.1" auf 6.9") wird an einem einzigen Screen verifiziert, bevor 9 Screens × 2 Sprachen produziert werden.

Output: Lauffähiges Kit-Projekt, ein Pilot-Screen in zwei Sprachen als finale PNG (1320×2868), dokumentierte Tooling-Entscheidung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-app-store-screenshots/33-CONTEXT.md
@.planning/phases/33-app-store-screenshots/33-RESEARCH.md
@.planning/phases/33-app-store-screenshots/33-00-OVERVIEW.md

<key_facts>
- **Design-Kanvas (finale Asset-Größe):** 1320×2868 (iPhone 6.9" Pro Max — REQUIRED durch App Store Connect)
- **Source-Screenshot-Auflösung:** 6.1"-Klasse aus iPhone 16 Simulator (NICHT Pro Max). Nominell 1125×2436 laut CONTEXT.md; tatsächlich liefert iPhone 16 Simulator typisch 1170×2532 oder 1179×2556 — alle 6.1"-Klassen-Werte sind akzeptabel. Das Kit skaliert intern korrekt.
- **Simulator-UDID iPhone 16:** D421F591-7696-411F-BFB1-C164C31163D3
- **App Bundle-ID:** de.simonluthe.ValetudiOS
- **Logo-Farben:** #56BDE3 (türkis), #3B87F6 (blau), #CEE7F6 (hellblau)
- **Screen 1 Headline DE:** "Live dabei." / "Karte in Echtzeit — du siehst jeden Meter."
- **Screen 1 Headline EN:** "Watch live." / "Real-time map. Every move, as it happens."
- **bun v1.3.12 und Node v25.9.0** bereits installiert
- **Kit-Installation:** `npx skills add ParthJadhav/app-store-screenshots -a claude-code` — Skill-Mechanik ruft den Agent-Workflow auf der das Kit in das aktuelle Verzeichnis scaffoldet.
</key_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kit-Skill installieren und in ValetudoApp/AppStore/screenshots/ scaffolden</name>
  <files>
    ValetudoApp/AppStore/screenshots/package.json,
    ValetudoApp/AppStore/screenshots/src/app/layout.tsx,
    ValetudoApp/AppStore/screenshots/src/app/page.tsx,
    ValetudoApp/AppStore/screenshots/.gitignore
  </files>
  <action>
    ParthJadhav/app-store-screenshots Kit als Claude-Code-Skill installieren und im Ziel-Verzeichnis scaffolden.

    **Bevorzugter Weg — Skill-Installation:**
    1. Aus dem Repo-Root: `npx skills add ParthJadhav/app-store-screenshots -a claude-code` ausführen (`-a claude-code` registriert es als Agent-Skill, alternativ `-g` für global).
    2. Skill-Anleitung folgen — typischerweise scaffoldet der Skill-Agent das Next.js-Projekt in das aktuelle oder ein angegebenes Verzeichnis. Ziel-Verzeichnis: `ValetudoApp/AppStore/screenshots/`.
    3. Falls der Skill-Agent kein Auto-Scaffold macht: Manuell durchführen wie in der Skill-Anleitung beschrieben (Next.js + Tailwind + html-to-image-Dependencies in `ValetudoApp/AppStore/screenshots/package.json`).

    **Fallback wenn `npx skills add` fehlschlägt:**
    - `git clone https://github.com/ParthJadhav/app-store-screenshots ValetudoApp/AppStore/screenshots`
    - `.git`-Ordner entfernen: `rm -rf ValetudoApp/AppStore/screenshots/.git`

    **Nach Scaffold:**
    - Dependencies installieren: `cd ValetudoApp/AppStore/screenshots && bun install` (oder `npm install`)
    - Dev-Server probeweise starten: `bun dev` (oder `npm run dev`) → prüfen dass localhost:3000 erreichbar ist und die Default-Demo lädt. Danach wieder stoppen.

    **Projekt-Struktur-Check:**
    - `src/app/page.tsx` muss existieren (Single-File-Generator)
    - `public/` muss existieren
    - `public/mockup.png` muss existieren (Kit-mitgeliefertes iPhone-Mockup)

    **.gitignore-Setup:**
    - `.gitignore` im Kit-Projekt: `node_modules/`, `.next/` ignorieren — `export/` und `public/screenshots/` MÜSSEN getrackt werden
    - Root-`.gitignore` des Haupt-Repos prüfen: keine Konflikte mit `ValetudoApp/AppStore/screenshots/`-Pfad
  </action>
  <verify>
    <automated>cd ValetudoApp/AppStore/screenshots &amp;&amp; test -f package.json &amp;&amp; test -f src/app/page.tsx &amp;&amp; test -f public/mockup.png &amp;&amp; test -d node_modules &amp;&amp; echo OK</automated>
  </verify>
  <done>Kit-Projekt liegt in `ValetudoApp/AppStore/screenshots/`, Dependencies installiert, Dev-Server läuft erfolgreich auf localhost:3000 (manuell verifiziert), `public/mockup.png` ist vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: Pilot-Source-Screenshot aus iPhone-16-Simulator (6.1") in DE + EN aufnehmen</name>
  <files>
    ValetudoApp/AppStore/screenshots/public/screenshots/de/01-live-map.png,
    ValetudoApp/AppStore/screenshots/public/screenshots/en/01-live-map.png
  </files>
  <action>
    **Wichtig:** Source-Shot wird aus dem **iPhone 16 Simulator (6.1"-Klasse)** gezogen, NICHT aus iPhone 16/17 Pro Max. Das Kit designt bei 6.9" und skaliert intern; ein 6.9"-Source würde doppelt skaliert (Qualitätsverlust). Die Kit-README empfiehlt 6.1" explizit.

    Für Screen 1 (Live-Karte): Roboter in Bewegung auf Live-Karte, Karte zentriert, Roboter-Position sichtbar, saubere UI ohne Modals.

    Schritte:

    1. **Simulator booten:**
       ```bash
       xcrun simctl boot D421F591-7696-411F-BFB1-C164C31163D3
       open -a Simulator
       ```

    2. **Statusbar auf Apple-Convention setzen** (vor JEDEM Shot wiederholen, Simulator-Reset löscht Override):
       ```bash
       xcrun simctl status_bar D421F591-7696-411F-BFB1-C164C31163D3 override \
         --time "9:41" \
         --dataNetwork wifi --wifiMode active --wifiBars 3 \
         --cellularMode active --cellularBars 4 \
         --batteryState charged --batteryLevel 100
       ```

    3. **App im Simulator installieren:** Aus Xcode Build & Run mit Scheme `ValetudoApp` → Destination `iPhone 16` (NICHT Pro Max).

    4. **Roboter im Simulator konfigurieren:** Echte Valetudo-Instanz im LAN ODER Debug-Mock. Zur Robot-Detail-Ansicht navigieren, Live-Karte öffnen, Reinigung starten damit Roboter sich bewegt (Position auf Karte sichtbar).

    5. **DE-Aufnahme:**
       ```bash
       xcrun simctl launch D421F591-7696-411F-BFB1-C164C31163D3 de.simonluthe.ValetudiOS \
         -AppleLanguages "(de)" -AppleLocale "de_DE"
       ```
       App-Zustand manuell herstellen (Robot-Detail → Live-Karte mit aktiver Reinigung).
       Statusbar-Override erneut setzen (Step 2).
       Screenshot aufnehmen:
       ```bash
       mkdir -p ValetudoApp/AppStore/screenshots/public/screenshots/de
       xcrun simctl io D421F591-7696-411F-BFB1-C164C31163D3 screenshot \
         ValetudoApp/AppStore/screenshots/public/screenshots/de/01-live-map.png
       ```

    6. **EN-Aufnahme:**
       ```bash
       xcrun simctl launch D421F591-7696-411F-BFB1-C164C31163D3 de.simonluthe.ValetudiOS \
         -AppleLanguages "(en)" -AppleLocale "en_US"
       ```
       Gleicher App-Zustand, Statusbar-Override erneut setzen.
       ```bash
       mkdir -p ValetudoApp/AppStore/screenshots/public/screenshots/en
       xcrun simctl io D421F591-7696-411F-BFB1-C164C31163D3 screenshot \
         ValetudoApp/AppStore/screenshots/public/screenshots/en/01-live-map.png
       ```

    7. **Auflösung verifizieren:**
       ```bash
       sips -g pixelWidth -g pixelHeight \
         ValetudoApp/AppStore/screenshots/public/screenshots/de/01-live-map.png
       ```
       Erwartung: 6.1"-Klasse — eine der folgenden Werte ist akzeptabel:
       - 1125×2436 (nominelle CONTEXT-Vorgabe)
       - 1170×2532 (iPhone 16-Klasse modern)
       - 1179×2556 (iPhone 16 Pro)

       NICHT akzeptabel: 1320×2868 (das wäre Pro Max — falsches Device gebootet).

       Wenn die Auflösung 1320×2868 ist → falsches Device → Simulator schließen, korrektes Device booten (UDID prüfen).
  </action>
  <verify>
    <automated>test -f ValetudoApp/AppStore/screenshots/public/screenshots/de/01-live-map.png &amp;&amp; test -f ValetudoApp/AppStore/screenshots/public/screenshots/en/01-live-map.png &amp;&amp; W=$(sips -g pixelWidth ValetudoApp/AppStore/screenshots/public/screenshots/de/01-live-map.png | awk '/pixelWidth/ {print $2}') &amp;&amp; [ "$W" -ge 1100 ] &amp;&amp; [ "$W" -le 1200 ] &amp;&amp; echo "OK: width $W in 6.1\" range"</automated>
  </verify>
  <done>Zwei Source-Screenshots (DE + EN) liegen in `public/screenshots/{de,en}/01-live-map.png`. Beide haben 6.1"-Klassen-Auflösung (Breite 1125-1179px). Beide zeigen Live-Karte mit sichtbarem Roboter und sauberer Statusbar (9:41, voller Akku, voller Empfang).</done>
</task>

<task type="auto">
  <name>Task 3: Pilot-Screen 1 im page.tsx konfigurieren (Container 1320×2868, Gradient-BG, Device-Frame, Headline DE+EN)</name>
  <files>
    ValetudoApp/AppStore/screenshots/src/app/page.tsx,
    ValetudoApp/AppStore/screenshots/src/app/layout.tsx
  </files>
  <action>
    `src/app/page.tsx` so anpassen, dass der Pilot-Screen "Live-Karte" als Slide-Definition vorhanden ist und in DE+EN gerendert wird. Container ist 1320×2868 (Design-Kanvas). Die 6.1"-Source-Shots werden vom Kit innerhalb des Mockups platziert.

    **Theme-Definition** (oben in page.tsx, vereinheitlicht für späteres Theme-Preset-System):
    ```typescript
    const VALETUDIOS_THEME = {
      bgGradientFrom: "#56BDE3",  // Logo türkis
      bgGradientTo: "#3B87F6",    // Logo blau
      blobLight: "#56BDE3",
      blobDark: "#3B87F6",
      blobHighlight: "#CEE7F6",   // Logo hellblau
      textPrimary: "#FFFFFF",
      textSecondary: "rgba(255,255,255,0.88)",
    } as const
    ```

    **Pilot-Slide-Definitionen:**
    ```typescript
    const pilotScreens = [
      {
        id: "01-live-map",
        locale: "de",
        headline: "Live dabei.",
        subline: "Karte in Echtzeit — du siehst jeden Meter.",
        screenshot: "/screenshots/de/01-live-map.png",
      },
      {
        id: "01-live-map",
        locale: "en",
        headline: "Watch live.",
        subline: "Real-time map. Every move, as it happens.",
        screenshot: "/screenshots/en/01-live-map.png",
      },
    ]
    ```

    **Layout-Anforderungen:**
    - Container exakt 1320×2868: `style={{ width: "1320px", height: "2868px", position: "relative", overflow: "hidden" }}`
    - Hintergrund: Linear-Gradient 145deg von `#56BDE3` zu `#3B87F6` plus 2-3 radiale Blob-Layer mit Blur (siehe RESEARCH.md Abschnitt 6 für exakte CSS — `blur(80-140px)`, Opacity 0.4-0.7)
    - Headline (Claim): SF Pro Display Bold, ca. 96-120px, weiß, oben mit Padding (~160px top)
    - Subline (Feature-Zeile): SF Pro Text Regular, ca. 48-56px, weiß 88% Opacity, unter Claim
    - Device-Mockup: `public/mockup.png` (Kit-intern). Source-Screenshot wird im Mockup-Bildschirm-Bereich positioniert (mit Padding/Border-Radius an Mockup-Maße angepasst)
    - Wenn Kit-Mockup kein Dynamic Island hat: in PILOT_DECISION (Task 5) notieren — ggf. Apple-Design-Resources iPhone 16 PNG einsetzen

    **Font-Setup in layout.tsx:**
    - System-Font-Stack via CSS: `font-family: -apple-system, "SF Pro Display", "SF Pro Text", system-ui, sans-serif`
    - Chrome verwenden für Rendering (Firefox hat bekannte html-to-image-Bugs)

    **Beide Locales gleichzeitig rendern:** page.tsx zeigt beide Pilot-Screens (DE + EN) untereinander oder via Tab-UI. Pro Screen ein Download-Button (html-to-image `toPng({ pixelRatio: 1, cacheBust: true })` → Browser-Download als PNG).

    **pixelRatio:** Auf 1 setzen wenn der Container bereits 1320×2868 ist — sonst entsteht 2640×5736 Output. Falls Browser-DPR-Probleme: `pixelRatio: 1320 / containerCSSWidth` berechnen.
  </action>
  <verify>
    <automated>cd ValetudoApp/AppStore/screenshots &amp;&amp; grep -q "Live dabei" src/app/page.tsx &amp;&amp; grep -q "Watch live" src/app/page.tsx &amp;&amp; grep -q "56BDE3" src/app/page.tsx &amp;&amp; grep -q "1320" src/app/page.tsx &amp;&amp; grep -q "2868" src/app/page.tsx</automated>
  </verify>
  <done>page.tsx enthält beide Pilot-Screens (DE + EN) mit Gradient-Background, Device-Mockup, Headlines und Sublines. Container-Dimension 1320×2868 im Code verankert. Dev-Server rendert beide Previews ohne Layout-Fehler.</done>
</task>

<task type="auto">
  <name>Task 4: Pilot-Screens als 1320×2868 PNG exportieren (DE + EN)</name>
  <files>
    ValetudoApp/AppStore/screenshots/export/de/01-live-map.png,
    ValetudoApp/AppStore/screenshots/export/en/01-live-map.png
  </files>
  <action>
    Beide Pilot-Screens (DE + EN) aus dem Kit als 1320×2868 PNGs exportieren.

    Schritte:
    1. `mkdir -p ValetudoApp/AppStore/screenshots/export/de ValetudoApp/AppStore/screenshots/export/en`
    2. `cd ValetudoApp/AppStore/screenshots && bun dev` starten
    3. **Chrome** öffnen (NICHT Firefox — bekannter html-to-image-Font-Bug). localhost:3000 laden.
    4. Auf Download-Button für DE-Screen klicken → PNG nach `~/Downloads/...` → in `export/de/01-live-map.png` umbenennen/verschieben
    5. Gleiches für EN-Screen → `export/en/01-live-map.png`

    **Validierung:**
    - Auflösung exakt prüfen:
      ```bash
      sips -g pixelWidth -g pixelHeight ValetudoApp/AppStore/screenshots/export/de/01-live-map.png
      sips -g pixelWidth -g pixelHeight ValetudoApp/AppStore/screenshots/export/en/01-live-map.png
      ```
      MUSS exakt 1320×2868 ausgeben.
    - PNG-Format: `file ValetudoApp/AppStore/screenshots/export/de/01-live-map.png` → "PNG image data, 1320 x 2868"
    - Visuell: Headlines lesbar, Gradient sauber, Mockup scharf, Source-Screenshot im Mockup-Bildschirm sichtbar

    **Wenn Output ≠ 1320×2868:**
    - Output zu klein (z.B. 660×1434): Browser-DPR-Issue → `pixelRatio` in toPng-Aufruf erhöhen
    - Output zu groß (z.B. 2640×5736): Doppelskalierung → `pixelRatio: 1` setzen ODER Container-CSS-Größe halbieren und `pixelRatio: 2` lassen
    - Notlösung: `sips --resampleWidth 1320 export/de/01-live-map.png` — aber nicht in Produktion, sondern Kit-Config korrigieren
  </action>
  <verify>
    <automated>test -f ValetudoApp/AppStore/screenshots/export/de/01-live-map.png &amp;&amp; test -f ValetudoApp/AppStore/screenshots/export/en/01-live-map.png &amp;&amp; W_DE=$(sips -g pixelWidth ValetudoApp/AppStore/screenshots/export/de/01-live-map.png | awk '/pixelWidth/ {print $2}') &amp;&amp; H_DE=$(sips -g pixelHeight ValetudoApp/AppStore/screenshots/export/de/01-live-map.png | awk '/pixelHeight/ {print $2}') &amp;&amp; [ "$W_DE" = "1320" ] &amp;&amp; [ "$H_DE" = "2868" ] &amp;&amp; file ValetudoApp/AppStore/screenshots/export/de/01-live-map.png | grep -q "PNG"</automated>
  </verify>
  <done>Zwei exportierte PNGs in `export/de/01-live-map.png` und `export/en/01-live-map.png`, beide exakt 1320×2868, PNG-Format, lesbar und visuell sauber.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Pilot-Gate — visuelle Bewertung und Go/No-Go-Entscheidung</name>
  <files>ValetudoApp/AppStore/screenshots/PILOT_DECISION.md</files>
  <what-built>
    Pilot-Screen 1 "Live-Karte" in DE und EN wurde durch die komplette Kit-Pipeline gerendert:
    - Source-Screenshot aus iPhone-16-Simulator (6.1"-Klasse, 1125-1179 Pixel breit)
    - Im Kit-Container 1320×2868 platziert
    - Gradient-Background aus Logo-Farben (#56BDE3 → #3B87F6 mit Blobs)
    - Device-Mockup (Kit-intern ODER Apple Design Resources iPhone 16)
    - Headlines + Sublines in SF Pro
    - Finaler PNG-Export 1320×2868

    Dateien: `export/de/01-live-map.png` und `export/en/01-live-map.png`
  </what-built>
  <how-to-verify>
    Beide PNGs öffnen (Preview.app oder Finder Quick Look) und nach diesen Kriterien bewerten — jedes mit ✓ oder ✗ beantworten:

    **Qualitäts-Kriterien (5 Checks):**

    1. **SF-Pro-Typografie:** Claim und Subline sehen aus wie echte SF Pro (nicht Helvetica/Arial — subtile Unterschiede: SF Pro hat schmaleres "a", rundere "0", spezifische Ziffern). Wenn Font klar Fallback ist → ✗
    2. **Gradient-Qualität:** Blobs sind sauber geblurrt, keine Color-Banding-Streifen, keine harten Kanten. Übergang türkis → blau wirkt organisch → ✓
    3. **Device-Mockup:** Zeigt Dynamic Island (Pro-Max-Identifikator), Titanium-Look erkennbar, Schatten unter dem Gerät. Wenn Mockup aussieht wie iPhone 11/12 → ✗ (Apple-Design-Resources-PNG nötig)
    4. **Auflösung + Schärfe:** Beim Hereinzoomen sind Text und Mockup scharf, keine Pixelation, keine JPG-Artefakte. Source-Shot im Mockup-Bildschirm ist nicht verzerrt (6.1"-Source in 6.9"-Mockup wird vom Kit korrekt skaliert).
    5. **Layout + Hierarchie:** Claim dominiert visuell, Subline darunter klar nachgeordnet, Device-Mockup prominent (Mitte oder untere Hälfte), keine Überlappungen, Padding/Spacing wirkt ausgewogen.

    **Sprachqualität (2 Checks):**

    6. **DE-Screen:** Headline "Live dabei." korrekt, Subline "Karte in Echtzeit — du siehst jeden Meter." exakt (Em-Dash, kein Hyphen), keine Abschneidung.
    7. **EN-Screen:** Headline "Watch live." korrekt, Subline "Real-time map. Every move, as it happens." exakt, keine Abschneidung.

    **Entscheidungsregel:**
    - Alle 7 ✓ → **GO:** Kit bleibt, Plans 02-05 weiter wie geplant
    - 1 Kriterium ✗ aber fixable → **GO mit Fix-Hinweis:** Notiz in PILOT_DECISION.md, Plans 02-05 weiter
    - ≥2 Kriterien ✗ → **NO-GO:** Fallback auf SwiftUI/ImageRenderer aktivieren. Plans 02 und 04 müssen in SwiftUI-Variante neu geschrieben werden BEVOR sie ausgeführt werden.

    **PILOT_DECISION.md erstellen** mit:
    - Datum + Bewerter-Name
    - 7 Kriterien mit ✓/✗ + Kommentar pro Kriterium
    - Finale Entscheidung: GO / GO-mit-Fix / NO-GO
    - Bei NO-GO: Begründung + Handover-Notizen für SwiftUI-Fallback (welche Komponenten zuerst, welche Bibliotheken)
    - Bei GO: Liste der Fixes die in Plan 02 noch gemacht werden müssen (z.B. "WOFF2 statt System-Font", "Mockup-PNG ersetzen")
  </how-to-verify>
  <resume-signal>
    Antworte mit einem der drei Werte:
    - `GO` — alle Kriterien ok, Kit weiterführen
    - `GO-mit-Fix: [Beschreibung]` — Kit ok, aber kleine Anpassungen in Plan 02
    - `NO-GO: [Begründung]` — auf SwiftUI-Fallback umschwenken
  </resume-signal>
</task>

</tasks>

<verification>
Phase-Ende-Check:
- `ValetudoApp/AppStore/screenshots/` ist lauffähiges Kit-Projekt
- Zwei Source-Screenshots in `public/screenshots/{de,en}/01-live-map.png` (6.1"-Klassen-Auflösung)
- Zwei finale PNG-Exporte in `export/{de,en}/01-live-map.png` mit exakt 1320×2868
- PILOT_DECISION.md existiert und enthält klare Entscheidung
- Wenn NO-GO: Plans 02 und 04 müssen auf SwiftUI umgestellt werden BEVOR sie gestartet werden
</verification>

<success_criteria>
1. Kit läuft (`bun dev` öffnet localhost:3000 ohne Fehler)
2. Pilot-Source-Screenshots in DE+EN liegen in `public/screenshots/{de,en}/01-live-map.png`, beide 6.1"-Klasse (Breite 1125-1179px)
3. page.tsx enthält Gradient-Background, Device-Mockup, Pilot-Screen-Definitionen für DE+EN, Container 1320×2868
4. `export/de/01-live-map.png` und `export/en/01-live-map.png` existieren, beide exakt 1320×2868, PNG
5. PILOT_DECISION.md mit expliziter Entscheidung (GO/GO-mit-Fix/NO-GO) committed
6. Entscheidung kommuniziert, nächster Plan (02) klar definiert
</success_criteria>

<output>
Nach Completion: `.planning/phases/33-app-store-screenshots/33-01-SUMMARY.md` erstellen mit:
- Tooling-Entscheidung (Kit vs. SwiftUI)
- Tatsächliche Source-Shot-Auflösung aus iPhone 16 Simulator (1125 / 1170 / 1179)
- Bekannte Probleme aus Pilot (Font, Mockup, Gradient)
- Handover-Notizen für Plan 02 (welche Anpassungen nötig)
- Zeit-Aufwand tatsächlich vs. geplant
</output>
