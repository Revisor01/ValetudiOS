---
phase: 33
plan: 04
type: execute
wave: 3
depends_on:
  - "33-02"
  - "33-03"
files_modified:
  - ValetudoApp/AppStore/screenshots/export/de/01-live-map.png
  - ValetudoApp/AppStore/screenshots/export/de/02-map-edit.png
  - ValetudoApp/AppStore/screenshots/export/de/03-goto-spots.png
  - ValetudoApp/AppStore/screenshots/export/de/04-room-order.png
  - ValetudoApp/AppStore/screenshots/export/de/05-notifications.png
  - ValetudoApp/AppStore/screenshots/export/de/06-update-flow.png
  - ValetudoApp/AppStore/screenshots/export/de/07-robot-list.png
  - ValetudoApp/AppStore/screenshots/export/de/08-consumables.png
  - ValetudoApp/AppStore/screenshots/export/de/09-valetudo-control.png
  - ValetudoApp/AppStore/screenshots/export/en/01-live-map.png
  - ValetudoApp/AppStore/screenshots/export/en/02-map-edit.png
  - ValetudoApp/AppStore/screenshots/export/en/03-goto-spots.png
  - ValetudoApp/AppStore/screenshots/export/en/04-room-order.png
  - ValetudoApp/AppStore/screenshots/export/en/05-notifications.png
  - ValetudoApp/AppStore/screenshots/export/en/06-update-flow.png
  - ValetudoApp/AppStore/screenshots/export/en/07-robot-list.png
  - ValetudoApp/AppStore/screenshots/export/en/08-consumables.png
  - ValetudoApp/AppStore/screenshots/export/en/09-valetudo-control.png
  - ValetudoApp/AppStore/screenshots/EXPORT_LOG.md
autonomous: false
requirements:
  - SCREENSHOTS-07
  - SCREENSHOTS-08

must_haves:
  truths:
    - "Alle 9 Slides rendern mit echten Source-Shots (kein Placeholder mehr)"
    - "18 finale PNGs in export/{de,en}/ existieren — jede exakt 1320×2868"
    - "Visueller Review-Pass pro Slide bestanden (Typografie sitzt, kein Abschneiden, Kontrast gut)"
    - "Naming-Convention konsistent: NN-screen-slug.png in export/{locale}/ Verzeichnissen"
  artifacts:
    - path: "ValetudoApp/AppStore/screenshots/export/de/"
      provides: "9 finale deutsche PNGs (1320×2868)"
    - path: "ValetudoApp/AppStore/screenshots/export/en/"
      provides: "9 finale englische PNGs (1320×2868)"
    - path: "ValetudoApp/AppStore/screenshots/EXPORT_LOG.md"
      provides: "Export-Pass-Log mit Review-Notes pro Slide"
  key_links:
    - from: "page.tsx mit allen 18 Slides"
      to: "export/{locale}/{NN-slug}.png"
      via: "html-to-image toPng → Browser-Download → manuelle/scripted Verschiebung in export/-Ordner"
      pattern: "toPng|html-to-image"
---

<objective>
Alle 18 finalen Screenshots produzieren: Die in Plan 02 aufgebauten Slide-Definitionen werden mit den in Plan 03 aufgenommenen echten Source-Shots gerendert und als 1320×2868 PNGs exportiert. Visueller Review-Pass stellt sicher dass jeder Slide upload-tauglich ist.

Purpose: Aus Template + Source-Shots werden die Asset-Dateien die in App Store Connect hochgeladen werden.

Output: 18 PNGs in `export/{de,en}/`, jede exakt 1320×2868, durch visuellen Review-Pass freigegeben. Optional: zusätzliche Bonus-Größen (6.5"/6.3"/6.1") falls das Kit die "gratis" liefert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-app-store-screenshots/33-CONTEXT.md
@.planning/phases/33-app-store-screenshots/33-00-OVERVIEW.md
@.planning/phases/33-app-store-screenshots/33-02-SUMMARY.md
@.planning/phases/33-app-store-screenshots/33-03-SUMMARY.md
@ValetudoApp/AppStore/screenshots/PILOT_DECISION.md
@ValetudoApp/AppStore/screenshots/SOURCE_SHOTS.md
@ValetudoApp/AppStore/screenshots/src/data/screens.ts

<key_facts>
- 18 finale PNGs (9 Screens × 2 Locales)
- Container im Kit ist 1320×2868 → toPng mit pixelRatio: 1 produziert 1320×2868-Output
- Source-Shots in 6.1" (1125-1179px breit) werden im Mockup durch DeviceMockup-Komponente korrekt skaliert
- Naming: `{NN-id}-{locale}.png` z.B. `01-live-map-de.png`, ABER finale Ablage in `export/de/01-live-map.png` (Locale via Ordner, nicht via Suffix — Apple-Upload-Convention)
- Chrome verwenden für Export (Firefox-Bug mit html-to-image)
</key_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-Shots für Screens 2-9 sind im Template eingebunden — Smoke-Test im Dev-Server</name>
  <files>
    ValetudoApp/AppStore/screenshots/EXPORT_LOG.md
  </files>
  <action>
    Bevor exportiert wird: Verifizieren dass alle 18 Slides im Dev-Server mit echten Shots rendern (keine Placeholder/Broken-Image-Icons).

    Schritte:

    1. **Dev-Server starten:**
       ```bash
       cd ValetudoApp/AppStore/screenshots && bun dev
       ```

    2. **Chrome öffnen** und localhost:3000 laden (NICHT Firefox).

    3. **Visueller Smoke-Test** über alle 18 Slides:
       - Alle Source-Shots sind sichtbar in den Mockups (kein graues Broken-Image-Icon)
       - Headlines lesbar (DE und EN je richtige Sprache)
       - Layout-Rotation funktioniert (Adjacent Slides unterscheiden sich visuell)
       - Gradient-Variation pro Screen sichtbar
       - Keine offensichtlichen Layout-Bugs (überlappender Text, abgeschnittene Mockups)

    4. **EXPORT_LOG.md anlegen** mit Smoke-Test-Resultat:
       ```markdown
       # Export Log

       ## Smoke-Test (vor Export)

       **Datum:** [YYYY-MM-DD]
       **Browser:** Chrome [version]
       **Dev-Server:** http://localhost:3000

       | Screen | DE Render | EN Render | Notes |
       |--------|-----------|-----------|-------|
       | 01-live-map | ✓ | ✓ | OK |
       | 02-map-edit | ✓ | ✓ | OK |
       | 03-goto-spots | ✓ | ✓ | Subline knapp am Rand — fixen |
       | ... | | | |
       ```

    5. **Bei kritischen Layout-Bugs:** zurück zu Plan 02 (Template anpassen) bevor Export. Kleinere kosmetische Issues (Padding, Spacing) hier inline fixen — page.tsx oder Komponenten-CSS direkt anpassen.

    Falls 1-2 Slides Probleme haben (z.B. extra-langer Subline schiebt Mockup nach unten):
    - Screen-spezifischer fontSize-Override in screens.ts ergänzen (optionales Feld `sublineFontSize?: number`)
    - Oder Layout für diesen Screen wechseln (z.B. headline-bottom statt headline-overlay)
  </action>
  <verify>
    <automated>test -f ValetudoApp/AppStore/screenshots/EXPORT_LOG.md &amp;&amp; grep -q "Smoke-Test" ValetudoApp/AppStore/screenshots/EXPORT_LOG.md</automated>
  </verify>
  <done>EXPORT_LOG.md mit Smoke-Test-Tabelle (alle 18 Slides ✓ oder dokumentierter Fix), keine Broken-Image-Icons im Dev-Server.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 18 finale PNGs aus Kit exportieren (Browser-Click + Verschieben in export/-Struktur)</name>
  <files>
    ValetudoApp/AppStore/screenshots/export/de/01-live-map.png,
    ValetudoApp/AppStore/screenshots/export/de/02-map-edit.png,
    ValetudoApp/AppStore/screenshots/export/de/03-goto-spots.png,
    ValetudoApp/AppStore/screenshots/export/de/04-room-order.png,
    ValetudoApp/AppStore/screenshots/export/de/05-notifications.png,
    ValetudoApp/AppStore/screenshots/export/de/06-update-flow.png,
    ValetudoApp/AppStore/screenshots/export/de/07-robot-list.png,
    ValetudoApp/AppStore/screenshots/export/de/08-consumables.png,
    ValetudoApp/AppStore/screenshots/export/de/09-valetudo-control.png,
    ValetudoApp/AppStore/screenshots/export/en/01-live-map.png,
    ValetudoApp/AppStore/screenshots/export/en/02-map-edit.png,
    ValetudoApp/AppStore/screenshots/export/en/03-goto-spots.png,
    ValetudoApp/AppStore/screenshots/export/en/04-room-order.png,
    ValetudoApp/AppStore/screenshots/export/en/05-notifications.png,
    ValetudoApp/AppStore/screenshots/export/en/06-update-flow.png,
    ValetudoApp/AppStore/screenshots/export/en/07-robot-list.png,
    ValetudoApp/AppStore/screenshots/export/en/08-consumables.png,
    ValetudoApp/AppStore/screenshots/export/en/09-valetudo-control.png
  </files>
  <what-built>
    Kit-Dev-Server läuft mit allen 18 Slide-Previews (Plan 02-Template + Plan 03-Source-Shots). Pro Slide existiert ein "Export"-Button der via html-to-image den 1320×2868-Container als PNG runterlädt.

    Warum Mensch im Loop: Browser-Download-Klicks sind nicht trivial automatisierbar (Browser-Permissions, Download-Pfad). Alternativ Playwright-Script aufsetzen — aber für 18 Klicks ist manueller Pass schneller.
  </what-built>
  <how-to-verify>
    **Workflow:**

    1. **Output-Ordner anlegen:**
       ```bash
       mkdir -p ValetudoApp/AppStore/screenshots/export/{de,en}
       ```

    2. **Browser-Download-Pfad festlegen** (Chrome Settings → Downloads → "Ask where to save each file" auf OFF, Download-Folder auf z.B. `~/Downloads/screenshots-export/`).

    3. **Pro Slide:**
       - Im Dev-Server auf Export-Button klicken
       - PNG landet in Download-Folder mit Name `{NN-slug}-{locale}.png` (laut page.tsx-Logik aus Plan 02)
       - Datei in korrekten Zielordner verschieben:
         ```bash
         mv ~/Downloads/screenshots-export/01-live-map-de.png \
            ValetudoApp/AppStore/screenshots/export/de/01-live-map.png
         ```
       (Suffix `-de`/`-en` wird beim Verschieben entfernt — Locale steckt im Ordner)

    4. **Bulk-Move-Script** (nach allen Klicks):
       ```bash
       cd ~/Downloads/screenshots-export
       for f in *-de.png; do
         dest_name=${f%-de.png}.png
         mv "$f" "ValetudoApp/AppStore/screenshots/export/de/$dest_name"
       done
       for f in *-en.png; do
         dest_name=${f%-en.png}.png
         mv "$f" "ValetudoApp/AppStore/screenshots/export/en/$dest_name"
       done
       ```

    5. **Validierung pro PNG:**
       ```bash
       for f in ValetudoApp/AppStore/screenshots/export/{de,en}/*.png; do
         w=$(sips -g pixelWidth "$f" | awk '/pixelWidth/ {print $2}')
         h=$(sips -g pixelHeight "$f" | awk '/pixelHeight/ {print $2}')
         echo "$f: ${w}×${h}"
       done
       ```
       Erwartung: alle 18 Files exakt 1320×2868.

    6. **Wenn Output ≠ 1320×2868:**
       - Zu klein (660×1434): pixelRatio im toPng-Aufruf erhöhen → Plan 02 page.tsx fixen
       - Zu groß (2640×5736): pixelRatio: 1 setzen
       - Notlösung pro File: `sips --resampleWidth 1320 file.png` (verlustbehaftet, nur als letzter Ausweg)

    7. **EXPORT_LOG.md aktualisieren:**
       ```markdown
       ## Export Pass

       **Datum:** [YYYY-MM-DD]
       **Anzahl exportiert:** 18
       **Größen-Validierung:** alle 18 = 1320×2868 ✓
       **Bekannte Re-Renders:** [Liste falls Slides nachgerendert wurden wegen Layout-Issue]
       ```

    **Fortschritts-Tracking:**
    ```
    DE: 1✓ 2✓ 3✓ 4✓ 5_ 6_ 7_ 8_ 9_
    EN: 1_ 2_ 3_ 4_ 5_ 6_ 7_ 8_ 9_
    ```
  </how-to-verify>
  <resume-signal>
    Antworte mit:
    - `READY: alle 18 PNGs in export/-Struktur, alle 1320×2868`
    - `BLOCKED: Auflösung falsch — [Details]` (zurück zu Plan 02 Fix nötig)
    - `BLOCKED: Layout-Issue in Slide X — [Details]` (Template-Fix in Plan 02 nötig)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Visueller Review-Pass + Bonus-Größen-Export (optional)</name>
  <files>
    ValetudoApp/AppStore/screenshots/EXPORT_LOG.md,
    ValetudoApp/AppStore/screenshots/export/6.5/,
    ValetudoApp/AppStore/screenshots/export/6.3/,
    ValetudoApp/AppStore/screenshots/export/6.1/
  </files>
  <action>
    **Teil A — Visueller Review-Pass über alle 18 finalen PNGs:**

    Pro PNG in Preview.app öffnen und gegen Checkliste prüfen:

    1. Auflösung exakt 1320×2868 (sips-Validation aus Task 2)
    2. Headline lesbar in 1-Sekunden-Test (Headline 1-Sekunden-Regel aus Kit-README)
    3. Subline lesbar, kein Abschneiden, keine Hyphen-Trennung mitten im Wort
    4. Mockup zentral, Schatten sichtbar, Source-Shot nicht verzerrt
    5. Gradient sauber (kein Banding)
    6. Statusbar im Source-Shot zeigt 9:41 (aus Plan 03 schon validiert, hier Endkontrolle)
    7. Adjacent Slides (z.B. 02 und 03 nebeneinander legen) → klare Layout-Variation
    8. Kein Leak von Browser-UI im Export (kein Scrollbar, kein Address-Bar)

    Befunde in EXPORT_LOG.md festhalten:
    ```markdown
    ## Review Pass

    | Slide | Locale | OK | Issue |
    |-------|--------|----|----|
    | 01-live-map | de | ✓ | — |
    | 01-live-map | en | ✓ | — |
    | 02-map-edit | de | ✗ | Subline-Wort abgeschnitten — re-export nötig |
    | ... | | | |
    ```

    Bei Issues: betroffene Slides in Plan 02 Template fixen → Re-Export → erneut Review.

    **Teil B — Bonus-Größen exportieren (optional, falls Kit das unterstützt):**

    Wenn das Kit eine Bulk-Export-Mechanik für mehrere Größen hat (laut RESEARCH/Kit-README):
    1. Container in page.tsx oder via Kit-Setting auf 1284×2778 (6.5") setzen → 18 Re-Exporte → in `export/6.5/{de,en}/`
    2. Container auf 1206×2622 (6.3") → in `export/6.3/{de,en}/`
    3. Container auf 1170×2532 (6.1") → in `export/6.1/{de,en}/`

    Wenn das Kit das NICHT trivial unterstützt: Bonus-Größen sind explizit OPTIONAL — Apple skaliert 6.9" automatisch. Plan in EXPORT_LOG.md notieren als "skipped — Apple skaliert 6.9 → kleinere Größen automatisch".

    **Standard-Ablage bleibt:** `export/de/NN-slug.png` und `export/en/NN-slug.png` (das sind die 6.9"-Hauptassets — die einzigen die wirklich gebraucht werden).
  </action>
  <verify>
    <automated>COUNT=$(ls ValetudoApp/AppStore/screenshots/export/de/*.png 2&gt;/dev/null | wc -l) &amp;&amp; [ "$COUNT" -eq 9 ] &amp;&amp; COUNT_EN=$(ls ValetudoApp/AppStore/screenshots/export/en/*.png 2&gt;/dev/null | wc -l) &amp;&amp; [ "$COUNT_EN" -eq 9 ] &amp;&amp; for f in ValetudoApp/AppStore/screenshots/export/de/*.png; do W=$(sips -g pixelWidth "$f" | awk '/pixelWidth/ {print $2}'); [ "$W" = "1320" ] || exit 1; done &amp;&amp; grep -q "Review Pass" ValetudoApp/AppStore/screenshots/EXPORT_LOG.md</automated>
  </verify>
  <done>9 PNGs pro Locale in `export/{de,en}/`, alle exakt 1320×2868, Review-Pass-Tabelle in EXPORT_LOG.md mit Befunden pro Slide, kritische Issues vor Plan-Ende behoben. Bonus-Größen entweder exportiert oder explizit als "skipped" dokumentiert.</done>
</task>

</tasks>

<verification>
- 18 PNGs in `export/{de,en}/` (9 pro Locale)
- Alle exakt 1320×2868
- EXPORT_LOG.md mit Smoke-Test, Export-Pass und Review-Pass dokumentiert
- Keine kritischen Visual-Issues offen
</verification>

<success_criteria>
1. `export/de/01-live-map.png` bis `export/de/09-valetudo-control.png` existieren (9 Files)
2. `export/en/01-live-map.png` bis `export/en/09-valetudo-control.png` existieren (9 Files)
3. Alle 18 PNGs sind exakt 1320×2868
4. Visueller Review-Pass dokumentiert in EXPORT_LOG.md
5. Adjacent Slides nutzen unterschiedliche Layouts (visuelle Bestätigung)
6. Bonus-Größen entweder vorhanden ODER explizit als optional/skipped notiert
</success_criteria>

<output>
Summary: `.planning/phases/33-app-store-screenshots/33-04-SUMMARY.md` mit:
- Anzahl exportierter Assets (18 Pflicht + ggf. Bonus)
- Re-Render-Liste (welche Slides mussten 2× exportiert werden, warum)
- Pixelratio-Konfig die funktioniert hat
- Übergabe an Plan 05 (welche Files in welcher Struktur)
</output>
