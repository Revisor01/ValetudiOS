---
phase: 25-view-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  - ValetudoApp/ValetudoApp/Views/Settings/AutoEmptyDockSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/Settings/QuirksView.swift
  - ValetudoApp/ValetudoApp/Views/Settings/WifiSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/Settings/MQTTSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/Settings/NTPSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/Settings/StationSettingsView.swift
autonomous: true
requirements: [VIEW-02]

must_haves:
  truths:
    - "Jeder der 6 Structs aus RobotSettingsSections.swift lebt in einer eigenen Datei unter Views/Settings/"
    - "RobotSettingsSections.swift existiert nicht mehr"
    - "Jede neue Datei hat einen dateispezifischen Logger (nicht den alten sectionsLogger)"
    - "Build kompiliert erfolgreich — alle NavigationLink-Referenzen auf StationSettingsView etc. funktionieren weiterhin"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/Settings/AutoEmptyDockSettingsView.swift"
      provides: "Auto-Empty-Dock Einstellungen"
    - path: "ValetudoApp/ValetudoApp/Views/Settings/QuirksView.swift"
      provides: "Quirks-Verwaltung"
    - path: "ValetudoApp/ValetudoApp/Views/Settings/WifiSettingsView.swift"
      provides: "WiFi-Konfiguration"
    - path: "ValetudoApp/ValetudoApp/Views/Settings/MQTTSettingsView.swift"
      provides: "MQTT-Konfiguration"
    - path: "ValetudoApp/ValetudoApp/Views/Settings/NTPSettingsView.swift"
      provides: "NTP-Konfiguration"
    - path: "ValetudoApp/ValetudoApp/Views/Settings/StationSettingsView.swift"
      provides: "Station/Dock-Einstellungen"
  key_links:
    - from: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      to: "Views/Settings/StationSettingsView.swift"
      via: "NavigationLink(destination:)"
      pattern: "StationSettingsView\\(robot:"
    - from: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      to: "Views/Settings/*.swift"
      via: "NavigationLink(destination:)"
      pattern: "(QuirksView|WifiSettingsView|MQTTSettingsView|NTPSettingsView|AutoEmptyDockSettingsView)\\("
---

<objective>
RobotSettingsSections.swift (1079 Zeilen, 6 eigenstaendige Structs) in einzelne Dateien unter Views/Settings/ aufteilen.

Purpose: VIEW-02 — Reine Dateitrennung, kein Code-Umbau. Die Structs sind bereits vollstaendig eigenstaendig.
Output: 6 neue Dateien in Views/Settings/, RobotSettingsSections.swift geloescht.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-view-architecture/25-RESEARCH.md
@ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Erstelle Views/Settings/ und verschiebe alle 6 Structs in eigene Dateien</name>
  <files>
    ValetudoApp/ValetudoApp/Views/Settings/AutoEmptyDockSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/Settings/QuirksView.swift,
    ValetudoApp/ValetudoApp/Views/Settings/WifiSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/Settings/MQTTSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/Settings/NTPSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/Settings/StationSettingsView.swift
  </files>
  <action>
    Lies RobotSettingsSections.swift vollstaendig. Erstelle das Verzeichnis ValetudoApp/ValetudoApp/Views/Settings/.

    Verschiebe jeden Struct in seine eigene Datei. Die Zeilenbereiche aus der Research:
    - AutoEmptyDockSettingsView: Zeilen 7-81
    - QuirksView: Zeilen 84-192
    - WifiSettingsView: Zeilen 195-408
    - MQTTSettingsView: Zeilen 411-585
    - NTPSettingsView: Zeilen 587-772
    - StationSettingsView: Zeilen 774-1079

    **WICHTIG fuer jede neue Datei:**
    1. `import SwiftUI` am Anfang
    2. `import os` (fuer Logger)
    3. Ersetze den alten `sectionsLogger` durch einen dateispezifischen Logger:
       ```swift
       private let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudoApp", category: "StructName")
       ```
       wobei "StructName" der jeweilige Struct-Name ist (z.B. "QuirksView", "WifiSettingsView")
    4. Pruefe ob die Datei weitere Imports braucht (z.B. `import Foundation` falls explizit verwendet)
    5. Kopiere den gesamten Struct-Code 1:1 — keine inhaltlichen Aenderungen

    Lies die Datei genau — der `sectionsLogger` auf Zeile 4 wird in allen Structs referenziert. In jeder neuen Datei muss `sectionsLogger` durch `logger` ersetzt werden (oder der Logger-Name beibehalten, aber dateilokal definiert).
  </action>
  <verify>
    <automated>ls ValetudoApp/ValetudoApp/Views/Settings/*.swift | wc -l</automated>
  </verify>
  <done>6 neue Swift-Dateien in Views/Settings/, jede mit eigenem Logger und vollstaendigem Struct</done>
</task>

<task type="auto">
  <name>Task 2: Loesche RobotSettingsSections.swift und verifiziere Build</name>
  <files>ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift</files>
  <action>
    1. Loesche ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift komplett (rm)
    2. Fuehre `cd ValetudoApp && xcodegen generate` aus, damit die neuen Dateien im Projekt registriert und die alte entfernt wird
    3. Fuehre `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet` aus

    **Moegliche Build-Fehler und Fixes:**
    - "Use of unresolved identifier 'sectionsLogger'" → Logger-Replacement in der betroffenen Datei vergessen
    - "Invalid redeclaration of 'StationSettingsView'" → RobotSettingsSections.swift wurde nicht geloescht
    - "Cannot find 'QuirksView' in scope" → Datei nicht im richtigen Verzeichnis oder xcodegen nicht ausgefuehrt
  </action>
  <verify>
    <automated>test ! -f ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift && cd ValetudoApp && xcodegen generate 2>&1 && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>RobotSettingsSections.swift existiert nicht mehr, Build kompiliert erfolgreich, alle 6 Settings-Views sind unter Views/Settings/ auffindbar</done>
</task>

</tasks>

<verification>
- `test ! -f ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift` — Quelldatei geloescht
- `ls ValetudoApp/ValetudoApp/Views/Settings/ | wc -l` zeigt 6 Dateien
- `xcodebuild build` kompiliert ohne Fehler
- Keine Datei in Views/Settings/ ueberschreitet 400 Zeilen (groesste ist StationSettingsView mit ~306 Zeilen)
</verification>

<success_criteria>
- RobotSettingsSections.swift existiert nicht mehr
- 6 einzelne Dateien in Views/Settings/
- Jede Datei hat dateispezifischen Logger
- Xcode-Build erfolgreich
</success_criteria>

<output>
After completion, create `.planning/phases/25-view-architecture/25-02-SUMMARY.md`
</output>
