---
phase: 25-view-architecture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/MapView.swift
  - ValetudoApp/ValetudoApp/Views/MapDrawingOverlay.swift
  - ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift
autonomous: true
requirements: [VIEW-03]

must_haves:
  truths:
    - "MapView.swift (enthaelt MapContentView) ist unter 400 Zeilen"
    - "Alle Drawing/Overlay-Logik und finishDrawing leben in separaten Dateien als Extensions auf MapContentView"
    - "Keine funktionale Aenderung — identisches Verhalten wie vorher"
    - "Build kompiliert erfolgreich"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/MapDrawingOverlay.swift"
      provides: "Drawing-Overlay, Split-Line-Handles, finishDrawing, calculateMapParams, combinedGesture als Extension auf MapContentView"
    - path: "ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift"
      provides: "GoTo-Marker, Preset-Marker, Restriction-Delete-Overlay als Extension auf MapContentView"
    - path: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      provides: "MapContentView body + State, unter 400 Zeilen"
  key_links:
    - from: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      to: "MapDrawingOverlay.swift"
      via: "Swift extension (same struct, different file)"
      pattern: "extension MapContentView"
    - from: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      to: "MapOverlayViews.swift"
      via: "Swift extension (same struct, different file)"
      pattern: "extension MapContentView"
---

<objective>
MapView.swift (863 Zeilen, davon MapContentView ~663 Zeilen) unter 400 Zeilen reduzieren.

Purpose: VIEW-03 — Die @State-Properties bleiben korrekt in der View (Gesture/Layout-State). Die Datei wird durch Auslagerung von Methoden in Extension-Dateien verkleinert. Swift erlaubt Extensions auf denselben Struct in verschiedenen Dateien.
Output: MapView.swift unter 400 Zeilen, 2 neue Extension-Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-view-architecture/25-RESEARCH.md
@ValetudoApp/ValetudoApp/Views/MapView.swift
@ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrahiere Drawing/Gesture-Methoden in MapDrawingOverlay.swift</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapDrawingOverlay.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </files>
  <action>
    Lies MapView.swift vollstaendig.

    Erstelle MapDrawingOverlay.swift mit einer Extension auf MapContentView, die folgende Methoden enthaelt:

    1. **drawingOverlay(geometry:)** (aktuell Zeilen ~474-498) — @ViewBuilder func, Drawing-Gesture-Overlay
    2. **splitLineHandles(geometry:)** (aktuell Zeilen ~501-589) — @ViewBuilder func, Split-Room-Drag-Handles
    3. **finishDrawing(in:)** (aktuell Zeilen ~676-781) — func, konvertiert Zeichnungs-Koordinaten und dispatcht an ViewModel
    4. **calculateMapParams(layers:pixelSize:size:)** (aktuell Zeilen ~784-808) — func, lokale MapParams-Berechnung
    5. **combinedGesture** (aktuell Zeilen ~811-832) — computed var, Pinch+Drag-Gesture

    Die Datei:
    ```swift
    import SwiftUI

    // MARK: - MapContentView Drawing & Gesture Extensions
    extension MapContentView {
        // drawingOverlay, splitLineHandles, finishDrawing, calculateMapParams, combinedGesture
    }
    ```

    **WICHTIG:**
    - Alle diese Methoden greifen auf `self.currentDrawStart`, `self.scale`, `self.offset` etc. zu — das funktioniert, weil es Extensions auf denselben Struct sind
    - `screenToMapCoords` und `mapToScreenCoords` bleiben in MapView.swift (werden im body referenziert) ODER wandern ebenfalls hierher — entscheide basierend auf dem Ergebnis. Wenn sie nur von den Drawing-Methoden aufgerufen werden, hierher. Wenn sie auch im body direkt aufgerufen werden (z.B. fuer GoTo-Marker), in MapView.swift lassen oder in beide Extension-Files duplizieren (NEIN — lieber eine eigene Extension-Datei oder in MapView.swift lassen)
    - `screenToMapCoords` und `mapToScreenCoords` werden sowohl im body (GoTo-Marker Drag) als auch in den Drawing-Methoden verwendet — sie sollten in genau EINER Extension-Datei leben und von allen anderen referenziert werden. Am besten in MapDrawingOverlay.swift, da sie thematisch zur Koordinaten-Transformation gehoeren

    Loesche die verschobenen Methoden aus MapView.swift.
  </action>
  <verify>
    <automated>test -f ValetudoApp/ValetudoApp/Views/MapDrawingOverlay.swift && echo "File exists"</automated>
  </verify>
  <done>MapDrawingOverlay.swift existiert mit allen 5+ Methoden als Extension auf MapContentView</done>
</task>

<task type="auto">
  <name>Task 2: Extrahiere Overlay-Views in MapOverlayViews.swift und verifiziere Build</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </files>
  <action>
    Erstelle MapOverlayViews.swift mit einer weiteren Extension auf MapContentView, die folgende Methoden enthaelt:

    1. **restrictionDeleteOverlay(params:restrictions:viewSize:)** (aktuell Zeilen ~592-674) — @ViewBuilder func, Loeschen-Buttons auf Restriktionen

    Zusaetzlich: Extrahiere die GoTo-Marker-View und die Preset-Marker-View aus dem body in MapView.swift als eigene @ViewBuilder-Methoden in dieser Extension:

    2. **goToMarkerOverlay(params:pixelSize:geometry:)** — Der Block ab Zeile ~254-292 (Circle mit DragGesture fuer GoTo/SavePreset)
    3. **presetMarkersOverlay(params:pixelSize:geometry:)** — Der Block ab Zeile ~295-323 (ForEach ueber presetStore.presets)

    Im body von MapContentView (MapView.swift) ersetze die inline-Bloecke durch Aufrufe:
    ```swift
    // Statt inline GoTo-Marker:
    goToMarkerOverlay(params: params, pixelSize: pixelSize, geometry: geometry)

    // Statt inline Preset-Markers:
    presetMarkersOverlay(params: params, pixelSize: pixelSize, geometry: geometry)
    ```

    Die Datei:
    ```swift
    import SwiftUI

    // MARK: - MapContentView Overlay Extensions
    extension MapContentView {
        // restrictionDeleteOverlay, goToMarkerOverlay, presetMarkersOverlay
    }
    ```

    **Nach allen Extraktionen: Pruefe Zeilenzahl von MapView.swift.** Ziel ist unter 400 Zeilen.

    Berechnung:
    - Original: 863 Zeilen
    - Entfernt: drawingOverlay (~25) + splitLineHandles (~89) + restrictionDeleteOverlay (~83) + finishDrawing (~106) + calculateMapParams (~25) + combinedGesture (~22) + screenToMapCoords/mapToScreenCoords (~14) + GoTo-Marker-Inline (~41) + Preset-Markers-Inline (~29) = ~434 Zeilen
    - Plus neue Aufrufe statt Inline: ~6 Zeilen
    - Ergebnis: 863 - 434 + 6 = ~435 Zeilen

    Falls MapView.swift nach Extraktion immer noch ueber 400 Zeilen ist: Verschiebe die Toolbar-Methoden oder die Sheet-Definitionen (.sheet Modifier-Bloecke, Zeilen ~404-452, ~48 Zeilen) als weitere Extension in MapOverlayViews.swift oder MapDrawingOverlay.swift. Das wuerde auf ~387 Zeilen bringen.

    Fuehre `cd ValetudoApp && xcodegen generate` und dann `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet` aus.
  </action>
  <verify>
    <automated>cd ValetudoApp && xcodegen generate 2>&1 && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5 && echo "---" && wc -l ValetudoApp/Views/MapView.swift</automated>
  </verify>
  <done>MapView.swift unter 400 Zeilen, Build kompiliert erfolgreich, MapDrawingOverlay.swift und MapOverlayViews.swift existieren</done>
</task>

</tasks>

<verification>
- `wc -l ValetudoApp/ValetudoApp/Views/MapView.swift` zeigt unter 400 Zeilen
- `wc -l ValetudoApp/ValetudoApp/Views/MapDrawingOverlay.swift ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift` zeigt die extrahierten Dateien
- `xcodebuild build` kompiliert ohne Fehler
- Keine funktionale Aenderung
</verification>

<success_criteria>
- MapView.swift unter 400 Zeilen
- Drawing/Gesture/Overlay-Logik sauber in Extension-Dateien ausgelagert
- Xcode-Build erfolgreich
- @State-Properties bleiben korrekt in MapContentView (nicht in ViewModel verschoben)
</success_criteria>

<output>
After completion, create `.planning/phases/25-view-architecture/25-03-SUMMARY.md`
</output>
