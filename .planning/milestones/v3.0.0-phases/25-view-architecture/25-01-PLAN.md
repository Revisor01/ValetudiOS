---
phase: 25-view-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/UpdateStatusBannerView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/UpdateOverlayView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/RobotControlSectionView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/LiveStatsChipView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/AttachmentChipsView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/ConsumablesPreviewSectionView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/StatisticsSectionView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/RoomsSectionView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/EventsSectionView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/CleanRouteSectionView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/ObstaclesSectionView.swift
autonomous: true
requirements: [VIEW-01]

must_haves:
  truths:
    - "RobotDetailView.swift enthaelt nur Struct-Definition, init, body-Orchestrierung und Modifiers — keine Section-Implementierungen"
    - "Jede Section (Update-Banner, Status-Header, Control, Consumables, Statistics, Rooms, Events, CleanRoute, Obstacles, Attachments, LiveStats) lebt als eigenstaendiger Struct in Views/Detail/"
    - "Alle Sub-Views erhalten viewModel als let/Bindable-Parameter — kein Environment-Workaround"
    - "RobotDetailView.swift ist unter 250 Zeilen"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/Detail/UpdateStatusBannerView.swift"
      provides: "Update-Status-Banner (5 Zustaende)"
    - path: "ValetudoApp/ValetudoApp/Views/Detail/RobotControlSectionView.swift"
      provides: "Control-Buttons und Intensitaets-Menus"
    - path: "ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift"
      provides: "Kompakter Status-Header mit Batterie"
    - path: "ValetudoApp/ValetudoApp/Views/Detail/StatisticsSectionView.swift"
      provides: "Statistik-Accordion mit Rows"
    - path: "ValetudoApp/ValetudoApp/Views/Detail/RoomsSectionView.swift"
      provides: "Raeume-Section mit Auswahl"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "Orchestrierungs-View unter 250 Zeilen"
  key_links:
    - from: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      to: "Views/Detail/*.swift"
      via: "Direct struct instantiation in body"
      pattern: "View\\(viewModel:"
---

<objective>
RobotDetailView.swift (1210 Zeilen) in eigenstaendige Section-Sub-Views aufteilen.

Purpose: VIEW-01 — Jede Section wird ein eigenstaendiger Struct unter Views/Detail/. RobotDetailView.swift wird zur reinen Orchestrierungs-View (~150-200 Zeilen).
Output: 12 neue Dateien in Views/Detail/, RobotDetailView.swift stark reduziert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-view-architecture/25-RESEARCH.md
@ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Erstelle Views/Detail/ Verzeichnis und extrahiere alle Section-Sub-Views</name>
  <files>
    ValetudoApp/ValetudoApp/Views/Detail/UpdateStatusBannerView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/UpdateOverlayView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/RobotControlSectionView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/LiveStatsChipView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/AttachmentChipsView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/ConsumablesPreviewSectionView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/StatisticsSectionView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/RoomsSectionView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/EventsSectionView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/CleanRouteSectionView.swift,
    ValetudoApp/ValetudoApp/Views/Detail/ObstaclesSectionView.swift
  </files>
  <action>
    Lies RobotDetailView.swift vollstaendig. Erstelle das Verzeichnis ValetudoApp/ValetudoApp/Views/Detail/.

    Fuer JEDE @ViewBuilder private var Section in RobotDetailView.swift:

    1. **UpdateStatusBannerView** (Zeilen ~26-178 im body, der gesamte if/else if-Block fuer Update-Zustaende):
       - Struct mit `let viewModel: RobotDetailViewModel` und `@Binding var showUpdateWarning: Bool`
       - Alle 5 Zustaende (updateAvailable, downloading, readyToApply, inProgress, error) plus Fallback
       - `import SwiftUI`

    2. **UpdateOverlayView** (Zeilen ~319-358, updateOverlayView + updateOverlayTitle + updateOverlaySubtitle):
       - Struct mit `let viewModel: RobotDetailViewModel`
       - Enthaelt die computed properties updateOverlayTitle und updateOverlaySubtitle

    3. **RobotStatusHeaderView** (Zeilen ~360-477, compactStatusHeader + localizedStatus + statusColor + batteryIcon + batteryColor):
       - Struct mit `let viewModel: RobotDetailViewModel`
       - Alle Hilfs-Properties (localizedStatus, statusColor, batteryIcon, batteryColor) wandern in diesen Struct

    4. **RobotControlSectionView** (Zeilen ~478-669, controlSection + displayNameForOperationMode + iconForOperationMode):
       - Struct mit `@Bindable var viewModel: RobotDetailViewModel` (braucht Binding fuer Picker)
       - displayNameForOperationMode und iconForOperationMode als private Funktionen im Struct

    5. **LiveStatsChipView** (Zeilen ~670-715, liveStatsChip + PulseAnimation):
       - Struct mit `let viewModel: RobotDetailViewModel`
       - PulseAnimation-Struct bleibt in derselben Datei

    6. **AttachmentChipsView** (Zeilen ~724-773, attachmentChips + attachmentChip + hasAnyAttachmentInfo):
       - Struct mit `let viewModel: RobotDetailViewModel`
       - hasAnyAttachmentInfo und attachmentChip(icon:label:attached:) als private Members

    7. **ConsumablesPreviewSectionView** (Zeilen ~775-846):
       - Struct mit `let viewModel: RobotDetailViewModel`

    8. **StatisticsSectionView** (Zeilen ~847-992, statisticsSection + statisticRow + icon/color/label/formattedValue-Hilfsfunktionen):
       - Struct mit `let viewModel: RobotDetailViewModel`
       - Alle Hilfsfunktionen (iconForStatType, colorForStatType, labelForStatType, formattedValue) wandern mit

    9. **RoomsSectionView** (Zeilen ~993-1098):
       - Struct mit `let viewModel: RobotDetailViewModel`
       - Braucht @Environment(RobotManager.self) falls auf robotManager zugegriffen wird — pruefe den Code

    10. **EventsSectionView** (Zeilen ~1099-1140):
        - Struct mit `let viewModel: RobotDetailViewModel`

    11. **CleanRouteSectionView** (Zeilen ~1140-1161):
        - Struct mit `@Bindable var viewModel: RobotDetailViewModel` (falls Picker-Binding)

    12. **ObstaclesSectionView** (Zeilen ~1161-Ende):
        - Struct mit `let viewModel: RobotDetailViewModel`

    **Fuer alle neuen Dateien:**
    - `import SwiftUI` am Anfang
    - Kein Logger noetig (Views loggen nicht, ViewModel loggt)
    - Da RobotDetailViewModel @Observable (class) ist, reicht `let viewModel` fuer Lese-Zugriff. Nur `@Bindable var viewModel` wo $-Binding noetig ist (Picker, Toggle etc.)
    - KEIN @Environment(RobotManager.self) in Sub-Views — der ViewModel hat bereits robotManager als Property
  </action>
  <verify>
    <automated>ls ValetudoApp/ValetudoApp/Views/Detail/*.swift | wc -l</automated>
  </verify>
  <done>12 neue Swift-Dateien existieren in Views/Detail/, jede enthaelt genau einen eigenstaendigen View-Struct</done>
</task>

<task type="auto">
  <name>Task 2: RobotDetailView.swift auf Orchestrierung reduzieren und Build verifizieren</name>
  <files>ValetudoApp/ValetudoApp/Views/RobotDetailView.swift</files>
  <action>
    Ersetze in RobotDetailView.swift ALLE inline-Sections und @ViewBuilder-Properties durch Aufrufe der neuen Sub-View-Structs:

    **body soll so aussehen (Pseudocode):**
    ```swift
    var body: some View {
        List {
            UpdateStatusBannerView(viewModel: viewModel, showUpdateWarning: $showUpdateWarning)

            Section {
                RobotStatusHeaderView(viewModel: viewModel)
                    .listRowSeparator(.hidden)

                if viewModel.status?.isOnline == true {
                    MapPreviewView(robot: viewModel.robot, showFullMap: $showFullMap)
                        .listRowSeparator(.hidden)

                    HStack(spacing: 8) {
                        if AttachmentChipsView.hasAnyAttachmentInfo(viewModel) {
                            AttachmentChipsView(viewModel: viewModel)
                        }
                        Spacer()
                        LiveStatsChipView(viewModel: viewModel)
                    }
                    .listRowInsets(...)
                    .listRowSeparator(.hidden)
                }
            }

            if viewModel.status?.isOnline == true {
                RobotControlSectionView(viewModel: viewModel)
                CleanRouteSectionView(viewModel: viewModel)
                RoomsSectionView(viewModel: viewModel)
                ConsumablesPreviewSectionView(viewModel: viewModel)
                EventsSectionView(viewModel: viewModel)
                ObstaclesSectionView(viewModel: viewModel)
                StatisticsSectionView(viewModel: viewModel)

                // Settings Section bleibt inline (nur NavigationLinks, ~40 Zeilen)
                Section { ... NavigationLinks ... } header: { ... }
            }
        }
        // .navigationTitle, .sheet, .alert, .overlay, .task, .refreshable, .onChange, .onAppear, .onDisappear bleiben
        .overlay {
            if showUpdateOverlay {
                UpdateOverlayView(viewModel: viewModel)
            }
        }
    }
    ```

    **Loesche ALLE @ViewBuilder private var Sections** und die beiden Extension-Bloecke aus RobotDetailView.swift. Behalte NUR:
    - Struct-Definition mit @State, @Environment, init (~22 Zeilen)
    - showUpdateOverlay computed property (~8 Zeilen)
    - var body mit Sub-View-Aufrufen und Modifiers (~100-150 Zeilen)

    **hasAnyAttachmentInfo:** Diese Property muss entweder als statische Methode auf AttachmentChipsView oder als computed property im ViewModel leben. Entscheide pragmatisch — am einfachsten: die Pruefung direkt im body von AttachmentChipsView machen (leere View wenn keine Attachments) und den Guard in RobotDetailView entfernen.

    Fuehre `xcodegen generate` aus dem ValetudoApp/-Verzeichnis aus. Dann `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet` um den Build zu verifizieren.
  </action>
  <verify>
    <automated>cd ValetudoApp && xcodegen generate 2>&1 && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>RobotDetailView.swift ist unter 250 Zeilen, Build kompiliert erfolgreich, alle Sub-Views in Views/Detail/ werden korrekt referenziert</done>
</task>

</tasks>

<verification>
- `wc -l ValetudoApp/ValetudoApp/Views/RobotDetailView.swift` zeigt unter 250 Zeilen
- `ls ValetudoApp/ValetudoApp/Views/Detail/ | wc -l` zeigt 12 Dateien
- `xcodebuild build` kompiliert ohne Fehler
- Keine @ViewBuilder private var mehr in RobotDetailView.swift (ausser body)
</verification>

<success_criteria>
- RobotDetailView.swift unter 250 Zeilen — reine Orchestrierung
- 12 eigenstaendige Sub-View-Structs in Views/Detail/
- Xcode-Build erfolgreich
- Keine funktionale Aenderung — nur strukturelle Aufteilung
</success_criteria>

<output>
After completion, create `.planning/phases/25-view-architecture/25-01-SUMMARY.md`
</output>
