---
phase: 24-map-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
autonomous: true
requirements: [PERF-01, PERF-03]

must_haves:
  truths:
    - "Room-Tap auf der Karte nutzt O(1) Set-Lookup statt linearem Pixel-Scan"
    - "segmentInfos wird einmal pro Map-Update berechnet und zwischen tapTargets und orderBadges geteilt"
    - "Hit-Testing fuehlt sich instant an — kein spuerbarer Delay beim Tippen auf Raeume"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "segmentPixelSets Dictionary und cachedSegmentInfos Array"
      contains: "segmentPixelSets"
    - path: "ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift"
      provides: "O(1) Hit-Testing und gecachte segmentInfos Nutzung"
      contains: "pixelSet.contains"
  key_links:
    - from: "MapViewModel.map didSet"
      to: "rebuildSegmentPixelSets + updateCachedSegmentInfos"
      via: "called when map property changes"
      pattern: "rebuildSegmentPixelSets"
    - from: "InteractiveMapView.handleCanvasTap"
      to: "MapViewModel.segmentPixelSets"
      via: "O(1) Set lookup per segment"
      pattern: "pixelSet.contains"
    - from: "InteractiveMapView.tapTargetsOverlay"
      to: "MapViewModel.cachedSegmentInfos"
      via: "reads cached array instead of computing"
      pattern: "cachedSegmentInfos"
---

<objective>
Spatial Lookup fuer Hit-Testing und segmentInfos-Caching implementieren.

Purpose: Ersetzt den O(n) linearen Pixel-Scan beim Room-Tap durch O(1) Set-Lookup. Cached segmentInfos einmal pro Map-Update statt bei jedem Overlay-Render.
Output: MapViewModel haelt segmentPixelSets und cachedSegmentInfos, InteractiveMapView nutzt diese gecachten Strukturen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-map-performance/24-RESEARCH.md

@ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
@ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift

<interfaces>
<!-- Key types and contracts the executor needs -->

From MapInteractiveView.swift:
```swift
struct InteractiveMapView: View {
    let map: RobotMap
    let segments: [Segment]
    @Binding var selectedSegmentIds: [String]
    let viewSize: CGSize

    // Private struct — will be moved to MapViewModel:
    private struct SegmentInfo: Identifiable {
        let id: String   // segmentId
        let name: String  // display name
        let midX: Int     // pixel coordinate centroid X
        let midY: Int     // pixel coordinate centroid Y
    }

    // Private method — will be moved to MapViewModel:
    private func segmentInfos(from layers: [MapLayer]) -> [SegmentInfo]
    // Called in: tapTargetsOverlay (line 151), orderBadgesOverlay (line 199)

    // Private method — will use Set lookup:
    private func handleCanvasTap(at location: CGPoint, size: CGSize)
    // Current: O(n) linear scan of decompressedPixels per segment layer
}
```

From MapViewModel.swift:
```swift
@MainActor @Observable
final class MapViewModel {
    var map: RobotMap?
    var segments: [Segment] = []
    @ObservationIgnored private var refreshTask: Task<Void, Never>?

    func loadMap() async  // sets self.map and self.segments
    func startMapRefresh()  // updates self.map on each SSE event
}
```

From RobotMap model:
```swift
struct RobotMap: Codable {
    var pixelSize: Int?
    var layers: [MapLayer]?
    var entities: [MapEntity]?
}

struct MapLayer: Codable {
    var type: String       // "floor", "segment", "wall"
    var metaData: MapLayerMetaData?
    var decompressedPixels: [Int]  // flat [x,y,x,y,...] pairs
    var dimensions: MapLayerDimensions?
}

struct MapLayerMetaData: Codable {
    var segmentId: String?
    var name: String?
    var material: String?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SegmentInfo und segmentPixelSets in MapViewModel (PERF-01 + PERF-03)</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
    - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  </read_first>
  <action>
MapViewModel um drei Dinge erweitern:

**1. SegmentInfo struct (aus InteractiveMapView hierher verschieben):**

Direkt oberhalb der MapViewModel-Klasse oder als nested type definieren:
```swift
struct SegmentInfo: Identifiable {
    let id: String
    let name: String
    let midX: Int
    let midY: Int
}
```

**2. Neue Properties auf MapViewModel:**

Im `// MARK: - Map Data State` Bereich hinzufuegen:
```swift
// MARK: - Cached Computations (PERF-01, PERF-03)
@ObservationIgnored var segmentPixelSets: [String: Set<Int>] = [:]
var cachedSegmentInfos: [SegmentInfo] = []
```

`segmentPixelSets` ist `@ObservationIgnored` weil SwiftUI es nicht beobachten muss (nur fuer Hit-Testing). `cachedSegmentInfos` ist NICHT ignored weil die Overlays darauf reagieren muessen.

**3. Zwei private rebuild-Methoden:**

```swift
// MARK: - Performance Caching

private func rebuildSegmentPixelSets() {
    guard let layers = map?.layers else {
        segmentPixelSets = [:]
        return
    }
    var sets: [String: Set<Int>] = [:]
    for layer in layers where layer.type == "segment" {
        guard let id = layer.metaData?.segmentId else { continue }
        let pixels = layer.decompressedPixels
        var set = Set<Int>(minimumCapacity: pixels.count / 2)
        var i = 0
        while i < pixels.count - 1 {
            set.insert(pixels[i] &<< 16 | pixels[i + 1])
            i += 2
        }
        sets[id] = set
    }
    segmentPixelSets = sets
}

private func updateCachedSegmentInfos() {
    guard let layers = map?.layers else {
        cachedSegmentInfos = []
        return
    }
    var infos: [SegmentInfo] = []
    for layer in layers where layer.type == "segment" {
        guard let segmentId = layer.metaData?.segmentId else { continue }
        var midX: Int? = layer.dimensions?.x?.mid
        var midY: Int? = layer.dimensions?.y?.mid
        if midX == nil || midY == nil {
            let pixels = layer.decompressedPixels
            if pixels.count >= 2 {
                var sumX = 0, sumY = 0, count = 0
                var i = 0
                while i < pixels.count - 1 {
                    sumX += pixels[i]
                    sumY += pixels[i + 1]
                    count += 1
                    i += 2
                }
                if count > 0 {
                    midX = midX ?? (sumX / count)
                    midY = midY ?? (sumY / count)
                }
            }
        }
        guard let finalMidX = midX, let finalMidY = midY else { continue }
        let name = segments.first { $0.id == segmentId }?.displayName
            ?? layer.metaData?.name
            ?? String(localized: "map.room") + " \(segmentId)"
        infos.append(SegmentInfo(id: segmentId, name: name, midX: finalMidX, midY: finalMidY))
    }
    cachedSegmentInfos = infos
}
```

**4. Aufrufe in loadMap() und startMapRefresh() einbauen:**

In `loadMap()`, nach `map = loadedMap` und `segments = loadedSegments` (ca. Zeile 144-145):
```swift
map = loadedMap
segments = loadedSegments
rebuildSegmentPixelSets()
updateCachedSegmentInfos()
```

In `startMapRefresh()`, nach jedem `self.map = newMap` — das betrifft ALLE Stellen im SSE-Loop und im Fallback-Code wo `self.map` gesetzt wird. An jeder Stelle nach `self.map = newMap` hinzufuegen:
```swift
self.rebuildSegmentPixelSets()
self.updateCachedSegmentInfos()
```

HINWEIS: Falls Plan 01 schon ausgefuehrt wurde und `startMapRefresh()` den SSE-Code enthaelt, muessen die Aufrufe an allen Stellen im SSE-Code eingefuegt werden wo `self.map = newMap` steht. Falls Plan 01 noch NICHT ausgefuehrt wurde (alter Polling-Code), an der Stelle `self.map = newMap` im Polling-Loop einfuegen.

**5. Auch bei Segment-Aenderungen (rename, join, split):**

In `renameRoom()`, nach `segments = newSegments` (ca. Zeile 278):
```swift
updateCachedSegmentInfos()
```

In `joinRooms()`, nach `self.map = newMap` und nach `self.segments = newSegments`:
```swift
rebuildSegmentPixelSets()
updateCachedSegmentInfos()
```

In `splitRoom()`, nach `self.map = newMap` und nach `self.segments = newSegments`:
```swift
rebuildSegmentPixelSets()
updateCachedSegmentInfos()
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "segmentPixelSets\|cachedSegmentInfos\|rebuildSegmentPixelSets\|updateCachedSegmentInfos" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - SegmentInfo struct existiert (oeffentlich zugaenglich, nicht private)
    - segmentPixelSets ist @ObservationIgnored und enthaelt Set<Int> pro Segment-ID
    - cachedSegmentInfos ist ein [SegmentInfo] Array
    - rebuildSegmentPixelSets() wird nach jedem map-Update aufgerufen
    - updateCachedSegmentInfos() wird nach jedem map- oder segments-Update aufgerufen
    - Pixel-Encoding nutzt `&<< 16 |` fuer Int-Key
  </acceptance_criteria>
  <done>MapViewModel cached segmentPixelSets und segmentInfos, rebuilt bei jedem Map-Update</done>
</task>

<task type="auto">
  <name>Task 2: InteractiveMapView auf gecachte Daten umstellen (PERF-01 + PERF-03)</name>
  <files>ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
    - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  </read_first>
  <action>
InteractiveMapView auf die gecachten Daten aus MapViewModel umstellen:

**1. Neue Properties hinzufuegen:**

Unter den bestehenden Properties (nach `var showRoomLabels: Bool = true`):
```swift
var segmentPixelSets: [String: Set<Int>] = [:]
var cachedSegmentInfos: [SegmentInfo] = []
```

**2. Private SegmentInfo struct ENTFERNEN:**

Die private struct `SegmentInfo` (Zeilen 257-262) komplett entfernen — sie ist jetzt in MapViewModel definiert.

**3. Private Methode `segmentInfos(from:)` ENTFERNEN:**

Die gesamte Methode (Zeilen 264-303) entfernen — die Berechnung passiert jetzt in MapViewModel.

**4. handleCanvasTap() auf Set-Lookup umstellen:**

Die bestehende Implementierung (Zeilen 228-253) ersetzen:

```swift
private func handleCanvasTap(at location: CGPoint, size: CGSize) {
    guard editMode == .none || editMode == .roomEdit else { return }
    guard let layers = map.layers else { return }

    let pixelSize = map.pixelSize ?? 5
    guard let p = calculateMapParams(layers: layers, pixelSize: pixelSize, size: size) else { return }

    let pixelX = Int(((location.x - p.offsetX) / p.scale).rounded())
    let pixelY = Int(((location.y - p.offsetY) / p.scale).rounded())

    // O(1) lookup per segment statt linearem Pixel-Scan
    let key = pixelX &<< 16 | pixelY
    for (segmentId, pixelSet) in segmentPixelSets {
        if pixelSet.contains(key) {
            toggleSegment(segmentId)
            return
        }
    }
}
```

**5. tapTargetsOverlay auf cachedSegmentInfos umstellen:**

In `tapTargetsOverlay` (Zeile 151): Ersetze `segmentInfos(from: layers)` durch `cachedSegmentInfos`:

```swift
ForEach(cachedSegmentInfos, id: \.id) { info in
```

Die `if let layers = map.layers` Guard kann vereinfacht werden — sie wird nur noch fuer `calculateMapParams` benoetigt, nicht mehr fuer segmentInfos.

**6. orderBadgesOverlay auf cachedSegmentInfos umstellen:**

In `orderBadgesOverlay` (Zeile 199): Ersetze `segmentInfos(from: layers).first(where:)` durch `cachedSegmentInfos.first(where:)`:

```swift
if let info = cachedSegmentInfos.first(where: { $0.id == segmentId }) {
```

**7. Alle Aufrufstellen in MapContentView aktualisieren:**

Suche nach `InteractiveMapView(` in MapView.swift und fuege die neuen Parameter hinzu:
```swift
InteractiveMapView(
    map: ...,
    segments: ...,
    selectedSegmentIds: ...,
    viewSize: ...,
    // ... bestehende Parameter ...
    segmentPixelSets: viewModel.segmentPixelSets,
    cachedSegmentInfos: viewModel.cachedSegmentInfos
)
```

Suche alle Stellen wo InteractiveMapView instanziiert wird und fuege die neuen Parameter ueberall hinzu.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && echo "=== Set lookup ===" && grep -c "pixelSet.contains" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift && echo "=== No more segmentInfos method ===" && grep -c "private func segmentInfos" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift && echo "=== Uses cached ===" && grep -c "cachedSegmentInfos" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `handleCanvasTap` nutzt `pixelSet.contains(key)` statt linearem while-Loop ueber decompressedPixels
    - Private `segmentInfos(from:)` Methode ist entfernt
    - Private `SegmentInfo` struct ist entfernt
    - `tapTargetsOverlay` nutzt `cachedSegmentInfos` statt `segmentInfos(from:)`
    - `orderBadgesOverlay` nutzt `cachedSegmentInfos.first(where:)` statt `segmentInfos(from:).first(where:)`
    - Alle InteractiveMapView-Instanziierungen haben die neuen Parameter
  </acceptance_criteria>
  <done>InteractiveMapView nutzt O(1) Set-Lookup fuer Hit-Testing und gecachte segmentInfos fuer Overlays</done>
</task>

</tasks>

<verification>
1. `grep "pixelSet.contains" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` — 1 Treffer
2. `grep "private func segmentInfos" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` — 0 Treffer (entfernt)
3. `grep "cachedSegmentInfos" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` — mindestens 2 Treffer
4. `grep "segmentPixelSets" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — mindestens 3 Treffer
5. Xcode Build erfolgreich
</verification>

<success_criteria>
- Room-Tap nutzt O(1) Set-Lookup statt linearem Pixel-Scan
- segmentInfos wird einmal pro Map-Update berechnet, nicht bei jedem Overlay-Render
- Beide Overlays (tapTargets, orderBadges) nutzen die gecachten Daten
- Hit-Testing-Code ist sauber und gut lesbar
</success_criteria>

<output>
After completion, create `.planning/phases/24-map-performance/24-02-SUMMARY.md`
</output>
