---
phase: 24-map-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/Services/MapCacheService.swift
autonomous: true
requirements: [PERF-02, PERF-05]

must_haves:
  truths:
    - "Map updates arrive via SSE stream, not 2-second HTTP polling"
    - "HTTP polling activates only as fallback when SSE connection fails"
    - "MapCacheService writes to disk only when map data actually changed"
    - "SSE reconnects with exponential backoff (2s, 5s, 30s) on failure"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "SSE-based startMapRefresh with poll fallback"
      contains: "streamMapLines"
    - path: "ValetudoApp/ValetudoApp/Services/MapCacheService.swift"
      provides: "Hash-based write deduplication"
      contains: "saveIfChanged"
  key_links:
    - from: "MapViewModel.startMapRefresh"
      to: "ValetudoAPI.streamMapLines"
      via: "SSE stream loop with for-try-await"
      pattern: "streamMapLines"
    - from: "MapViewModel.startMapRefresh"
      to: "MapCacheService.saveIfChanged"
      via: "called on each new map from SSE or poll fallback"
      pattern: "saveIfChanged"
---

<objective>
SSE Map-Streaming und Cache-Deduplication implementieren.

Purpose: Ersetzt das 2-Sekunden-HTTP-Polling durch einen SSE-Stream fuer Echtzeit-Map-Updates. MapCacheService schreibt nur noch bei tatsaechlicher Datenaenderung auf Disk statt bei jedem Update.
Output: MapViewModel nutzt SSE mit Poll-Fallback, MapCacheService hat hash-basierte Deduplication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-map-performance/24-RESEARCH.md

@ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
@ValetudoApp/ValetudoApp/Services/MapCacheService.swift
@ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift
@ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift

<interfaces>
<!-- Key types and contracts the executor needs -->

From ValetudoAPI.swift (line 648):
```swift
func streamMapLines() async throws -> URLSession.AsyncBytes
// Endpoint: /api/v2/robot/state/map/sse
// Uses sseSession with infinite timeout, auth headers included
```

From SSEConnectionManager.swift (reference pattern for SSE reconnect):
```swift
actor SSEConnectionManager {
    // streamWithReconnect pattern:
    // - for try await line in bytes.lines
    // - guard line.hasPrefix("data:") else { continue }
    // - Exponential backoff: 1s -> 5s -> 30s
    // - CancellationError -> break (clean exit)
}
```

From MapCacheService.swift:
```swift
final class MapCacheService {
    static let shared = MapCacheService()
    func save(_ map: RobotMap, for robotId: UUID) async  // current — no dedup
    func load(for robotId: UUID) async -> RobotMap?
}
```

From MapViewModel.swift:
```swift
@MainActor @Observable
final class MapViewModel {
    var map: RobotMap?
    var isOffline: Bool = false
    @ObservationIgnored private var refreshTask: Task<Void, Never>?
    var api: ValetudoAPI? { robotManager.getAPI(for: robot.id) }

    func startMapRefresh()   // current: 2-second poll loop
    func stopMapRefresh()    // cancels refreshTask
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MapCacheService hash-basierte Deduplication (PERF-05)</name>
  <files>ValetudoApp/ValetudoApp/Services/MapCacheService.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Services/MapCacheService.swift
  </read_first>
  <action>
MapCacheService erweitern:

1. Neues Property hinzufuegen:
```swift
private var lastDataHash: [UUID: Int] = [:]
```

2. Neue Methode `saveIfChanged` erstellen (NICHT die bestehende `save` umbenennen — `save` bleibt fuer den initialen Load in `loadMap()` bestehen):
```swift
func saveIfChanged(_ map: RobotMap, for robotId: UUID) async {
    do {
        let url = try cacheURL(for: robotId)
        let data = try JSONEncoder().encode(map)
        let newHash = data.hashValue

        if lastDataHash[robotId] == newHash {
            return  // Keine Aenderung — Disk-Write ueberspringen
        }
        lastDataHash[robotId] = newHash
        try data.write(to: url, options: .atomic)
        logger.debug("MapCache saved (changed) for \(robotId.uuidString, privacy: .public)")
    } catch {
        logger.error("MapCache save failed for \(robotId.uuidString, privacy: .public): \(error.localizedDescription, privacy: .public)")
    }
}
```

3. Die bestehende `save()` Methode bleibt unveraendert — sie wird weiterhin in `MapViewModel.loadMap()` beim initialen Load genutzt (dort ist Deduplication nicht noetig, weil es nur einmal aufgerufen wird).

Hinweis: `data.hashValue` ist ausreichend fuer Cache-Dedup. Worst case bei Hash-Kollision ist ein unnoetig uebersprungener Write, was akzeptabel ist. Kein CryptoKit noetig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -n "saveIfChanged\|lastDataHash" ValetudoApp/ValetudoApp/Services/MapCacheService.swift</automated>
  </verify>
  <acceptance_criteria>
    - MapCacheService hat eine `saveIfChanged(_:for:)` Methode
    - `lastDataHash` Dictionary speichert den letzten Hash pro Robot-ID
    - Bei unveraendertem Hash wird kein Disk-Write ausgefuehrt
    - Bestehende `save()` Methode bleibt fuer initialen Load erhalten
  </acceptance_criteria>
  <done>MapCacheService hat hash-basierte Write-Deduplication via saveIfChanged</done>
</task>

<task type="auto">
  <name>Task 2: MapViewModel SSE-Streaming mit Poll-Fallback (PERF-02)</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
    - ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift
    - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  </read_first>
  <action>
`startMapRefresh()` in MapViewModel komplett ersetzen. Die neue Implementierung folgt dem SSEConnectionManager-Pattern:

```swift
func startMapRefresh() {
    refreshTask?.cancel()
    refreshTask = Task { [weak self] in
        guard let self else { return }
        guard let api = self.api else { return }

        var retryCount = 0

        while !Task.isCancelled {
            do {
                let bytes = try await api.streamMapLines()
                retryCount = 0
                logger.info("Map SSE connected for \(self.robot.id, privacy: .public)")

                for try await line in bytes.lines {
                    if Task.isCancelled { break }
                    guard line.hasPrefix("data:") else { continue }
                    let jsonString = String(line.dropFirst(5)).trimmingCharacters(in: .whitespaces)
                    guard !jsonString.isEmpty,
                          let jsonData = jsonString.data(using: .utf8) else { continue }

                    do {
                        let newMap = try JSONDecoder().decode(RobotMap.self, from: jsonData)
                        self.map = newMap
                        self.isOffline = false
                        await MapCacheService.shared.saveIfChanged(newMap, for: self.robot.id)
                    } catch {
                        // SSE event war kein vollstaendiges RobotMap — fallback: einzelner HTTP-GET
                        logger.warning("Map SSE decode failed, falling back to HTTP GET: \(error.localizedDescription, privacy: .public)")
                        if let fallbackMap = try? await api.getMap() {
                            self.map = fallbackMap
                            self.isOffline = false
                            await MapCacheService.shared.saveIfChanged(fallbackMap, for: self.robot.id)
                        }
                    }
                }

            } catch is CancellationError {
                break
            } catch {
                logger.warning("Map SSE error: \(error.localizedDescription, privacy: .public) — falling back to HTTP poll")
                // Einmaliger HTTP-Poll als Fallback
                if let newMap = try? await api.getMap() {
                    self.map = newMap
                    self.isOffline = false
                    await MapCacheService.shared.saveIfChanged(newMap, for: self.robot.id)
                } else {
                    // HTTP-Poll auch fehlgeschlagen
                    if self.map == nil, let cachedMap = await MapCacheService.shared.load(for: self.robot.id) {
                        self.map = cachedMap
                        self.isOffline = true
                    } else if self.map != nil {
                        self.isOffline = true
                    }
                }

                retryCount += 1
                let delay: Double = retryCount == 1 ? 2 : retryCount == 2 ? 5 : 30
                logger.info("Map SSE retry \(retryCount, privacy: .public) — waiting \(delay, privacy: .public)s")
                do {
                    try await Task.sleep(for: .seconds(delay))
                } catch is CancellationError {
                    break
                } catch {
                    break
                }
            }
        }
    }
}
```

WICHTIG:
- `self.map = newMap` muss auf MainActor laufen — MapViewModel ist bereits `@MainActor`, daher ist `self.map` Zugriff sicher.
- `stopMapRefresh()` bleibt unveraendert (cancelt refreshTask).
- `loadMap()` bleibt unveraendert — nutzt weiterhin `MapCacheService.shared.save()` (nicht saveIfChanged) fuer den initialen Load.
- Auch in `loadMap()` den Aufruf `await MapCacheService.shared.save(loadedMap, for: robot.id)` NICHT aendern — dort ist kein Dedup noetig.
- Die bestehende `while !Task.isCancelled` Poll-Schleife wird komplett durch den SSE-Loop ersetzt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "streamMapLines\|saveIfChanged" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift && grep -c "sleep(for: .seconds(2))" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - `startMapRefresh()` ruft `api.streamMapLines()` auf statt `api.getMap()` in einer 2s-Schleife
    - SSE-Daten werden als `RobotMap` dekodiert mit Fehler-Logging (kein stilles `try?`)
    - Bei SSE-Decode-Fehler wird ein einzelner HTTP-GET als Fallback gemacht
    - Bei SSE-Connection-Fehler: HTTP-Poll + exponentieller Backoff (2s, 5s, 30s)
    - `saveIfChanged` wird statt `save` im Refresh-Loop genutzt
    - Kein `sleep(for: .seconds(2))` Polling-Intervall mehr vorhanden
    - `loadMap()` bleibt unveraendert
  </acceptance_criteria>
  <done>MapViewModel nutzt SSE-Stream fuer Map-Updates mit HTTP-Poll-Fallback und exponentieller Reconnect-Strategie</done>
</task>

</tasks>

<verification>
1. `grep "streamMapLines" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — mindestens 1 Treffer
2. `grep "saveIfChanged" ValetudoApp/ValetudoApp/Services/MapCacheService.swift` — mindestens 1 Treffer
3. `grep "sleep(for: .seconds(2))" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — 0 Treffer (Polling entfernt)
4. `grep "lastDataHash" ValetudoApp/ValetudoApp/Services/MapCacheService.swift` — mindestens 1 Treffer
5. Xcode Build erfolgreich
</verification>

<success_criteria>
- Map-Updates kommen via SSE-Stream statt 2-Sekunden HTTP-Polling
- HTTP-Polling aktiviert sich nur als Fallback bei SSE-Fehler
- MapCacheService schreibt nur bei Hash-Aenderung auf Disk
- SSE-Reconnect nutzt exponentiellen Backoff (2s, 5s, 30s)
</success_criteria>

<output>
After completion, create `.planning/phases/24-map-performance/24-01-SUMMARY.md`
</output>
