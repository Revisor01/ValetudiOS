---
phase: 23-error-handling-robustness-patterns
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/SupportManager.swift
autonomous: true
requirements: [DEBT-07]

must_haves:
  truths:
    - "Fehlende StoreKit Product IDs werden beim Laden via logger.error geloggt"
    - "User sieht weiterhin alle verfuegbaren Produkte auch wenn eines fehlt"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/SupportManager.swift"
      provides: "Product ID Validierung nach loadProducts()"
      contains: "missingIds"
  key_links:
    - from: "ValetudoApp/ValetudoApp/Services/SupportManager.swift"
      to: "Constants.supportProductIds"
      via: "Set-Differenz fuer Validierung"
      pattern: "supportProductIds\\.subtracting"
---

<objective>
StoreKit Product IDs beim Laden validieren — wenn App Store Connect weniger Produkte zurueckgibt als in Constants.supportProductIds konfiguriert, wird ein logger.error mit den fehlenden IDs geschrieben.

Purpose: DEBT-07 (StoreKit Product IDs Runtime-Validierung) — stille Fehlkonfiguration erkennen
Output: SupportManager.loadProducts() mit Mismatch-Logging
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-error-handling-robustness-patterns/23-RESEARCH.md

@ValetudoApp/ValetudoApp/Services/SupportManager.swift
@ValetudoApp/ValetudoApp/Utilities/Constants.swift

<interfaces>
From ValetudoApp/ValetudoApp/Utilities/Constants.swift:
```swift
enum Constants {
    static let supportSmallId = "de.godsapp.valetudoapp.support.small"
    static let supportMediumId = "de.godsapp.valetudoapp.support.medium"
    static let supportLargeId = "de.godsapp.valetudoapp.support.large"
    static let supportProductIds: Set<String> = [supportSmallId, supportMediumId, supportLargeId]
}
```

From ValetudoApp/ValetudoApp/Services/SupportManager.swift:
```swift
@MainActor @Observable class SupportManager {
    static let shared = SupportManager()
    private let logger = Logger(...)
    var products: [Product] = []
    func loadProducts() async { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StoreKit Product ID Validierung in SupportManager.loadProducts() (DEBT-07)</name>
  <files>ValetudoApp/ValetudoApp/Services/SupportManager.swift</files>
  <read_first>
    ValetudoApp/ValetudoApp/Services/SupportManager.swift,
    ValetudoApp/ValetudoApp/Utilities/Constants.swift
  </read_first>
  <action>
In SupportManager.swift die `loadProducts()`-Funktion erweitern. Nach dem erfolgreichen Laden der Produkte eine Validierung hinzufuegen:

Ersetze den bestehenden do-Block in loadProducts():
```swift
func loadProducts() async {
    isLoading = true
    defer { isLoading = false }

    do {
        let loaded = try await Product.products(for: Constants.supportProductIds)
            .sorted { $0.price < $1.price }
        products = loaded

        // DEBT-07: Validiere dass alle konfigurierten Product IDs geladen wurden
        let loadedIds = Set(loaded.map { $0.id })
        let missingIds = Constants.supportProductIds.subtracting(loadedIds)
        if !missingIds.isEmpty {
            logger.error("StoreKit Product IDs not found in App Store: \(missingIds.sorted().joined(separator: ", "), privacy: .public)")
        }
        let unexpectedIds = loadedIds.subtracting(Constants.supportProductIds)
        if !unexpectedIds.isEmpty {
            logger.warning("StoreKit returned unexpected Product IDs: \(unexpectedIds.sorted().joined(separator: ", "), privacy: .public)")
        }
    } catch {
        logger.error("Failed to load products: \(error.localizedDescription, privacy: .public)")
    }
}
```

Die Validierung:
1. Bildet ein Set der geladenen Product IDs
2. Berechnet die Differenz zu den konfigurierten IDs (missingIds)
3. Loggt fehlende IDs als error (= Fehlkonfiguration in App Store Connect)
4. Loggt zusaetzlich unerwartete IDs als warning (defensiv, sollte nicht vorkommen)
5. Produkte werden trotzdem angezeigt — kein User-Alert noetig, nur Logging fuer Debugging
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -n "missingIds\|subtracting\|unexpectedIds" ValetudoApp/ValetudoApp/Services/SupportManager.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep "missingIds" in SupportManager.swift returns >= 2 matches (declaration + if-check)
    - grep "supportProductIds.subtracting" in SupportManager.swift returns >= 1 match
    - grep "logger.error.*Product IDs not found" in SupportManager.swift returns 1 match
    - Die bestehende products-Zuweisung bleibt erhalten — Produkte werden immer angezeigt
    - Xcode Build kompiliert erfolgreich
  </acceptance_criteria>
  <done>SupportManager.loadProducts() validiert geladene Product IDs gegen Constants.supportProductIds. Fehlende IDs werden via logger.error geloggt. Unerwartete IDs werden via logger.warning geloggt. User-Erlebnis bleibt unveraendert.</done>
</task>

</tasks>

<verification>
1. `grep -n "missingIds" ValetudoApp/ValetudoApp/Services/SupportManager.swift` — >= 2 matches
2. `grep -n "subtracting" ValetudoApp/ValetudoApp/Services/SupportManager.swift` — >= 1 match
3. Xcode Build: `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5` — BUILD SUCCEEDED
</verification>

<success_criteria>
- loadProducts() vergleicht geladene IDs mit konfigurierten IDs
- Fehlende Product IDs werden via logger.error geloggt
- Produkte werden weiterhin angezeigt (kein Alert, kein Abbruch)
- Projekt kompiliert ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/23-error-handling-robustness-patterns/23-03-SUMMARY.md`
</output>
