---
phase: 27-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/MapView.swift
  - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  - ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
  - ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements: [A11Y-04, A11Y-05]

must_haves:
  truths:
    - "VoiceOver liest Map-Canvas als 'Grundriss mit Roboterposition' (ein Element, Kinder ignoriert)"
    - "VoiceOver liest Raum-Tap-Targets mit Raumnamen und Auswahlhinweis"
    - "VoiceOver liest tag-Button als 'Raumlabels einblenden/ausblenden, Taste'"
    - "VoiceOver liest Map-Reset-Button als 'Kartenansicht zuruecksetzen, Taste'"
    - "VoiceOver liest xmark-Button in MapView als 'Karte schliessen, Taste'"
    - "VoiceOver liest splitRoomBar-Reset als 'Splitlinie zuruecksetzen, Taste'"
    - "VoiceOver liest Restriction-Delete-Buttons mit spezifischem Kontext"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift"
      provides: "accessibilityElement auf Canvas, accessibilityLabel auf Room-Chips"
      contains: ".accessibilityElement(children: .ignore)"
    - path: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      provides: "accessibilityLabel auf tag/reset/xmark Buttons"
      contains: ".accessibilityLabel"
    - path: "ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift"
      provides: "accessibilityLabel auf splitRoomBar Reset-Button"
      contains: ".accessibilityLabel"
    - path: "ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift"
      provides: "accessibilityLabel auf alle drei Restriction-Delete-Buttons"
      contains: ".accessibilityLabel"
  key_links:
    - from: "MapInteractiveView.swift"
      to: "Localizable.xcstrings"
      via: "String(localized:) fuer Canvas-Label und Room-Hints"
      pattern: "map\\.canvas_label"
    - from: "MapOverlayViews.swift"
      to: "Localizable.xcstrings"
      via: "String(localized:) fuer Restriction-Delete-Labels"
      pattern: "map\\.delete_"
---

<objective>
Accessibility-Labels fuer alle Map-bezogenen Views: Canvas-Summary, Raum-Tap-Targets, Map-Toolbar-Buttons, Control-Bar-Buttons und Restriction-Overlay-Delete-Buttons.

Purpose: VoiceOver-Nutzer koennen die Kartenansicht navigieren und verstehen was dargestellt wird.
Output: 4 Swift-Dateien mit Accessibility-Modifiern, Localizable.xcstrings mit Map-spezifischen Keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-accessibility/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lokalisierungsschluessel fuer Map + Canvas accessibilityElement + Room-Chips</name>
  <files>
    ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
    ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  </files>
  <action>
    **Schritt 1: Localizable.xcstrings erweitern (Map-Keys)**
    Folgende Keys eintragen (deutsch / englisch):
    - `map.canvas_label` — "Grundriss mit Roboterposition" / "Floor plan with robot position"
    - `map.room_select_hint` — "Doppeltippen zum Auswaehlen" / "Double-tap to select"
    - `map.room_deselect_hint` — "Doppeltippen zum Abwaehlen" / "Double-tap to deselect"
    - `map.show_room_labels` — "Raumlabels einblenden" / "Show room labels"
    - `map.hide_room_labels` — "Raumlabels ausblenden" / "Hide room labels"
    - `map.reset_view` — "Kartenansicht zuruecksetzen" / "Reset map view"
    - `map.close` — "Karte schliessen" / "Close map"
    - `map.reset_split_line` — "Splitlinie zuruecksetzen" / "Reset split line"
    - `map.delete_virtual_wall` — "Virtuelle Wand loeschen" / "Delete virtual wall"
    - `map.delete_nogo_zone` — "No-Go-Zone loeschen" / "Delete no-go zone"
    - `map.delete_nomop_zone` — "No-Mop-Zone loeschen" / "Delete no-mop zone"

    **HINWEIS:** Falls Plan 01 parallel laeuft und xcstrings bereits bearbeitet hat: Die Keys sind disjunkt (Plan 01 hat robot.*/battery.*/action.*/consumables.*/timers.*/robots.*, Plan 02 hat map.*). Trotzdem die aktuelle Datei lesen und nur die neuen Keys hinzufuegen.

    **Schritt 2: MapInteractiveView.swift — Canvas**
    Auf dem Canvas (der `Canvas { context, size in ... }` Block):
    ```swift
    .accessibilityElement(children: .ignore)
    .accessibilityLabel(String(localized: "map.canvas_label"))
    ```
    WICHTIG: `children: .ignore` verwenden, NICHT `children: .combine` — Canvas hat keine sinnvollen Kinder fuer VoiceOver.

    **Schritt 3: MapInteractiveView.swift — Room-Tap-Target-Buttons**
    Die Buttons im Tap-Target-Overlay (die Raum-Label-Chips) brauchen jeweils:
    ```swift
    .accessibilityLabel(info.name)
    .accessibilityHint(isSelected
        ? String(localized: "map.room_deselect_hint")
        : String(localized: "map.room_select_hint"))
    ```
    Dabei `isSelected` aus dem aktuellen Selection-State ableiten (z.B. `selectedSegmentIds.contains(info.id)`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Canvas hat .accessibilityElement(children: .ignore) und .accessibilityLabel
    - Room-Tap-Targets haben .accessibilityLabel mit Raumname und .accessibilityHint mit Auswahl-Kontext
    - Alle Map-Lokalisierungsschluessel in xcstrings vorhanden
    - Build kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: Map-Toolbar, ControlBars und Restriction-Overlay Labels</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapView.swift
    ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
    ValetudoApp/ValetudoApp/Views/MapOverlayViews.swift
  </files>
  <action>
    **MapView.swift — drei Icon-only-Buttons:**
    1. tag/tag.fill Button (Zeile ~309-316): `.accessibilityLabel(viewModel.showRoomLabels ? String(localized: "map.hide_room_labels") : String(localized: "map.show_room_labels"))` auf den Button
    2. arrow.counterclockwise.circle.fill Button (Zeile ~318-327): `.accessibilityLabel(String(localized: "map.reset_view"))` auf den Button
    3. xmark Button (Zeile ~363-369): `.accessibilityLabel(String(localized: "map.close"))` auf den Button

    **MapControlBarsView.swift — splitRoomBar Reset-Button:**
    arrow.counterclockwise Button (Zeile ~493-500): `.accessibilityLabel(String(localized: "map.reset_split_line"))` auf den Button

    **MapOverlayViews.swift — drei Restriction-Delete-Buttons:**
    Alle drei xmark-Buttons in `restrictionDeleteOverlay` bekommen kontextspezifische Labels:
    1. Virtuelle-Wand-Delete (Zeile ~102-118): `.accessibilityLabel(String(localized: "map.delete_virtual_wall"))`
    2. No-Go-Zone-Delete (Zeile ~127-143): `.accessibilityLabel(String(localized: "map.delete_nogo_zone"))`
    3. No-Mop-Zone-Delete (Zeile ~152-168): `.accessibilityLabel(String(localized: "map.delete_nomop_zone"))`

    **WICHTIG:** Alle Labels auf Button-Ebene, nicht auf Image-Ebene. Die Zeilennummern aus der Research sind Richtwerte — die tatsaechlichen Positionen durch Lesen der Datei bestimmen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MapView: tag-Button, Reset-Button und xmark-Button haben beschreibende accessibilityLabels
    - MapControlBarsView: splitRoomBar Reset-Button hat accessibilityLabel
    - MapOverlayViews: alle drei Restriction-Delete-Buttons haben kontextspezifische Labels
    - Build kompiliert fehlerfrei
  </done>
</task>

</tasks>

<verification>
- `xcodebuild build` kompiliert fehlerfrei
- Grep nach `.accessibilityLabel` in MapView.swift findet mindestens 3 Treffer
- Grep nach `.accessibilityElement(children: .ignore)` in MapInteractiveView.swift findet 1 Treffer
- Grep nach `.accessibilityLabel` in MapOverlayViews.swift findet mindestens 3 Treffer
</verification>

<success_criteria>
- A11Y-04 (Map-Anteil): Alle 7 Icon-only-Buttons in Map-Views haben beschreibende Labels
- A11Y-05: Canvas hat .accessibilityElement(children: .ignore) + Summary-Label, Room-Chips haben .accessibilityLabel mit Raumname
</success_criteria>

<output>
After completion, create `.planning/phases/27-accessibility/27-02-SUMMARY.md`
</output>
