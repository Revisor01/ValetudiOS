---
phase: 06-new-capabilities
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - CAP-04

must_haves:
  truths:
    - "Benutzer sieht in RobotDetailView eine Properties-Section mit Modell, Firmware-Version und Seriennummer"
    - "Daten werden einmalig beim Öffnen von RobotDetailView geladen"
    - "Section erscheint immer (kein Capability-Gate — /api/v2/robot/properties ist kein Capability-Endpoint)"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      provides: "getRobotProperties() API-Methode und RobotProperties Struct"
      contains: "robot/properties"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "robotProperties @Published property + loadRobotProperties() Logik"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "Properties-Section mit Modell, Firmware und Seriennummer als LabeledContent"
  key_links:
    - from: "RobotDetailView"
      to: "RobotDetailViewModel.robotProperties"
      via: "@Published binding"
    - from: "RobotDetailViewModel.loadData()"
      to: "ValetudoAPI.getRobotProperties()"
      via: "async Task"
---

<objective>
Integriert Robot Properties (/api/v2/robot/properties): API-Methode, Model-Struct, ViewModel-State und Info-Section in RobotDetailView.

Purpose: Benutzer kann Modell, Firmware und Seriennummer des Roboters direkt in der App einsehen.
Output: Neue Properties-Section in RobotDetailView (kein Capability-Gate). API-Methode getRobotProperties() in ValetudoAPI. RobotProperties Struct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Analoges Pattern aus ValetudoAPI.swift für einfaches GET + Decodable Struct -->

Pattern (getSystemHostInfo als Vorlage):
```swift
func getSystemHostInfo() async throws -> SystemHostInfo {
    try await request("/system/host/info")
}
```

Neuer Endpoint: GET `/api/v2/robot/properties`

Valetudo API Response für /api/v2/robot/properties:
```json
{
  "firmwareVersion": "2023.08.1",
  "model": "Roborock S5 Max",
  "manufacturer": "Roborock",
  "supportedCapabilities": [...],
  "metaData": {
    "manufacturerSerialNumber": "12345ABCDE"
  }
}
```

Hinweis: Die genauen Feldnamen können je nach Valetudo-Version variieren. Alle Felder als optional definieren (String?) damit Decoding robust bleibt.

RobotDetailViewModel Lademuster — loadData() Funktion (aus RobotDetailViewModel.swift):
```swift
func loadData() async {
    guard let api = api else { return }
    isLoading = true
    defer { isLoading = false }
    // ... bestehende async calls
}
```

LabeledContent View-Pattern (iOS 16+, passt zu List-Style):
```swift
LabeledContent("Modell", value: "Roborock S5 Max")
```

CONTEXT.md-Entscheidung: Robot Properties in RobotDetailView (nicht Settings), da es Info ist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RobotProperties API-Methode und Model-Struct</name>
  <files>ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift</files>
  <action>
1. Definiere RobotProperties Struct (kann am Ende der Datei nach den anderen Structs stehen):

```swift
struct RobotProperties: Codable {
    let firmwareVersion: String?
    let model: String?
    let manufacturer: String?
    let metaData: RobotPropertiesMetaData?

    enum CodingKeys: String, CodingKey {
        case firmwareVersion
        case model
        case manufacturer
        case metaData
    }
}

struct RobotPropertiesMetaData: Codable {
    let manufacturerSerialNumber: String?
}
```

Alle Felder optional (String?) — unterschiedliche Valetudo-Versionen liefern unterschiedliche Felder.

2. Füge API-Methode in der ValetudoAPI extension im MARK-Block "Robot Info" ein (nach getRobotInfo()):

```swift
func getRobotProperties() async throws -> RobotProperties {
    try await request("/robot/properties")
}
```
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>ValetudoAPI kompiliert fehlerfrei mit getRobotProperties() und RobotProperties / RobotPropertiesMetaData Structs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RobotProperties ViewModel-State und View-Section</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift, ValetudoApp/ValetudoApp/Views/RobotDetailView.swift, ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings</files>
  <action>
**RobotDetailViewModel.swift:**

1. Füge @Published property nach den Statistics-Properties hinzu:
```swift
@Published var robotProperties: RobotProperties?
```

2. Ergänze in `loadData()` (oder der äquivalenten Lademethode — prüfe den genauen Methodennamen) einen neuen async Block:
```swift
// Load robot properties
do {
    robotProperties = try await api.getRobotProperties()
} catch {
    logger.debug("Robot properties not available: \(error, privacy: .public)")
    // Non-fatal: section wird ausgeblendet wenn nil
}
```

Falls loadData() nicht existiert und stattdessen mehrere separate load-Methoden vorhanden sind: Füge den Block in die Haupt-Lademethode ein, die beim Öffnen der View aufgerufen wird (die, die in .task { await viewModel.loadXxx() } referenziert wird).

**RobotDetailView.swift:**

Füge eine Properties-Section in der List ein. Position: Nach der Statistics-Section oder als letzte Section vor dem Update-Banner (prüfe die tatsächliche Reihenfolge in der Datei). Die Section soll immer erscheinen wenn robotProperties != nil:

```swift
// Robot Properties Section
if let props = viewModel.robotProperties {
    Section {
        if let model = props.model {
            LabeledContent(String(localized: "robot_properties.model"), value: model)
        }
        if let firmware = props.firmwareVersion {
            LabeledContent(String(localized: "robot_properties.firmware"), value: firmware)
        }
        if let serial = props.metaData?.manufacturerSerialNumber {
            LabeledContent(String(localized: "robot_properties.serial"), value: serial)
        }
        if let manufacturer = props.manufacturer {
            LabeledContent(String(localized: "robot_properties.manufacturer"), value: manufacturer)
        }
    } header: {
        Label(String(localized: "robot_properties.title"), systemImage: "cpu")
    }
}
```

**Localizable.xcstrings:**

Füge folgende Keys alphabetisch bei "r" (nach bestehenden "robot."-Keys) ein:

```json
"robot_properties.firmware" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Firmware"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Firmware"
      }
    }
  }
},
"robot_properties.manufacturer" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Hersteller"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Manufacturer"
      }
    }
  }
},
"robot_properties.model" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Modell"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Model"
      }
    }
  }
},
"robot_properties.serial" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Seriennummer"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Serial Number"
      }
    }
  }
},
"robot_properties.title" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Geräteinformationen"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Device Info"
      }
    }
  }
},
```
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>App kompiliert fehlerfrei. robotProperties als @Published in RobotDetailViewModel. Properties-Section in RobotDetailView erscheint wenn robotProperties != nil. LabeledContent zeigt Modell, Firmware, Seriennummer und Hersteller. Alle 5 Lokalisierungskeys für de + en vorhanden.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -target ValetudoApp -configuration Debug` erfolgreich
- getRobotProperties() in ValetudoAPI.swift mit Endpoint /robot/properties
- RobotProperties + RobotPropertiesMetaData Structs mit optionalen Feldern
- robotProperties @Published property in RobotDetailViewModel
- Properties-Section in RobotDetailView mit if let props = viewModel.robotProperties Guard
- Alle 5 Lokalisierungskeys (robot_properties.*) für de + en vorhanden
</verification>

<success_criteria>
- Build erfolgreich ohne neue Compiler-Fehler
- /api/v2/robot/properties vollständig integriert: API → ViewModel → View
- Section erscheint nur wenn Daten geladen (nil-Guard), kein Capability-Gate nötig
- Alle Felder optional → robustes Decoding über Valetudo-Versionen hinweg
- LabeledContent zeigt nur Felder die tatsächlich in der API-Antwort enthalten sind
</success_criteria>

<output>
Nach Completion: `.planning/phases/06-new-capabilities/06-04-SUMMARY.md` erstellen.
</output>
