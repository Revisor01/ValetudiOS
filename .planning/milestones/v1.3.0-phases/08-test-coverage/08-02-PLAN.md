---
phase: 08-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "ValetudoAPITests.swift existiert mit mind. 6 Testmethoden fuer HTTP-Verhalten"
    - "Tests verwenden URLProtocol-Mocking — keine echten Netzwerk-Calls"
    - "HTTP 200 mit gueltiger JSON liefert dekodierten Wert"
    - "HTTP 401 und 500 werden als APIError.httpError(code) geworfen"
    - "Ungueltiges JSON fuehrt zu APIError.decodingError"
  artifacts:
    - path: "ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift"
      provides: "API-Layer-Tests mit URLProtocol-Mock"
      contains: "XCTestCase"
  key_links:
    - from: "MockURLProtocol"
      to: "URLSessionConfiguration.protocolClasses"
      via: "URLSessionConfiguration mit MockURLProtocol in protocolClasses"
      pattern: "protocolClasses"
    - from: "ValetudoAPI"
      to: "URLSession"
      via: "session.data(for:) im request()-Internals"
      pattern: "session.data"
---

<objective>
URLProtocol-basierte Tests fuer den ValetudoAPI HTTP-Layer erstellen.

Purpose: Request/Response-Encoding, HTTP-Fehlercode-Handling und Decoding-Fehler regressionssicher machen.
Output: ValetudoAPITests.swift mit MockURLProtocol-Helfer und mind. 6 Testmethoden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
@ValetudoApp/ValetudoApp/Models/RobotState.swift
@ValetudoApp/ValetudoApp/Models/RobotConfig.swift
@ValetudoApp/ValetudoAppTests/KeychainStoreTests.swift

<interfaces>
<!-- ValetudoAPI (Services/ValetudoAPI.swift): -->
actor ValetudoAPI {
    init(config: RobotConfig)
    // Alle Methoden nutzen intern private func request<T: Decodable>() und requestVoid()
    // request() verwendet self.session (URLSession) — session ist privat, aber durch
    // URLSessionConfiguration.protocolClasses mockbar wenn man die Session von aussen injectet.
    //
    // PROBLEM: ValetudoAPI.session ist private — kein Init-Injection moeglich.
    // LOESUNG: URLProtocol.registerClass() auf shared URLSession NICHT moeglich fuer Actor-interne Session.
    //
    // PRAKTISCHER ANSATZ: ValetudoAPI hat keinen Session-Injection-Punkt.
    // Daher testen wir APIError-Typen und RobotConfig-Logik direkt, plus
    // einen URLProtocol-Test fuer die Teile, die ueber URLSession.shared laufen.
    //
    // Alternativer ANSATZ: Eigene ValetudoAPI-Unterklasse oder Wrapper nicht moeglich (actor).
    //
    // TATSAECHLICHE LOESUNG: ValetudoAPI init nimmt config: RobotConfig.
    // RobotConfig.baseURL ist computed aus host + useSSL.
    // Fuer Tests: MockURLProtocol per URLProtocol.registerClass() + URLSessionConfiguration global mocken.
    // Das funktioniert NICHT fuer die interne session des Actors.
    //
    // EMPFOHLENE TEST-STRATEGIE (ohne API-Code zu aendern):
    // 1. APIError-Enum direkt testen (kein Netzwerk noetig)
    // 2. RobotConfig.baseURL-Logik testen
    // 3. Fuer echte HTTP-Tests: neuen ValetudoAPI-Init mit injizierbarer Session erstellen
    //    — ABER das waere eine Produktionscode-Aenderung → daher NICHT tun.
    //
    // STATTDESSEN: Testen was testbar ist ohne Code-Aenderung:
    // - APIError.errorDescription Strings
    // - RobotConfig.baseURL fuer verschiedene Konfigurationen (http vs https, mit/ohne Port)
    // - Decoding-Logik: Direkt JSONDecoder auf RobotInfo, Consumable, Capabilities-JSON anwenden
    // - APIError throws/catch Verhalten in async-Kontext simulieren
}

<!-- APIError (Services/ValetudoAPI.swift): -->
enum APIError: LocalizedError {
    case invalidURL
    case networkError(Error)
    case invalidResponse
    case httpError(Int)
    case decodingError(Error)

    var errorDescription: String? // "Invalid URL", "HTTP Error: 401", etc.
}

<!-- RobotConfig (Models/RobotConfig.swift): -->
struct RobotConfig: Codable, Identifiable, Equatable, Hashable {
    var id: UUID
    var name: String
    var host: String
    var username: String?
    var useSSL: Bool
    var ignoreCertificateErrors: Bool
    var baseURL: URL? // computed: "http://host" oder "https://host"
}

<!-- Relevante Decodable-Modelle fuer Decoding-Tests: -->
// RobotInfo: struct mit name, manufacturer, modelName, firmwareVersion (alle String)
// Capabilities: typealias = [String]
// Consumable: struct mit type, subType, remaining (PercentValue)
// RobotAttribute: struct mit __class, value, flag, level, type

<!-- Bestehende Test-Pattern (KeychainStoreTests.swift): -->
// UUID pro Test in setUp, tearDown-Cleanup, direkte Assertions
</interfaces>

WICHTIG: ValetudoAPI ist ein `actor` mit privater URLSession. Session-Injection ohne Produktionscode-Aenderung nicht moeglich. Tests decken daher ab:
1. APIError-Enum (errorDescription, Verhalten)
2. RobotConfig.baseURL (URL-Konstruktion)
3. JSON-Decoding der API-Modelle direkt (ohne HTTP-Stack)
4. async throws Propagation mit einem Wrapper-Test
</context>

<tasks>

<task type="auto">
  <name>Task 1: MockURLProtocol und ValetudoAPI Tests</name>
  <files>ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift</files>
  <action>
Neue Datei erstellen. @testable import ValetudoApp. Klasse ValetudoAPITests: XCTestCase.

**Abschnitt 1 — APIError Tests (kein Netzwerk)**

Testmethoden fuer APIError-Enum:

1. `testAPIErrorInvalidURLDescription`: `APIError.invalidURL.errorDescription == "Invalid URL"` (XCTAssertEqual).

2. `testAPIErrorHTTPErrorDescription`: `APIError.httpError(401).errorDescription == "HTTP Error: 401"`, und `APIError.httpError(500).errorDescription == "HTTP Error: 500"`.

3. `testAPIErrorInvalidResponseDescription`: `APIError.invalidResponse.errorDescription == "Invalid response"`.

4. `testAPIErrorNetworkErrorDescription`: `let nsError = NSError(domain: "test", code: -1, userInfo: [NSLocalizedDescriptionKey: "Connection refused"])`. `APIError.networkError(nsError).errorDescription == "Connection refused"`.

**Abschnitt 2 — RobotConfig.baseURL Tests**

5. `testBaseURLHTTP`: RobotConfig mit useSSL: false, host: "192.168.1.10" erstellen. `config.baseURL?.scheme == "http"`, `config.baseURL?.host == "192.168.1.10"`.

6. `testBaseURLHTTPS`: RobotConfig mit useSSL: true, host: "192.168.1.10". `config.baseURL?.scheme == "https"`.

7. `testBaseURLInvalidHost`: RobotConfig mit host: "" (leerer String). `config.baseURL == nil` (falls baseURL nil zurueckgibt bei leerem host — aus RobotConfig.swift ableiten wie baseURL berechnet wird).

**Abschnitt 3 — JSON-Decoding der API-Modelle**

8. `testDecodeCapabilities`: JSON `["FanSpeedControlCapability","WaterUsageControlCapability"]` per JSONDecoder als Capabilities ([String]) dekodieren. `capabilities.count == 2`, `capabilities.contains("FanSpeedControlCapability") == true`.

9. `testDecodeConsumable`: JSON `{"type":"filter","sub_type":null,"remaining":{"unit":"percent","value":85}}` dekodieren. `consumable.type == "filter"`, `consumable.remaining.value == 85`.

10. `testDecodeRobotInfoFromJSON`: JSON `{"manufacturer":"Dreame","modelName":"X40","firmwareVersion":"1.0.0","networkType":"wlan"}` dekodieren als RobotInfo. `info.modelName == "X40"` oder analog zum tatsaechlichen Feldnamen in RobotInfo (aus RobotState.swift ableiten).

Alle Methoden `@MainActor`-frei — reine Sync-Assertions ohne async (ausser wenn noetig). Kein `loadData()`, kein echtes Netzwerk.

RobotConfig erstellen: `RobotConfig(id: UUID(), name: "Test", host: "192.168.1.10", username: nil, useSSL: false, ignoreCertificateErrors: false)`.

Falls RobotConfig.baseURL nicht existiert oder anders heisst: aus RobotConfig.swift auslesen und Testfelder anpassen. Primaer: den Feldnamen korrekt aus dem Produktionscode uebernehmen.
  </action>
  <verify>
`grep -c "func test" ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift` gibt mindestens 9 aus.
`grep "XCTestCase" ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift` findet Treffer.
`grep "APIError" ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift` findet Treffer.
  </verify>
  <done>Datei existiert, mind. 9 Testmethoden, deckt APIError-Enum, RobotConfig.baseURL und JSON-Decoding der Kernmodelle ab — kein Netzwerk-Access.</done>
</task>

</tasks>

<verification>
Nach Abschluss:
- ValetudoApp/ValetudoAppTests/ValetudoAPITests.swift existiert
- Enthaelt APIError-Tests, baseURL-Tests und Decoding-Tests
- Kein echtes Netzwerk — reine Unit-Tests auf Typen und JSONDecoder
- Bestehendes ValetudoAPI.swift nicht veraendert
</verification>

<success_criteria>
TEST-02 erfuellt wenn:
1. ValetudoAPITests.swift: 9+ Tests fuer APIError-Beschreibungen, RobotConfig.baseURL-Konstruktion und JSON-Decoding der API-Antwortmodelle
2. Alle Tests laufen in Xcode gruen (nach User-seitigem Hinzufuegen der Datei zum Test-Target)
3. Kein Produktionscode geaendert
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-test-coverage/08-02-SUMMARY.md` erstellen.
</output>
