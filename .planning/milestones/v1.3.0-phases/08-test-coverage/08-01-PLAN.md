---
phase: 08-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoAppTests/RobotDetailViewModelTests.swift
  - ValetudoApp/ValetudoAppTests/RobotSettingsViewModelTests.swift
  - ValetudoApp/ValetudoAppTests/MapViewModelTests.swift
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "RobotDetailViewModelTests.swift existiert mit mind. 5 Testmethoden fuer State-Transitions"
    - "RobotSettingsViewModelTests.swift existiert mit mind. 3 Testmethoden fuer Settings-Logik"
    - "MapViewModelTests.swift existiert mit mind. 3 Testmethoden fuer Map-State"
    - "Alle Tests laufen gruen (kein Compiler-Fehler, kein Test-Failure)"
  artifacts:
    - path: "ValetudoApp/ValetudoAppTests/RobotDetailViewModelTests.swift"
      provides: "ViewModel State-Transition Tests"
      contains: "XCTestCase"
    - path: "ValetudoApp/ValetudoAppTests/RobotSettingsViewModelTests.swift"
      provides: "Settings ViewModel Tests"
      contains: "XCTestCase"
    - path: "ValetudoApp/ValetudoAppTests/MapViewModelTests.swift"
      provides: "Map ViewModel Tests"
      contains: "XCTestCase"
  key_links:
    - from: "ValetudoAppTests"
      to: "ValetudoApp module"
      via: "@testable import ValetudoApp"
      pattern: "@testable import"
---

<objective>
Unit-Tests fuer RobotDetailViewModel, RobotSettingsViewModel und MapViewModel erstellen.

Purpose: State-Transitions, Capability-Flags und einfache Logik-Checks regressionssicher machen ohne aufwaendiges Mocking.
Output: Drei neue XCTestCase-Dateien im ValetudoAppTests/-Verzeichnis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
@ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
@ValetudoApp/ValetudoApp/Services/RobotManager.swift
@ValetudoApp/ValetudoApp/Models/RobotState.swift
@ValetudoApp/ValetudoAppTests/MapLayerTests.swift

<interfaces>
<!-- Relevante Typen aus dem Produktionscode. Executor kann direkt verwenden. -->

<!-- RobotConfig (Models/RobotConfig.swift): -->
struct RobotConfig: Codable, Identifiable, Equatable, Hashable {
    var id: UUID
    var name: String
    var host: String
    var username: String?
    var useSSL: Bool
    var ignoreCertificateErrors: Bool
    // baseURL computed property existiert
}

<!-- RobotStatus (Services/RobotManager.swift): -->
struct RobotStatus {
    let isOnline: Bool
    let attributes: [RobotAttribute]
    let info: RobotInfo?

    // Computed: batteryLevel, batteryStatus, statusValue, statusFlag
    var statusValue: String? // z.B. "cleaning", "docked", "paused"
    var isCleaning: Bool { statusValue?.lowercased() == "cleaning" }
}

<!-- Capabilities (Models/RobotState.swift): -->
typealias Capabilities = [String]
// Beispiele: "HighResolutionManualControlCapability", "CleanRouteControlCapability",
//            "ObstacleImagesCapability", "AutoEmptyDockManualTriggerCapability"

<!-- RobotDetailViewModel (ViewModels/RobotDetailViewModel.swift): -->
@MainActor class RobotDetailViewModel: ObservableObject {
    init(robot: RobotConfig, robotManager: RobotManager)
    // Published properties:
    @Published var segments: [Segment] = []
    @Published var consumables: [Consumable] = []
    @Published var isLoading = false
    @Published var hasManualControl = false
    @Published var hasCleanRoute = false
    @Published var hasObstacleImages = false
    @Published var hasEvents = false
    @Published var events: [ValetudoEvent] = []
    @Published var cleanRoutePresets: [String] = []
    @Published var currentCleanRoute: String = ""
    @Published var robotProperties: RobotProperties?
    // Computed:
    var status: RobotStatus? { robotManager.robotStates[robot.id] }
    var isCleaning: Bool
    var isPaused: Bool
    var isRunning: Bool
    var hasConsumableWarning: Bool // consumables.contains { $0.remainingPercent < 20 }
}

<!-- RobotSettingsViewModel (ViewModels/RobotSettingsViewModel.swift): -->
@MainActor class RobotSettingsViewModel: ObservableObject {
    init(robot: RobotConfig, robotManager: RobotManager)
    @Published var volume: Double = 80
    @Published var hasVoicePack = false
    @Published var hasMapSnapshots = false
    @Published var hasPendingMapChange = false
    @Published var mapSnapshots: [MapSnapshot] = []
    @Published var voicePacks: [VoicePack] = []
    @Published var isLoading = false
    @Published var pendingMapChangeEnabled = false
}

<!-- MapViewModel (ViewModels/MapViewModel.swift): -->
@MainActor class MapViewModel: ObservableObject {
    init(robot: RobotConfig, robotManager: RobotManager)
    @Published var map: RobotMap?
    @Published var segments: [Segment] = []
    @Published var isLoading = true
    @Published var loadError: String?
    @Published var hasZoneCleaning = false
    @Published var hasGoTo = false
    @Published var editMode: MapEditMode = .none
    @Published var drawnZones: [CleaningZone] = []
    @Published var errorMessage: String? = nil
    @Published var selectedSegmentIds: Set<String> = []
    @Published var selectedIterations: Int = 1
    @Published var isCleaning = false
    func cancelEditMode()
    func splitRoom(...) async
}

<!-- Bestehendes Test-Pattern (aus MapLayerTests.swift): -->
import XCTest
@testable import ValetudoApp

final class SomeTests: XCTestCase {
    // Direkte Property-Assertions ohne Mocks
    // JSONDecoder fuer Struct-Konstruktion
    // @MainActor auf einzelnen Test-Methoden (nicht Klasse)
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RobotDetailViewModel Tests</name>
  <files>ValetudoApp/ValetudoAppTests/RobotDetailViewModelTests.swift</files>
  <action>
Neue Datei erstellen. @testable import ValetudoApp. Klasse RobotDetailViewModelTests: XCTestCase.

Zu testende Szenarien — alle ohne API-Mocking, direkt ueber ViewModel-Properties:

1. **Initialisierung**: ViewModel mit minimalem RobotConfig (UUID, name, host, useSSL: false) und RobotManager erstellen. Pruefen dass `isLoading == false`, `segments.isEmpty`, `consumables.isEmpty`, `events.isEmpty`.

2. **Capability-Flags Default**: Frisch erstelltes ViewModel hat `hasCleanRoute == false`, `hasObstacleImages == false`, `hasEvents == false` (sofern DebugConfig.showAllCapabilities == false — das ist der Default in Tests).

3. **hasConsumableWarning — kein Warning bei leerer Liste**: `consumables == []` → `hasConsumableWarning == false`.

4. **hasConsumableWarning — Warning bei niedrigem Verbrauchsmittel**: Consumable-Objekt mit remainingPercent < 20 direkt ueber JSONDecoder konstruieren (wie in MapLayerTests). JSON: `{"type":"filter","sub_type":null,"remaining":{"unit":"percent","value":15}}`. Consumable-Array auf ViewModel setzen. `hasConsumableWarning == true`.

5. **isCleaning / isPaused / isRunning aus RobotStatus**: RobotStatus direkt als Struct mit Attributen konstruieren. RobotManager.robotStates[robot.id] setzen (robotStates ist @Published, direkt beschreibbar). Pruefen dass isCleaning/isPaused/isRunning korrekte Bool-Werte liefern.
   - StatusStateAttribute mit value: "cleaning" → isCleaning == true, isRunning == true, isPaused == false
   - StatusStateAttribute mit value: "docked" → alle drei == false

6. **isRunning bei "returning"**: StatusStateAttribute value: "returning" → isRunning == true, isCleaning == false.

RobotAttribute konstruieren per JSON: `{"__class":"StatusStateAttribute","value":"cleaning","flag":"none"}`.

Jede Testmethode mit `@MainActor` annotieren (nicht die Klasse). setUp() erstellt frischen RobotManager und RobotConfig (UUID().uuidString als host).

WICHTIG: Nicht `loadData()` aufrufen — das wuerde echte API-Calls ausfuehren. Nur Published-Properties direkt setzen und Computed Properties pruefen.
  </action>
  <verify>
Datei ValetudoApp/ValetudoAppTests/RobotDetailViewModelTests.swift existiert.
`grep -c "func test" ValetudoApp/ValetudoAppTests/RobotDetailViewModelTests.swift` gibt mindestens 6 aus.
`grep "XCTestCase" ValetudoApp/ValetudoAppTests/RobotDetailViewModelTests.swift` findet Treffer.
  </verify>
  <done>Datei existiert, enthaelt XCTestCase, mind. 6 Testmethoden fuer State-Transitions ohne API-Calls.</done>
</task>

<task type="auto">
  <name>Task 2: RobotSettingsViewModel und MapViewModel Tests</name>
  <files>ValetudoApp/ValetudoAppTests/RobotSettingsViewModelTests.swift
ValetudoApp/ValetudoAppTests/MapViewModelTests.swift</files>
  <action>
**Datei 1: RobotSettingsViewModelTests.swift**

Klasse RobotSettingsViewModelTests: XCTestCase.

Szenarien:

1. **Initialisierung Default-Werte**: ViewModel erstellen. `volume == 80`, `isLoading == false`, `mapSnapshots.isEmpty`, `voicePacks.isEmpty`, `pendingMapChangeEnabled == false`.

2. **Capability-Flags Default**: `hasVoicePack == false`, `hasMapSnapshots == false`, `hasPendingMapChange == false` (DebugConfig.showAllCapabilities == false in Tests).

3. **VoicePack-Liste leer = Section implizit nicht angezeigt**: `voicePacks.isEmpty == true` nach Init — kein API-Call noetig, direkte Property-Pruefung genuegt.

4. **MapSnapshots leer nach Init**: `mapSnapshots.isEmpty && !isRestoringSnapshot` → kein Restore-Spinner.

**Datei 2: MapViewModelTests.swift**

Klasse MapViewModelTests: XCTestCase.

Szenarien:

1. **Initialisierung Default-Werte**: `isLoading == true`, `map == nil`, `segments.isEmpty`, `editMode == .none`, `drawnZones.isEmpty`, `errorMessage == nil`, `selectedSegmentIds.isEmpty`, `selectedIterations == 1`.

2. **Capability-Flags Default**: `hasZoneCleaning == false`, `hasGoTo == false` nach Init.

3. **cancelEditMode setzt editMode zurueck**: `editMode` auf `.drawZone` setzen, dann `cancelEditMode()` aufrufen. `editMode == .none`, `drawnZones.isEmpty`.

4. **isCleaning Default**: `isCleaning == false` nach Init.

5. **errorMessage setzbar**: `errorMessage = "Test Fehler"` → `errorMessage == "Test Fehler"`. (Prueft @Published-Wiring ohne async.)

Jede Testmethode `@MainActor` annotieren. setUp() erstellt RobotManager + RobotConfig pro Test neu (UUID als host).

MapEditMode ist ein Enum in MapViewModel — enum MapEditMode { case none, drawZone, drawNoGo, ... }. Falls der genaue Case-Name unklar ist, aus MapViewModel.swift auslesen (Zeile ~30: `@Published var editMode: MapEditMode = .none`).
  </action>
  <verify>
`grep -c "func test" ValetudoApp/ValetudoAppTests/RobotSettingsViewModelTests.swift` gibt mindestens 4 aus.
`grep -c "func test" ValetudoApp/ValetudoAppTests/MapViewModelTests.swift` gibt mindestens 5 aus.
Beide Dateien enthalten `XCTestCase` und `@testable import ValetudoApp`.
  </verify>
  <done>Beide Dateien existieren, enthalten XCTestCase, decken Default-State und einfache State-Mutations ohne API-Calls ab.</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
- 3 neue .swift-Dateien in ValetudoApp/ValetudoAppTests/ (neben den 4 bestehenden)
- Jede Datei: `@testable import ValetudoApp`, XCTestCase, mind. 3 Testmethoden
- Keine der bestehenden Test-Dateien wurde veraendert
- Keine Datei enthaelt `loadData()`, `loadSettings()` oder andere async API-aufrufende Methoden ohne Mock
</verification>

<success_criteria>
TEST-01 erfuellt wenn:
1. RobotDetailViewModelTests.swift: 6+ Tests fuer State (isCleaning, isPaused, isRunning, hasConsumableWarning, Capability-Defaults, Init-Defaults)
2. RobotSettingsViewModelTests.swift: 4+ Tests fuer Settings-Default-State und Capability-Flags
3. MapViewModelTests.swift: 5+ Tests fuer Map-State, cancelEditMode, errorMessage
4. Alle Tests laufen in Xcode gruen (nach User-seitigem Hinzufuegen der Dateien zum Test-Target)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-test-coverage/08-01-SUMMARY.md` erstellen.
</output>
