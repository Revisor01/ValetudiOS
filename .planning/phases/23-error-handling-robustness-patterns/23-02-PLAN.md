---
phase: 23-error-handling-robustness-patterns
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  - ValetudoApp/ValetudoApp/Services/RobotManager.swift
  - ValetudoApp/ValetudoApp/Services/UpdateService.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
autonomous: true
requirements: [DEBT-05, DEBT-06]

must_haves:
  truths:
    - "onChange-Handler in RobotSettingsView feuern nicht waehrend loadSettings() laeuft"
    - "Bei einem zweiten loadSettings()-Aufruf werden onChange-Handler erneut blockiert"
    - "Capabilities werden aus dem Cache geladen wenn TTL (24h) nicht abgelaufen ist"
    - "Nach einem OTA-Reboot werden Capabilities beim naechsten Laden frisch von der API geholt"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "settingsLoaded Two-Phase-Pattern statt isInitialLoad"
      contains: "settingsLoaded"
    - path: "ValetudoApp/ValetudoApp/Services/RobotManager.swift"
      provides: "Capabilities-TTL-Cache mit invalidate"
      contains: "capabilitiesCache"
    - path: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      provides: "Capabilities-Invalidierung nach OTA-Reboot"
      contains: "invalidateCapabilities"
  key_links:
    - from: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      to: "RobotManager.cachedCapabilities"
      via: "Cache-Lookup vor API-Call"
      pattern: "robotManager\\.cachedCapabilities"
    - from: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      to: "RobotManager.invalidateCapabilities"
      via: "Call nach pollUntilReboot success"
      pattern: "invalidateCapabilities"
---

<objective>
Das fragile isInitialLoad-Boolean-Pattern in RobotSettingsViewModel durch ein robustes Two-Phase-Load-Pattern ersetzen, und einen TTL-basierten Capabilities-Cache in RobotManager einfuehren mit Force-Invalidierung nach OTA-Updates.

Purpose: DEBT-05 (isInitialLoad Race-Condition und Re-Load-Problem) und DEBT-06 (redundante Capabilities-API-Calls + fehlender Refresh nach OTA) beseitigen
Output: settingsLoaded-Pattern, Capabilities-Cache in RobotManager, OTA-Invalidierungs-Hook
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-error-handling-robustness-patterns/23-RESEARCH.md
@.planning/phases/23-error-handling-robustness-patterns/23-01-SUMMARY.md

@ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
@ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
@ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
@ValetudoApp/ValetudoApp/Services/RobotManager.swift
@ValetudoApp/ValetudoApp/Services/UpdateService.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift

<interfaces>
From ValetudoApp/ValetudoApp/Services/RobotManager.swift:
```swift
@MainActor @Observable class RobotManager {
    var robots: [RobotConfig] = []
    var robotStates: [UUID: RobotStatus] = [:]
    var roomSelections: [UUID: [String]] = [:]
    var iterationSelections: [UUID: Int] = [:]
    // ... (Plan 02 adds capabilitiesCache here)
}
```

From ValetudoApp/ValetudoApp/Services/UpdateService.swift:
```swift
@MainActor @Observable final class UpdateService {
    var phase: UpdatePhase = .idle
    private let api: ValetudoAPI
    private let robotManager: RobotManager  // bereits vorhanden
    private let robotId: UUID               // bereits vorhanden
    private func pollUntilReboot() async { ... }
    // pollUntilReboot setzt phase auf .idle nach erfolgreichem Reboot
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-Phase-Load-Pattern in RobotSettingsViewModel und StationSettingsView (DEBT-05)</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  </read_first>
  <action>
**In RobotSettingsViewModel.swift:**
1. Zeile 74: Rename `var isInitialLoad = true` zu `private(set) var settingsLoaded = false` (invertierte Semantik: false = noch am Laden, true = fertig geladen)
2. In `loadSettings()` (Funktion suchen): Am ANFANG der Funktion `settingsLoaded = false` setzen. Am ENDE (Zeile 297, wo aktuell `isInitialLoad = false` steht) durch `settingsLoaded = true` ersetzen. Idealerweise mit defer:
```swift
func loadSettings() async {
    settingsLoaded = false
    isLoading = true
    defer {
        isLoading = false
        settingsLoaded = true
    }
    // ... rest der Funktion bleibt gleich ...
}
```
3. Entferne die alte `isInitialLoad`-Property komplett.

**In RobotSettingsView.swift:**
Alle `guard !viewModel.isInitialLoad else { return }` (Zeilen 79, 93, 107, 121, 135, 153, 173, 195, 341) ersetzen durch:
```swift
guard viewModel.settingsLoaded else { return }
```
Beachte: Die Semantik ist invertiert — `isInitialLoad == true` bedeutete "noch am Laden", `settingsLoaded == true` bedeutet "fertig geladen". Also `!isInitialLoad` wird zu `settingsLoaded` (ohne Negation).

Fuer Zeilen die zusaetzliche Bedingungen haben (z.B. Zeile 153 `guard !viewModel.isInitialLoad && !newValue.isEmpty else { return }`), wird es:
```swift
guard viewModel.settingsLoaded && !newValue.isEmpty else { return }
```

**In RobotSettingsSections.swift (StationSettingsView):**
1. Zeile 779: `@State private var isInitialLoad = true` bleibt als lokaler @State (kein ViewModel hier), aber wird ebenfalls umbenannt zu `@State private var stationLoaded = false`
2. Zeilen 831, 854, 872, 890: Alle `guard !isInitialLoad` ersetzen durch `guard stationLoaded`
Fuer Zeilen mit zusaetzlichen Bedingungen (z.B. `guard !isInitialLoad && !newValue.isEmpty`): `guard stationLoaded && !newValue.isEmpty`
3. Zeile 1006: `isInitialLoad = false` ersetzen durch `stationLoaded = true`
4. Am Anfang der `loadStation()`-Funktion: `stationLoaded = false` hinzufuegen (damit Re-Loads korrekt funktionieren)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -rn "isInitialLoad" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "isInitialLoad" in allen drei Dateien returns 0 matches (komplett entfernt)
    - grep "settingsLoaded" in RobotSettingsViewModel.swift returns >= 3 matches (declaration + false-set + true-set)
    - grep "settingsLoaded" in RobotSettingsView.swift returns >= 9 matches (alle onChange-Guards)
    - grep "stationLoaded" in RobotSettingsSections.swift returns >= 6 matches (declaration + guards + set)
    - grep "stationLoaded = false" in RobotSettingsSections.swift returns 1 match (am Anfang von loadStation)
    - grep "settingsLoaded = false" in RobotSettingsViewModel.swift returns 1 match (am Anfang von loadSettings)
  </acceptance_criteria>
  <done>isInitialLoad ist komplett durch settingsLoaded (ViewModel) und stationLoaded (StationSettingsView) ersetzt. Beide werden bei Re-Loads zurueckgesetzt. Keine isInitialLoad-Referenzen mehr in der Codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Capabilities-TTL-Cache in RobotManager mit OTA-Invalidierung (DEBT-06)</name>
  <files>
    ValetudoApp/ValetudoApp/Services/RobotManager.swift,
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Services/RobotManager.swift,
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  </read_first>
  <action>
**In RobotManager.swift:**
1. Nach den bestehenden Properties (roomSelections, iterationSelections) folgende Cache-Properties hinzufuegen:
```swift
// MARK: - Capabilities Cache (DEBT-06)
@ObservationIgnored private var capabilitiesCache: [UUID: [String]] = [:]
@ObservationIgnored private var capabilitiesCacheDate: [UUID: Date] = [:]
private let capabilitiesTTL: TimeInterval = 86400 // 24 Stunden
```

2. Drei Methoden hinzufuegen:
```swift
func cachedCapabilities(for robotId: UUID) -> [String]? {
    guard let date = capabilitiesCacheDate[robotId],
          Date().timeIntervalSince(date) < capabilitiesTTL else { return nil }
    return capabilitiesCache[robotId]
}

func cacheCapabilities(_ capabilities: [String], for robotId: UUID) {
    capabilitiesCache[robotId] = capabilities
    capabilitiesCacheDate[robotId] = Date()
}

func invalidateCapabilities(for robotId: UUID) {
    capabilitiesCache.removeValue(forKey: robotId)
    capabilitiesCacheDate.removeValue(forKey: robotId)
}
```

**In UpdateService.swift:**
1. Pruefe ob UpdateService bereits eine Referenz auf `robotManager` hat (suche nach `robotManager` Property). Falls ja, nutze diese.
2. Falls nein: Fuege `private let robotManager: RobotManager` als Property hinzu und passe den init() an.
3. Pruefe ob UpdateService eine `robotId: UUID` kennt. Falls nicht, muss sie als Property existieren (suche nach wie der Robot identifiziert wird — vermutlich ueber `api` die per Robot erstellt wird).
4. In `pollUntilReboot()`, direkt NACH `setPhase(.idle)` (Zeile 234) und VOR `return`:
```swift
setPhase(.idle)
robotManager.invalidateCapabilities(for: robotId)
return
```
Falls kein direkter robotId verfuegbar ist, suche wie UpdateService den Robot identifiziert und leite die UUID ab.

**In RobotDetailViewModel.swift (loadCapabilities):**
In der `loadCapabilities()`-Funktion (suche nach `func loadCapabilities()`): VOR dem API-Call pruefen ob Cache vorhanden:
```swift
func loadCapabilities() async {
    guard let api = api else { return }
    
    // Cache-Check
    if let cached = robotManager.cachedCapabilities(for: robot.id) {
        applyCapabilities(cached)
        return
    }
    
    do {
        let capabilities = try await api.getCapabilities()
        robotManager.cacheCapabilities(capabilities, for: robot.id)
        applyCapabilities(capabilities)
    } catch {
        logger.error("Failed to load capabilities: \(error, privacy: .public)")
    }
}
```
Falls `applyCapabilities` noch nicht existiert: Extrahiere die bestehende Capability-Zuweisung (hasManualControl = ..., etc.) in eine private Hilfsmethode `applyCapabilities(_ capabilities: [String])`.

**In RobotSettingsViewModel.swift (loadSettings):**
Gleicher Ansatz — in dem Teil wo `api.getCapabilities()` aufgerufen wird:
```swift
let capabilities: [String]
if let cached = robotManager.cachedCapabilities(for: robot.id) {
    capabilities = cached
} else {
    capabilities = try await api.getCapabilities()
    robotManager.cacheCapabilities(capabilities, for: robot.id)
}
// ... bestehende hasX = DebugConfig.showAllCapabilities || capabilities.contains(...) Zeilen bleiben
```
Stelle sicher dass `robotManager` im ViewModel verfuegbar ist (suche nach bestehender robotManager-Property).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -n "capabilitiesCache\|cachedCapabilities\|cacheCapabilities\|invalidateCapabilities" ValetudoApp/ValetudoApp/Services/RobotManager.swift ValetudoApp/ValetudoApp/Services/UpdateService.swift ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep "capabilitiesCache" in RobotManager.swift returns >= 4 matches (declaration + 3 methods)
    - grep "cachedCapabilities" in RobotDetailViewModel.swift returns >= 1 match
    - grep "cachedCapabilities" in RobotSettingsViewModel.swift returns >= 1 match
    - grep "cacheCapabilities" in RobotDetailViewModel.swift returns >= 1 match
    - grep "invalidateCapabilities" in UpdateService.swift returns >= 1 match
    - grep "capabilitiesTTL" in RobotManager.swift returns 1 match mit Wert 86400
    - Xcode Build kompiliert erfolgreich
  </acceptance_criteria>
  <done>Capabilities werden pro Robot in RobotManager gecacht (24h TTL). Alle ViewModels pruefen zuerst den Cache. Nach OTA-Reboot wird der Cache invalidiert, sodass beim naechsten Oeffnen frische Capabilities geladen werden.</done>
</task>

</tasks>

<verification>
1. `grep -rn "isInitialLoad" ValetudoApp/ValetudoApp/` — 0 matches in der gesamten App
2. `grep -n "settingsLoaded = false" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift` — genau 1 match am Anfang von loadSettings()
3. `grep -n "invalidateCapabilities" ValetudoApp/ValetudoApp/Services/UpdateService.swift` — >= 1 match in pollUntilReboot()
4. `grep -n "cachedCapabilities" ValetudoApp/ValetudoApp/ViewModels/` — mindestens 2 matches (RobotDetailVM + RobotSettingsVM)
5. Xcode Build: `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5` — BUILD SUCCEEDED
</verification>

<success_criteria>
- isInitialLoad existiert nirgends mehr — ersetzt durch settingsLoaded und stationLoaded
- settingsLoaded wird bei jedem loadSettings()-Aufruf zurueckgesetzt (Re-Load-sicher)
- Capabilities-Cache in RobotManager mit 24h TTL
- Alle ViewModels nutzen Cache vor API-Call
- UpdateService invalidiert Cache nach erfolgreichem OTA-Reboot
- Projekt kompiliert ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/23-error-handling-robustness-patterns/23-02-SUMMARY.md`
</output>
