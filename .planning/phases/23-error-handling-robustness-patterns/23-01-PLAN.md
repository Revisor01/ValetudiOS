---
phase: 23-error-handling-robustness-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/MapView.swift
autonomous: true
requirements: [DEBT-03, DEBT-04]

must_haves:
  truths:
    - "Benutzer sieht einen Alert wenn locate() fehlschlaegt"
    - "Benutzer sieht einen Alert wenn joinRooms() fehlschlaegt"
    - "Benutzer sieht einen Alert wenn splitRoom() fehlschlaegt"
    - "Jeder API-Fehler in catch-Bloecken von RobotSettingsViewModel wird geloggt, auch wenn DebugConfig.showAllCapabilities true ist"
    - "Jeder API-Fehler in catch-Bloecken von RobotDetailViewModel wird geloggt"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "ErrorRouter injection + do/catch statt try? in locate()"
      contains: "errorRouter"
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "ErrorRouter injection + do/catch in joinRooms/splitRoom"
      contains: "errorRouter"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "logger.error in allen catch-Bloecken + ErrorRouter fuer User-Aktionen"
      contains: "errorRouter"
  key_links:
    - from: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      to: "RobotDetailViewModel.errorRouter"
      via: ".task { viewModel.errorRouter = errorRouter }"
      pattern: "viewModel\\.errorRouter\\s*=\\s*errorRouter"
    - from: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      to: "MapViewModel.errorRouter"
      via: ".task { viewModel.errorRouter = errorRouter }"
      pattern: "viewModel\\.errorRouter\\s*=\\s*errorRouter"
---

<objective>
ErrorRouter in alle ViewModels injizieren und stille try?-Fehler in Benutzeraktionen durch do/catch mit ErrorRouter.show() ersetzen. Gleichzeitig alle catch-Bloecke in RobotSettingsViewModel und RobotDetailViewModel mit logger.error() versehen, damit API-Fehler nie mehr stillschweigend verschluckt werden.

Purpose: DEBT-03 (kein try? in User-Aktionen) und DEBT-04 (DebugConfig maskiert keine Fehler mehr) beseitigen
Output: ViewModels mit ErrorRouter-Property, alle User-Aktionen mit do/catch, alle catch-Bloecke mit Logging
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-error-handling-robustness-patterns/23-RESEARCH.md

@ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
@ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
@ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
@ValetudoApp/ValetudoApp/Views/MapView.swift

<interfaces>
<!-- ErrorRouter API — bereits vorhanden, wird von ViewModels genutzt -->
From ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift:
```swift
@MainActor @Observable final class ErrorRouter {
    var currentError: Error?
    var retryAction: (() async -> Void)?
    func show(_ error: Error, retry: (() async -> Void)? = nil)
    func dismiss()
}
```

<!-- withErrorAlert ist bereits auf Root-Level (ValetudoApp.swift) angewendet -->
<!-- Kein zusaetzliches .withErrorAlert() in einzelnen Views noetig -->
</interfaces>

<already_handled>
Die folgenden User-Aktionen verwenden BEREITS korrektes do/catch mit logger.error() und benoetigen KEINE Aenderungen in diesem Plan:

1. **renameRoom()** in MapViewModel.swift (Zeile 251): Verwendet do/catch mit `logger.error("renameSegment: ...")` und `logger.debug`. Korrekt implementiert.

2. **cleanSelectedRooms()** in MapViewModel.swift (Zeile 195): Verwendet do/catch mit `logger.error("cleanSelectedRooms FAILED: ...")` und setzt `errorMessage`. Korrekt implementiert.

3. **cleanSelectedRooms()** in RobotDetailViewModel.swift (Zeile 343): Verwendet do/catch mit `logger.error("Clean failed: ...")`. Korrekt implementiert.

Diese Funktionen sind NICHT Teil der "7 try?-Stellen" aus RESEARCH.md und muessen nicht angefasst werden.
</already_handled>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorRouter-Property in alle drei ViewModels + try?-Ersetzung in User-Aktionen (DEBT-03)</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </read_first>
  <action>
**In RobotDetailViewModel.swift:**
1. Add property: `var errorRouter: ErrorRouter?` nach den Capability-Flags (ca. Zeile 39)
2. Zeile 340 — Replace `try? await api.locate()` mit:
```swift
do {
    try await api.locate()
} catch {
    logger.error("locate failed: \(error, privacy: .public)")
    errorRouter?.show(error)
}
```

**In MapViewModel.swift:**
1. Add property: `var errorRouter: ErrorRouter?` nach den Capability-Properties (ca. Zeile 30)
2. Zeile 301/304 in `joinRooms()` — Die try? nach dem erfolgreichen `api.joinSegments()` Call (Map/Segments-Reload) in do/catch umwandeln. Bei Fehler: loggen UND errorRouter?.show(error). Der Join selbst ist bereits in einem do/catch — die Reloads danach sind es nicht:
```swift
// Nach erfolgreichem joinSegments:
do {
    let newMap = try await api.getMap()
    map = newMap
} catch {
    logger.error("Map reload after join failed: \(error, privacy: .public)")
    errorRouter?.show(error)
}
do {
    let newSegments = try await api.getSegments()
    segments = newSegments
} catch {
    logger.error("Segments reload after join failed: \(error, privacy: .public)")
    errorRouter?.show(error)
}
```
3. Zeile 348/351 in `splitRoom()` — Identisches Pattern wie joinRooms: die try? fuer Map/Segments-Reload nach erfolgreichem splitSegment ersetzen.

**In RobotSettingsViewModel.swift:**
1. Add property: `var errorRouter: ErrorRouter?` nach den Capability-Flags (ca. Zeile 49)
2. Zeile 497 in `setVoicePack()` — Replace `if let state = try? await api.getVoicePackState()` mit do/catch:
```swift
do {
    let state = try await api.getVoicePackState()
    currentVoicePackId = state.currentLanguage.id
} catch {
    logger.error("getVoicePackState failed in setVoicePack: \(error, privacy: .public)")
    errorRouter?.show(error)
}
```
3. Zeile 510 in `restoreMapSnapshot()` — Replace `(try? await api.getMapSnapshots()) ?? mapSnapshots` mit do/catch:
```swift
do {
    mapSnapshots = try await api.getMapSnapshots()
} catch {
    logger.error("getMapSnapshots reload failed: \(error, privacy: .public)")
    // mapSnapshots bleibt unveraendert — kein errorRouter noetig, ist nur ein Reload
}
```

**In RobotDetailView.swift:**
1. Add `@Environment(ErrorRouter.self) var errorRouter` als Property (nach `@State private var viewModel`)
2. In `.task {}` (Zeile 292) — VOR `await viewModel.loadData()` den errorRouter setzen:
```swift
.task {
    viewModel.errorRouter = errorRouter
    await viewModel.loadData()
}
```

**In MapView.swift (MapContentView, Zeile 171):**
MapContentView haelt `@State var viewModel: MapViewModel` (Zeile 176). Hier muss der ErrorRouter ebenfalls injiziert werden:
1. Add `@Environment(ErrorRouter.self) var errorRouter` als Property in MapContentView (nach den bestehenden @State Properties, ca. Zeile 191)
2. In `.task {}` (Zeile 390, der Task-Block der `viewModel.loadMap()` und `viewModel.startMapRefresh()` aufruft) — VOR dem ersten async Call den errorRouter setzen:
```swift
.task {
    viewModel.errorRouter = errorRouter
    await viewModel.loadMap()
    viewModel.startMapRefresh()
}
```

**HINWEIS zu RobotSettingsViewModel:** Die View die dieses ViewModel als @State haelt muss separat gefunden werden. Suche nach `@State.*RobotSettingsViewModel` — falls in einer der bereits in <files> gelisteten Views oder einer anderen View: dort ebenfalls das ErrorRouter-Injection-Pattern anwenden. Falls RobotSettingsView das ViewModel nicht als @State haelt sondern es anders empfaengt, die Injection dort anpassen.

**NICHT aendern:** try? in Background/System-Operationen (Task.sleep, Polling-Loops, Siri Intents, BackgroundMonitor, Discovery). Nur die 7 try? aus der "Benutzer-initiierte Aktionen"-Liste im RESEARCH.md.

**BEREITS KORREKT (nicht anfassen):** renameRoom() (MapViewModel Zeile 251) und cleanSelectedRooms() (MapViewModel Zeile 195, RobotDetailViewModel Zeile 343) verwenden bereits do/catch mit logger.error(). Siehe <already_handled> im Context.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "try?" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift && grep -c "errorRouter" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift ValetudoApp/ValetudoApp/Views/MapView.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep "var errorRouter: ErrorRouter?" in RobotDetailViewModel.swift returns 1 match
    - grep "var errorRouter: ErrorRouter?" in MapViewModel.swift returns 1 match
    - grep "var errorRouter: ErrorRouter?" in RobotSettingsViewModel.swift returns 1 match
    - grep "try? await api.locate()" in RobotDetailViewModel.swift returns 0 matches
    - grep "try? await api.getMap()" in joinRooms/splitRoom context of MapViewModel.swift returns 0 matches (background polling try? bleibt)
    - grep "try? await api.getVoicePackState()" in RobotSettingsViewModel.swift returns 0 matches
    - grep "try? await api.getMapSnapshots()" in RobotSettingsViewModel.swift returns 0 matches
    - grep "viewModel.errorRouter = errorRouter" in RobotDetailView.swift returns 1 match
    - grep "viewModel.errorRouter = errorRouter" in MapView.swift returns 1 match (in MapContentView)
    - grep "errorRouter?.show(error)" in RobotDetailViewModel.swift returns at least 1 match
    - grep "errorRouter?.show(error)" in MapViewModel.swift returns at least 2 matches
  </acceptance_criteria>
  <done>Alle 7 try? in User-Aktionen sind durch do/catch mit ErrorRouter.show() ersetzt. ErrorRouter ist als Optional Property in allen drei ViewModels. Views (RobotDetailView, MapContentView) setzen errorRouter in .task.</done>
</task>

<task type="auto">
  <name>Task 2: Logger.error in alle catch-Bloecke von RobotSettingsViewModel und RobotDetailViewModel (DEBT-04)</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  </read_first>
  <action>
In RobotSettingsViewModel.swift gibt es catch-Bloecke die KEIN logger.error() haben — nur `if !DebugConfig.showAllCapabilities { hasX = false }`. Fuer JEDEN dieser catch-Bloecke eine logger.error()-Zeile DAVOR einfuegen:

**Catch-Bloecke OHNE Logging (muessen logger.error bekommen):**
- Zeile 98: `catch { if !DebugConfig.showAllCapabilities { hasVolumeControl = false } }` — Add: `logger.error("getSpeakerVolume failed: \(error, privacy: .public)")`
- Zeile 104-105: catch fuer getCarpetMode — Add: `logger.error("getCarpetMode failed: \(error, privacy: .public)")`
- Zeile 111-112: catch fuer getPersistentMap — Add: `logger.error("getPersistentMap failed: \(error, privacy: .public)")`
- Zeile 137-138: catch fuer getCapabilities — Add: `logger.error("getCapabilities failed: \(error, privacy: .public)")`
- Zeile 145-146: catch fuer getKeyLock — Add: `logger.error("getKeyLock failed: \(error, privacy: .public)")`
- Zeile 154-155: catch fuer getObstacleAvoidance — Add: `logger.error("getObstacleAvoidance failed: \(error, privacy: .public)")`
- Zeile 163-164: catch fuer getPetObstacleAvoidance — Add: `logger.error("getPetObstacleAvoidance failed: \(error, privacy: .public)")`
- Zeile 175-176: catch fuer getCarpetSensorMode — Add: `logger.error("getCarpetSensorMode failed: \(error, privacy: .public)")`
- Zeile 187-188: catch fuer getCollisionAvoidantNavigation — Add: `logger.error("getCollisionAvoidantNavigation failed: \(error, privacy: .public)")`
- Zeile 196-197: catch fuer getFloorMaterialNavigation — Add: `logger.error("getFloorMaterialNavigation failed: \(error, privacy: .public)")`
- Zeile 205-206: catch fuer getMopDockAutoDrying — Add: `logger.error("getMopDockAutoDrying failed: \(error, privacy: .public)")`

**Catch-Bloecke die BEREITS loggen (nur pruefen, nicht aendern):**
- Zeile 219-220 (hasMopDockWashTemperature) — hat bereits logger.debug
- Zeile 237-238 (hasMopDockDryingTime) — hat bereits logger.debug
- Zeile 248-249 (hasMapSnapshots) — hat bereits logger.debug
- Zeile 259-260 (hasPendingMapChange) — hat bereits logger.debug
- Zeile 271-272 (hasVoicePack) — hat bereits logger.debug
- Zeile 289-290 (hasAutoEmptyDockDuration) — hat bereits logger.debug

Fuer die Bloecke die `logger.debug` haben: Upgrade auf `logger.error` wenn der catch-Block die Capability deaktiviert (da ein echter API-Fehler vorliegen koennte, nicht nur "nicht unterstuetzt"). Lasse logger.debug nur dort wo es ein erwarteter "nicht unterstuetzt"-Fall ist UND der catch-Block eine spezifische Fehlernachricht wie "not supported" loggt.

**DebugConfig.showAllCapabilities-Semantik bleibt UNVERAENDERT** — das Flag steuert weiterhin ob die Capability-Flag auf false gesetzt wird. Der Unterschied: jetzt wird IMMER geloggt, unabhaengig vom Flag.

In RobotDetailViewModel.swift:
- Zeile 183 catch nach loadCapabilities() hat bereits `logger.error("Failed to load capabilities...")` — OK
- Zeilen 192, 201, 210 (presets) haben `logger.debug(...)` — OK, sind "nicht unterstuetzt"-Faelle
- Zeile 239 und 251 — pruefen ob logger vorhanden, falls nicht: hinzufuegen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -n "} catch {" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift | wc -l && grep -n "logger\.\(error\|debug\)" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Jeder catch-Block in RobotSettingsViewModel.swift der `DebugConfig.showAllCapabilities` referenziert hat auch eine logger.error() oder logger.debug() Zeile DAVOR
    - grep "catch {" in RobotSettingsViewModel.swift und grep "logger." in RobotSettingsViewModel.swift: die Anzahl der logger-Aufrufe ist >= Anzahl der catch-Bloecke (jeder catch loggt)
    - Kein catch-Block in RobotSettingsViewModel.swift hat NUR `if !DebugConfig.showAllCapabilities { ... }` ohne logger-Aufruf
    - grep -A1 "} catch {" RobotSettingsViewModel.swift zeigt nie eine Zeile mit nur DebugConfig ohne vorherigen logger-Aufruf
  </acceptance_criteria>
  <done>Alle catch-Bloecke in RobotSettingsViewModel und RobotDetailViewModel loggen API-Fehler via logger.error(), unabhaengig vom DebugConfig-Flag. Kein API-Fehler wird mehr stillschweigend verschluckt.</done>
</task>

</tasks>

<verification>
1. `grep -rn "try? await api\." ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` — nur Task.sleep-try? erlaubt, keine API-Calls
2. `grep -rn "try? await api\.\(getMap\|getSegments\)" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — nur in Background-Polling (Zeile ~170), nicht in joinRooms/splitRoom
3. `grep -rn "try? await api\." ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift` — 0 matches
4. `grep -c "errorRouter" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` — >= 3 (declaration + usages)
5. `grep -c "errorRouter" ValetudoApp/ValetudoApp/Views/MapView.swift` — >= 2 (declaration + injection)
6. Xcode Build: `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5` — BUILD SUCCEEDED
</verification>

<success_criteria>
- Kein try? mehr in benutzer-initiierten Aktionen (locate, joinRooms, splitRoom, setVoicePack recovery, restoreMapSnapshot reload)
- ErrorRouter als var errorRouter: ErrorRouter? in allen drei ViewModels
- Views setzen errorRouter in .task bevor loadData() aufgerufen wird (RobotDetailView UND MapContentView)
- renameRoom() und cleanSegments() sind bereits korrekt mit do/catch — keine Aenderungen noetig
- Alle catch-Bloecke in RobotSettingsViewModel haben logger.error() oder logger.debug()
- DebugConfig-Semantik fuer Capability-Flags bleibt unveraendert
- Projekt kompiliert ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/23-error-handling-robustness-patterns/23-01-SUMMARY.md`
</output>
