---
phase: 03-api-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoApp/Models/RobotState.swift
  - ValetudoApp/ValetudoApp/Models/RobotMap.swift
autonomous: true
requirements: [API-01, API-02, API-03, API-04, UX-04]

must_haves:
  truths:
    - "ValetudoAPI hat Methoden fuer MapSnapshot list+restore, PendingMapChange get+handle, CleanRoute get+set, Events get, ObstacleImage binary fetch"
    - "Model-Structs MapSnapshot, PendingMapChangeState, CleanRouteState, ValetudoEvent existieren als Codable"
    - "EntityMetaData hat id und label Properties fuer Obstacle-Foto-Feature"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      provides: "8 neue API-Methoden fuer Phase-3 Capabilities"
      contains: "getMapSnapshots"
    - path: "ValetudoApp/ValetudoApp/Models/RobotState.swift"
      provides: "MapSnapshot, PendingMapChangeState, CleanRouteState, ValetudoEvent structs"
      contains: "struct ValetudoEvent"
    - path: "ValetudoApp/ValetudoApp/Models/RobotMap.swift"
      provides: "Erweiterte EntityMetaData mit id und label"
      contains: "let id: String?"
  key_links:
    - from: "ValetudoAPI.getMapSnapshots()"
      to: "MapSnapshot struct"
      via: "request<T> generic decode"
      pattern: "request.*MapSnapshotCapability"
    - from: "ValetudoAPI.getEvents()"
      to: "ValetudoEvent struct"
      via: "request<T> generic decode"
      pattern: "request.*valetudo/events"
    - from: "ValetudoAPI.getObstacleImage(id:)"
      to: "URLSession.data(for:)"
      via: "raw binary fetch without JSON decode"
      pattern: "ObstacleImagesCapability/img"
---

<objective>
API-Methoden und Model-Structs fuer alle Phase-3 Capabilities anlegen.

Purpose: Alle nachfolgenden Plans (UI, Notifications) benoetigen die API-Schicht und Datenmodelle. Dieses Plan stellt die Contracts bereit.
Output: 8 neue API-Methoden in ValetudoAPI.swift, 4 neue Model-Structs in RobotState.swift, erweiterte EntityMetaData in RobotMap.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-completeness/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From ValetudoAPI.swift — base request methods:
```swift
actor ValetudoAPI {
    private func request<T: Decodable>(_ endpoint: String, method: String = "GET", body: Data? = nil) async throws -> T
    private func requestVoid(_ endpoint: String, method: String = "PUT", body: Data? = nil) async throws
    // Auth header is set automatically from config.username + KeychainStore.password(for: config.id)
    // URL pattern: /api/v2{endpoint}
}
```

From RobotState.swift — Capabilities type:
```swift
typealias Capabilities = [String]
// Capability names are strings like "MapSnapshotCapability", "PendingMapChangeHandlingCapability" etc.
```

From RobotMap.swift — current EntityMetaData:
```swift
struct EntityMetaData: Codable {
    let angle: Int?
}
```

From ValetudoAPI.swift — existing pattern for encoded body:
```swift
func basicControl(action: BasicAction) async throws {
    let body = try JSONEncoder().encode(BasicControlRequest(action: action.rawValue))
    try await requestVoid("/robot/capabilities/BasicControlCapability", body: body)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model-Structs fuer neue API-Antworten</name>
  <files>ValetudoApp/ValetudoApp/Models/RobotState.swift, ValetudoApp/ValetudoApp/Models/RobotMap.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Models/RobotState.swift (vollstaendig lesen — alle bestehenden Structs, Enums, CodingKeys-Pattern verstehen)
    - ValetudoApp/ValetudoApp/Models/RobotMap.swift (EntityMetaData Zeile ~143 lesen)
    - .planning/phases/03-api-completeness/03-RESEARCH.md (Pattern 2: Model-Structs, Code Examples Abschnitt)
  </read_first>
  <action>
    In RobotState.swift am Ende der Datei (vor dem letzten schliessenden Block) folgende Structs hinzufuegen, jeweils mit MARK-Kommentar:

    1. `// MARK: - Map Snapshots` (per D-01, API-01)
       ```swift
       struct MapSnapshot: Codable, Identifiable {
           let id: String
       }
       ```
       Hinweis: Laut OpenAPI-Spec gibt GET /robot/capabilities/MapSnapshotCapability ein Array von Objekten mit "id" zurueck. Kein timestamp-Feld in der Spec — nur id als String.

    2. `// MARK: - Pending Map Change` (per D-01, API-02)
       ```swift
       struct PendingMapChangeState: Codable {
           let enabled: Bool
       }
       ```

    3. `// MARK: - Clean Route` (per D-02, API-03)
       ```swift
       struct CleanRouteState: Codable {
           let route: String
       }
       ```

    4. `// MARK: - Valetudo Events` (per D-04, UX-04)
       ```swift
       struct ValetudoEvent: Codable, Identifiable {
           let __class: String
           let id: String
           let timestamp: String
           let processed: Bool
           let type: String?
           let subType: String?
           let message: String?

           enum CodingKeys: String, CodingKey {
               case __class = "__class"
               case id, timestamp, processed, type, subType, message
           }

           var displayName: String {
               switch __class {
               case "DustBinFullValetudoEvent": return String(localized: "event.dustbin_full")
               case "ConsumableDepletedValetudoEvent": return String(localized: "event.consumable_depleted")
               case "MopAttachmentReminderValetudoEvent": return String(localized: "event.mop_reminder")
               case "ErrorStateValetudoEvent": return String(localized: "event.error")
               case "MissingResourceValetudoEvent": return String(localized: "event.missing_resource")
               case "PendingMapChangeValetudoEvent": return String(localized: "event.pending_map_change")
               default: return __class
               }
           }

           var iconName: String {
               switch __class {
               case "DustBinFullValetudoEvent": return "trash.fill"
               case "ConsumableDepletedValetudoEvent": return "wrench.fill"
               case "MopAttachmentReminderValetudoEvent": return "drop.fill"
               case "ErrorStateValetudoEvent": return "exclamationmark.triangle.fill"
               case "MissingResourceValetudoEvent": return "questionmark.circle.fill"
               case "PendingMapChangeValetudoEvent": return "map.fill"
               default: return "bell.fill"
               }
           }
       }
       ```

    In RobotMap.swift die bestehende `EntityMetaData` erweitern (per D-03, API-04):
    ```swift
    struct EntityMetaData: Codable {
        let angle: Int?
        let id: String?       // UUID fuer ObstacleImagesCapability/img/{id}
        let label: String?    // z.B. "Pedestal (89%)"
    }
    ```
    WICHTIG: Die bestehende `let angle: Int?` Property beibehalten und die neuen Properties darunter hinzufuegen.

    Localization-Keys fuer ValetudoEvent.displayName muessen in Localizable.xcstrings existieren — der Executor soll diese NICHT manuell anlegen (Xcode generiert sie automatisch beim Build wenn String(localized:) verwendet wird, oder sie werden spaeter in Plan 03 beim UI-Bau hinzugefuegt).
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "struct MapSnapshot: Codable, Identifiable" ValetudoApp/ValetudoApp/Models/RobotState.swift
    - grep -q "struct PendingMapChangeState: Codable" ValetudoApp/ValetudoApp/Models/RobotState.swift
    - grep -q "struct CleanRouteState: Codable" ValetudoApp/ValetudoApp/Models/RobotState.swift
    - grep -q "struct ValetudoEvent: Codable, Identifiable" ValetudoApp/ValetudoApp/Models/RobotState.swift
    - grep -q "var displayName: String" ValetudoApp/ValetudoApp/Models/RobotState.swift
    - grep -q "var iconName: String" ValetudoApp/ValetudoApp/Models/RobotState.swift
    - grep -q 'let id: String?' ValetudoApp/ValetudoApp/Models/RobotMap.swift
    - grep -q 'let label: String?' ValetudoApp/ValetudoApp/Models/RobotMap.swift
  </acceptance_criteria>
  <done>Alle 4 Model-Structs kompilieren, EntityMetaData hat id+label Properties, Build erfolgreich</done>
</task>

<task type="auto">
  <name>Task 2: ValetudoAPI-Methoden fuer alle neuen Capabilities</name>
  <files>ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift (vollstaendig lesen — request/requestVoid Methoden, Auth-Handling, bestehende MARK-Sections)
    - ValetudoApp/ValetudoApp/Models/RobotState.swift (die in Task 1 erstellten Structs verifizieren)
    - .planning/phases/03-api-completeness/03-RESEARCH.md (Pattern 1 und Pattern 4, alle API-Endpunkte)
  </read_first>
  <action>
    In ValetudoAPI.swift (im `extension ValetudoAPI` Block) folgende MARK-Sections mit Methoden hinzufuegen. Platzierung: nach den bestehenden Capability-Methoden, vor dem Ende des extension-Blocks.

    1. `// MARK: - Map Snapshots` (API-01)
       ```swift
       func getMapSnapshots() async throws -> [MapSnapshot] {
           try await request("/robot/capabilities/MapSnapshotCapability")
       }

       func restoreMapSnapshot(id: String) async throws {
           let body = try JSONEncoder().encode(["action": "restore", "id": id])
           try await requestVoid("/robot/capabilities/MapSnapshotCapability", body: body)
       }
       ```

    2. `// MARK: - Pending Map Change` (API-02)
       ```swift
       func getPendingMapChange() async throws -> PendingMapChangeState {
           try await request("/robot/capabilities/PendingMapChangeHandlingCapability")
       }

       func handlePendingMapChange(action: String) async throws {
           let body = try JSONEncoder().encode(["action": action])
           try await requestVoid("/robot/capabilities/PendingMapChangeHandlingCapability", body: body)
       }
       ```

    3. `// MARK: - Clean Route Control` (API-03)
       ```swift
       func getCleanRoute() async throws -> CleanRouteState {
           try await request("/robot/capabilities/CleanRouteControlCapability")
       }

       func setCleanRoute(route: String) async throws {
           let body = try JSONEncoder().encode(["route": route])
           try await requestVoid("/robot/capabilities/CleanRouteControlCapability", body: body)
       }
       ```

    4. `// MARK: - Valetudo Events` (UX-04)
       Events-Endpunkt gibt moeglicherweise ein Dictionary `[String: ValetudoEvent]` zurueck (nicht Array). Defensiv implementieren:
       ```swift
       func getEvents() async throws -> [ValetudoEvent] {
           // Events API may return dictionary {id: event} or array — try dict first
           do {
               let dict: [String: ValetudoEvent] = try await request("/valetudo/events")
               return Array(dict.values).sorted { $0.timestamp > $1.timestamp }
           } catch {
               // Fallback: try as array
               return try await request("/valetudo/events")
           }
       }
       ```

    5. `// MARK: - Obstacle Images` (API-04)
       Binary fetch — kann NICHT ueber `request<T: Decodable>` gehen. Eigene Methode mit direktem URLSession-Aufruf:
       ```swift
       func getObstacleImage(id: String) async throws -> Data {
           guard let baseURL = config.baseURL,
                 let url = URL(string: "/api/v2/robot/capabilities/ObstacleImagesCapability/img/\(id)", relativeTo: baseURL) else {
               throw APIError.invalidURL
           }

           var request = URLRequest(url: url)
           request.httpMethod = "GET"

           if let username = config.username, !username.isEmpty,
              let password = KeychainStore.password(for: config.id) {
               let credentials = "\(username):\(password)"
               if let data = credentials.data(using: .utf8) {
                   request.setValue("Basic \(data.base64EncodedString())", forHTTPHeaderField: "Authorization")
               }
           }

           let (data, response) = try await session.data(for: request)

           guard let httpResponse = response as? HTTPURLResponse else {
               throw APIError.invalidResponse
           }

           guard (200...299).contains(httpResponse.statusCode) else {
               throw APIError.httpError(httpResponse.statusCode)
           }

           return data
       }
       ```

    NICHT implementieren: Event-Interact (PUT /valetudo/events/{id}/interact) — Interaction-Payload ist unklar (LOW confidence), wird als Stretch-Goal in Plan 03 behandelt falls noetig.
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func getMapSnapshots" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func restoreMapSnapshot" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func getPendingMapChange" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func handlePendingMapChange" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func getCleanRoute" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func setCleanRoute" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func getEvents" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "func getObstacleImage" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
    - grep -q "ObstacleImagesCapability/img" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  </acceptance_criteria>
  <done>Alle 8 API-Methoden kompilieren erfolgreich, ObstacleImage nutzt raw Data statt JSON-Decode, Events-Methode behandelt Dict- und Array-Format defensiv</done>
</task>

</tasks>

<verification>
- Build kompiliert fehlerfrei: `xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet`
- Alle neuen Methoden und Structs sind in den jeweiligen Dateien per grep auffindbar
</verification>

<success_criteria>
- 4 neue Model-Structs (MapSnapshot, PendingMapChangeState, CleanRouteState, ValetudoEvent) in RobotState.swift
- 8 neue API-Methoden in ValetudoAPI.swift
- EntityMetaData um id und label erweitert
- Build erfolgreich ohne Warnungen in den geaenderten Dateien
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-completeness/03-01-SUMMARY.md`
</output>
