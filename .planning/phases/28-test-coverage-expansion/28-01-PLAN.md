---
phase: 28-test-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift
  - ValetudoApp/ValetudoAppTests/MapGeometryTests.swift
  - ValetudoApp/ValetudoAppTests/HitTestLogicTests.swift
  - ValetudoApp/ValetudoAppTests/SSEBackoffTests.swift
  - ValetudoApp/ValetudoAppTests/MapCacheServiceTests.swift
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04]

must_haves:
  truths:
    - "MapGeometry-Koordinaten-Transforms haben Unit-Tests die Round-Trip-Korrektheit beweisen"
    - "calculateMapParams gibt nil zurueck bei leeren Layern und korrekte Werte bei bekannten Pixeln"
    - "Hit-Test Pixel-Key-Encoding ist durch Tests abgesichert"
    - "SSE-Backoff-Werte 1s/5s/30s sind durch Tests gegen eine pure function validiert"
    - "MapCacheService Save/Load-Zyklus und Corrupted-Cache-Handling haben Tests"
  artifacts:
    - path: "ValetudoApp/ValetudoAppTests/MapGeometryTests.swift"
      provides: "Koordinaten-Transform und calculateMapParams Tests"
      min_lines: 60
    - path: "ValetudoApp/ValetudoAppTests/HitTestLogicTests.swift"
      provides: "Pixel-Key-Encoding Tests"
      min_lines: 25
    - path: "ValetudoApp/ValetudoAppTests/SSEBackoffTests.swift"
      provides: "Backoff-Delay Tests gegen static func"
      min_lines: 20
    - path: "ValetudoApp/ValetudoAppTests/MapCacheServiceTests.swift"
      provides: "Save/Load/Corrupt Cache Tests"
      min_lines: 40
  key_links:
    - from: "ValetudoApp/ValetudoAppTests/SSEBackoffTests.swift"
      to: "ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift"
      via: "SSEConnectionManager.backoffDelay(retryCount:) static func"
      pattern: "backoffDelay.*retryCount"
    - from: "ValetudoApp/ValetudoAppTests/MapGeometryTests.swift"
      to: "ValetudoApp/ValetudoApp/Utilities/MapGeometry.swift"
      via: "Direct call to free functions"
      pattern: "screenToMapCoords|mapToScreenCoords|calculateMapParams"
---

<objective>
Unit-Tests fuer MapGeometry (Koordinaten-Transforms + calculateMapParams), Hit-Test-Pixel-Key-Logik, SSE-Backoff-Timing und MapCacheService erstellen.

Purpose: TEST-01, TEST-03 und TEST-04 abdecken — die pure-function und filesystem-basierten Testbereiche.
Output: 4 neue Testdateien, 1 minimale Produktionscode-Aenderung (Backoff-Extraktion in SSEConnectionManager).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-test-coverage-expansion/28-RESEARCH.md

@ValetudoApp/ValetudoApp/Utilities/MapGeometry.swift
@ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift
@ValetudoApp/ValetudoApp/Services/MapCacheService.swift
@ValetudoApp/ValetudoApp/Map/MapInteractiveView.swift
@ValetudoApp/ValetudoApp/Map/MapViewModel.swift

<interfaces>
<!-- Aus MapGeometry.swift — free functions, direkt testbar -->
struct MapParams {
    let scale: CGFloat
    let offsetX: CGFloat
    let offsetY: CGFloat
    let minX: Int
    let minY: Int
}

func calculateMapParams(
    layers: [MapLayer], pixelSize: Int, size: CGSize, padding: CGFloat = 20
) -> MapParams?

func screenToMapCoords(
    _ point: CGPoint, scale: CGFloat, offset: CGSize, viewSize: CGSize
) -> CGPoint

func mapToScreenCoords(
    _ point: CGPoint, scale: CGFloat, offset: CGSize, viewSize: CGSize
) -> CGPoint

<!-- Aus SSEConnectionManager.swift — actor, backoff inline bei Lines 101-108 -->
actor SSEConnectionManager {
    // Backoff-Logik ist inline: retryCount 1→1s, 2→5s, >=3→30s
    // Muss als static func extrahiert werden fuer Testbarkeit
}

<!-- Aus MapCacheService.swift — singleton -->
final class MapCacheService {
    static let shared = MapCacheService()
    func saveIfChanged(_ map: RobotMap, for robotId: UUID) async
    func save(_ map: RobotMap, for robotId: UUID) async
    func load(for robotId: UUID) async -> RobotMap?
    func deleteCache(for robotId: UUID)
}

<!-- Hit-Test Key-Formel aus MapInteractiveView.swift:271 und MapViewModel.swift:291 -->
// let key = pixelX &<< 16 | pixelY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE-Backoff extrahieren + MapGeometry/HitTest/SSE/MapCache Tests schreiben</name>
  <files>
    ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift,
    ValetudoApp/ValetudoAppTests/MapGeometryTests.swift,
    ValetudoApp/ValetudoAppTests/HitTestLogicTests.swift,
    ValetudoApp/ValetudoAppTests/SSEBackoffTests.swift,
    ValetudoApp/ValetudoAppTests/MapCacheServiceTests.swift
  </files>
  <behavior>
    MapGeometryTests:
    - calculateMapParams mit einem Layer bei bekanntem Pixel (100, 200) gibt korrekte minX/minY/scale zurueck
    - calculateMapParams mit leeren Layern gibt nil zurueck
    - screenToMapCoords bei Identitaet (scale=1, offset=zero) am Zentrum gibt Zentrum zurueck
    - screenToMapCoords → mapToScreenCoords Round-Trip mit scale=2.5, offset=(30,-20) ergibt Originalwert (accuracy 0.001)
    - screenToMapCoords/mapToScreenCoords Round-Trip mit scale=1.0 und offset=zero

    HitTestLogicTests:
    - pixelX=42, pixelY=17: key = 42 &lt;&lt; 16 | 17, Set.contains ist true
    - Anderer X (43) oder anderer Y (18) findet key nicht
    - Grenzfall: pixelX=0, pixelY=0 ergibt key=0
    - Grosser Wert: pixelX=1000, pixelY=2000 funktioniert korrekt

    SSEBackoffTests:
    - backoffDelay(retryCount: 1) == 1.0
    - backoffDelay(retryCount: 2) == 5.0
    - backoffDelay(retryCount: 3) == 30.0
    - backoffDelay(retryCount: 100) == 30.0 (cap)
    - backoffDelay(retryCount: 0) hat definiertes Verhalten

    MapCacheServiceTests:
    - Save gefolgt von Load gibt Map mit gleichem pixelSize zurueck
    - Load fuer unbekannte UUID gibt nil zurueck
    - Corrupted Cache (ungültiges JSON in Datei geschrieben) gibt nil bei Load zurueck
    - deleteCache entfernt die Datei, anschliessend Load gibt nil zurueck
  </behavior>
  <action>
    **Schritt 1 — SSE-Backoff-Extraktion (minimaler Produktionscode-Change):**
    In `SSEConnectionManager.swift` eine `static func backoffDelay(retryCount: Int) -> Double` hinzufuegen (per TEST-03). Die bestehende inline switch-Logik (Lines 101-108) durch Aufruf dieser static func ersetzen:
    ```swift
    static func backoffDelay(retryCount: Int) -> Double {
        switch retryCount {
        case 1:  return 1
        case 2:  return 5
        default: return 30
        }
    }
    ```
    Im bestehenden `streamWithReconnect` dann: `let delay = SSEConnectionManager.backoffDelay(retryCount: retryCount)`.

    **Schritt 2 — Tests schreiben (TDD: RED zuerst, dann sollten sie sofort GREEN sein da die Produktionslogik bereits existiert):**

    `MapGeometryTests.swift`: Testklasse `MapGeometryTests: XCTestCase`. Importiere `@testable import ValetudoApp`. Teste die 5 Behaviors oben. Fuer `calculateMapParams` eine MapLayer via `JSONDecoder().decode(MapLayer.self, ...)` mit `compressedPixels: [100, 200, 1]` erzeugen (bestehendes Pattern aus MapLayerTests). Pixelgroesse 5, ViewSize 400x400.

    `HitTestLogicTests.swift`: Testklasse `HitTestLogicTests: XCTestCase`. Die Pixel-Key-Formel `pixelX &<< 16 | pixelY` direkt im Test nachbauen (kein Import von MapViewModel noetig — die Logik ist ein Bit-Pattern). 4 Testfaelle.

    `SSEBackoffTests.swift`: Testklasse `SSEBackoffTests: XCTestCase`. Importiere `@testable import ValetudoApp`. Rufe `SSEConnectionManager.backoffDelay(retryCount:)` auf. 5 Testfaelle. WICHTIG: Die korrekten Werte sind 1s/5s/30s (NICHT 2s/5s/30s wie in der Phasenbeschreibung faelschlicherweise angegeben).

    `MapCacheServiceTests.swift`: Testklasse `MapCacheServiceTests: XCTestCase`. Verwende `MapCacheService.shared` (Singleton). Jeder Test benutzt eine eigene `UUID()` um Isolation zu gewaehrleisten. In `tearDown()` den Cache via `deleteCache(for:)` aufraeumen. Fuer `makeMinimalRobotMap()` ein `RobotMap(size: nil, pixelSize: 5, layers: [], entities: [])` erzeugen. Fuer den Corrupted-Cache-Test: den Dateipfad `<Documents>/MapCache/<uuid>.json` direkt mit ungueltigem JSON ueberschreiben, dann `load` aufrufen und `nil` erwarten. Tests sind `async throws` da MapCacheService async-Methoden hat.

    **Bestehendes Test-Pattern beibehalten:** Kein Mock-Framework, kein Quick/Nimble. Nur XCTest + @testable import.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 17' test 2>&1 | grep -E "Test Suite|passed|failed|error:|MapGeometry|HitTest|SSEBackoff|MapCacheService"</automated>
  </verify>
  <done>
    - MapGeometryTests: 5+ Tests grueen (calculateMapParams, screenToMapCoords, mapToScreenCoords, Round-Trip, nil-Fall)
    - HitTestLogicTests: 4+ Tests grueen (key encoding, miss-X, miss-Y, Grenzfaelle)
    - SSEBackoffTests: 5 Tests grueen (1s, 5s, 30s, cap, edge)
    - MapCacheServiceTests: 4+ Tests grueen (save/load, unknown-uuid, corrupted, delete)
    - Bestehende 57 Tests laufen weiterhin grueen
    - SSEConnectionManager Produktionscode nutzt die extrahierte static func
  </done>
</task>

</tasks>

<verification>
Alle Tests (alt + neu) laufen grueen:
```bash
xcodebuild -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 17' test 2>&1 | tail -20
```
</verification>

<success_criteria>
- 4 neue Testdateien existieren in ValetudoAppTests/
- Mindestens 18 neue Tests grueen
- SSEConnectionManager.backoffDelay ist als static func extrahiert
- Bestehende Tests nicht gebrochen
</success_criteria>

<output>
After completion, create `.planning/phases/28-test-coverage-expansion/28-01-SUMMARY.md`
</output>
