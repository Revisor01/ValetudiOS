---
phase: 28-test-coverage-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/UpdateService.swift
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoAppTests/UpdateServiceTests.swift
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "UpdateService-Phasentransitionen sind durch Unit-Tests abgesichert"
    - "Alle 8 UpdatePhase-Zustaende werden durch Tests abgedeckt"
    - "Error-Recovery (reset → idle) ist getestet"
    - "Mock-API via Protokoll-Abstraktion ermoeglicht isolierte Tests ohne Netzwerk"
  artifacts:
    - path: "ValetudoApp/ValetudoAppTests/UpdateServiceTests.swift"
      provides: "UpdateService State-Machine Tests"
      min_lines: 100
    - path: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      provides: "ValetudoAPIProtocol-basierter init"
      contains: "ValetudoAPIProtocol"
  key_links:
    - from: "ValetudoApp/ValetudoAppTests/UpdateServiceTests.swift"
      to: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      via: "UpdateService(api: MockValetudoAPI())"
      pattern: "MockValetudoAPI"
    - from: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      to: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      via: "ValetudoAPIProtocol conformance"
      pattern: "ValetudoAPIProtocol"
---

<objective>
Protokoll-Abstraktion fuer ValetudoAPI einfuehren und UpdateService-State-Machine mit allen 8 Phase-Transitions + Error-Recovery durch Unit-Tests abdecken.

Purpose: TEST-02 — die UpdateService State Machine ist der kritischste Pfad (Firmware-Updates). Fehlerhafte Transitionen koennen den Roboter in inkonsistente Zustaende bringen.
Output: 1 Protokoll (ValetudoAPIProtocol), ValetudoAPI-Conformance, UpdateService refactored auf Protokoll, MockValetudoAPI + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-test-coverage-expansion/28-RESEARCH.md

@ValetudoApp/ValetudoApp/Services/UpdateService.swift
@ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift

<interfaces>
<!-- Aus UpdateService.swift -->
enum UpdatePhase: Equatable {
    case idle
    case checking
    case updateAvailable
    case downloading
    case readyToApply
    case applying
    case rebooting
    case error(String)
}

@MainActor @Observable
class UpdateService {
    private(set) var phase: UpdatePhase = .idle
    init(api: ValetudoAPI)  // AKTUELL: konkreter Typ — muss auf Protokoll umgestellt werden

    func loadVersionInfo() async
    func checkForUpdates() async
    func startDownload() async
    func startApply() async       // ACHTUNG: ruft UIApplication.shared — in Tests nicht aufrufbar
    func reset()

    private func mapUpdaterState(_ state: UpdaterState) -> UpdatePhase
    // Maps __class strings to UpdatePhase enum values
}

<!-- Aus ValetudoAPI.swift — actor, final, nicht subclassable -->
actor ValetudoAPI {
    func getValetudoVersion() async throws -> ValetudoVersion
    func getUpdaterState() async throws -> UpdaterState
    func checkForUpdates() async throws
    func downloadUpdate() async throws
    func applyUpdate() async throws
}

<!-- UpdaterState __class Mappings (aus UpdateService.swift:253-267) -->
// "ValetudoUpdaterIdleState"              → .idle
// "ValetudoUpdaterApprovalPendingState"   → .updateAvailable
// "ValetudoUpdaterDownloadingState"       → .downloading
// "ValetudoUpdaterApplyPendingState"      → .readyToApply
// "ValetudoUpdaterDisabledState"          → .idle
// "ValetudoUpdaterErrorState"             → .error(message)
// "ValetudoUpdaterNoUpdateRequiredState"  → .idle
// default                                 → .idle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ValetudoAPIProtocol extrahieren und UpdateService umstellen</name>
  <files>
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  </files>
  <action>
    **Schritt 1 — Protokoll definieren (in UpdateService.swift, oberhalb der Klasse):**

    ```swift
    protocol ValetudoAPIProtocol: AnyObject, Sendable {
        func checkForUpdates() async throws
        func getUpdaterState() async throws -> UpdaterState
        func downloadUpdate() async throws
        func applyUpdate() async throws
        func getValetudoVersion() async throws -> ValetudoVersion
    }
    ```

    Nur die 5 Methoden die UpdateService tatsaechlich aufruft. NICHT in einer separaten Datei — das Protokoll gehoert semantisch zum UpdateService.

    **Schritt 2 — ValetudoAPI conformance:**
    In `ValetudoAPI.swift` eine Extension hinzufuegen:
    ```swift
    extension ValetudoAPI: ValetudoAPIProtocol {}
    ```
    Alle 5 Methoden existieren bereits im actor — die Extension ist leer (rein additive Aenderung).

    **Schritt 3 — UpdateService.init umstellen:**
    In `UpdateService.swift` die `api`-Property und `init` aendern:
    ```swift
    private let api: any ValetudoAPIProtocol

    init(api: any ValetudoAPIProtocol) {
        self.api = api
    }
    ```

    Das ist die EINZIGE Aenderung am UpdateService selbst. Alle Aufrufer uebergeben bereits eine `ValetudoAPI`-Instanz die nun automatisch das Protokoll erfuellt.

    **Wichtig:** `startApply()` bleibt unveraendert — es ruft `UIApplication.shared.beginBackgroundTask` auf. Das wird in Tests NICHT aufgerufen (siehe Task 2 — startApply wird nicht direkt getestet).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 17' build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:"</automated>
  </verify>
  <done>
    - ValetudoAPIProtocol existiert mit 5 Methoden
    - ValetudoAPI conformt via Extension
    - UpdateService.init akzeptiert `any ValetudoAPIProtocol`
    - Build kompiliert ohne Fehler
    - Bestehende Funktionalitaet unveraendert (rein additive Aenderung)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UpdateService State-Machine Tests schreiben</name>
  <files>ValetudoApp/ValetudoAppTests/UpdateServiceTests.swift</files>
  <behavior>
    Phase-Transitions:
    1. idle → checking → updateAvailable: checkForUpdates() mit Mock der ApprovalPendingState liefert
    2. idle → checking → idle: checkForUpdates() mit Mock der IdleState liefert (kein Update)
    3. idle → checking → error: checkForUpdates() mit Mock der einen Error wirft
    4. updateAvailable → downloading: startDownload() nach ApprovalPendingState
    5. downloading → readyToApply: Mock liefert ApplyPendingState beim Poll
    6. downloading → error: startDownload() mit Mock der einen Error wirft
    7. downloading → error("Download unterbrochen"): Poll liefert IdleState statt ApplyPendingState

    Error-Recovery:
    8. reset() aus error-State → idle
    9. reset() aus beliebigem State → idle

    mapUpdaterState-Mapping:
    10. "ValetudoUpdaterIdleState" → .idle
    11. "ValetudoUpdaterApprovalPendingState" → .updateAvailable
    12. "ValetudoUpdaterDownloadingState" → .downloading
    13. "ValetudoUpdaterApplyPendingState" → .readyToApply
    14. "ValetudoUpdaterDisabledState" → .idle
    15. "ValetudoUpdaterErrorState" → .error(message)
    16. "ValetudoUpdaterNoUpdateRequiredState" → .idle
    17. Unbekannter __class → .idle

    NICHT getestet (bewusst ausgelassen):
    - startApply() — benoetigt UIApplication.shared (nicht in Unit-Tests verfuegbar)
    - applying → rebooting — erfordert startApply() Aufruf
  </behavior>
  <action>
    Erstelle `UpdateServiceTests.swift` mit:

    **MockValetudoAPI:**
    ```swift
    final class MockValetudoAPI: ValetudoAPIProtocol, @unchecked Sendable {
        var updaterStateToReturn: UpdaterState = UpdaterState(
            __class: "ValetudoUpdaterIdleState",
            busy: nil, currentVersion: nil, version: nil,
            releaseTimestamp: nil, downloadUrl: nil, downloadPath: nil, metaData: nil
        )
        var versionToReturn: ValetudoVersion = ValetudoVersion(release: "2024.01.0", commit: "abc123")
        var shouldThrowOnCheck = false
        var shouldThrowOnDownload = false
        var getUpdaterStateCallCount = 0
        // Fuer Poll-Tests: Nach N Aufrufen den State wechseln
        var stateSequence: [UpdaterState]? = nil

        func checkForUpdates() async throws {
            if shouldThrowOnCheck { throw URLError(.badServerResponse) }
        }
        func getUpdaterState() async throws -> UpdaterState {
            getUpdaterStateCallCount += 1
            if let seq = stateSequence, getUpdaterStateCallCount <= seq.count {
                return seq[getUpdaterStateCallCount - 1]
            }
            return updaterStateToReturn
        }
        func downloadUpdate() async throws {
            if shouldThrowOnDownload { throw URLError(.badServerResponse) }
        }
        func applyUpdate() async throws {}
        func getValetudoVersion() async throws -> ValetudoVersion { versionToReturn }
    }
    ```

    **Helper fuer UpdaterState:**
    ```swift
    private func makeUpdaterState(_ className: String, message: String? = nil) -> UpdaterState {
        UpdaterState(__class: className, busy: nil, currentVersion: nil, version: nil,
                     releaseTimestamp: nil, downloadUrl: nil, downloadPath: nil, metaData: nil)
    }
    ```
    Fuer ErrorState mit Message: pruefen ob UpdaterState ein `message`-Feld hat und das ggf. setzen.

    **Tests — alle `@MainActor func testXxx() async throws`:**
    Die 17 Behaviors oben als einzelne Testmethoden implementieren.

    Fuer Transition-Tests die `checkForUpdates()` aufrufen: Mock konfigurieren, dann `await service.checkForUpdates()`, dann `XCTAssertEqual(service.phase, .expectedPhase)`.

    Fuer `startDownload()` Tests: Erst Mock so konfigurieren dass `checkForUpdates()` zu `.updateAvailable` fuehrt, dann `startDownload()` aufrufen. ACHTUNG: `startDownload()` hat intern einen Guard `guard phase == .updateAvailable` — der Vorzustand muss stimmen.

    Fuer Poll-Tests (`downloading → readyToApply`): Die `stateSequence` im Mock nutzen. `startDownload()` pollt intern via `pollUntilReadyToApply()` — der Mock muss nach dem 1. Poll-Aufruf `ApplyPendingState` liefern.

    Fuer `reset()`: Service in Error-State bringen (via shouldThrowOnCheck), dann `service.reset()`, dann `XCTAssertEqual(service.phase, .idle)`.

    **ACHTUNG — mapUpdaterState ist private.** Die Mapping-Tests werden INDIREKT getestet: Mock liefert den jeweiligen __class-String in getUpdaterState, dann wird `checkForUpdates()` aufgerufen, und `service.phase` geprueft. Das testet die Mapping-Logik end-to-end.

    **Bestehendes Test-Pattern:** Kein Mock-Framework, `@testable import ValetudoApp`, nur XCTest.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 17' test 2>&1 | grep -E "Test Suite|passed|failed|error:|UpdateService"</automated>
  </verify>
  <done>
    - UpdateServiceTests.swift existiert mit MockValetudoAPI
    - Mindestens 12 Tests grueen (8 Transitionen + Error-Recovery + State-Mappings)
    - Alle Tests laufen ohne Netzwerk und ohne UIApplication
    - Bestehende Tests nicht gebrochen
  </done>
</task>

</tasks>

<verification>
Alle Tests (alt + neu) laufen grueen:
```bash
xcodebuild -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 17' test 2>&1 | tail -20
```

Spezifisch UpdateService-Tests:
```bash
xcodebuild -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ValetudoAppTests/UpdateServiceTests test 2>&1 | grep -E "passed|failed"
```
</verification>

<success_criteria>
- ValetudoAPIProtocol existiert und ValetudoAPI conformt
- UpdateService.init akzeptiert `any ValetudoAPIProtocol`
- MockValetudoAPI ermoeglicht isolierte Tests
- Mindestens 12 UpdateService-Tests grueen
- startApply() wird bewusst nicht getestet (UIKit-Dependency)
- Bestehende Tests und Produktionscode funktionieren unveraendert
</success_criteria>

<output>
After completion, create `.planning/phases/28-test-coverage-expansion/28-02-SUMMARY.md`
</output>
