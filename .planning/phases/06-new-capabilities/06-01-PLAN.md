---
phase: 06-new-capabilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - CAP-01

must_haves:
  truths:
    - "Benutzer sieht in RobotSettingsView eine Voice-Pack-Section mit dem aktuell installierten Sprachpaket"
    - "Benutzer kann aus einer Liste verfügbarer Sprachpakete eines auswählen"
    - "Nach Auswahl wird das Sprachpaket per PUT-Request aktiviert"
    - "Section erscheint nur wenn VoicePackManagementCapability gemeldet wird"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      provides: "getVoicePacks(), setVoicePack(id:) API-Methoden"
      contains: "VoicePackManagementCapability"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "voicePacks, currentVoicePackId, hasVoicePack published properties + loadVoicePacks(), setVoicePack() actions"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "Voice Pack Section mit Picker, capability-gated"
  key_links:
    - from: "RobotSettingsView"
      to: "RobotSettingsViewModel.setVoicePack()"
      via: "Picker onChange"
    - from: "RobotSettingsViewModel"
      to: "ValetudoAPI.setVoicePack(id:)"
      via: "async call"
---

<objective>
Integriert VoicePackManagementCapability vollständig: API-Methoden, ViewModel-State und UI-Section.

Purpose: Benutzer kann das Sprachpaket des Roboters direkt in der App wechseln.
Output: Neue Voice-Pack-Section in RobotSettingsView, capability-gated. API-Methoden in ValetudoAPI.swift. ViewModel-State in RobotSettingsViewModel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Patterns aus ValetudoAPI.swift — direkt wiederverwenden -->

Preset-GET Pattern (VoicePack-Liste abrufen):
```swift
func getVoicePackPresets() async throws -> [VoicePack] {
    try await request("/robot/capabilities/VoicePackManagementCapability")
}
```

Preset-SET Pattern (VoicePack aktivieren):
```swift
func setVoicePack(id: String) async throws {
    let body = try JSONEncoder().encode(["action": "download", "id": id])
    try await requestVoid("/robot/capabilities/VoicePackManagementCapability", body: body)
}
```

Valetudo API Response-Format für VoicePackManagementCapability (GET):
```json
{
  "current_language": { "id": "de", "name": "German", "installed": true },
  "supported_languages": [
    { "id": "en", "name": "English", "installed": true },
    { "id": "de", "name": "German", "installed": true }
  ]
}
```

PUT-Body Format:
```json
{ "action": "download", "id": "en" }
```

Capability-Check Pattern (aus RobotSettingsViewModel.loadSettings()):
```swift
hasVoicePack = DebugConfig.showAllCapabilities || capabilities.contains("VoicePackManagementCapability")
```

Picker-with-onChange Pattern (aus RobotSettingsView):
```swift
Picker(selection: $viewModel.carpetSensorMode) {
    ForEach(viewModel.carpetSensorModePresets, id: \.self) { preset in
        Text(displayNameForCarpetSensorMode(preset)).tag(preset)
    }
} label: { ... }
.onChange(of: viewModel.carpetSensorMode) { _, newValue in
    guard !viewModel.isInitialLoad && !newValue.isEmpty else { return }
    Task { await viewModel.setCarpetSensorMode(newValue) }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: VoicePack API-Methoden und Model-Structs</name>
  <files>ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift</files>
  <action>
Füge in ValetudoAPI.swift nach dem MARK "Mop Dock" Block einen neuen MARK-Block "Voice Pack" hinzu:

1. Definiere Codable Structs (können am Ende der Datei oder als separate Erweiterung stehen — bevorzuge Ende der Datei vor der letzten schließenden Klammer der extension):

```swift
struct VoicePack: Codable, Identifiable {
    let id: String
    let name: String
    let installed: Bool
}

struct VoicePackState: Codable {
    let currentLanguage: VoicePack
    let supportedLanguages: [VoicePack]

    enum CodingKeys: String, CodingKey {
        case currentLanguage = "current_language"
        case supportedLanguages = "supported_languages"
    }
}
```

2. Füge API-Methoden in der ValetudoAPI extension hinzu:

```swift
// MARK: - Voice Pack
func getVoicePackState() async throws -> VoicePackState {
    try await request("/robot/capabilities/VoicePackManagementCapability")
}

func setVoicePack(id: String) async throws {
    let body = try JSONEncoder().encode(["action": "download", "id": id])
    try await requestVoid("/robot/capabilities/VoicePackManagementCapability", body: body)
}
```

Build-Test: `xcodebuild build -target ValetudoApp -configuration Debug 2>&1 | tail -5`
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>ValetudoAPI kompiliert fehlerfrei mit getVoicePackState() und setVoicePack(id:) Methoden sowie VoicePack + VoicePackState Structs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VoicePack ViewModel-State und View-Section</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift, ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift, ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings</files>
  <action>
**RobotSettingsViewModel.swift:**

1. Füge @Published properties nach dem `hasPendingMapChange` property hinzu:
```swift
@Published var hasVoicePack = DebugConfig.showAllCapabilities
@Published var voicePacks: [VoicePack] = []
@Published var currentVoicePackId: String = ""
@Published var isSettingVoicePack = false
```

2. Ergänze in `loadSettings()` nach dem Capabilities-Check den hasVoicePack-Check:
```swift
hasVoicePack = DebugConfig.showAllCapabilities || capabilities.contains("VoicePackManagementCapability")
```
(Dies innerhalb des bestehenden do/catch für getCapabilities() hinzufügen.)

3. Ergänze in `loadSettings()` nach dem Pending-Map-Change-Block:
```swift
// Load voice packs
if hasVoicePack {
    do {
        let state = try await api.getVoicePackState()
        voicePacks = state.supportedLanguages
        currentVoicePackId = state.currentLanguage.id
    } catch {
        if !DebugConfig.showAllCapabilities { hasVoicePack = false }
        logger.debug("Voice pack not supported: \(error, privacy: .public)")
    }
}
```

4. Füge Action-Methode am Ende der MARK "Actions" Region hinzu:
```swift
func setVoicePack(_ id: String) async {
    guard let api = api else { return }
    isSettingVoicePack = true
    defer { isSettingVoicePack = false }
    do {
        try await api.setVoicePack(id: id)
        logger.info("Voice pack set to: \(id, privacy: .public)")
    } catch {
        logger.error("Failed to set voice pack: \(error, privacy: .public)")
        // Reload current state on error
        if let state = try? await api.getVoicePackState() {
            currentVoicePackId = state.currentLanguage.id
        }
    }
}
```

**RobotSettingsView.swift:**

Füge eine neue Section nach dem Pending-Map-Change-Block (vor dem Quirks-Block) ein:
```swift
// Voice Pack Section
if viewModel.hasVoicePack && !viewModel.voicePacks.isEmpty {
    Section {
        Picker(selection: $viewModel.currentVoicePackId) {
            ForEach(viewModel.voicePacks) { pack in
                Text(pack.name).tag(pack.id)
            }
        } label: {
            HStack {
                Image(systemName: "speaker.wave.2.bubble")
                    .foregroundStyle(.indigo)
                Text(String(localized: "voice_pack.label"))
            }
        }
        .onChange(of: viewModel.currentVoicePackId) { _, newValue in
            guard !viewModel.isInitialLoad && !newValue.isEmpty else { return }
            Task { await viewModel.setVoicePack(newValue) }
        }
        .disabled(viewModel.isSettingVoicePack)
    } header: {
        Label(String(localized: "voice_pack.title"), systemImage: "speaker.wave.2.bubble")
    } footer: {
        Text(String(localized: "voice_pack.footer"))
    }
}
```

**Localizable.xcstrings:**

Füge folgende String-Keys im JSON-Objekt `"strings"` an der alphabetisch korrekten Position ("voice_pack" nach "v") ein:

```json
"voice_pack.footer" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Das ausgewählte Sprachpaket wird vom Roboter heruntergeladen und installiert."
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "The selected language pack will be downloaded and installed by the robot."
      }
    }
  }
},
"voice_pack.label" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Sprachpaket"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Language Pack"
      }
    }
  }
},
"voice_pack.title" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Sprachausgabe"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Voice Output"
      }
    }
  }
},
```
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>App kompiliert fehlerfrei. RobotSettingsView zeigt Voice-Pack-Picker wenn hasVoicePack=true. ViewModel hat currentVoicePackId + voicePacks als @Published properties. onChange ruft setVoicePack(_:) auf.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -target ValetudoApp -configuration Debug` schlägt nicht fehl
- VoicePack + VoicePackState Structs existieren und sind Codable
- getVoicePackState() und setVoicePack(id:) in ValetudoAPI.swift vorhanden
- hasVoicePack, voicePacks, currentVoicePackId in RobotSettingsViewModel als @Published vorhanden
- Voice-Pack-Section in RobotSettingsView capability-gated (if viewModel.hasVoicePack)
- Neue Localization-Keys für de + en vorhanden
</verification>

<success_criteria>
- Build erfolgreich ohne neue Compiler-Fehler oder -Warnungen
- VoicePackManagementCapability vollständig integriert: API → ViewModel → View
- Section erscheint nur wenn Capability gemeldet wird (capability-gated)
- Picker-Änderung triggert setVoicePack() via async Task
- isInitialLoad-Guard verhindert false-positive onChange beim ersten Laden
</success_criteria>

<output>
Nach Completion: `.planning/phases/06-new-capabilities/06-01-SUMMARY.md` erstellen.
</output>
