---
phase: 06-new-capabilities
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - CAP-02

must_haves:
  truths:
    - "Benutzer sieht in RobotSettingsView einen Picker für die Absaugdauer der Auto-Empty-Station"
    - "Picker zeigt verfügbare Preset-Werte aus der API"
    - "Auswahl eines Presets sendet PUT-Request an AutoEmptyDockAutoEmptyDurationControlCapability/preset"
    - "Section erscheint nur wenn AutoEmptyDockAutoEmptyDurationControlCapability gemeldet wird"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      provides: "getAutoEmptyDockDurationPresets(), setAutoEmptyDockDuration(preset:) API-Methoden"
      contains: "AutoEmptyDockAutoEmptyDurationControlCapability"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "autoEmptyDockDurationPresets, currentAutoEmptyDockDuration, hasAutoEmptyDockDuration published properties"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "Absaugdauer-Picker in Auto-Empty-Section, capability-gated"
  key_links:
    - from: "RobotSettingsView"
      to: "RobotSettingsViewModel.setAutoEmptyDockDuration()"
      via: "Picker onChange"
    - from: "RobotSettingsViewModel"
      to: "ValetudoAPI.setAutoEmptyDockDuration(preset:)"
      via: "async call"
---

<objective>
Integriert AutoEmptyDockAutoEmptyDurationControlCapability: API-Methoden, ViewModel-State und Preset-Picker in der Auto-Empty-Section.

Purpose: Benutzer kann die Saugdauer der Auto-Empty-Station direkt in der App einstellen.
Output: Neuer Preset-Picker in der bestehenden Auto-Empty-Section von RobotSettingsView. API-Methoden folgen exakt dem Interval-Pattern (AutoEmptyDockAutoEmptyIntervalControlCapability).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Bestehende analoge Methoden in ValetudoAPI.swift als direkte Vorlage -->

Analoges Pattern (AutoEmptyDockInterval — exakt dasselbe Schema):
```swift
func getAutoEmptyDockIntervalPresets() async throws -> [String] {
    try await request("/robot/capabilities/AutoEmptyDockAutoEmptyIntervalControlCapability/presets")
}

func setAutoEmptyDockInterval(preset: String) async throws {
    let body = try JSONEncoder().encode(PresetControlRequest(name: preset))
    try await requestVoid("/robot/capabilities/AutoEmptyDockAutoEmptyIntervalControlCapability/preset", body: body)
}
```

Neue Endpoints (Duration statt Interval):
- GET `/api/v2/robot/capabilities/AutoEmptyDockAutoEmptyDurationControlCapability/presets`
- PUT `/api/v2/robot/capabilities/AutoEmptyDockAutoEmptyDurationControlCapability/preset`

PresetControlRequest Struct (bereits in Codebase vorhanden):
```swift
struct PresetControlRequest: Codable {
    let name: String
}
```

Capability-Check in RobotSettingsViewModel.loadSettings():
```swift
hasAutoEmptyDock = DebugConfig.showAllCapabilities || capabilities.contains("AutoEmptyDockAutoEmptyIntervalControlCapability")
```

Picker-Pattern aus RobotSettingsView (CarpetSensorMode als Vorlage):
```swift
if viewModel.hasCarpetSensorMode && !viewModel.carpetSensorModePresets.isEmpty {
    Picker(selection: $viewModel.carpetSensorMode) {
        ForEach(viewModel.carpetSensorModePresets, id: \.self) { preset in
            Text(displayNameForCarpetSensorMode(preset)).tag(preset)
        }
    } label: { ... }
    .onChange(of: viewModel.carpetSensorMode) { _, newValue in
        guard !viewModel.isInitialLoad && !newValue.isEmpty else { return }
        Task { await viewModel.setCarpetSensorMode(newValue) }
    }
}
```

Preset-Localization Pattern (PresetHelpers.swift):
Existing keys: "preset.low", "preset.medium", "preset.high", "preset.max", "preset.min", "preset.off", "preset.turbo"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AutoEmptyDockDuration API-Methoden</name>
  <files>ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift</files>
  <action>
Füge in ValetudoAPI.swift im MARK-Block "Auto Empty Dock" (nach der existierenden `triggerAutoEmptyDock()` Methode) zwei neue Methoden hinzu:

```swift
func getAutoEmptyDockDurationPresets() async throws -> [String] {
    try await request("/robot/capabilities/AutoEmptyDockAutoEmptyDurationControlCapability/presets")
}

func setAutoEmptyDockDuration(preset: String) async throws {
    let body = try JSONEncoder().encode(PresetControlRequest(name: preset))
    try await requestVoid("/robot/capabilities/AutoEmptyDockAutoEmptyDurationControlCapability/preset", body: body)
}
```

Kein neuer Struct nötig — PresetControlRequest existiert bereits in der Codebase.
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>ValetudoAPI kompiliert fehlerfrei mit getAutoEmptyDockDurationPresets() und setAutoEmptyDockDuration(preset:).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AutoEmptyDockDuration ViewModel-State und View-Picker</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift, ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift, ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings</files>
  <action>
**RobotSettingsViewModel.swift:**

1. Füge @Published properties nach `hasAutoEmptyDock` hinzu:
```swift
@Published var hasAutoEmptyDockDuration = DebugConfig.showAllCapabilities
@Published var autoEmptyDockDurationPresets: [String] = []
@Published var currentAutoEmptyDockDuration: String = ""
```

2. Ergänze im Capabilities-Check-Block in `loadSettings()`:
```swift
hasAutoEmptyDockDuration = DebugConfig.showAllCapabilities || capabilities.contains("AutoEmptyDockAutoEmptyDurationControlCapability")
```

3. Füge Ladelogik in `loadSettings()` nach dem AutoEmptyDock-Interval-Block ein (oder nach dem Mop-Dock-WashTemperature-Block):
```swift
// Load auto empty dock duration presets
if hasAutoEmptyDockDuration {
    do {
        autoEmptyDockDurationPresets = try await api.getAutoEmptyDockDurationPresets()
        // Read current value from robot state attributes
        if let attr = robotManager.robotStates[robot.id]?.attributes.first(where: {
            $0.__class == "PresetSelectionStateAttribute" && $0.type == "auto_empty_dock_auto_empty_duration"
        }) {
            currentAutoEmptyDockDuration = attr.value ?? ""
        }
        if currentAutoEmptyDockDuration.isEmpty, let first = autoEmptyDockDurationPresets.first {
            currentAutoEmptyDockDuration = first
        }
    } catch {
        if !DebugConfig.showAllCapabilities { hasAutoEmptyDockDuration = false }
        autoEmptyDockDurationPresets = []
        logger.debug("Auto empty dock duration not supported: \(error, privacy: .public)")
    }
}
```

4. Füge Action-Methode hinzu:
```swift
func setAutoEmptyDockDuration(_ preset: String) async {
    guard let api = api else { return }
    do {
        try await api.setAutoEmptyDockDuration(preset: preset)
        logger.info("Auto empty dock duration set to: \(preset, privacy: .public)")
    } catch {
        logger.error("Failed to set auto empty dock duration: \(error, privacy: .public)")
    }
}
```

**RobotSettingsView.swift:**

Finde die bestehende Auto-Empty-Section (suche nach `"settings.auto_empty"` im Code). Die Section enthält den Interval-Picker und den Trigger-Button. Füge nach dem bestehenden Interval-Picker (oder vor dem Trigger-Button) den Duration-Picker ein:

```swift
if viewModel.hasAutoEmptyDockDuration && !viewModel.autoEmptyDockDurationPresets.isEmpty {
    Picker(selection: $viewModel.currentAutoEmptyDockDuration) {
        ForEach(viewModel.autoEmptyDockDurationPresets, id: \.self) { preset in
            Text(localizedPresetName(preset)).tag(preset)
        }
    } label: {
        HStack {
            Image(systemName: "timer")
                .foregroundStyle(.purple)
            Text(String(localized: "auto_empty.duration_label"))
        }
    }
    .onChange(of: viewModel.currentAutoEmptyDockDuration) { _, newValue in
        guard !viewModel.isInitialLoad && !newValue.isEmpty else { return }
        Task { await viewModel.setAutoEmptyDockDuration(newValue) }
    }
}
```

Hinweis: `localizedPresetName(_:)` — prüfe ob eine solche Helper-Funktion in RobotSettingsView oder PresetHelpers.swift existiert. Falls ja, diese verwenden. Falls nein, direkt `Text(preset.capitalized)` schreiben (kein neuer Helper nötig, da PresetHelpers.swift bereits `localizedPresetName` für fan/water/operation bereitstellt — prüfe die genaue Signatur dort).

**Localizable.xcstrings:**

Füge folgende Keys alphabetisch bei "a" (nach "auto_empty"-Keys) ein:

```json
"auto_empty.duration_label" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Absaugdauer"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Suction Duration"
      }
    }
  }
},
```
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>App kompiliert fehlerfrei. hasAutoEmptyDockDuration, autoEmptyDockDurationPresets, currentAutoEmptyDockDuration als @Published in ViewModel. Duration-Picker in Auto-Empty-Section sichtbar wenn Capability vorhanden. onChange ruft setAutoEmptyDockDuration() auf.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -target ValetudoApp -configuration Debug` erfolgreich
- getAutoEmptyDockDurationPresets() und setAutoEmptyDockDuration(preset:) in ValetudoAPI.swift
- hasAutoEmptyDockDuration Capability-Flag in loadSettings() gesetzt
- Duration-Picker in Auto-Empty-Section capability-gated
- Lokalisierungskey "auto_empty.duration_label" für de + en vorhanden
</verification>

<success_criteria>
- Build erfolgreich ohne neue Compiler-Fehler
- AutoEmptyDockAutoEmptyDurationControlCapability vollständig integriert
- Picker folgt exakt demselben Pattern wie Interval-Picker in derselben Section
- isInitialLoad-Guard verhindert false-positive onChange
</success_criteria>

<output>
Nach Completion: `.planning/phases/06-new-capabilities/06-02-SUMMARY.md` erstellen.
</output>
