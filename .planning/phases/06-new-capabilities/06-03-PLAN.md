---
phase: 06-new-capabilities
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - CAP-03

must_haves:
  truths:
    - "Benutzer sieht in RobotSettingsView einen Picker für die Trocknungszeit der Mop-Station"
    - "Picker zeigt verfügbare Preset-Werte aus der API"
    - "Auswahl eines Presets sendet PUT-Request an MopDockMopDryingTimeControlCapability/preset"
    - "Section erscheint nur wenn MopDockMopDryingTimeControlCapability gemeldet wird"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      provides: "getMopDockDryingTimePresets(), setMopDockDryingTime(preset:) API-Methoden"
      contains: "MopDockMopDryingTimeControlCapability"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "mopDockDryingTimePresets, currentMopDockDryingTime, hasMopDockDryingTime published properties"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "Trocknungszeit-Picker in Mop-Dock-Section, capability-gated"
  key_links:
    - from: "RobotSettingsView"
      to: "RobotSettingsViewModel.setMopDockDryingTime()"
      via: "Picker onChange"
    - from: "RobotSettingsViewModel"
      to: "ValetudoAPI.setMopDockDryingTime(preset:)"
      via: "async call"
---

<objective>
Integriert MopDockMopDryingTimeControlCapability: API-Methoden, ViewModel-State und Preset-Picker in der Mop-Dock-Section.

Purpose: Benutzer kann die Trocknungszeit der Mop-Station direkt in der App einstellen.
Output: Neuer Preset-Picker in der bestehenden Mop-Dock-Section von RobotSettingsView. Pattern identisch mit MopDockWashTemperature (bereits implementiert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Analoges Pattern aus ValetudoAPI.swift — exakt dasselbe Schema wie WashTemperature -->

Vorlage (MopDockWashTemperature — identisches Schema):
```swift
func getMopDockWashTemperaturePresets() async throws -> [String] {
    try await request("/robot/capabilities/MopDockMopWashTemperatureControlCapability/presets")
}

func setMopDockWashTemperature(preset: String) async throws {
    let body = try JSONEncoder().encode(PresetControlRequest(name: preset))
    try await requestVoid("/robot/capabilities/MopDockMopWashTemperatureControlCapability/preset", body: body)
}
```

Neue Endpoints:
- GET `/api/v2/robot/capabilities/MopDockMopDryingTimeControlCapability/presets`
- PUT `/api/v2/robot/capabilities/MopDockMopDryingTimeControlCapability/preset`

ViewModel-State für WashTemperature (als Vorlage):
```swift
@Published var hasMopDockWashTemperature = DebugConfig.showAllCapabilities
@Published var mopDockWashTemperaturePresets: [String] = []
@Published var currentWashTemperature: String = ""
```

RobotState-Attribut-Lookup (WashTemperature als Vorlage):
```swift
if let tempAttr = robotManager.robotStates[robot.id]?.attributes.first(where: {
    $0.__class == "PresetSelectionStateAttribute" && $0.type == "mop_dock_mop_cleaning_water_temperature"
}) {
    currentWashTemperature = tempAttr.value ?? ""
}
```

Für DryingTime ist der Attribut-Typ vermutlich: `"mop_dock_mop_drying_time"` (analog zum API-Capability-Name).

View-Pattern (MopDockWashTemperature in RobotSettingsView):
Suche nach `hasMopDockWashTemperature` in RobotSettingsView.swift — füge DryingTime analog daneben ein.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MopDockDryingTime API-Methoden</name>
  <files>ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift</files>
  <action>
Füge in ValetudoAPI.swift im MARK-Block "Mop Dock Wash Temperature" (nach der existierenden `setMopDockWashTemperature` Methode) zwei neue Methoden hinzu:

```swift
func getMopDockDryingTimePresets() async throws -> [String] {
    try await request("/robot/capabilities/MopDockMopDryingTimeControlCapability/presets")
}

func setMopDockDryingTime(preset: String) async throws {
    let body = try JSONEncoder().encode(PresetControlRequest(name: preset))
    try await requestVoid("/robot/capabilities/MopDockMopDryingTimeControlCapability/preset", body: body)
}
```

PresetControlRequest existiert bereits — kein neuer Struct nötig.
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>ValetudoAPI kompiliert fehlerfrei mit getMopDockDryingTimePresets() und setMopDockDryingTime(preset:).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MopDockDryingTime ViewModel-State und View-Picker</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift, ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift, ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings</files>
  <action>
**RobotSettingsViewModel.swift:**

1. Füge @Published properties nach `mopDockWashTemperaturePresets` / `currentWashTemperature` hinzu:
```swift
@Published var hasMopDockDryingTime = DebugConfig.showAllCapabilities
@Published var mopDockDryingTimePresets: [String] = []
@Published var currentMopDockDryingTime: String = ""
```

2. Ergänze im Capabilities-Check-Block in `loadSettings()`:
```swift
hasMopDockDryingTime = DebugConfig.showAllCapabilities || capabilities.contains("MopDockMopDryingTimeControlCapability")
```

3. Füge Ladelogik in `loadSettings()` nach dem WashTemperature-Block ein:
```swift
// Load mop dock drying time presets
if hasMopDockDryingTime {
    do {
        mopDockDryingTimePresets = try await api.getMopDockDryingTimePresets()
        if let attr = robotManager.robotStates[robot.id]?.attributes.first(where: {
            $0.__class == "PresetSelectionStateAttribute" && $0.type == "mop_dock_mop_drying_time"
        }) {
            currentMopDockDryingTime = attr.value ?? ""
        }
        if currentMopDockDryingTime.isEmpty, let first = mopDockDryingTimePresets.first {
            currentMopDockDryingTime = first
        }
    } catch {
        if !DebugConfig.showAllCapabilities { hasMopDockDryingTime = false }
        mopDockDryingTimePresets = []
        logger.debug("Mop dock drying time not supported: \(error, privacy: .public)")
    }
}
```

4. Füge Action-Methode nach `setMopDockWashTemperature` hinzu:
```swift
func setMopDockDryingTime(_ preset: String) async {
    guard let api = api else { return }
    do {
        try await api.setMopDockDryingTime(preset: preset)
        logger.info("Mop dock drying time set to: \(preset, privacy: .public)")
    } catch {
        logger.error("Failed to set mop dock drying time: \(error, privacy: .public)")
    }
}
```

**RobotSettingsView.swift:**

Finde die Mop-Dock-Section (suche nach `"settings.mop_dock"` oder `hasMopDockWashTemperature`). Füge nach dem WashTemperature-Picker (oder dem AutoDrying-Toggle) den DryingTime-Picker ein:

```swift
if viewModel.hasMopDockDryingTime && !viewModel.mopDockDryingTimePresets.isEmpty {
    Picker(selection: $viewModel.currentMopDockDryingTime) {
        ForEach(viewModel.mopDockDryingTimePresets, id: \.self) { preset in
            Text(preset.capitalized).tag(preset)
        }
    } label: {
        HStack {
            Image(systemName: "fan")
                .foregroundStyle(.cyan)
            Text(String(localized: "mop_dock.drying_time_label"))
        }
    }
    .onChange(of: viewModel.currentMopDockDryingTime) { _, newValue in
        guard !viewModel.isInitialLoad && !newValue.isEmpty else { return }
        Task { await viewModel.setMopDockDryingTime(newValue) }
    }
}
```

**Localizable.xcstrings:**

Füge folgenden Key alphabetisch bei "m" (nach bestehenden "mop_dock"-Keys, falls vorhanden, sonst nach "map"-Keys) ein:

```json
"mop_dock.drying_time_label" : {
  "extractionState" : "manual",
  "localizations" : {
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Trocknungszeit"
      }
    },
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Drying Time"
      }
    }
  }
},
```
  </action>
  <verify>
    <automated>xcodebuild build -target ValetudoApp -configuration Debug -quiet 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -3</automated>
  </verify>
  <done>App kompiliert fehlerfrei. hasMopDockDryingTime, mopDockDryingTimePresets, currentMopDockDryingTime als @Published in ViewModel. Drying-Time-Picker in Mop-Dock-Section capability-gated. onChange ruft setMopDockDryingTime() auf.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -target ValetudoApp -configuration Debug` erfolgreich
- getMopDockDryingTimePresets() und setMopDockDryingTime(preset:) in ValetudoAPI.swift
- hasMopDockDryingTime Capability-Flag in loadSettings() gesetzt
- DryingTime-Picker in Mop-Dock-Section capability-gated
- Lokalisierungskey "mop_dock.drying_time_label" für de + en vorhanden
</verification>

<success_criteria>
- Build erfolgreich ohne neue Compiler-Fehler
- MopDockMopDryingTimeControlCapability vollständig integriert
- Picker liegt in derselben Section wie WashTemperature (Mop-Dock-Section)
- isInitialLoad-Guard aktiv
</success_criteria>

<output>
Nach Completion: `.planning/phases/06-new-capabilities/06-03-SUMMARY.md` erstellen.
</output>
