---
phase: 26-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift
  - ValetudoApp/ValetudoApp/Views/AddRobotView.swift
  - ValetudoApp/ValetudoApp/Views/SettingsView.swift
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Bei HTTP-Verbindung (useSSL=false) zeigt RobotStatusHeaderView ein oranges Lock-Icon"
    - "Bei ignoreCertificateErrors=true zeigt AddRobotView ein prominentes Warning-Banner statt nur Footer-Text"
    - "EditRobotView zeigt useSSL-Toggle und ignoreCertificateErrors-Toggle"
    - "EditRobotView zeigt die gleiche prominente Warnung wie AddRobotView bei ignoreCertificateErrors"
    - "EditRobotView.saveRobot() uebergibt useSSL und ignoreCertificateErrors an RobotConfig"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift"
      provides: "HTTP Security-Indikator Icon"
      contains: "lock.open.fill"
    - path: "ValetudoApp/ValetudoApp/Views/AddRobotView.swift"
      provides: "Prominentes Warning-Banner bei ignoreCertificateErrors"
      contains: "exclamationmark.triangle.fill"
    - path: "ValetudoApp/ValetudoApp/Views/SettingsView.swift"
      provides: "SSL-Felder und Warnung in EditRobotView"
      contains: "ignoreCertificateErrors"
  key_links:
    - from: "RobotStatusHeaderView"
      to: "RobotDetailViewModel.robot.useSSL"
      via: "viewModel.robot.useSSL == false check"
      pattern: "viewModel\\.robot\\.useSSL"
    - from: "EditRobotView.saveRobot()"
      to: "RobotConfig init"
      via: "useSSL und ignoreCertificateErrors Parameter"
      pattern: "useSSL.*ignoreCertificateErrors"
---

<objective>
Security-Indikatoren in der UI: HTTP-Warnung im Status-Header und prominente SSL-Bypass-Warnungen in Add/Edit Robot Views.

Purpose: Benutzer soll auf einen Blick erkennen ob die Verbindung unsicher ist (HTTP) und beim Konfigurieren deutlich gewarnt werden wenn Zertifikatsfehler ignoriert werden. EditRobotView hat aktuell keine SSL-Felder — diese werden ergaenzt damit useSSL/ignoreCertificateErrors nicht beim Speichern auf false zurueckgesetzt werden.

Output: Drei modifizierte View-Dateien mit Security-Indikatoren und vollstaendiger SSL-Konfiguration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-security-hardening/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From ValetudoApp/ValetudoApp/Models/RobotConfig.swift:
```swift
struct RobotConfig: Codable, Identifiable, Equatable, Hashable {
    let id: UUID
    var name: String
    var host: String
    var username: String?
    var password: String?
    var useSSL: Bool
    var ignoreCertificateErrors: Bool

    var baseURL: URL? {
        let scheme = useSSL ? "https" : "http"
        return URL(string: "\(scheme)://\(host)")
    }
}
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift:
```swift
@Observable
final class RobotDetailViewModel {
    let robot: RobotConfig        // <-- SEC-01 nutzt robot.useSSL
    let robotManager: RobotManager
    // ...
}
```

From ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift:
```swift
struct RobotStatusHeaderView: View {
    let viewModel: RobotDetailViewModel
    // HStack mit: Status-Dot, Status-Text, Model-Name, Spacer, Consumable-Warning, Locate-Button, Battery-Pill
}
```

From ValetudoApp/ValetudoApp/Views/SettingsView.swift (EditRobotView, Zeile 176-297):
```swift
struct EditRobotView: View {
    let robot: RobotConfig
    @State private var name: String
    @State private var host: String
    @State private var useAuthentication: Bool
    @State private var username: String
    @State private var password: String
    // FEHLT: useSSL, ignoreCertificateErrors State-Variablen
    // saveRobot() erstellt RobotConfig OHNE useSSL/ignoreCertificateErrors
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP-Indikator in RobotStatusHeaderView und prominentes Warning-Banner in AddRobotView</name>
  <files>ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift, ValetudoApp/ValetudoApp/Views/AddRobotView.swift</files>
  <action>
**RobotStatusHeaderView.swift (SEC-01):**
Im HStack, direkt NACH dem Consumable-Warning-Block (Zeile 40-44: `if viewModel.hasConsumableWarning { ... }`) und VOR dem Locate-Button (Zeile 47), folgendes einfuegen:

```swift
// HTTP connection warning (SEC-01)
if !viewModel.robot.useSSL {
    Image(systemName: "lock.open.fill")
        .font(.caption)
        .foregroundStyle(.orange)
        .accessibilityLabel(String(localized: "security.http_connection"))
}
```

Dieses Pattern ist identisch zum bestehenden Consumable-Warning-Icon (gleiche font, gleiche foregroundStyle).

**AddRobotView.swift (SEC-02):**
Den bestehenden Footer-Text (Zeile 70-74) ersetzen durch ein prominentes Warning-Banner. Statt:
```swift
} footer: {
    if useSSL && ignoreCertificateErrors {
        Text(String(localized: "settings.ignore_certificate_errors.warning"))
            .foregroundStyle(.orange)
    }
}
```

Aendern zu:
```swift
}

if useSSL && ignoreCertificateErrors {
    Section {
        HStack(spacing: 8) {
            Image(systemName: "exclamationmark.triangle.fill")
                .foregroundStyle(.orange)
            Text(String(localized: "settings.ignore_certificate_errors.warning"))
                .font(.footnote)
                .foregroundStyle(.secondary)
        }
        .padding(.vertical, 4)
    }
}
```

Die Warnung wird damit eine eigene Section mit Icon statt nur ein subtiler Footer-Text.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>RobotStatusHeaderView zeigt oranges lock.open.fill Icon wenn useSSL==false. AddRobotView zeigt prominentes Warning-Banner mit Icon statt nur Footer-Text bei ignoreCertificateErrors.</done>
</task>

<task type="auto">
  <name>Task 2: SSL-Felder und Warnung in EditRobotView ergaenzen</name>
  <files>ValetudoApp/ValetudoApp/Views/SettingsView.swift</files>
  <action>
**EditRobotView in SettingsView.swift (SEC-02):**

1. **State-Variablen hinzufuegen** (nach `@State private var password: String`, ca. Zeile 186):
```swift
@State private var useSSL: Bool
@State private var ignoreCertificateErrors: Bool
```

2. **init(robot:) erweitern** — nach `_password = State(...)` (Zeile 196):
```swift
_useSSL = State(initialValue: robot.useSSL)
_ignoreCertificateErrors = State(initialValue: robot.ignoreCertificateErrors)
```

3. **SSL-Section in Form einfuegen** — nach der Authentication-Section (nach Zeile 218, vor der Test-Connection-Section):
```swift
Section {
    Toggle(String(localized: "settings.use_ssl"), isOn: $useSSL)

    if useSSL {
        Toggle(String(localized: "settings.ignore_certificate_errors"), isOn: $ignoreCertificateErrors)
    }
} header: {
    Text(String(localized: "settings.connection"))
}

if useSSL && ignoreCertificateErrors {
    Section {
        HStack(spacing: 8) {
            Image(systemName: "exclamationmark.triangle.fill")
                .foregroundStyle(.orange)
            Text(String(localized: "settings.ignore_certificate_errors.warning"))
                .font(.footnote)
                .foregroundStyle(.secondary)
        }
        .padding(.vertical, 4)
    }
}
```

Das Warning-Banner ist identisch zu dem in AddRobotView (Task 1) fuer Konsistenz.

4. **saveRobot() korrigieren** (Zeile 269-278) — useSSL und ignoreCertificateErrors an RobotConfig uebergeben:
```swift
private func saveRobot() {
    let updatedRobot = RobotConfig(
        id: robot.id,
        name: name,
        host: host,
        username: useAuthentication ? username : nil,
        password: useAuthentication ? password : nil,
        useSSL: useSSL,
        ignoreCertificateErrors: useSSL ? ignoreCertificateErrors : false
    )
    robotManager.updateRobot(updatedRobot)
    dismiss()
}
```

5. **testConnection() korrigieren** (Zeile 281-296) — gleicher Fix fuer Test-Config:
```swift
let testConfig = RobotConfig(
    name: name,
    host: host,
    username: useAuthentication ? username : nil,
    password: useAuthentication ? password : nil,
    useSSL: useSSL,
    ignoreCertificateErrors: useSSL ? ignoreCertificateErrors : false
)
```

WICHTIG: Ohne diesen Fix setzt `saveRobot()` useSSL und ignoreCertificateErrors auf `false` (Default-Werte), selbst wenn der Benutzer sie beim Erstellen aktiviert hatte. Das ist ein bestehender Bug.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>EditRobotView zeigt useSSL-Toggle und bedingten ignoreCertificateErrors-Toggle mit prominentem Warning-Banner. saveRobot() und testConnection() uebergeben useSSL und ignoreCertificateErrors korrekt an RobotConfig.</done>
</task>

</tasks>

<verification>
- Build kompiliert ohne Fehler
- RobotStatusHeaderView enthaelt `lock.open.fill` Icon mit `.orange` Farbe
- AddRobotView zeigt `exclamationmark.triangle.fill` Banner statt nur Footer-Text
- EditRobotView hat useSSL und ignoreCertificateErrors State-Variablen
- EditRobotView.saveRobot() uebergibt useSSL und ignoreCertificateErrors
- EditRobotView.testConnection() uebergibt useSSL und ignoreCertificateErrors
</verification>

<success_criteria>
- SEC-01: HTTP-Verbindungen zeigen oranges Lock-Icon in RobotStatusHeaderView
- SEC-02: AddRobotView und EditRobotView zeigen prominente Warnung bei ignoreCertificateErrors
- EditRobotView verliert useSSL/ignoreCertificateErrors nicht mehr beim Speichern
</success_criteria>

<output>
After completion, create `.planning/phases/26-security-hardening/26-01-SUMMARY.md`
</output>
