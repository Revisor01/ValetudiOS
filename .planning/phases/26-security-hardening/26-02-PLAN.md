---
phase: 26-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/KeychainStore.swift
  - ValetudoApp/ValetudoApp/Services/RobotManager.swift
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "Robot-Config (host, username, useSSL, ignoreCertificateErrors) wird im Keychain gespeichert"
    - "Bestehende Installs migrieren automatisch von UserDefaults in Keychain"
    - "Nach Migration wird UserDefaults-Blob geloescht"
    - "Robot-ID-Liste bleibt in UserDefaults (nicht sensibel — nur UUIDs)"
    - "Passwort-Handling bleibt unveraendert (bereits im Keychain)"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/KeychainStore.swift"
      provides: "robotConfig/saveRobotConfig/deleteRobotConfig API"
      contains: "com.valetudio.robot.config"
    - path: "ValetudoApp/ValetudoApp/Services/RobotManager.swift"
      provides: "Keychain-basierte loadRobots/saveRobots mit Migration"
      contains: "KeychainStore.robotConfig"
  key_links:
    - from: "RobotManager.saveRobots()"
      to: "KeychainStore.saveRobotConfig"
      via: "Iteration ueber robots Array"
      pattern: "KeychainStore\\.saveRobotConfig"
    - from: "RobotManager.loadRobots()"
      to: "KeychainStore.robotConfig(for:)"
      via: "Iteration ueber gespeicherte Robot-IDs"
      pattern: "KeychainStore\\.robotConfig\\(for:"
    - from: "RobotManager.loadRobots()"
      to: "UserDefaults fallback"
      via: "Migration bei erstem Start nach Update"
      pattern: "UserDefaults\\.standard\\.data.*storageKey"
---

<objective>
Robot-Config verschluesselt im Keychain speichern statt in unverschluesseltem UserDefaults.

Purpose: Host, Username, useSSL und ignoreCertificateErrors sind Konfigurationsdaten die nicht unverschluesselt auf dem Geraet liegen sollten. Die bestehende KeychainStore-Infrastruktur wird um Config-Blob-Speicherung erweitert. Bestehende Installs migrieren automatisch beim naechsten App-Start.

Output: Erweiterte KeychainStore API und migrierte RobotManager-Persistenz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-security-hardening/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From ValetudoApp/ValetudoApp/Services/KeychainStore.swift:
```swift
struct KeychainStore {
    private static let service = "com.valetudio.robot.password"
    private static let logger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "ValetudiOS", category: "KeychainStore")

    static func password(for robotId: UUID) -> String?
    @discardableResult static func save(password: String, for robotId: UUID) -> Bool
    static func delete(for robotId: UUID)
}
```

From ValetudoApp/ValetudoApp/Services/RobotManager.swift (Persistenz, Zeile 305-340):
```swift
private let storageKey = "valetudo_robots"

private func loadRobots() {
    guard let data = UserDefaults.standard.data(forKey: storageKey),
          let decoded = try? JSONDecoder().decode([RobotConfig].self, from: data) else { return }
    // ... Password-Migration zu Keychain ...
    robots = migratedRobots
    if migrationOccurred { saveRobots() }
}

private func saveRobots() {
    if let encoded = try? JSONEncoder().encode(robots) {
        UserDefaults.standard.set(encoded, forKey: storageKey)
    }
}
```

From ValetudoApp/ValetudoApp/Models/RobotConfig.swift:
```swift
struct RobotConfig: Codable, Identifiable, Equatable, Hashable {
    // CodingKeys schliesst 'password' aus — wird NICHT encoded
    private enum CodingKeys: String, CodingKey {
        case id, name, host, username, useSSL, ignoreCertificateErrors
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KeychainStore um RobotConfig-API erweitern</name>
  <files>ValetudoApp/ValetudoApp/Services/KeychainStore.swift</files>
  <action>
KeychainStore um drei neue statische Methoden erweitern fuer Config-Blob-Speicherung. Neuer Service-Key: `"com.valetudio.robot.config"`.

**Neue Properties und Methoden hinzufuegen** (nach der bestehenden `delete(for:)` Methode, vor dem schliessenden `}`):

```swift
// MARK: - Robot Config Storage (SEC-03)
private static let configService = "com.valetudio.robot.config"

static func robotConfig(for robotId: UUID) -> RobotConfig? {
    let query: [CFString: Any] = [
        kSecClass: kSecClassGenericPassword,
        kSecAttrService: configService,
        kSecAttrAccount: robotId.uuidString,
        kSecReturnData: true,
        kSecMatchLimit: kSecMatchLimitOne
    ]
    var result: AnyObject?
    let status = SecItemCopyMatching(query as CFDictionary, &result)
    guard status == errSecSuccess, let data = result as? Data else {
        if status != errSecItemNotFound {
            logger.error("SecItemCopyMatching (robotConfig) failed: \(status, privacy: .public)")
        }
        return nil
    }
    do {
        return try JSONDecoder().decode(RobotConfig.self, from: data)
    } catch {
        logger.error("Failed to decode robot config: \(error.localizedDescription, privacy: .public)")
        return nil
    }
}

@discardableResult
static func saveRobotConfig(_ config: RobotConfig, for robotId: UUID) -> Bool {
    // Delete-then-add pattern (identisch zum bestehenden Password-Pattern)
    let deleteQuery: [CFString: Any] = [
        kSecClass: kSecClassGenericPassword,
        kSecAttrService: configService,
        kSecAttrAccount: robotId.uuidString
    ]
    let deleteStatus = SecItemDelete(deleteQuery as CFDictionary)
    if deleteStatus != errSecSuccess && deleteStatus != errSecItemNotFound {
        logger.error("SecItemDelete (saveRobotConfig) failed: \(deleteStatus, privacy: .public)")
    }

    guard let data = try? JSONEncoder().encode(config) else {
        logger.error("Failed to encode robot config for \(robotId.uuidString, privacy: .public)")
        return false
    }
    let addQuery: [CFString: Any] = [
        kSecClass: kSecClassGenericPassword,
        kSecAttrService: configService,
        kSecAttrAccount: robotId.uuidString,
        kSecValueData: data,
        kSecAttrAccessible: kSecAttrAccessibleWhenUnlockedThisDeviceOnly
    ]
    let addStatus = SecItemAdd(addQuery as CFDictionary, nil)
    if addStatus != errSecSuccess {
        logger.error("SecItemAdd (saveRobotConfig) failed: \(addStatus, privacy: .public)")
    }
    return addStatus == errSecSuccess
}

static func deleteRobotConfig(for robotId: UUID) {
    let query: [CFString: Any] = [
        kSecClass: kSecClassGenericPassword,
        kSecAttrService: configService,
        kSecAttrAccount: robotId.uuidString
    ]
    let deleteStatus = SecItemDelete(query as CFDictionary)
    if deleteStatus != errSecSuccess && deleteStatus != errSecItemNotFound {
        logger.error("SecItemDelete (deleteRobotConfig) failed: \(deleteStatus, privacy: .public)")
    }
}
```

WICHTIG:
- Gleiche Security-Attribute wie bestehende Password-API (`kSecAttrAccessibleWhenUnlockedThisDeviceOnly`)
- Gleicher delete-then-add Pattern wie bestehender `save(password:for:)`
- Gleicher Error-Logging-Pattern mit `logger.error`
- RobotConfig.CodingKeys schliesst `password` bereits aus — Config-Blob enthaelt kein Passwort
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>KeychainStore hat drei neue Methoden: robotConfig(for:), saveRobotConfig(_:for:), deleteRobotConfig(for:). Alle nutzen configService "com.valetudio.robot.config" mit gleichen Security-Attributen wie Password-API.</done>
</task>

<task type="auto">
  <name>Task 2: RobotManager-Persistenz auf Keychain migrieren</name>
  <files>ValetudoApp/ValetudoApp/Services/RobotManager.swift</files>
  <action>
RobotManager.loadRobots() und saveRobots() umstellen: Config-Daten gehen in Keychain, Robot-ID-Liste bleibt in UserDefaults (nur UUIDs, nicht sensibel).

**1. Neuen Storage-Key fuer ID-Liste hinzufuegen** (neben dem bestehenden `storageKey`):
```swift
private let robotIdsKey = "valetudo_robot_ids"
```

**2. loadRobots() komplett ersetzen** (Zeile 306-334):
```swift
private func loadRobots() {
    var loadedRobots: [RobotConfig] = []

    // Primaerer Pfad: Lade aus Keychain via ID-Liste
    if let idsData = UserDefaults.standard.data(forKey: robotIdsKey),
       let ids = try? JSONDecoder().decode([UUID].self, from: idsData) {
        loadedRobots = ids.compactMap { KeychainStore.robotConfig(for: $0) }
        // Wenn IDs vorhanden aber keine Configs gefunden: ID-Liste ist stale, ignorieren
        if !ids.isEmpty && loadedRobots.isEmpty {
            loadedRobots = [] // Wird unten via UserDefaults-Fallback gefuellt
        }
    }

    // Fallback: Migration von UserDefaults (bestehende Installs)
    if loadedRobots.isEmpty,
       let data = UserDefaults.standard.data(forKey: storageKey),
       let decoded = try? JSONDecoder().decode([RobotConfig].self, from: data) {

        // Bestehende Passwort-Migration beibehalten
        var migratedRobots = decoded
        for (index, robot) in decoded.enumerated() {
            // Passwort in Keychain migrieren (falls noch nicht geschehen)
            if KeychainStore.password(for: robot.id) == nil,
               let legacyPassword = robot.password, !legacyPassword.isEmpty {
                KeychainStore.save(password: legacyPassword, for: robot.id)
            }
            migratedRobots[index].password = nil

            // Config in Keychain speichern
            KeychainStore.saveRobotConfig(migratedRobots[index], for: robot.id)
        }

        // Robot-ID-Liste in UserDefaults speichern
        if let idsData = try? JSONEncoder().encode(decoded.map { $0.id }) {
            UserDefaults.standard.set(idsData, forKey: robotIdsKey)
        }

        // Alten UserDefaults-Blob loeschen
        UserDefaults.standard.removeObject(forKey: storageKey)

        loadedRobots = migratedRobots
    }

    robots = loadedRobots
    for robot in robots {
        apis[robot.id] = ValetudoAPI(config: robot)
    }
}
```

**3. saveRobots() komplett ersetzen** (Zeile 336-340):
```swift
private func saveRobots() {
    // Config-Blobs einzeln in Keychain speichern
    for robot in robots {
        KeychainStore.saveRobotConfig(robot, for: robot.id)
    }
    // ID-Liste in UserDefaults (nur UUIDs, nicht sensibel)
    if let idsData = try? JSONEncoder().encode(robots.map { $0.id }) {
        UserDefaults.standard.set(idsData, forKey: robotIdsKey)
    }
}
```

**4. removeRobot erweitern** — in der bestehenden `removeRobot(_:)` Methode (suche nach `func removeRobot`), nach `KeychainStore.delete(for: id)` auch Config loeschen:
```swift
KeychainStore.deleteRobotConfig(for: id)
```

WICHTIG:
- Die ID-Liste bleibt in UserDefaults weil Keychain-Items nicht effizient auflistbar sind. UUIDs sind nicht sensibel.
- Der alte `storageKey` ("valetudo_robots") wird nach erfolgreicher Migration geloescht via `removeObject(forKey:)`.
- Passwort-Migration-Logik bleibt erhalten fuer Installs die noch nie gestartet wurden seit Phase 1.
- `password` wird immer auf nil gesetzt vor dem Config-Speichern — CodingKeys schliesst password ohnehin aus, aber explizites nil ist sicherer.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>RobotManager speichert Config-Daten im Keychain statt UserDefaults. Migration von UserDefaults erfolgt automatisch beim ersten Start. Alter UserDefaults-Blob wird nach Migration geloescht. Robot-ID-Liste in UserDefaults fuer Keychain-Lookup.</done>
</task>

</tasks>

<verification>
- Build kompiliert ohne Fehler
- KeychainStore.swift enthaelt `configService = "com.valetudio.robot.config"`
- KeychainStore hat robotConfig(for:), saveRobotConfig(_:for:), deleteRobotConfig(for:) Methoden
- RobotManager.loadRobots() liest primaer aus Keychain, mit UserDefaults-Fallback fuer Migration
- RobotManager.saveRobots() schreibt in Keychain statt UserDefaults
- RobotManager.removeRobot() ruft KeychainStore.deleteRobotConfig(for:) auf
- UserDefaults-Blob wird nach Migration geloescht
</verification>

<success_criteria>
- SEC-03: Robot-Config (host, username, useSSL, ignoreCertificateErrors) wird im Keychain gespeichert
- Bestehende Installs migrieren automatisch von UserDefaults in Keychain
- Passwort-Handling bleibt unveraendert (weiterhin separater Keychain-Eintrag)
- Kein Datenverlust bei Migration
</success_criteria>

<output>
After completion, create `.planning/phases/26-security-hardening/26-02-SUMMARY.md`
</output>
