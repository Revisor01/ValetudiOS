---
phase: 29-ux-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/Views/TimersView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/SettingsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotListView.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - ROBUST-01
  - ROBUST-02

must_haves:
  truths:
    - "Fehlgeschlagene benutzer-initiierte Aktionen zeigen eine Fehlermeldung im Alert"
    - "Timer-Loeschen zeigt Bestaetigungsdialog vor dem API-Call"
    - "Snapshot-Restore zeigt Bestaetigungsdialog vor dem API-Call"
    - "Roboter-Entfernen zeigt Bestaetigungsdialog in SettingsView und RobotListView"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "errorRouter.show(error) in allen user-initiierten catch-Bloecken"
      contains: "errorRouter?.show(error)"
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "errorRouter.show(error) in cleanZones, goToPoint, renameRoom, saveRestrictions, confirmEditMode"
      contains: "errorRouter?.show(error)"
    - path: "ValetudoApp/ValetudoApp/Views/TimersView.swift"
      provides: "confirmationDialog fuer Timer-Delete"
      contains: "showDeleteConfirm"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "alert fuer Snapshot-Restore"
      contains: "showRestoreConfirm"
    - path: "ValetudoApp/ValetudoApp/Views/SettingsView.swift"
      provides: "confirmationDialog fuer Robot-Remove"
      contains: "showRemoveConfirm"
    - path: "ValetudoApp/ValetudoApp/Views/RobotListView.swift"
      provides: "confirmationDialog fuer Robot-Remove"
      contains: "showRemoveConfirm"
  key_links:
    - from: "RobotDetailViewModel catch-Bloecke"
      to: "ErrorRouter"
      via: "errorRouter?.show(error)"
      pattern: "errorRouter\\?\\.show\\(error\\)"
    - from: "MapViewModel catch-Bloecke"
      to: "ErrorRouter"
      via: "errorRouter?.show(error)"
      pattern: "errorRouter\\?\\.show\\(error\\)"
    - from: "TimersView .onDelete"
      to: "confirmationDialog"
      via: "showDeleteConfirm State"
      pattern: "showDeleteConfirm"
---

<objective>
ErrorRouter fuer alle benutzer-initiierten Aktionen verdrahten und Bestaetigungsdialoge fuer destruktive Aktionen hinzufuegen.

Purpose: Stumme Fehler bei Button-Aktionen eliminieren (ROBUST-01) und versehentliches Loeschen/Zuruecksetzen verhindern (ROBUST-02).
Output: Alle catch-Bloecke in RobotDetailViewModel und MapViewModel zeigen Fehler via ErrorRouter. Timer-Delete, Snapshot-Restore, Robot-Remove haben Confirmation-Dialogs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ux-robustness/29-RESEARCH.md

@ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
@ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
@ValetudoApp/ValetudoApp/Views/TimersView.swift
@ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
@ValetudoApp/ValetudoApp/Views/SettingsView.swift
@ValetudoApp/ValetudoApp/Views/RobotListView.swift
@ValetudoApp/ValetudoApp/Views/ConsumablesView.swift

<interfaces>
<!-- Bestehendes ErrorRouter-Pattern (aus ErrorRouter.swift + RobotDetailViewModel.swift): -->

// ErrorRouter — @Observable, injiziert via .environment()
// Zeigt: .alert mit error.localizedDescription + optionaler Retry-Action
var errorRouter: ErrorRouter?

// Bestehende Nutzung in catch-Block (z.B. locate()):
} catch {
    logger.error("...")
    errorRouter?.show(error)
}

// Bestehendes confirmationDialog-Pattern (aus ConsumablesView.swift):
@State private var showResetConfirm = false
@State private var consumableToReset: String?

.confirmationDialog("consumables.reset_title", isPresented: $showResetConfirm, titleVisibility: .visible) {
    Button("consumables.reset_confirm", role: .destructive) { ... }
    Button("settings.cancel", role: .cancel) {}
}

// Bestehendes .alert-Pattern (aus RobotSettingsView.swift fuer Map-Reset):
@State private var showMapResetConfirm = false
.alert("settings.map_reset_confirm", isPresented: $showMapResetConfirm) {
    Button("settings.cancel", role: .cancel) {}
    Button("settings.map_reset_action", role: .destructive) { ... }
} message: { Text("settings.map_reset_warning") }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorRouter in alle stummen catch-Bloecke einbauen (ROBUST-01)</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  </files>
  <action>
    In RobotDetailViewModel.swift — `errorRouter?.show(error)` nach jedem `logger.error(...)` in diesen Funktionen hinzufuegen (per ROBUST-01):

    - `performAction(_:)` (Start/Stop/Pause/Home) — Zeile ~353
    - `cleanSelectedRooms()` — Zeile ~377
    - `setFanSpeed(_:)` — Zeile ~397
    - `setWaterUsage(_:)` — analog
    - `setOperationMode(_:)` — analog
    - `triggerAutoEmpty()` — analog
    - `triggerMopDockClean()` — analog
    - `triggerMopDockDry()` — analog
    - `resetConsumable(_:)` — analog
    - `dismissEvent(_:)` — analog
    - `setCleanRoute(_:)` — analog

    In MapViewModel.swift — `errorRouter?.show(error)` nach jedem `logger.error(...)` in diesen Funktionen hinzufuegen:

    - `cleanZones()` — Zeile ~484
    - `goToPoint(x:y:)` — Zeile ~497
    - `renameRoom(id:name:)` — Zeile ~554
    - `saveRestrictions()` — Zeile ~711
    - `confirmEditMode(...)` zone-catch — Zeile ~744
    - `confirmEditMode(...)` deleteRestriction-catch — Zeile ~777

    WICHTIG: NUR user-initiierte Aktionen. NICHT in loadData(), loadSegments(), loadConsumables(), loadCapabilities() etc. — diese sind Hintergrundoperationen (siehe Research Pitfall 2).

    Pattern: Direkt nach der bestehenden `logger.error(...)` Zeile einfuegen:
    ```swift
    errorRouter?.show(error)
    ```

    Keine anderen Aenderungen an den catch-Bloecken. Der Logger-Aufruf bleibt bestehen.
  </action>
  <verify>
    <automated>cd ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Alle 17 stummen catch-Bloecke in user-initiierten Aktionen rufen errorRouter?.show(error) auf. Hintergrund-Ladeoperationen bleiben unberuehrt. Build kompiliert ohne Fehler.</done>
</task>

<task type="auto">
  <name>Task 2: Bestaetigungsdialoge fuer destruktive Aktionen (ROBUST-02)</name>
  <files>
    ValetudoApp/ValetudoApp/Views/TimersView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/SettingsView.swift,
    ValetudoApp/ValetudoApp/Views/RobotListView.swift,
    ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
  </files>
  <action>
    Vier fehlende Bestaetigungsdialoge hinzufuegen (per ROBUST-02). Bestehendes Pattern aus ConsumablesView und RobotSettingsView uebernehmen.

    **1. TimersView.swift — Timer-Delete:**
    - Neuen State hinzufuegen: `@State private var showDeleteConfirm = false` und `@State private var timerToDelete: ValetudoTimer?`
    - `deleteTimers(at:)` aendern: Statt direkt API aufzurufen, den Timer in `timerToDelete` speichern und `showDeleteConfirm = true` setzen. WICHTIG: Item nach ID speichern, nicht nach Index (Research Pitfall 1).
    - `.confirmationDialog` Modifier auf die List hinzufuegen:
      ```swift
      .confirmationDialog(String(localized: "timer.delete_title"), isPresented: $showDeleteConfirm, titleVisibility: .visible) {
          Button(String(localized: "timer.delete_confirm"), role: .destructive) {
              if let timer = timerToDelete {
                  Task { /* bestehenden Delete-API-Call hierhin verschieben */ }
              }
          }
          Button(String(localized: "settings.cancel"), role: .cancel) {}
      }
      ```

    **2. RobotSettingsView.swift — Snapshot-Restore:**
    - Neuen State: `@State private var showRestoreConfirm = false` und `@State private var snapshotToRestore: MapSnapshot?`
    - Den bestehenden Button in der Snapshot-ForEach aendern: Statt `Task { await viewModel.restoreMapSnapshot(snapshot) }` nur `snapshotToRestore = snapshot; showRestoreConfirm = true`
    - `.alert` Modifier hinzufuegen:
      ```swift
      .alert(String(localized: "snapshots.restore_warning_title"), isPresented: $showRestoreConfirm) {
          Button(String(localized: "settings.cancel"), role: .cancel) {}
          Button(String(localized: "snapshots.restore_confirm"), role: .destructive) {
              if let snapshot = snapshotToRestore {
                  Task { await viewModel.restoreMapSnapshot(snapshot) }
              }
          }
      } message: {
          Text(String(localized: "snapshots.restore_warning_message"))
      }
      ```

    **3. SettingsView.swift — Robot-Remove (EditRobotView Button + onDelete):**
    - In EditRobotView-Bereich: `@State private var showRemoveConfirm = false`
    - Den destructive Button aendern: Statt `robotManager.removeRobot(robot.id); dismiss()` nur `showRemoveConfirm = true`
    - `.alert` hinzufuegen:
      ```swift
      .alert(String(localized: "robot.remove_warning_title"), isPresented: $showRemoveConfirm) {
          Button(String(localized: "settings.cancel"), role: .cancel) {}
          Button(String(localized: "robot.remove_confirm"), role: .destructive) {
              robotManager.removeRobot(robot.id)
              dismiss()
          }
      } message: {
          Text(String(localized: "robot.remove_warning_message"))
      }
      ```
    - Fuer `deleteRobots(at:)` in SettingsView: `@State private var robotToRemove: RobotConfig?` und `@State private var showRemoveListConfirm = false`. onDelete speichert Robot, zeigt confirmationDialog.

    **4. RobotListView.swift — Robot-Remove (onDelete):**
    - Gleich wie SettingsView: `@State private var robotToRemove: RobotConfig?` und `@State private var showRemoveConfirm = false`
    - `deleteRobots(at:)` speichert Robot in State, setzt showRemoveConfirm = true
    - `.confirmationDialog` Modifier auf die List

    **5. Localizable.xcstrings — Neue Keys:**
    Folgende Keys in der JSON-Struktur hinzufuegen (en + de):
    - `timer.delete_title`: en "Delete Timer?", de "Timer loeschen?"
    - `timer.delete_confirm`: en "Delete", de "Loeschen"
    - `snapshots.restore_warning_title`: en "Restore Snapshot?", de "Snapshot wiederherstellen?"
    - `snapshots.restore_warning_message`: en "This will replace the current map with the selected snapshot. This action cannot be undone.", de "Die aktuelle Karte wird durch den ausgewaehlten Snapshot ersetzt. Diese Aktion kann nicht rueckgaengig gemacht werden."
    - `snapshots.restore_confirm`: en "Restore", de "Wiederherstellen"
    - `robot.remove_warning_title`: en "Remove Robot?", de "Roboter entfernen?"
    - `robot.remove_warning_message`: en "This will remove the robot and all stored credentials. This action cannot be undone.", de "Der Roboter und alle gespeicherten Zugangsdaten werden entfernt. Diese Aktion kann nicht rueckgaengig gemacht werden."
    - `robot.remove_confirm`: en "Remove", de "Entfernen"

    Fuer die xcstrings-Datei: Die bestehende JSON-Struktur analysieren und die neuen Keys im exakt gleichen Format einfuegen. Bestehende Keys wie `consumables.reset_confirm` und `settings.map_reset_confirm` als Vorlage fuer das JSON-Format verwenden.
  </action>
  <verify>
    <automated>cd ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Timer-Delete, Snapshot-Restore, Robot-Remove (EditRobotView + RobotListView + SettingsView onDelete) zeigen Bestaetigungsdialog. Alle neuen Localization-Keys sind in en + de vorhanden. Build kompiliert ohne Fehler.</done>
</task>

</tasks>

<verification>
1. Build kompiliert: `xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16'`
2. Grep-Check ROBUST-01: `grep -c "errorRouter?.show(error)" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` — erwartetes Ergebnis: mindestens 12 (11 neue + 1 bestehende locate())
3. Grep-Check ROBUST-01: `grep -c "errorRouter?.show(error)" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — erwartetes Ergebnis: mindestens 8 (6 neue + bestehende)
4. Grep-Check ROBUST-02: `grep -c "showDeleteConfirm\|showRestoreConfirm\|showRemoveConfirm" ValetudoApp/ValetudoApp/Views/TimersView.swift ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift ValetudoApp/ValetudoApp/Views/SettingsView.swift ValetudoApp/ValetudoApp/Views/RobotListView.swift` — alle Dateien haben Treffer
</verification>

<success_criteria>
- Jede user-initiierte Aktion in RobotDetailViewModel und MapViewModel zeigt bei Fehler einen Alert via ErrorRouter
- Timer-Delete, Snapshot-Restore und Robot-Remove (alle 4 Einstiegspunkte) zeigen Bestaetigungsdialog
- Keine Hintergrund-Ladeoperationen zeigen Fehler-Alerts
- Alle neuen Localization-Keys in en + de vorhanden
- Build kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/29-ux-robustness/29-01-SUMMARY.md`
</output>
