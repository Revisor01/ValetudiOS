---
phase: 18-map-caching
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - ValetudoApp/ValetudoApp/Views/MapView.swift
  - ValetudoApp/ValetudoApp/Services/RobotManager.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: false
requirements: [CACHE-02, CACHE-01]

must_haves:
  truths:
    - "Offline-Banner erscheint auf der Karte wenn isOffline true ist"
    - "Offline-Banner verschwindet automatisch wenn Verbindung wieder steht"
    - "Cache wird geloescht wenn Roboter entfernt wird"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      provides: "Offline-Banner-Overlay"
      contains: "isOffline"
    - path: "ValetudoApp/ValetudoApp/Services/RobotManager.swift"
      provides: "Cache-Cleanup bei removeRobot()"
      contains: "MapCacheService"
    - path: "ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings"
      provides: "Lokalisierungskey map.offline"
      contains: "map.offline"
  key_links:
    - from: "MapView.swift"
      to: "MapViewModel.isOffline"
      via: "viewModel.isOffline im ZStack-Overlay"
      pattern: "viewModel\\.isOffline"
    - from: "RobotManager.removeRobot()"
      to: "MapCacheService.deleteCache()"
      via: "Aufruf nach saveRobots()"
      pattern: "MapCacheService\\.shared\\.deleteCache"
---

<objective>
Offline-Banner in MapView anzeigen, Cache-Cleanup in RobotManager und Lokalisierung — die sichtbare Benutzer-Erfahrung fuer den Offline-Modus.

Purpose: CACHE-02 (Offline-Indikator UI), CACHE-01 (Cache-Cleanup bei Roboter-Loeschung)
Output: MapView.swift (Overlay), RobotManager.swift (deleteCache), Localizable.xcstrings (map.offline)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-map-caching/18-CONTEXT.md
@.planning/phases/18-map-caching/18-RESEARCH.md
@.planning/phases/18-map-caching/18-01-SUMMARY.md

<interfaces>
<!-- Von Plan 01 erstellte Interfaces -->

From ValetudoApp/ValetudoApp/Services/MapCacheService.swift (Plan 01):
```swift
final class MapCacheService {
    static let shared = MapCacheService()
    func save(_ map: RobotMap, for robotId: UUID) async
    func load(for robotId: UUID) async -> RobotMap?
    func deleteCache(for robotId: UUID)   // synchron
}
```

From ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift (Plan 01):
```swift
@Published var isOffline: Bool = false   // true wenn gecachte Karte geladen
```

From ValetudoApp/ValetudoApp/Views/MapView.swift (bestehend):
```swift
// MapContentView.body enthält ZStack ab Zeile 227
// ContentUnavailableView ab Zeile 337-340
// Innerer ZStack (Zeile 227) ist der richtige Ort fuer Overlay
```

From ValetudoApp/ValetudoApp/Services/RobotManager.swift (bestehend):
```swift
func removeRobot(_ id: UUID) {
    Task { await sseManager.disconnect(robotId: id) }
    KeychainStore.delete(for: id)
    robots.removeAll { $0.id == id }
    apis.removeValue(forKey: id)
    robotStates.removeValue(forKey: id)
    previousStates.removeValue(forKey: id)
    saveRobots()
    // <-- hier MapCacheService.shared.deleteCache(for: id)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline-Banner in MapView und Cache-Cleanup in RobotManager</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapView.swift
    ValetudoApp/ValetudoApp/Services/RobotManager.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/MapView.swift
    ValetudoApp/ValetudoApp/Services/RobotManager.swift
    ValetudoApp/ValetudoApp/Services/MapCacheService.swift
  </read_first>
  <action>
ZWEI Dateien editieren:

**1. MapView.swift — Offline-Banner-Overlay in MapContentView:**

Im inneren ZStack von MapContentView.body (ab Zeile 227, der ZStack der InteractiveMapView enthaelt), NACH allen bestehenden Overlays (vor dem `.gesture(combinedGesture)`), ein Overlay einfuegen:

```swift
if viewModel.isOffline {
    VStack {
        HStack(spacing: 6) {
            Image(systemName: "wifi.slash")
                .font(.caption)
            Text(String(localized: "map.offline"))
                .font(.caption)
                .fontWeight(.medium)
        }
        .padding(.horizontal, 12)
        .padding(.vertical, 6)
        .background(.ultraThinMaterial)
        .clipShape(Capsule())
        .padding(.top, 8)
        Spacer()
    }
}
```

Per CONTEXT.md: dezent, nicht-blockierend, am oberen Rand der Karte. Das Banner erscheint NUR wenn viewModel.isOffline == true (also wenn eine gecachte Karte geladen wurde). Die bestehende ContentUnavailableView (Zeile 337-340) bleibt fuer den Fall ohne Cache.

ZUSAETZLICH: In der Bedingung fuer ContentUnavailableView (Zeile 336 `} else {`) muss geprueft werden, dass auch `!viewModel.isOffline` gilt — wenn isOffline true ist und map gesetzt, wird die Karte angezeigt (nicht ContentUnavailableView). Die bestehende Logik `if let map = viewModel.map` deckt das bereits ab — die gecachte Karte wird in viewModel.map gesetzt. Keine Aenderung an der else-Bedingung noetig.

**2. RobotManager.swift — Cache-Cleanup:**

In `removeRobot(_ id: UUID)` (Zeile 58-67), NACH `saveRobots()` (Zeile 66), eine Zeile einfuegen:
```swift
MapCacheService.shared.deleteCache(for: id)
```

deleteCache ist synchron — kein async/await noetig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "viewModel.isOffline" ValetudoApp/ValetudoApp/Views/MapView.swift && grep -c "wifi.slash" ValetudoApp/ValetudoApp/Views/MapView.swift && grep -c "MapCacheService.shared.deleteCache" ValetudoApp/ValetudoApp/Services/RobotManager.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep "viewModel.isOffline" MapView.swift
    - grep "wifi.slash" MapView.swift
    - grep "map.offline" MapView.swift
    - grep "ultraThinMaterial" MapView.swift
    - grep "MapCacheService.shared.deleteCache" RobotManager.swift
  </acceptance_criteria>
  <done>Offline-Banner erscheint als Capsule-Overlay mit wifi.slash Icon wenn isOffline true; RobotManager loescht Cache bei removeRobot()</done>
</task>

<task type="auto">
  <name>Task 2: Lokalisierungskey map.offline in String Catalog eintragen</name>
  <files>ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings</files>
  <read_first>
    ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
  </read_first>
  <action>
In Localizable.xcstrings den Key `map.offline` hinzufuegen.

Die xcstrings-Datei ist JSON-Format. Neuen Eintrag in das `strings`-Objekt einfuegen:

```json
"map.offline" : {
  "localizations" : {
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Offline — Cached Map"
      }
    },
    "de" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Offline — Gespeicherte Karte"
      }
    }
  }
}
```

Falls die xcstrings-Datei andere Sprachen enthaelt (pruefen!), diese ebenfalls befuellen. Falls nur en/de: nur en und de.

WICHTIG: Alphabetische Sortierung der Keys in der xcstrings-Datei beibehalten (nach bestehenden "map.*" Keys einfuegen).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "map.offline" ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings</automated>
  </verify>
  <acceptance_criteria>
    - grep "map.offline" Localizable.xcstrings
    - grep "Offline" Localizable.xcstrings (Uebersetzungstext)
  </acceptance_criteria>
  <done>Lokalisierungskey map.offline ist in der xcstrings-Datei mit en/de Uebersetzungen vorhanden</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visueller Check — Offline-Banner und Caching-Verhalten</name>
  <files>n/a</files>
  <action>
Benutzer verifiziert das vollstaendige Map-Caching-Feature visuell und funktional.
  </action>
  <what-built>
    Map Caching: Letzte Karte wird auf Disk gespeichert, Offline-Banner bei nicht-erreichbarem Roboter, automatische Aktualisierung bei Wiederverbindung, Cache-Cleanup bei Roboter-Loeschung.
  </what-built>
  <how-to-verify>
    1. App oeffnen, Roboter-Karte laden — Karte sollte normal angezeigt werden
    2. Roboter ausschalten oder WLAN trennen — nach wenigen Sekunden sollte "Offline — Gespeicherte Karte" Banner als dezente Capsule oben auf der Karte erscheinen
    3. Roboter wieder einschalten / WLAN verbinden — Banner sollte automatisch verschwinden, Karte aktualisiert sich live
    4. Einen Roboter loeschen und App-Sandbox pruefen — Documents/MapCache/ sollte keine Datei fuer den geloeschten Roboter mehr enthalten
  </how-to-verify>
  <verify>Benutzer bestaetigt visuell</verify>
  <done>Benutzer hat Offline-Banner, Cache-Laden und automatische Wiederherstellung bestaetigt</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Offline-Banner erscheint in MapView wenn isOffline true
- RobotManager loescht Cache bei removeRobot()
- Lokalisierungskey map.offline existiert
- Projekt kompiliert: `cd ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5`
</verification>

<success_criteria>
- Offline-Banner zeigt dezent "Offline" mit wifi.slash Icon auf der Karte
- Banner verschwindet automatisch bei Wiederverbindung (CACHE-03)
- Cache wird bei Roboter-Loeschung entfernt
- Lokalisierung in en/de vorhanden
- Benutzer bestaetigt visuelles Ergebnis
</success_criteria>

<output>
After completion, create `.planning/phases/18-map-caching/18-02-SUMMARY.md`
</output>
