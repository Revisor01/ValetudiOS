---
phase: 21-cleaning-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  - ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift
  - ValetudoApp/ValetudoApp/Views/MapView.swift
autonomous: true
requirements:
  - ROOM-02

must_haves:
  truths:
    - "selectedSegmentIds in MapViewModel ist ein [String] Array statt Set<String>"
    - "selectedSegments in RobotDetailViewModel ist ein [String] Array statt Set<String>"
    - "Reihenfolge der Auswahl bleibt beim API-Call erhalten — Array wird direkt übergeben"
    - "Alle insert/remove Set-Operationen sind durch append/removeAll(where:) ersetzt"
    - "Keine Array(selectedSegmentIds) Wrapper mehr vorhanden"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "Ordered selectedSegmentIds as [String]"
      contains: "var selectedSegmentIds: [String] = []"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "Ordered selectedSegments as [String]"
      contains: "var selectedSegments: [String] = []"
    - path: "ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift"
      provides: "Binding updated to [String], toggleSegment uses append"
      contains: "@Binding var selectedSegmentIds: [String]"
  key_links:
    - from: "MapInteractiveView.toggleSegment"
      to: "MapViewModel.selectedSegmentIds"
      via: "@Binding"
      pattern: "selectedSegmentIds\\.append"
    - from: "MapViewModel.cleanSelectedRooms"
      to: "ValetudoAPI.cleanSegments"
      via: "direct array pass"
      pattern: "cleanSegments\\(ids: selectedSegmentIds"
---

<objective>
Set→Array Migration für Reinigungsreihenfolge

Purpose: `selectedSegmentIds` und `selectedSegments` von `Set<String>` zu `[String]` ändern, damit die Einfügereihenfolge erhalten bleibt und als Reinigungsreihenfolge an die API übergeben werden kann.

Output: Alle ViewModels, Views und Bindings verwenden `[String]` statt `Set<String>` — die App kompiliert und funktioniert identisch, nur mit deterministischer Reihenfolge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cleaning-order/21-CONTEXT.md
@.planning/phases/21-cleaning-order/21-RESEARCH.md

<interfaces>
<!-- Aktuelle Typen die geändert werden müssen -->

From ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift (Zeile 47):
```swift
var selectedSegmentIds: Set<String> = []
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift (Zeile 18):
```swift
var selectedSegments: Set<String> = []
```

From ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift (Zeile 7):
```swift
@Binding var selectedSegmentIds: Set<String>
```

From ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift (Zeilen 183-189):
```swift
private func toggleSegment(_ id: String) {
    if selectedSegmentIds.contains(id) {
        selectedSegmentIds.remove(id)
    } else {
        selectedSegmentIds.insert(id)
    }
}
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift (Zeilen 351-357):
```swift
func toggleSegment(_ id: String) {
    if selectedSegments.contains(id) {
        selectedSegments.remove(id)
    } else {
        selectedSegments.insert(id)
    }
}
```

From ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift (Zeile 346):
```swift
Task { await viewModel.joinRooms(ids: Array(viewModel.selectedSegmentIds)) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set→Array in ViewModels und API-Calls</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  </read_first>
  <action>
**MapViewModel.swift:**
1. Zeile 47: `var selectedSegmentIds: Set<String> = []` ändern zu `var selectedSegmentIds: [String] = []`
2. Zeile 195: `try await api.cleanSegments(ids: Array(selectedSegmentIds), iterations: selectedIterations)` ändern zu `try await api.cleanSegments(ids: selectedSegmentIds, iterations: selectedIterations)` — den `Array()` Wrapper entfernen
3. Alle anderen Stellen mit `selectedSegmentIds` prüfen — `.removeAll()`, `.isEmpty`, `.count`, `.first` bleiben unverändert (Array hat gleiche Signatur)

**RobotDetailViewModel.swift:**
1. Zeile 18: `var selectedSegments: Set<String> = []` ändern zu `var selectedSegments: [String] = []`
2. Zeile 342: `try await api.cleanSegments(ids: Array(selectedSegments), iterations: selectedIterations)` ändern zu `try await api.cleanSegments(ids: selectedSegments, iterations: selectedIterations)` — den `Array()` Wrapper entfernen
3. Zeilen 351-357: `toggleSegment` ändern — `selectedSegments.remove(id)` wird zu `selectedSegments.removeAll(where: { $0 == id })` und `selectedSegments.insert(id)` wird zu `selectedSegments.append(id)`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -n "Set<String>" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift | grep -v "selectedSegmentsToJoin" | grep -c "selectedSegment" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
- `grep "var selectedSegmentIds: \[String\] = \[\]" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` findet genau 1 Treffer
- `grep "var selectedSegments: \[String\] = \[\]" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` findet genau 1 Treffer
- `grep "Array(selectedSegmentIds)" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` findet 0 Treffer
- `grep "Array(selectedSegments)" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` findet 0 Treffer
- `grep "\.append(id)" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` findet mindestens 1 Treffer
- `grep "removeAll(where:" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` findet mindestens 1 Treffer
  </acceptance_criteria>
  <done>Beide ViewModels verwenden [String] statt Set, API-Calls übergeben Array direkt ohne Wrapper, toggleSegment nutzt append/removeAll(where:)</done>
</task>

<task type="auto">
  <name>Task 2: Binding und Views auf Array-Typ umstellen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift,
    ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift,
    ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift,
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </read_first>
  <action>
**MapInteractiveView.swift:**
1. Zeile 7: `@Binding var selectedSegmentIds: Set<String>` ändern zu `@Binding var selectedSegmentIds: [String]`
2. Zeilen 183-189: `toggleSegment` ändern — `selectedSegmentIds.remove(id)` wird zu `selectedSegmentIds.removeAll(where: { $0 == id })` und `selectedSegmentIds.insert(id)` wird zu `selectedSegmentIds.append(id)`
3. Alle anderen Stellen (`.contains`, Zeile 61 und 151) bleiben unverändert — Array hat gleiche `.contains()` Signatur

**MapControlBarsView.swift:**
1. Zeile 346: `Task { await viewModel.joinRooms(ids: Array(viewModel.selectedSegmentIds)) }` ändern zu `Task { await viewModel.joinRooms(ids: viewModel.selectedSegmentIds) }` — den `Array()` Wrapper entfernen
2. Alle anderen Stellen (`.isEmpty`, `.count`, `.first`) bleiben unverändert

**MapView.swift:**
1. Zeile 231: `selectedSegmentIds: $viewModel.selectedSegmentIds` — keine Änderung nötig, Binding-Typ folgt automatisch aus MapViewModel

**Danach Build prüfen:**
```bash
cd ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep "@Binding var selectedSegmentIds: \[String\]" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet genau 1 Treffer
- `grep "\.append(id)" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet mindestens 1 Treffer
- `grep "removeAll(where:" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet mindestens 1 Treffer
- `grep "Array(viewModel.selectedSegmentIds)" ValetudoApp/ValetudoApp/Views/MapControlBarsView.swift` findet 0 Treffer
- `grep -c "Set<String>" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` ergibt 0
- xcodebuild endet mit "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>Alle Views und Bindings verwenden [String], keine Set-Referenzen mehr in den geänderten Dateien, Projekt kompiliert fehlerfrei</done>
</task>

</tasks>

<verification>
1. `grep -rn "Set<String>" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` — keine Treffer für selectedSegmentIds/selectedSegments
2. `grep -rn "Array(selectedSegmentIds)\|Array(selectedSegments)\|Array(viewModel.selectedSegmentIds)" ValetudoApp/` — keine Treffer
3. xcodebuild build erfolgreich
4. Bestehende Tests laufen durch (MapViewModelTests prüft nur `.isEmpty` — typ-agnostisch)
</verification>

<success_criteria>
- `selectedSegmentIds` ist `[String]` in MapViewModel
- `selectedSegments` ist `[String]` in RobotDetailViewModel
- `@Binding var selectedSegmentIds` ist `[String]` in InteractiveMapView
- Alle `insert`→`append`, `remove`→`removeAll(where:)` ersetzt
- Alle `Array()` Wrapper entfernt
- Build erfolgreich, Tests grün
</success_criteria>

<output>
After completion, create `.planning/phases/21-cleaning-order/21-01-SUMMARY.md`
</output>
