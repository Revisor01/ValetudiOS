---
phase: 21-cleaning-order
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
autonomous: false
requirements:
  - ROOM-01

must_haves:
  truths:
    - "Beim Auswählen eines Raums erscheint eine Zahl (1, 2, 3) als blauer Badge auf der Karte über dem Raum"
    - "Beim Abwählen eines Raums werden die Zahlen der verbleibenden Räume lückenlos neu nummeriert"
    - "Badges sind auch sichtbar wenn showRoomLabels == false"
    - "Badges erscheinen nicht im roomEdit- oder splitRoom-Modus"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift"
      provides: "orderBadgesOverlay ViewBuilder"
      contains: "orderBadgesOverlay"
  key_links:
    - from: "orderBadgesOverlay"
      to: "selectedSegmentIds"
      via: "enumerated() for index+1 numbering"
      pattern: "selectedSegmentIds\\.enumerated\\(\\)"
    - from: "orderBadgesOverlay"
      to: "segmentInfos"
      via: "midX/midY for positioning"
      pattern: "info\\.midX.*info\\.midY"
---

<objective>
Nummerierte Badges für Reinigungsreihenfolge auf der Karte

Purpose: Beim Auswählen von Räumen erscheinen blaue Badges mit Zahlen (1, 2, 3...) über dem Raum-Mittelpunkt — die Auswahl-Reihenfolge definiert die Reinigungsreihenfolge und ist visuell erkennbar.

Output: `orderBadgesOverlay` in InteractiveMapView zeigt nummerierte Badges für alle ausgewählten Räume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cleaning-order/21-CONTEXT.md
@.planning/phases/21-cleaning-order/21-RESEARCH.md
@.planning/phases/21-cleaning-order/21-01-SUMMARY.md

<interfaces>
<!-- Nach Plan 01 sind diese Typen [String] Arrays -->

From ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift:
```swift
@Binding var selectedSegmentIds: [String]  // Nach Plan 01

var editMode: MapEditMode = .none
var showRoomLabels: Bool = true

// Bestehender Overlay-Pattern (Zeilen 128-133):
.overlay {
    if showRoomLabels {
        tapTargetsOverlay
    }
}

// segmentInfos liefert Mittelpunkte (Zeilen 228-268):
private func segmentInfos(from layers: [MapLayer]) -> [SegmentInfo]

// calculateMapParams (Zeile 271):
private func calculateMapParams(layers: [MapLayer], pixelSize: Int, size: CGSize) -> MapParams?

// SegmentInfo Struct (Zeilen 221-226):
private struct SegmentInfo: Identifiable {
    let id: String
    let name: String
    let midX: Int
    let midY: Int
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: orderBadgesOverlay in InteractiveMapView hinzufügen</name>
  <files>ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift</files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  </read_first>
  <action>
1. **Neues `.overlay { }` nach dem bestehenden Label-Overlay hinzufügen** (nach Zeile ~133, nach dem `.overlay { if showRoomLabels { tapTargetsOverlay } }`):

```swift
.overlay {
    orderBadgesOverlay
}
```

2. **Neuen `@ViewBuilder` Property `orderBadgesOverlay` erstellen** (nach `tapTargetsOverlay`, vor `toggleSegment`):

```swift
@ViewBuilder
private var orderBadgesOverlay: some View {
    // Badges nur im normalen Modus zeigen, NICHT im roomEdit/splitRoom
    if editMode == .none, !selectedSegmentIds.isEmpty {
        GeometryReader { geometry in
            let params = calculateMapParams(
                layers: map.layers ?? [],
                pixelSize: map.pixelSize ?? 5,
                size: geometry.size
            )

            if let p = params, let layers = map.layers {
                ForEach(Array(selectedSegmentIds.enumerated()), id: \.element) { index, segmentId in
                    if let info = segmentInfos(from: layers).first(where: { $0.id == segmentId }) {
                        let x = CGFloat(info.midX) * p.scale + p.offsetX
                        let y = CGFloat(info.midY) * p.scale + p.offsetY

                        ZStack {
                            Circle()
                                .fill(Color.blue)
                                .frame(width: 24, height: 24)
                            Text("\(index + 1)")
                                .font(.system(size: 14, weight: .bold))
                                .foregroundStyle(.white)
                        }
                        .position(x: x, y: y - 20)
                    }
                }
            }
        }
    }
}
```

Wichtige Details:
- Badge-Groesse: 24pt Kreis, 14pt bold weisse Zahl (per CONTEXT.md Entscheidung)
- Position: `y - 20` ueber dem Raum-Mittelpunkt, damit Badge nicht mit Label ueberlappt
- `editMode == .none` Guard: Badges nur im normalen Modus, nicht bei roomEdit oder splitRoom (per RESEARCH.md Empfehlung)
- Unabhaengig von `showRoomLabels` — eigenes `.overlay { }`, nicht innerhalb der `if showRoomLabels`-Bedingung
- `enumerated()` liefert 0-basiertem Index, `index + 1` zeigt 1-basierte Nummer
- Bei Abwahl wird Element aus Array entfernt, `enumerated()` nummeriert automatisch lueckenlos neu

3. **Build pruefen**
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep "orderBadgesOverlay" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet mindestens 2 Treffer (Definition + Aufruf)
- `grep "selectedSegmentIds.enumerated()" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet 1 Treffer
- `grep "editMode == .none" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet mindestens 1 Treffer
- `grep "frame(width: 24, height: 24)" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet 1 Treffer
- `grep "font(.system(size: 14, weight: .bold))" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` findet 1 Treffer
- xcodebuild endet mit "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>orderBadgesOverlay zeigt nummerierte blaue Badges ueber ausgewaehlten Raeumen, Badges erscheinen nur im normalen Modus, unabhaengig von showRoomLabels, Build erfolgreich</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visuelle Pruefung der Reinigungsreihenfolge</name>
  <files>ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift</files>
  <action>
Keine Code-Aenderungen — reine visuelle Verifikation durch den User. Die App wurde in Task 1 gebaut und ist bereit zum Testen im Simulator.
  </action>
  <verify>User bestaetigt visuell im Simulator</verify>
  <done>User hat die Badge-Visualisierung geprueft und approved</done>
  <what-built>
Nummerierte blaue Badges auf der Karte die die Reinigungsreihenfolge der ausgewaehlten Raeume anzeigen. Die Auswahl-Reihenfolge wird beim Start der Reinigung an die API uebergeben.
  </what-built>
  <how-to-verify>
1. App im Simulator starten (iPhone 16)
2. Einen Roboter auswaehlen der Raeume auf der Karte hat
3. Ersten Raum antippen (auf Flaeche oder Label) — es erscheint Badge "1" als blauer Kreis mit weisser Zahl ueber dem Raum
4. Zweiten Raum antippen — Badge "2" erscheint, Badge "1" bleibt
5. Dritten Raum antippen — Badge "3" erscheint
6. Den zweiten Raum wieder antippen (abwaehlen) — Badge "2" verschwindet, der dritte Raum wird zu "2" (lueckenlos)
7. Room-Labels ausschalten (falls Toggle vorhanden) — Badges bleiben sichtbar
8. In den roomEdit-Modus wechseln — Badges verschwinden
9. Zurueck zum normalen Modus — Badges erscheinen wieder
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe Probleme (z.B. Badge-Position, Groesse, Verhalten)</resume-signal>
</task>

</tasks>

<verification>
1. `orderBadgesOverlay` existiert als ViewBuilder in InteractiveMapView
2. Badges zeigen Zahlen 1, 2, 3 in Auswahl-Reihenfolge
3. Abwaehlen nummeriert lueckenlos um
4. Badges unabhaengig von showRoomLabels
5. Keine Badges im roomEdit/splitRoom-Modus
6. Build erfolgreich
</verification>

<success_criteria>
- Blaue Badges mit weissen Zahlen erscheinen ueber ausgewaehlten Raeumen
- Zahlen entsprechen der Auswahl-Reihenfolge (1, 2, 3...)
- Abwaehlen bewirkt lueckenlose Umnummerierung
- Badges sind immer sichtbar (unabhaengig von showRoomLabels)
- Keine Badges im Edit-Modus
- User hat visuell bestaetigt
</success_criteria>

<output>
After completion, create `.planning/phases/21-cleaning-order/21-02-SUMMARY.md`
</output>
