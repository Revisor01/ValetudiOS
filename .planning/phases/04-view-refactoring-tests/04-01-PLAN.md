---
phase: 04-view-refactoring-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp.xcodeproj/project.pbxproj
  - ValetudoApp/ValetudoAppTests/TimerTests.swift
  - ValetudoApp/ValetudoAppTests/ConsumableTests.swift
  - ValetudoApp/ValetudoAppTests/MapLayerTests.swift
  - ValetudoApp/ValetudoAppTests/KeychainStoreTests.swift
autonomous: true
requirements: [DEBT-04]

must_haves:
  truths:
    - "XCTest target exists and compiles"
    - "Timer UTC/local conversion tests pass for positive and negative UTC offsets"
    - "Consumable remainingPercent tests pass for percent-unit and minutes-unit types"
    - "MapLayer RLE decompression tests pass for empty, single-run, and multi-run inputs"
    - "KeychainStore round-trip (save + read + delete) test passes"
  artifacts:
    - path: "ValetudoApp/ValetudoAppTests/TimerTests.swift"
      provides: "Timer conversion unit tests"
      contains: "XCTestCase"
    - path: "ValetudoApp/ValetudoAppTests/ConsumableTests.swift"
      provides: "Consumable percent calculation tests"
      contains: "XCTestCase"
    - path: "ValetudoApp/ValetudoAppTests/MapLayerTests.swift"
      provides: "Map RLE decompression tests"
      contains: "XCTestCase"
    - path: "ValetudoApp/ValetudoAppTests/KeychainStoreTests.swift"
      provides: "Keychain round-trip test"
      contains: "XCTestCase"
  key_links:
    - from: "ValetudoApp/ValetudoAppTests/TimerTests.swift"
      to: "ValetudoApp/ValetudoApp/Models/Timer.swift"
      via: "@testable import ValetudoApp"
      pattern: "@testable import ValetudoApp"
    - from: "ValetudoApp/ValetudoAppTests/MapLayerTests.swift"
      to: "ValetudoApp/ValetudoApp/Models/RobotMap.swift"
      via: "@testable import ValetudoApp"
      pattern: "computeDecompressedPixels\\|decompressedPixels"
---

<objective>
Create the XCTest target ValetudoAppTests and write unit tests for Timer UTC/local conversion, Consumable remaining-percent calculation, MapLayer RLE decompression, and KeychainStore round-trip.

Purpose: Establish test infrastructure (DEBT-04) and validate critical pure-logic functions that have zero test coverage.
Output: 4 test files in a new XCTest target, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ValetudoApp/ValetudoApp/Models/Timer.swift
@ValetudoApp/ValetudoApp/Models/Consumable.swift
@ValetudoApp/ValetudoApp/Models/RobotMap.swift
@ValetudoApp/ValetudoApp/Services/KeychainStore.swift

<interfaces>
<!-- Key types the test target needs access to via @testable import -->

From Models/Timer.swift:
```swift
struct ValetudoTimer: Codable, Identifiable {
    static func utcToLocal(hour: Int, minute: Int) -> (hour: Int, minute: Int)
    static func localToUTC(hour: Int, minute: Int) -> (hour: Int, minute: Int)
}
```

From Models/Consumable.swift:
```swift
struct Consumable: Codable, Identifiable {
    var remainingPercent: Double  // computed from remaining.value + remaining.unit
}
struct ConsumableRemaining: Codable {
    let value: Int
    let unit: String  // "percent" or "minutes"
}
```

From Models/RobotMap.swift:
```swift
struct MapLayer: Codable {
    let pixels: [Int]?
    let compressedPixels: [Int]?
    var decompressedPixels: [Int]  // computed, uses RLE: [x, y, count, x, y, count, ...]
    fileprivate func computeDecompressedPixels() -> [Int]
}
```
Note: computeDecompressedPixels is fileprivate. Tests must use the public `decompressedPixels` computed property or construct MapLayer with compressedPixels and access via cache.

From Services/KeychainStore.swift:
```swift
struct KeychainStore {
    static func password(for robotId: UUID) -> String?
    static func save(password: String, for robotId: UUID) -> Bool
    static func delete(for robotId: UUID)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XCTest target and Timer + Consumable tests</name>
  <files>
    ValetudoApp/ValetudoApp.xcodeproj/project.pbxproj,
    ValetudoApp/ValetudoAppTests/TimerTests.swift,
    ValetudoApp/ValetudoAppTests/ConsumableTests.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp.xcodeproj/project.pbxproj,
    ValetudoApp/ValetudoApp/Models/Timer.swift,
    ValetudoApp/ValetudoApp/Models/Consumable.swift
  </read_first>
  <action>
1. Create ValetudoAppTests directory at ValetudoApp/ValetudoAppTests/
2. Add a new XCTest unit test target "ValetudoAppTests" to the Xcode project. The target must:
   - Have bundle identifier com.valetudio.ValetudoAppTests
   - Link against ValetudoApp as host application
   - Include TEST_HOST and BUNDLE_LOADER build settings pointing to ValetudoApp.app
   - Be added to the existing ValetudoApp scheme (create scheme if needed) with testable reference
   - Use PRODUCT_MODULE_NAME = ValetudoAppTests
3. Create TimerTests.swift with `@testable import ValetudoApp`:
   - testUtcToLocalPositiveOffset: For UTC+2, input (10, 30) should produce (12, 30)
   - testUtcToLocalNegativeOffset: For UTC-5, input (3, 0) should produce (22, 0) previous day
   - testLocalToUtcRoundTrip: utcToLocal then localToUTC should return original values
   - testUtcToLocalMidnightWrap: input (23, 30) with UTC+2 should wrap to (1, 30)
   - Note: TimeZone.current is used internally. Tests must work regardless of runner timezone. Test the roundtrip property (localToUTC(utcToLocal(h, m)) == (h, m)) which holds for any timezone.
4. Create ConsumableTests.swift with `@testable import ValetudoApp`:
   - testRemainingPercentWithPercentUnit: remaining(value: 75, unit: "percent") -> 75.0
   - testRemainingPercentWithMinutesMainBrush: remaining(value: 9000, unit: "minutes") for type "brush", subType "main" (maxMinutes=18000) -> 50.0
   - testRemainingPercentWithMinutesSideBrush: remaining(value: 6000, unit: "minutes") for type "brush", subType "side_right" (maxMinutes=12000) -> 50.0
   - testRemainingPercentCapsAt100: remaining(value: 20000, unit: "minutes") for type "brush", subType "main" -> 100.0 (capped)
   - testRemainingPercentFilter: remaining(value: 4500, unit: "minutes") for type "filter" (maxMinutes=9000) -> 50.0
   - testIconColor: percent > 50 -> green, 20..50 -> orange, < 20 -> red

Important: Annotate individual test methods with @MainActor only if they access MainActor-isolated types. Timer and Consumable are plain structs, so no @MainActor needed.
  </action>
  <verify>
    <automated>xcodebuild test -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ValetudoAppTests/TimerTests -only-testing:ValetudoAppTests/ConsumableTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "XCTestCase" ValetudoApp/ValetudoAppTests/TimerTests.swift
    - grep -l "XCTestCase" ValetudoApp/ValetudoAppTests/ConsumableTests.swift
    - grep "testUtcToLocalPositiveOffset\|testLocalToUtcRoundTrip" ValetudoApp/ValetudoAppTests/TimerTests.swift
    - grep "testRemainingPercentWithPercentUnit\|testRemainingPercentCapsAt100" ValetudoApp/ValetudoAppTests/ConsumableTests.swift
    - grep "ValetudoAppTests" ValetudoApp/ValetudoApp.xcodeproj/project.pbxproj
  </acceptance_criteria>
  <done>XCTest target "ValetudoAppTests" compiles and Timer + Consumable tests all pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MapLayer RLE decompression + KeychainStore round-trip tests</name>
  <files>
    ValetudoApp/ValetudoAppTests/MapLayerTests.swift,
    ValetudoApp/ValetudoAppTests/KeychainStoreTests.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Models/RobotMap.swift,
    ValetudoApp/ValetudoApp/Services/KeychainStore.swift
  </read_first>
  <behavior>
    MapLayer decompression:
    - Empty compressedPixels [] -> decompressedPixels == []
    - Single run [5, 10, 3] -> [5, 10, 6, 10, 7, 10] (3 pixels starting at x=5, y=10)
    - Multi run [0, 0, 2, 10, 5, 1] -> [0, 0, 1, 0, 10, 5]
    - Plain pixels (not compressed): pixels [1, 2, 3, 4] -> decompressedPixels == [1, 2, 3, 4]

    KeychainStore round-trip:
    - Save password for UUID, read it back, values match
    - Delete password, read returns nil
    - Overwrite existing password, read returns new value
  </behavior>
  <action>
1. Create MapLayerTests.swift with `@testable import ValetudoApp`:
   - testDecompressEmptyCompressedPixels: MapLayer with compressedPixels: [] and pixels: nil -> decompressedPixels is []
   - testDecompressSingleRun: compressedPixels: [5, 10, 3] -> [5, 10, 6, 10, 7, 10]
   - testDecompressMultipleRuns: compressedPixels: [0, 0, 2, 10, 5, 1] -> [0, 0, 1, 0, 10, 5]
   - testPlainPixelsUsedWhenAvailable: pixels: [1, 2, 3, 4], compressedPixels: [99, 99, 1] -> decompressedPixels == [1, 2, 3, 4]
   - Construct MapLayer using JSONDecoder from JSON string (since init is auto-generated Codable). Example: `{"compressedPixels": [5, 10, 3]}`.

2. Create KeychainStoreTests.swift with `@testable import ValetudoApp`:
   - testSaveAndRetrieve: save "testPass123" for a test UUID, password(for:) returns "testPass123"
   - testDeleteRemovesPassword: save then delete, password(for:) returns nil
   - testOverwritePassword: save "old", save "new", password(for:) returns "new"
   - Each test uses a unique UUID to avoid Keychain collisions
   - tearDown: delete all test passwords to clean Keychain state

Note: KeychainStore tests run in the Simulator which has a functional Keychain. No mocking needed.
  </action>
  <verify>
    <automated>xcodebuild test -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ValetudoAppTests/MapLayerTests -only-testing:ValetudoAppTests/KeychainStoreTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "XCTestCase" ValetudoApp/ValetudoAppTests/MapLayerTests.swift
    - grep -l "XCTestCase" ValetudoApp/ValetudoAppTests/KeychainStoreTests.swift
    - grep "testDecompressSingleRun\|testDecompressMultipleRuns" ValetudoApp/ValetudoAppTests/MapLayerTests.swift
    - grep "testSaveAndRetrieve\|testDeleteRemovesPassword" ValetudoApp/ValetudoAppTests/KeychainStoreTests.swift
  </acceptance_criteria>
  <done>MapLayer decompression tests and KeychainStore round-trip tests all pass green. Full test suite: xcodebuild test runs 10+ tests with 0 failures.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
xcodebuild test -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "Test Suite|Executed|FAIL"
```
Expected: "Executed N tests, with 0 failures"
</verification>

<success_criteria>
- XCTest target "ValetudoAppTests" exists in project.pbxproj
- 4 test files exist in ValetudoApp/ValetudoAppTests/
- All tests pass: Timer conversion, Consumable percent, MapLayer RLE, Keychain round-trip
- At least 10 individual test methods across all files
</success_criteria>

<output>
After completion, create `.planning/phases/04-view-refactoring-tests/04-01-SUMMARY.md`
</output>
