---
phase: 07-bugfixes-robustness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/Services/RobotManager.swift
autonomous: true
requirements:
  - FIX-02

must_haves:
  truths:
    - "cleanSelectedRooms()-Fehler werden geloggt und der Nutzer sieht eine ErrorRouter-Alert"
    - "getCapabilities()-Fehler in MapViewModel werden mit logger.warning geloggt statt lautlos ignoriert"
    - "checkUpdaterState()-Fehler in RobotManager werden mit logger.warning geloggt statt lautlos ignoriert"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "Alle silent catch-Blöcke ersetzt durch logger.error/warning oder ErrorRouter"
    - path: "ValetudoApp/ValetudoApp/Services/RobotManager.swift"
      provides: "Silent catch in checkUpdaterState() ersetzt durch logger.warning"
  key_links:
    - from: "MapViewModel.cleanSelectedRooms()"
      to: "ErrorRouter.shared.show()"
      via: "catch block mit errorMessage @Published"
      pattern: "errorMessage ="
    - from: "MapViewModel.loadMap() capability check"
      to: "logger.warning"
      via: "catch block"
      pattern: "logger.warning.*capability"
---

<objective>
Stille Fehler in MapViewModel und RobotManager durch Logger-Warnungen und (wo user-facing) ErrorRouter-Alerts ersetzen.

Purpose: Nutzer und Entwickler erhalten Feedback wenn kritische Aktionen fehlschlagen. Keine Operation soll mehr lautlos verschwinden.
Output: Drei `catch { }` Blöcke ersetzt; bei cleanSelectedRooms() zusätzlich eine `@Published var errorMessage` für Alert-Anzeige.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<interfaces>
<!-- Bestehende Muster im Projekt -->

Aus STATE.md (Phase 02 Decision):
- ErrorRouter not wired to SSE failures — os.Logger warning + polling fallback
- Für user-facing Fehler: ErrorRouter.shared.show(error) oder @Published errorMessage

MapViewModel.swift — relevante silent catch-Stellen:
- Zeile 105-107: `catch { // Silently ignore capability check failures }`
- Zeile 171-173: `catch { // Silently ignore clean failures }`

RobotManager.swift:
- Zeile 196-198: `catch { // Silently ignore - not all robots support this }`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MapViewModel silent catches durch logging ersetzen</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift</files>
  <action>
Drei Änderungen in MapViewModel.swift:

**1. @Published errorMessage hinzufügen** (im @Published Properties-Block, nach anderen @Published vars):
```swift
@Published var errorMessage: String? = nil
```

**2. Capability-Check Fehler (Zeile ~105-107) loggen:**
Ersetze:
```swift
} catch {
    // Silently ignore capability check failures
}
```
mit:
```swift
} catch {
    logger.warning("loadMap: Capability check failed: \(error.localizedDescription, privacy: .public)")
}
```

**3. cleanSelectedRooms() Fehler (Zeile ~171-173) user-facing machen:**
Ersetze:
```swift
} catch {
    // Silently ignore clean failures
}
```
mit:
```swift
} catch {
    logger.error("cleanSelectedRooms FAILED: \(error.localizedDescription, privacy: .public)")
    errorMessage = error.localizedDescription
}
```

Kein ErrorRouter.shared.show() — MapViewModel nutzt @Published errorMessage als etabliertes ViewModel-Muster. Die View zeigt diesen Fehler mit `.alert("Error", isPresented: Binding(...), ...) { }` oder äquivalent (falls noch kein Alert existiert, reicht das Setzen von errorMessage — Phase 8 testet dies).
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -configuration Debug 2>&1 | grep -E "^.*error:" | head -20</automated>
  </verify>
  <done>MapViewModel.swift hat kein `// Silently ignore` mehr; errorMessage @Published existiert; Build grün</done>
</task>

<task type="auto">
  <name>Task 2: RobotManager checkUpdaterState silent catch loggen</name>
  <files>ValetudoApp/ValetudoApp/Services/RobotManager.swift</files>
  <action>
Zeile ~196-198: Ersetze:
```swift
} catch {
    // Silently ignore - not all robots support this
}
```
mit:
```swift
} catch {
    logger.warning("checkUpdaterState: Not supported or failed for robot \(id, privacy: .public): \(error.localizedDescription, privacy: .public)")
}
```

Der Kommentar-Kontext (nicht alle Robots unterstützen den Updater) bleibt als Logger-Text erhalten. Kein ErrorRouter — dieser Fehler ist für den Nutzer nicht actionable, nur für Debugging.
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -configuration Debug 2>&1 | grep -E "^.*error:" | head -20</automated>
  </verify>
  <done>RobotManager.swift hat kein `// Silently ignore` mehr im checkUpdaterState-Block; Build grün</done>
</task>

</tasks>

<verification>
```bash
xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -configuration Debug 2>&1 | tail -5
grep -n 'Silently ignore\|silently ignore' ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift ValetudoApp/ValetudoApp/Services/RobotManager.swift
```
Zweiter Befehl muss leer zurückkehren.
</verification>

<success_criteria>
- Keine `// Silently ignore`-Kommentare mehr in MapViewModel.swift oder RobotManager.swift
- Alle drei betroffenen catch-Blöcke enthalten logger-Aufrufe
- cleanSelectedRooms() setzt errorMessage bei Fehler
- Build kompiliert ohne neue Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/07-bugfixes-robustness/07-02-SUMMARY.md` erstellen.
</output>
