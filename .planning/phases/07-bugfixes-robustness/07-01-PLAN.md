---
phase: 07-bugfixes-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/NetworkScanner.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "NetworkScanner.checkHost() konstruiert keine URL mehr mit ! — ungültige Hosts erzeugen einen Logger-Fehler statt Crash"
    - "RobotDetailView.updateUrl-Link konstruiert keine URL mehr mit ! — fehlende/ungültige URL blendet den Link-Button aus"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/NetworkScanner.swift"
      provides: "Force-unwrap auf Zeile 154 ersetzt durch guard let"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "Force-unwrap auf URL(string: updateUrl)! ersetzt durch guard let / if let"
  key_links:
    - from: "NetworkScanner.checkHost()"
      to: "URLRequest"
      via: "guard let url = URL(string:) else { logger.error; return nil }"
      pattern: "guard let url = URL"
    - from: "RobotDetailView update link"
      to: "Link(destination:)"
      via: "if let url = URL(string: updateUrl)"
      pattern: "if let url = URL"
---

<objective>
Force-unwrap-URLs in NetworkScanner und RobotDetailView durch sichere optionale Bindung ersetzen.

Purpose: Crashes bei ungültigen Host-Strings oder fehlerhaften Update-URLs verhindern. Ungültige Eingaben werden mit Logger-Fehler still abgebrochen statt die App zu crashen.
Output: Zwei Dateien ohne `!` auf URL-Konstruktion; Logger-Einträge als Fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkScanner.checkHost() Force-unwrap entfernen</name>
  <files>ValetudoApp/ValetudoApp/Services/NetworkScanner.swift</files>
  <action>
Zeile 154: Ersetze `URL(string: "http://\(host)/api/v2/robot")!` durch:

```swift
guard let url = URL(string: "http://\(host)/api/v2/robot") else {
    logger.error("checkHost: Invalid URL for host \(host, privacy: .public)")
    return nil
}
```

Das `var request = URLRequest(url: url)` danach bleibt unverändert. Die Logger-Instanz ist bereits in NetworkScanner vorhanden (Zeile ~6).
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -configuration Debug 2>&1 | grep -E "error:|warning:.*NetworkScanner" | head -20</automated>
  </verify>
  <done>NetworkScanner.swift kompiliert ohne Fehler; kein `URL(string:)!` mehr in checkHost()</done>
</task>

<task type="auto">
  <name>Task 2: RobotDetailView updateUrl Force-unwrap entfernen</name>
  <files>ValetudoApp/ValetudoApp/Views/RobotDetailView.swift</files>
  <action>
Zeile ~106: Ersetze `Link(destination: URL(string: updateUrl)!)` durch eine sichere Bindung:

```swift
if let url = URL(string: updateUrl) {
    Link(destination: url) {
        HStack {
            // ... bestehender HStack-Inhalt unverändert ...
        }
    }
}
```

Der gesamte `Link { HStack { ... } }` Block wird in `if let url = URL(string: updateUrl) { Link(destination: url) { ... } }` eingewickelt. Falls `updateUrl` keine gültige URL ergibt, verschwindet der Link-Button still (kein crash, kein Logger-Eintrag nötig — UI-only).
  </action>
  <verify>
    <automated>xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -configuration Debug 2>&1 | grep -E "error:|warning:.*RobotDetailView" | head -20</automated>
  </verify>
  <done>RobotDetailView.swift kompiliert ohne Fehler; kein `URL(string:)!` mehr in der Datei</done>
</task>

</tasks>

<verification>
```bash
xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -configuration Debug 2>&1 | tail -5
grep -n 'URL(string:.*!)' ValetudoApp/ValetudoApp/Services/NetworkScanner.swift ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
```
Zweiter Befehl muss leer zurückkehren (keine Treffer).
</verification>

<success_criteria>
- Keine Force-unwrap URL-Konstruktionen mehr in NetworkScanner.swift oder RobotDetailView.swift
- Build kompiliert ohne neue Fehler
- Ungültige Hosts in checkHost() erzeugen einen Logger-Fehler und geben nil zurück
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/07-bugfixes-robustness/07-01-SUMMARY.md` erstellen.
</output>
