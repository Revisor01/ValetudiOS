---
phase: 27-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
  - ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift
  - ValetudoApp/ValetudoApp/Views/ConsumablesView.swift
  - ValetudoApp/ValetudoApp/Views/Detail/ConsumablesPreviewSectionView.swift
  - ValetudoApp/ValetudoApp/Views/TimersView.swift
  - ValetudoApp/ValetudoApp/Views/RobotListView.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements: [A11Y-01, A11Y-02, A11Y-03, A11Y-04]

must_haves:
  truths:
    - "VoiceOver liest ControlButtons als 'Pause, Taste' / 'Start, Taste' etc. mit lokalisiertem title"
    - "VoiceOver liest Battery-Pill als 'Batterie, 85%' und Status-Text als 'Roboterstatus, Reinigung'"
    - "VoiceOver liest Consumable-Fortschrittsbalken als 'Hauptbuerste, 73%'"
    - "VoiceOver liest Locate-Button als 'Roboter lokalisieren, Taste'"
    - "VoiceOver liest Timer-Plus-Button als 'Timer hinzufuegen, Taste'"
    - "VoiceOver liest Roboter-Plus-Button als 'Roboter hinzufuegen, Taste'"
    - "VoiceOver liest Consumable-Reset-Buttons als 'Hauptbuerste zuruecksetzen, Taste'"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift"
      provides: "accessibilityLabel auf ControlButton und DockActionButton"
      contains: ".accessibilityLabel(title)"
    - path: "ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift"
      provides: "accessibilityLabel + accessibilityValue fuer Status und Battery"
      contains: ".accessibilityValue"
    - path: "ValetudoApp/ValetudoApp/Views/ConsumablesView.swift"
      provides: "accessibilityValue auf ProgressBar, accessibilityLabel auf Reset-Button"
      contains: ".accessibilityLabel"
    - path: "ValetudoApp/ValetudoApp/Views/Detail/ConsumablesPreviewSectionView.swift"
      provides: "accessibilityValue auf ProgressBar, accessibilityLabel auf Reset-Button"
      contains: ".accessibilityLabel"
    - path: "ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings"
      provides: "Neue Lokalisierungsschluessel fuer Accessibility"
      contains: "robot.status"
  key_links:
    - from: "RobotDetailSections.swift"
      to: "Localizable.xcstrings"
      via: "title parameter already localized via String(localized:)"
      pattern: "accessibilityLabel\\(title\\)"
    - from: "RobotStatusHeaderView.swift"
      to: "Localizable.xcstrings"
      via: "String(localized:) fuer neue Keys"
      pattern: "String\\(localized:"
---

<objective>
Accessibility-Labels fuer alle Nicht-Map-Views: ControlButtons, DockActionButtons, Status-Header, Consumable-Fortschrittsbalken und Icon-only-Buttons in Listen-Views.

Purpose: VoiceOver-Nutzer koennen alle interaktiven Elemente ausserhalb der Map bedienen und verstehen.
Output: 6 Swift-Dateien mit Accessibility-Modifiern, Localizable.xcstrings mit neuen Keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-accessibility/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lokalisierungsschluessel anlegen + ControlButton/DockActionButton/Header Labels</name>
  <files>
    ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
    ValetudoApp/ValetudoApp/Views/Detail/RobotStatusHeaderView.swift
    ValetudoApp/ValetudoApp/Views/TimersView.swift
    ValetudoApp/ValetudoApp/Views/RobotListView.swift
  </files>
  <action>
    **Schritt 1: Localizable.xcstrings erweitern**
    Folgende Keys in die xcstrings-Datei eintragen (mit deutschem und englischem Wert):
    - `robot.status` — "Roboterstatus" / "Robot status"
    - `robot.battery` — "Batterie" / "Battery"
    - `battery.charging` — "wird geladen" / "charging"
    - `action.locate` — "Roboter lokalisieren" / "Locate robot"
    - `consumables.reset_button %@` — "%@ zuruecksetzen" / "Reset %@" (mit String-Interpolation)
    - `timers.add` — "Timer hinzufuegen" / "Add timer"
    - `robots.add_robot` — "Roboter hinzufuegen" / "Add robot"

    **Schritt 2: ControlButton (RobotDetailSections.swift)**
    Im `ControlButton` struct: `.accessibilityLabel(title)` auf den Button setzen (NICHT auf Image).
    Der `title` ist bereits lokalisiert via `String(localized:)` an der Aufrufstelle.

    **Schritt 3: DockActionButton (RobotDetailSections.swift)**
    Identisch: `.accessibilityLabel(title)` auf den Button.

    **Schritt 4: RobotStatusHeaderView.swift**
    - Status-Text: `.accessibilityLabel(String(localized: "robot.status"))` und `.accessibilityValue(localizedStatus(statusValue))` auf den Status-Text
    - Battery-Pill HStack: `.accessibilityElement(children: .combine)` plus `.accessibilityLabel(String(localized: "robot.battery"))` plus `.accessibilityValue("\(battery)%\(isCharging ? ", \(String(localized: "battery.charging"))" : "")")`
    - Locate-Button (Image "waveform"): `.accessibilityLabel(String(localized: "action.locate"))` auf den Button (NICHT auf Image)

    **Schritt 5: TimersView.swift**
    Plus-Button in Toolbar: `.accessibilityLabel(String(localized: "timers.add"))` auf den Button.

    **Schritt 6: RobotListView.swift**
    Plus-Button in Toolbar: `.accessibilityLabel(String(localized: "robots.add_robot"))` auf den Button.

    **WICHTIG:** accessibilityLabel immer auf den Button setzen, nicht auf die Image innerhalb des Labels. Sonst liest VoiceOver "Bild" statt "Taste".
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ControlButton und DockActionButton haben .accessibilityLabel(title)
    - RobotStatusHeaderView: Status-Text hat accessibilityLabel+Value, Battery-Pill hat combined element mit Label+Value, Locate-Button hat Label
    - TimersView Plus-Button hat accessibilityLabel
    - RobotListView Plus-Button hat accessibilityLabel
    - Alle neuen Lokalisierungsschluessel in xcstrings vorhanden
    - Build kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: Consumable-Views — ProgressBar accessibilityValue + Reset-Button Labels</name>
  <files>
    ValetudoApp/ValetudoApp/Views/ConsumablesView.swift
    ValetudoApp/ValetudoApp/Views/Detail/ConsumablesPreviewSectionView.swift
  </files>
  <action>
    **ConsumablesView.swift — ConsumableRow:**
    1. GeometryReader/ZStack (Custom-ProgressBar): `.accessibilityLabel(consumable.displayName)` und `.accessibilityValue(String(format: "%d%%", Int(consumable.remainingPercent)))` auf das aeussere Element (ZStack oder GeometryReader, je nachdem was den Frame hat)
    2. Reset-Button (arrow.counterclockwise.circle.fill): `.accessibilityLabel(String(localized: "consumables.reset_button \(consumable.displayName)"))` auf den Button

    **ConsumablesPreviewSectionView.swift:**
    1. GeometryReader/ZStack (ProgressBar): identisch — `.accessibilityLabel` mit displayName und `.accessibilityValue` mit Prozentwert
    2. Reset-Button (arrow.counterclockwise): `.accessibilityLabel(String(localized: "consumables.reset_button \(consumable.displayName)"))` auf den Button

    **Hinweis:** Der Lokalisierungsschluessel `consumables.reset_button %@` wurde bereits in Task 1 angelegt.

    **WICHTIG:** accessibilityLabel auf Button-Ebene, nicht auf Image. Und accessibilityValue als separater Modifier neben accessibilityLabel (nicht stattdessen).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ConsumableRow ProgressBar hat accessibilityLabel mit Name und accessibilityValue mit Prozent
    - ConsumablesPreviewSectionView ProgressBar hat identische Accessibility-Modifier
    - Beide Reset-Buttons haben beschreibende accessibilityLabels
    - Build kompiliert fehlerfrei
  </done>
</task>

</tasks>

<verification>
- `xcodebuild build` kompiliert fehlerfrei
- Grep nach `.accessibilityLabel` in allen 6 geaenderten Swift-Dateien findet mindestens einen Treffer pro Datei
- Grep nach `.accessibilityValue` in RobotStatusHeaderView, ConsumablesView, ConsumablesPreviewSectionView findet Treffer
</verification>

<success_criteria>
- A11Y-01: ControlButton und DockActionButton haben .accessibilityLabel(title)
- A11Y-02: Status-Text und Battery-Pill haben accessibilityLabel + accessibilityValue
- A11Y-03: Beide Consumable-ProgressBars haben accessibilityValue mit Prozent
- A11Y-04 (Teilmenge): Locate-Button, Timer-Plus, Roboter-Plus, Consumable-Reset-Buttons haben Labels
</success_criteria>

<output>
After completion, create `.planning/phases/27-accessibility/27-01-SUMMARY.md`
</output>
