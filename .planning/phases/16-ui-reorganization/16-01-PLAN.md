---
phase: 16-ui-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  - ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - REORG-01
  - REORG-02

must_haves:
  truths:
    - "Firmware-Version, Commit, Hostname, Uptime, CPU-Bar und Memory-Bar erscheinen in einer DisclosureGroup im RobotDetailView"
    - "Robot Properties (Model, Seriennummer, Hersteller) erscheinen in derselben DisclosureGroup"
    - "ValetudoInfoView ist nicht mehr ueber RobotSettingsView erreichbar"
    - "Die DisclosureGroup ist standardmaessig zugeklappt"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift"
      provides: "DeviceInfoSection struct"
      contains: "struct DeviceInfoSection"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "valetudoVersion and systemHostInfo properties + loadDeviceInfo method"
      contains: "valetudoVersion"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "DeviceInfoSection(viewModel:) call replacing robotPropertiesSection"
      contains: "DeviceInfoSection"
  key_links:
    - from: "RobotDetailViewModel.loadData()"
      to: "loadDeviceInfo()"
      via: "async let in loadData"
      pattern: "async let deviceInfoTask.*loadDeviceInfo"
    - from: "RobotDetailView body"
      to: "DeviceInfoSection"
      via: "DeviceInfoSection(viewModel: viewModel)"
      pattern: "DeviceInfoSection\\(viewModel:"
    - from: "DeviceInfoSection"
      to: "RobotDetailViewModel.valetudoVersion"
      via: "@ObservedObject var viewModel"
      pattern: "viewModel\\.valetudoVersion"
---

<objective>
Geraete-Informationen (Firmware, Host-Info, Memory, Uptime, Robot Properties) zu einer einheitlichen DisclosureGroup im Roboter-Detail zusammenfuehren. ValetudoInfoView aus den Einstellungen entfernen.

Purpose: Alle Geraetedaten an einem logischen Ort statt verteilt auf Settings und Detail
Output: DeviceInfoSection in RobotDetailSections.swift, erweiterte ViewModel-Properties, bereinigte Settings-Navigation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-reorganization/16-CONTEXT.md
@.planning/phases/16-ui-reorganization/16-RESEARCH.md
@.planning/phases/16-ui-reorganization/16-UI-SPEC.md

@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
@ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
@ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
@ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
@ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
@ValetudoApp/ValetudoApp/Models/RobotState.swift

<interfaces>
<!-- Key types the executor needs -->

From ValetudoApp/ValetudoApp/Models/RobotState.swift:
```swift
struct ValetudoVersion: Codable {
    let release: String
    let commit: String
}

struct SystemHostInfo: Codable {
    let hostname: String
    let arch: String
    let mem: MemInfo
    let uptime: Double
    let load: LoadInfo?

    struct MemInfo: Codable {
        let total: Int
        let free: Int
        let valetudo_current: Int
        let valetudo_max: Int
    }

    struct LoadInfo: Codable {
        let _1: Double
        let _5: Double
        let _15: Double
    }
}
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift:
```swift
@Published var robotProperties: RobotProperties?

private func loadRobotProperties() async { ... }

func loadData() async {
    // ... 10 async let tasks ...
    async let propertiesTask: () = loadRobotProperties()
    _ = await (segmentsTask, consumablesTask, capabilitiesTask, fanSpeedTask, updateTask, statsTask, eventsTask, cleanRouteTask, obstaclesTask, propertiesTask)
}
```

From ValetudoApp/ValetudoApp/Views/RobotDetailView.swift:
```swift
// Line 271: robotPropertiesSection usage
robotPropertiesSection

// Lines 943-964: robotPropertiesSection definition
@ViewBuilder
private var robotPropertiesSection: some View { ... }

// Lines 848-940: statisticsSection (DisclosureGroup reference pattern)
private var statisticsSection: some View { ... DisclosureGroup { ... } }
```

From ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift:
```swift
// Lines 408-417: NavigationLink to delete
NavigationLink {
    ValetudoInfoView(robot: robot, updateService: updateService)
} label: {
    HStack {
        Image(systemName: "info.circle")
            .foregroundStyle(.gray)
        Text("Valetudo")
    }
}
```

From ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift:
```swift
// Line 4: logger available
private let sectionsLogger = Logger(subsystem: ..., category: "RobotSettingsSections")

// Lines 774-989: ValetudoInfoView to delete after migration
struct ValetudoInfoView: View { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel erweitern und DeviceInfoSection erstellen</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift,
    ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift (lines 774-989 — ValetudoInfoView als Referenz fuer formatUptime/formatBytes und Memory-Bar),
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (lines 848-940 — statisticsSection als DisclosureGroup-Referenz),
    ValetudoApp/ValetudoApp/Models/RobotState.swift (lines 677-707 — ValetudoVersion und SystemHostInfo Structs),
    ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings
  </read_first>
  <action>
**RobotDetailViewModel.swift — neue Properties und Methode:**

1. Nach `@Published var robotProperties: RobotProperties?` (ca. Zeile 62) zwei neue Properties einfuegen:
```swift
@Published var valetudoVersion: ValetudoVersion?
@Published var systemHostInfo: SystemHostInfo?
```

2. Neue private Methode nach `loadRobotProperties()` (nach Zeile 267):
```swift
private func loadDeviceInfo() async {
    guard let api = api else { return }
    do {
        async let v = api.getValetudoVersion()
        async let h = api.getSystemHostInfo()
        valetudoVersion = try await v
        systemHostInfo = try await h
    } catch {
        logger.debug("Device info not available: \(error, privacy: .public)")
    }
}
```

3. In `loadData()` (Zeile 121-134): Neues `async let` hinzufuegen:
```swift
async let deviceInfoTask: () = loadDeviceInfo()
```
Und im await-Tuple ergaenzen:
```swift
_ = await (segmentsTask, consumablesTask, capabilitiesTask, fanSpeedTask, updateTask, statsTask, eventsTask, cleanRouteTask, obstaclesTask, propertiesTask, deviceInfoTask)
```

**RobotDetailSections.swift — DeviceInfoSection Struct:**

Am Ende der Datei (vor dem letzten `}` falls vorhanden, oder einfach am Ende) neue Struct einfuegen:

```swift
// MARK: - Device Info Section
struct DeviceInfoSection: View {
    @ObservedObject var viewModel: RobotDetailViewModel
    @State private var isExpanded = false

    var body: some View {
        let hasAnyData = viewModel.robotProperties != nil
            || viewModel.valetudoVersion != nil
            || viewModel.systemHostInfo != nil

        if hasAnyData {
            Section {
                DisclosureGroup(isExpanded: $isExpanded) {
                    // Hardware
                    if let props = viewModel.robotProperties {
                        if let model = props.model {
                            LabeledContent(String(localized: "device_info.model"), value: model)
                        }
                        if let manufacturer = props.manufacturer {
                            LabeledContent(String(localized: "device_info.manufacturer"), value: manufacturer)
                        }
                        if let serial = props.metaData?.manufacturerSerialNumber {
                            LabeledContent(String(localized: "device_info.serial"), value: serial)
                        }
                    }

                    // Valetudo
                    if let version = viewModel.valetudoVersion {
                        LabeledContent(String(localized: "device_info.valetudo_version"), value: version.release)
                        HStack {
                            Text(String(localized: "device_info.commit"))
                                .foregroundStyle(.secondary)
                            Spacer()
                            Text(String(version.commit.prefix(8)))
                                .font(.system(.caption, design: .monospaced))
                        }
                    }

                    // System
                    if let info = viewModel.systemHostInfo {
                        LabeledContent(String(localized: "device_info.hostname"), value: info.hostname)
                        LabeledContent(String(localized: "device_info.uptime"), value: formatUptime(info.uptime))

                        // CPU bar
                        if let load = info.load {
                            HStack {
                                Text(String(localized: "device_info.cpu"))
                                    .foregroundStyle(.secondary)
                                Spacer()
                                let normalizedLoad = min(load._1, 1.0)
                                ProgressView(value: normalizedLoad, total: 1.0)
                                    .tint(normalizedLoad > 0.8 ? .red : .blue)
                                    .frame(width: 100)
                            }
                        }

                        // Memory bar
                        HStack {
                            Text(String(localized: "device_info.memory"))
                                .foregroundStyle(.secondary)
                            Spacer()
                            Text(formatBytes(info.mem.total - info.mem.free))
                                .font(.caption)
                                .foregroundStyle(.secondary)
                            Text("/")
                                .font(.caption)
                                .foregroundStyle(.secondary)
                            Text(formatBytes(info.mem.total))
                                .font(.caption)
                                .foregroundStyle(.secondary)
                        }
                        let usedPercent = Double(info.mem.total - info.mem.free) / Double(info.mem.total)
                        GeometryReader { geometry in
                            ZStack(alignment: .leading) {
                                RoundedRectangle(cornerRadius: 4)
                                    .fill(Color.secondary.opacity(0.2))
                                RoundedRectangle(cornerRadius: 4)
                                    .fill(usedPercent > 0.8 ? Color.red : Color.blue)
                                    .frame(width: geometry.size.width * usedPercent)
                            }
                        }
                        .frame(height: 8)
                    }
                } label: {
                    Label(String(localized: "device_info.title"), systemImage: "cpu")
                }
            }
        }
    }

    private func formatUptime(_ seconds: Double) -> String {
        let days = Int(seconds) / 86400
        let hours = (Int(seconds) % 86400) / 3600
        let minutes = (Int(seconds) % 3600) / 60
        if days > 0 {
            return "\(days)d \(hours)h"
        } else if hours > 0 {
            return "\(hours)h \(minutes)m"
        } else {
            return "\(minutes)m"
        }
    }

    private func formatBytes(_ bytes: Int) -> String {
        let mb = Double(bytes) / 1024 / 1024
        if mb >= 1024 {
            return String(format: "%.1f GB", mb / 1024)
        }
        return String(format: "%.1f MB", mb)
    }
}
```

**Localizable.xcstrings — Neue Keys:**

Folgende Keys hinzufuegen (de/en/fr):

| Key | DE | EN | FR |
|-----|----|----|-----|
| `device_info.title` | Geraeteinformationen | Device Info | Infos appareil |
| `device_info.model` | Modell | Model | Modele |
| `device_info.manufacturer` | Hersteller | Manufacturer | Fabricant |
| `device_info.serial` | Seriennummer | Serial Number | Numero de serie |
| `device_info.valetudo_version` | Version | Version | Version |
| `device_info.commit` | Commit | Commit | Commit |
| `device_info.hostname` | Hostname | Hostname | Nom d'hote |
| `device_info.uptime` | Laufzeit | Uptime | Temps de fonctionnement |
| `device_info.cpu` | CPU-Last | CPU Load | Charge CPU |
| `device_info.memory` | Arbeitsspeicher | Memory | Memoire |

Format: Dem bestehenden xcstrings JSON-Format folgen (jeder Key hat ein `localizations`-Objekt mit `de`, `en`, `fr` Eintraegen mit `stringUnit`).
  </action>
  <verify>
    <automated>cd ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RobotDetailViewModel.swift enthaelt `@Published var valetudoVersion: ValetudoVersion?`
    - RobotDetailViewModel.swift enthaelt `@Published var systemHostInfo: SystemHostInfo?`
    - RobotDetailViewModel.swift enthaelt `private func loadDeviceInfo() async`
    - RobotDetailViewModel.swift loadData() enthaelt `async let deviceInfoTask`
    - RobotDetailSections.swift enthaelt `struct DeviceInfoSection: View`
    - RobotDetailSections.swift enthaelt `DisclosureGroup(isExpanded: $isExpanded)`
    - RobotDetailSections.swift enthaelt `ProgressView(value: normalizedLoad`
    - RobotDetailSections.swift enthaelt `private func formatUptime`
    - RobotDetailSections.swift enthaelt `private func formatBytes`
    - Localizable.xcstrings enthaelt `device_info.title`
    - Localizable.xcstrings enthaelt `device_info.cpu`
    - Localizable.xcstrings enthaelt `device_info.memory`
    - Xcode build erfolgreich (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>DeviceInfoSection Struct existiert in RobotDetailSections.swift mit DisclosureGroup, Hardware/Valetudo/System-Bloecken, CPU-ProgressView und Memory-Bar. ViewModel hat valetudoVersion und systemHostInfo Properties mit loadDeviceInfo() in loadData() integriert. Alle Localization-Keys sind definiert. Build kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Verdrahtung und Aufraeum-Arbeiten</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift,
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (vollstaendig — robotPropertiesSection Zeile 271 und Definition Zeile 943-964),
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift (Zeilen 408-417 — NavigationLink zu ValetudoInfoView),
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift (Zeilen 774-989 — ValetudoInfoView Struct zum Loeschen)
  </read_first>
  <action>
**RobotDetailView.swift — robotPropertiesSection ersetzen:**

1. Zeile 271: `robotPropertiesSection` ersetzen durch:
```swift
DeviceInfoSection(viewModel: viewModel)
```

2. Zeilen 943-964: Die gesamte `robotPropertiesSection` computed property loeschen (das `@ViewBuilder private var robotPropertiesSection: some View { ... }` Block).

**RobotSettingsView.swift — NavigationLink entfernen:**

Zeilen 408-417 komplett loeschen (der gesamte `// System Info` NavigationLink-Block):
```swift
// System Info
NavigationLink {
    ValetudoInfoView(robot: robot, updateService: updateService)
} label: {
    HStack {
        Image(systemName: "info.circle")
            .foregroundStyle(.gray)
        Text("Valetudo")
    }
}
```

Die "Valetudo System" Section mit WiFi, MQTT, NTP bleibt bestehen — NUR der Valetudo-Info NavigationLink wird entfernt.

**RobotSettingsSections.swift — ValetudoInfoView Struct loeschen:**

Zeilen 773-989 komplett loeschen (den `// MARK: - Valetudo Info View` Kommentar und die gesamte `struct ValetudoInfoView: View { ... }` Definition inkl. aller privaten Methoden `loadInfo()`, `formatUptime()`, `formatBytes()`).

WICHTIG: Sicherstellen dass der `// MARK: - Station Settings View` Kommentar und die `StationSettingsView` ab Zeile 991 erhalten bleiben.
  </action>
  <verify>
    <automated>cd ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RobotDetailView.swift enthaelt `DeviceInfoSection(viewModel: viewModel)`
    - RobotDetailView.swift enthaelt NICHT `robotPropertiesSection` (weder Aufruf noch Definition)
    - RobotSettingsView.swift enthaelt NICHT `ValetudoInfoView`
    - RobotSettingsView.swift enthaelt weiterhin `NavigationLink` zu `WifiSettingsView` und `MQTTSettingsView` und `NTPSettingsView`
    - RobotSettingsSections.swift enthaelt NICHT `struct ValetudoInfoView`
    - RobotSettingsSections.swift enthaelt weiterhin `struct StationSettingsView`
    - Xcode build erfolgreich (BUILD SUCCEEDED)
    - `grep -r "ValetudoInfoView" ValetudoApp/ValetudoApp/ --include="*.swift"` liefert null Treffer
  </acceptance_criteria>
  <done>DeviceInfoSection ist in RobotDetailView verdrahtet und ersetzt robotPropertiesSection. ValetudoInfoView NavigationLink ist aus RobotSettingsView entfernt. ValetudoInfoView Struct ist aus RobotSettingsSections.swift geloescht. Build kompiliert fehlerfrei, kein toter Code zurueckgeblieben.</done>
</task>

</tasks>

<verification>
1. `cd ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5` — BUILD SUCCEEDED
2. `grep -rn "ValetudoInfoView" ValetudoApp/ValetudoApp/ --include="*.swift"` — Null Treffer
3. `grep -rn "robotPropertiesSection" ValetudoApp/ValetudoApp/ --include="*.swift"` — Null Treffer
4. `grep -n "DeviceInfoSection" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift` — Treffer vorhanden
5. `grep -n "struct DeviceInfoSection" ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift` — Treffer vorhanden
6. `grep -n "valetudoVersion" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift` — Treffer vorhanden
7. `grep -n "device_info.title" ValetudoApp/ValetudoApp/Resources/Localizable.xcstrings` — Treffer vorhanden
</verification>

<success_criteria>
- Geraeteinformationen (Model, Serial, Manufacturer, Version, Commit, Hostname, Uptime, CPU-Bar, Memory-Bar) erscheinen in einer DisclosureGroup im RobotDetailView
- Die DisclosureGroup ist standardmaessig zugeklappt
- ValetudoInfoView ist aus RobotSettingsView komplett entfernt (kein NavigationLink, kein Struct)
- robotPropertiesSection ist durch DeviceInfoSection ersetzt
- Alle bestehenden Settings-Sections (WiFi, MQTT, NTP) bleiben unveraendert
- Build kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-reorganization/16-01-SUMMARY.md`
</output>
