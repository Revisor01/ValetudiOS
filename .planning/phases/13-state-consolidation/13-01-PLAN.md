---
phase: 13-state-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/UpdateService.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "grep -r 'isUpdating' ValetudoApp/ liefert null Treffer"
    - "grep -r '@Published var showUpdateWarning' ValetudoApp/ liefert null Treffer"
    - "grep -r '@Published var currentVersion' ValetudoApp/ValetudoApp/ViewModels/ liefert null Treffer"
    - "grep -r '@Published var latestVersion' ValetudoApp/ValetudoApp/ViewModels/ liefert null Treffer"
    - "grep -r '@Published var updateUrl' ValetudoApp/ValetudoApp/ViewModels/ liefert null Treffer"
    - "grep -r '@Published var updaterState' ValetudoApp/ValetudoApp/ViewModels/ liefert null Treffer"
    - "ValetudoInfoView hat keine eigene checkForUpdate()-Methode mehr"
    - "UpdateService besitzt currentVersion, latestVersion und updateUrl als Published Properties"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      provides: "Consolidated update state incl. version info and GitHub release"
      contains: "@Published private(set) var currentVersion"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "ViewModel without redundant update properties"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "View reading update state from updateService"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift"
      provides: "ValetudoInfoView without own checkForUpdate"
  key_links:
    - from: "RobotDetailView.swift"
      to: "UpdateService.phase"
      via: "viewModel.updateService?.phase pattern matching"
      pattern: "updateService\\?\\.phase"
    - from: "ValetudoInfoView"
      to: "UpdateService"
      via: "updateService?.currentVersion, updateService?.latestVersion"
      pattern: "updateService\\?"
---

<objective>
Redundante Update-Properties aus RobotDetailViewModel entfernen und doppelte Update-Check-Logik in ValetudoInfoView eliminieren. Nach dieser Phase ist UpdateService die einzige Source of Truth fuer Update-Zustand, Versionsnummern und GitHub-Release-Infos.

Purpose: CLEAN-01 und CLEAN-02 — kein doppelter Update-State, keine parallelen Check-Pfade.
Output: Bereinigte Dateien ohne redundante Properties, UpdateService mit Version-Info.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-state-consolidation/13-CONTEXT.md

<interfaces>
<!-- UpdateService — Phase 12 Output, wird erweitert -->
From ValetudoApp/ValetudoApp/Services/UpdateService.swift:
```swift
@MainActor
class UpdateService: ObservableObject {
    @Published private(set) var phase: UpdatePhase = .idle
    // KEINE currentVersion/latestVersion/updateUrl — muss hinzugefuegt werden

    init(api: ValetudoAPI)
    func checkForUpdates() async
    func startDownload() async
    func startApply() async
    func reset()
}
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift:
```swift
// Zu entfernende Properties (Zeilen 35-40):
@Published var currentVersion: String?
@Published var latestVersion: String?
@Published var updateUrl: String?
@Published var updaterState: UpdaterState?
@Published var isUpdating = false
@Published var showUpdateWarning = false

// Bestehendes UpdateService-Property:
private(set) var updateService: UpdateService?
var updateInProgress: Bool { ... }  // liest bereits von updateService
```

From ValetudoApp/ValetudoApp/Models/RobotState.swift:
```swift
struct GitHubRelease: Codable {
    let tag_name: String
    let html_url: String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UpdateService um Version-Properties erweitern und ViewModel-Properties entfernen (CLEAN-01)</name>
  <files>
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  </read_first>
  <action>
**1. UpdateService.swift erweitern:**

Drei neue `@Published` Properties hinzufuegen (nach `phase`):
```swift
@Published private(set) var currentVersion: String?
@Published private(set) var latestVersion: String?
@Published private(set) var updateUrl: String?
```

Neue Methode `loadVersionInfo()` erstellen, die:
- `api.getValetudoVersion()` aufruft und `currentVersion = version.release` setzt
- GitHub-Release-Check durchfuehrt (gleiche Logik wie bisher in ViewModel Zeile 297-301):
  `URLSession.shared.data(from: URL(string: Constants.githubApiLatestReleaseUrl))`,
  decode als `GitHubRelease`, setze `latestVersion = release.tag_name` und `updateUrl = release.html_url`
- Fehler werden geloggt, nicht geworfen

`checkForUpdates()` Methode anpassen: Am Anfang (vor dem Guard) `await loadVersionInfo()` aufrufen. Der Guard auf `.idle` bleibt fuer den Valetudo-Update-Check, aber die Version-Info wird immer geladen. ALTERNATIV: `loadVersionInfo()` als separaten Aufruf belassen, der auch im non-idle State funktioniert — da Version-Info kein State-Machine-Concern ist. Besser: `loadVersionInfo()` public machen und NICHT im idle-Guard einschliessen.

In `reset()`: `currentVersion`, `latestVersion`, `updateUrl` auf nil setzen.

**2. RobotDetailViewModel.swift bereinigen:**

Folgende 6 `@Published` Properties ENTFERNEN (Zeilen 35-40):
- `@Published var currentVersion: String?`
- `@Published var latestVersion: String?`
- `@Published var updateUrl: String?`
- `@Published var updaterState: UpdaterState?`
- `@Published var isUpdating = false`
- `@Published var showUpdateWarning = false`

Die private Methode `checkForUpdate()` (Zeilen 284-305) ERSETZEN durch:
```swift
private func checkForUpdate() async {
    setupUpdateService()
    await updateService?.loadVersionInfo()
    await updateService?.checkForUpdates()
}
```

Der GitHub-Release-Check und der `currentVersion`-Fetch werden NICHT mehr im ViewModel gemacht — beides liegt jetzt in `UpdateService.loadVersionInfo()`.

**3. RobotDetailView.swift Update-Sektionen umstellen:**

Die gesamte Update-Banner-Logik (Zeilen 16-126) umschreiben:
- `viewModel.updateInProgress` bleibt (liest bereits von updateService)
- `viewModel.updaterState` Referenzen ersetzen durch `viewModel.updateService?.phase` Pattern-Matching:
  - `state.isUpdateAvailable` → `case .updateAvailable = viewModel.updateService?.phase`
  - `state.isDownloading` → `case .downloading = viewModel.updateService?.phase`
  - `state.isReadyToApply` → `case .readyToApply = viewModel.updateService?.phase`
  - `state.version` (fuer Anzeige) → `viewModel.updateService?.latestVersion` (oder Valetudo-Info, je nach Verfuegbarkeit)
- `viewModel.currentVersion` → `viewModel.updateService?.currentVersion`
- `viewModel.latestVersion` → `viewModel.updateService?.latestVersion`
- `viewModel.updateUrl` → `viewModel.updateService?.updateUrl`
- `viewModel.showUpdateWarning` existiert nicht mehr — `showUpdateWarning` ist bereits `@State` in der View (Zeile 7), das ist korrekt
- Der GitHub-Fallback-Block (Zeilen 102-126: "if currentVersion != latestVersion") bleibt konzeptionell erhalten, liest aber aus `updateService`

WICHTIG: `@State private var showUpdateWarning = false` in Zeile 7 BLEIBT — das ist reines UI-State fuer Alert-Binding, kein Update-Logic-State.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -rn '@Published var isUpdating\|@Published var showUpdateWarning\|@Published var currentVersion\|@Published var latestVersion\|@Published var updateUrl\|@Published var updaterState' ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep '@Published var isUpdating' in RobotDetailViewModel.swift: 0 Treffer
    - grep '@Published var showUpdateWarning' in RobotDetailViewModel.swift: 0 Treffer
    - grep '@Published var currentVersion' in RobotDetailViewModel.swift: 0 Treffer
    - grep '@Published var latestVersion' in RobotDetailViewModel.swift: 0 Treffer
    - grep '@Published var updateUrl' in RobotDetailViewModel.swift: 0 Treffer
    - grep '@Published var updaterState' in RobotDetailViewModel.swift: 0 Treffer
    - grep 'currentVersion' in UpdateService.swift: mindestens 1 Treffer
    - grep 'latestVersion' in UpdateService.swift: mindestens 1 Treffer
    - grep 'updateUrl' in UpdateService.swift: mindestens 1 Treffer
    - RobotDetailView.swift referenziert updateService statt viewModel.updaterState
    - xcodebuild clean build (Projekt kompiliert fehlerfrei)
  </acceptance_criteria>
  <done>Alle 6 redundanten @Published Properties sind aus dem ViewModel entfernt. UpdateService hat currentVersion, latestVersion, updateUrl. RobotDetailView liest Update-State aus updateService. Projekt kompiliert.</done>
</task>

<task type="auto">
  <name>Task 2: ValetudoInfoView checkForUpdate() entfernen und an UpdateService anbinden (CLEAN-02)</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift (Zeilen 770-1000),
    ValetudoApp/ValetudoApp/Services/UpdateService.swift
  </read_first>
  <action>
**ValetudoInfoView in RobotSettingsSections.swift umstellen:**

1. `@State private var latestRelease: GitHubRelease?` Property ENTFERNEN — wird nicht mehr gebraucht, UpdateService haelt `latestVersion` und `updateUrl`.

2. Die private Methode `checkForUpdate()` (Zeilen 968-977) KOMPLETT ENTFERNEN.

3. `loadInfo()` Methode (Zeilen 951-966) anpassen:
   - Zeile `await checkForUpdate()` am Ende ENTFERNEN
   - Stattdessen: `await updateService?.loadVersionInfo()` aufrufen (laedt Version + GitHub-Release in UpdateService)
   - `version = try await api.getValetudoVersion()` und `hostInfo = try await api.getSystemHostInfo()` BLEIBEN — das sind View-lokale Infos (Commit-Hash, System-Info)

4. `hasUpdate` computed property (Zeilen 788-796) anpassen:
   - Entferne den Fallback-Block der `latestRelease` nutzt
   - Neuer Fallback: `guard let current = version?.release, let latest = updateService?.latestVersion else { return false }; return current != latest`

5. Body anpassen — alle `latestRelease` Referenzen ersetzen:
   - `latestRelease` in der "Update Available Banner" Section (Zeile 801): `if hasUpdate, let latest = updateService?.latestVersion, let urlStr = updateService?.updateUrl, let url = URL(string: urlStr)`
   - `latest.tag_name` → `latest` (ist bereits der String)
   - `latest.html_url` → `urlStr` / `url`
   - `URL(string: latest.html_url)!` → sichere URL-Konstruktion (Force-Unwrap entfernen per SAFE-01 Pattern)
   - Zeile 851 `Link(destination: URL(string: latest.html_url)!)` → sichere URL-Konstruktion mit `if let`
   - `latestRelease?.tag_name` → `updateService?.latestVersion`
   - `latestRelease != nil` (Zeile 835) → `updateService?.latestVersion != nil`
   - Zeile 850-862 "Latest" Section: `latest.tag_name` → `updateService?.latestVersion`, Link-URL → `updateService?.updateUrl`

6. WICHTIG: `@State private var version: ValetudoVersion?` BLEIBT — wird fuer Commit-Hash Anzeige gebraucht (nicht im UpdateService).

7. WICHTIG: Die zwei Force-Unwraps `URL(string: latest.html_url)!` in Zeilen 803 und 851 werden durch sichere `if let` Konstruktionen ersetzt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && echo "checkForUpdate in ValetudoInfoView:" && grep -c 'private func checkForUpdate' ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift && echo "latestRelease State:" && grep -c '@State private var latestRelease' ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift && echo "Force-unwraps:" && grep -c 'html_url)!' ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep 'private func checkForUpdate' in RobotSettingsSections.swift: 0 Treffer
    - grep '@State private var latestRelease' in RobotSettingsSections.swift: 0 Treffer
    - grep 'html_url)!' in RobotSettingsSections.swift: 0 Treffer (keine Force-Unwraps)
    - grep 'updateService?.latestVersion' in RobotSettingsSections.swift: mindestens 1 Treffer
    - xcodebuild clean build (Projekt kompiliert fehlerfrei)
  </acceptance_criteria>
  <done>ValetudoInfoView hat keine eigene checkForUpdate()-Methode mehr. Kein latestRelease State. Alle Version-Infos kommen aus UpdateService. Force-Unwraps eliminiert. Projekt kompiliert.</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
1. `grep -rn 'isUpdating' ValetudoApp/ValetudoApp/ViewModels/` → 0 Treffer
2. `grep -rn 'showUpdateWarning' ValetudoApp/ValetudoApp/ViewModels/` → 0 Treffer
3. `grep -rn '@Published var updaterState' ValetudoApp/` → 0 Treffer
4. `grep -rn 'private func checkForUpdate' ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift` → 0 Treffer
5. `grep -rn 'currentVersion' ValetudoApp/ValetudoApp/Services/UpdateService.swift` → mindestens 1 Treffer
6. Xcode Build erfolgreich
</verification>

<success_criteria>
- CLEAN-01: isUpdating, showUpdateWarning, currentVersion, latestVersion, updateUrl, updaterState existieren nicht mehr als @Published im ViewModel
- CLEAN-02: ValetudoInfoView hat keine eigene checkForUpdate()-Methode, nutzt UpdateService
- UpdateService ist alleinige Source of Truth fuer Version-Info und Update-Phase
- Projekt kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/13-state-consolidation/13-01-SUMMARY.md`
</output>
