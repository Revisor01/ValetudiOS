---
phase: 24-map-performance
plan: 03
type: execute
wave: 2
depends_on: [24-02]
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
  - ValetudoApp/ValetudoApp/Views/MapView.swift
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "Floor, Walls und Segments werden als CGImage vorgerendert — nicht jeden Frame neu gezeichnet"
    - "Canvas zeichnet nur dynamische Elemente (Selection-Border, Path, Robot, Charger, Restrictions) pro Frame"
    - "Pre-Rendering passiert auf einem Background-Thread — kein Main-Thread-Blocking"
    - "Statisches Image wird bei jedem Map-Update und bei View-Size-Aenderung neu gerendert"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "staticLayerImage CGImage und rebuildStaticLayerImage Methode"
      contains: "staticLayerImage"
    - path: "ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift"
      provides: "Canvas nutzt staticLayerImage + dynamische Elemente"
      contains: "context.draw"
  key_links:
    - from: "MapViewModel.map didSet"
      to: "rebuildStaticLayerImage"
      via: "called on map change to re-render static layers"
      pattern: "rebuildStaticLayerImage"
    - from: "InteractiveMapView Canvas"
      to: "MapViewModel.staticLayerImage"
      via: "context.draw for pre-rendered image"
      pattern: "staticLayerImage"
---

<objective>
Statische Map-Layer als CGImage vorrendern.

Purpose: Floor, Walls und Segments (Basis-Farbe) werden als ein CGImage gerendert und im Canvas als einzelnes Image gezeichnet statt tausende individuelle Pixel-Rects. Canvas zeichnet nur noch dynamische Elemente (Selection, Entities, Restrictions) pro Frame.
Output: MapViewModel haelt ein vorgerendertes staticLayerImage, InteractiveMapView nutzt es im Canvas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-map-performance/24-RESEARCH.md
@.planning/phases/24-map-performance/24-02-SUMMARY.md

@ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
@ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
@ValetudoApp/ValetudoApp/Views/MapView.swift

<interfaces>
<!-- Interfaces nach Plan 02 Ausfuehrung -->

From MapViewModel.swift (nach Plan 02):
```swift
@MainActor @Observable
final class MapViewModel {
    var map: RobotMap?
    var segments: [Segment] = []
    @ObservationIgnored var segmentPixelSets: [String: Set<Int>] = [:]
    var cachedSegmentInfos: [SegmentInfo] = []

    // Wird bei jedem map-Update aufgerufen:
    private func rebuildSegmentPixelSets()
    private func updateCachedSegmentInfos()
}
```

From InteractiveMapView.swift (nach Plan 02):
```swift
struct InteractiveMapView: View {
    let map: RobotMap
    let segments: [Segment]
    @Binding var selectedSegmentIds: [String]
    let viewSize: CGSize
    var segmentPixelSets: [String: Set<Int>] = [:]
    var cachedSegmentInfos: [SegmentInfo] = []

    // Canvas body zeichnet derzeit noch alle Pixel direkt:
    // - drawLayersDecompressed (floor)
    // - drawPixelsWithMaterial (segments)
    // - drawWalls
    // - drawSegmentBorder (selection — DYNAMIC)
    // - drawPath, drawCharger, drawRobot (entities — DYNAMIC)
    // - restrictions, zones, current drawing preview (DYNAMIC)
}
```

From MapView.swift:
```swift
struct MapContentView: View {
    @State var currentViewSize: CGSize = .zero
    // Updated in onAppear and onChange(of: geometry.size)
}
```

Key drawing methods in InteractiveMapView (STATIC — move to pre-render):
```swift
private func drawLayersDecompressed(context:layers:type:color:params:pixelSize:)
private func drawPixelsWithMaterial(context:pixels:color:material:params:pixelSize:)
private func drawWalls(context:layers:color:params:pixelSize:)
private func drawPixels(context:pixels:color:params:pixelSize:)
```

Key drawing methods in InteractiveMapView (DYNAMIC — keep in Canvas):
```swift
private func drawSegmentBorder(context:pixels:params:pixelSize:)
private func drawPath(context:entity:params:pixelSize:)
private func drawCharger(context:entity:params:pixelSize:)
private func drawRobot(context:entity:params:pixelSize:)
private func drawCleaningZone(context:zone:params:pixelSize:)
private func drawRestrictedZone(context:area:params:pixelSize:)
private func drawVirtualWall(context:wall:params:pixelSize:)
private func drawCurrentDrawing(context:start:end:mode:size:)
```

Segment colors array (needed for pre-render):
```swift
private let segmentColors: [Color] = [
    Color(red: 0.65, green: 0.80, blue: 0.92),  // Soft sky blue
    Color(red: 0.70, green: 0.88, blue: 0.75),  // Soft mint green
    Color(red: 0.92, green: 0.78, blue: 0.72),  // Soft peach
    Color(red: 0.82, green: 0.75, blue: 0.90),  // Soft lavender
    Color(red: 0.90, green: 0.85, blue: 0.65),  // Soft gold
    Color(red: 0.70, green: 0.85, blue: 0.85),  // Soft teal
    Color(red: 0.90, green: 0.72, blue: 0.78),  // Soft rose
    Color(red: 0.78, green: 0.88, blue: 0.72),  // Soft sage
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static Layer Pre-Rendering in MapViewModel (PERF-04)</name>
  <files>ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift, ValetudoApp/ValetudoApp/Views/MapView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
    - ValetudoApp/ValetudoApp/Views/MapView.swift
    - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
    - ValetudoApp/ValetudoApp/Utilities/MapGeometry.swift
  </read_first>
  <action>
**1. Neues Property auf MapViewModel:**

```swift
// MARK: - Static Layer Pre-rendering (PERF-04)
var staticLayerImage: CGImage?
@ObservationIgnored private var lastRenderSize: CGSize = .zero
```

`staticLayerImage` ist Observable damit Canvas auf Aenderungen reagiert. `lastRenderSize` ist ObservationIgnored weil es nur intern fuer Rebuild-Entscheidungen genutzt wird.

**2. Import UIKit hinzufuegen:**

Am Anfang der Datei: `import UIKit` (fuer UIGraphicsImageRenderer, UIColor).

**3. Segment-Farben als statische UIColor-Array definieren:**

```swift
private static let segmentUIColors: [UIColor] = [
    UIColor(red: 0.65, green: 0.80, blue: 0.92, alpha: 1),
    UIColor(red: 0.70, green: 0.88, blue: 0.75, alpha: 1),
    UIColor(red: 0.92, green: 0.78, blue: 0.72, alpha: 1),
    UIColor(red: 0.82, green: 0.75, blue: 0.90, alpha: 1),
    UIColor(red: 0.90, green: 0.85, blue: 0.65, alpha: 1),
    UIColor(red: 0.70, green: 0.85, blue: 0.85, alpha: 1),
    UIColor(red: 0.90, green: 0.72, blue: 0.78, alpha: 1),
    UIColor(red: 0.78, green: 0.88, blue: 0.72, alpha: 1),
]

private static func segmentUIColor(segmentId: String?) -> UIColor {
    if let id = segmentId, let num = Int(id) {
        return segmentUIColors[num % segmentUIColors.count]
    }
    return segmentUIColors[0]
}
```

**4. rebuildStaticLayerImage Methode:**

```swift
func rebuildStaticLayerImage(size: CGSize) {
    guard size.width > 0, size.height > 0 else { return }
    guard let map = map, let layers = map.layers, !layers.isEmpty else {
        staticLayerImage = nil
        return
    }

    lastRenderSize = size
    let pixelSize = map.pixelSize ?? 5
    guard let params = calculateMapParams(layers: layers, pixelSize: pixelSize, size: size) else { return }

    // Kopien fuer den Background-Thread erstellen
    let layersCopy = layers
    let scale = params.scale
    let offsetX = params.offsetX
    let offsetY = params.offsetY
    let pxSize = pixelSize

    Task.detached(priority: .userInitiated) {
        let renderer = UIGraphicsImageRenderer(size: size)
        let uiImage = renderer.image { ctx in
            let cgCtx = ctx.cgContext

            let pixelScale = scale * CGFloat(pxSize)

            // Floor
            for layer in layersCopy where layer.type == "floor" {
                let pixels = layer.decompressedPixels
                cgCtx.setFillColor(UIColor(white: 0.92, alpha: 1).cgColor)
                var i = 0
                while i < pixels.count - 1 {
                    let x = CGFloat(pixels[i]) * scale + offsetX
                    let y = CGFloat(pixels[i + 1]) * scale + offsetY
                    cgCtx.fill(CGRect(x: x, y: y, width: pixelScale + 0.5, height: pixelScale + 0.5))
                    i += 2
                }
            }

            // Segments (unselected base color mit Material-Textur)
            for layer in layersCopy where layer.type == "segment" {
                let pixels = layer.decompressedPixels
                guard !pixels.isEmpty else { continue }
                let baseColor = MapViewModel.segmentUIColor(segmentId: layer.metaData?.segmentId).withAlphaComponent(0.6)
                let material = layer.metaData?.material

                let textureInterval: Int
                let isHorizontal: Bool
                let isVertical: Bool
                switch material {
                case "tile":
                    textureInterval = 4; isHorizontal = true; isVertical = true
                case "wood", "wood_horizontal":
                    textureInterval = 3; isHorizontal = true; isVertical = false
                case "wood_vertical":
                    textureInterval = 3; isHorizontal = false; isVertical = true
                default:
                    textureInterval = 0; isHorizontal = false; isVertical = false
                }

                let accentColor = baseColor.withAlphaComponent(0.85 * 0.6)

                var i = 0
                while i < pixels.count - 1 {
                    let px = pixels[i]
                    let py = pixels[i + 1]
                    let x = CGFloat(px) * scale + offsetX
                    let y = CGFloat(py) * scale + offsetY
                    let rect = CGRect(x: x, y: y, width: pixelScale + 0.5, height: pixelScale + 0.5)

                    let shouldAccent: Bool
                    if textureInterval > 0 {
                        shouldAccent = (isHorizontal && py % textureInterval == 0) || (isVertical && px % textureInterval == 0)
                    } else {
                        shouldAccent = false
                    }

                    cgCtx.setFillColor(shouldAccent ? accentColor.cgColor : baseColor.cgColor)
                    cgCtx.fill(rect)
                    i += 2
                }
            }

            // Walls (thin, wie in InteractiveMapView.drawWalls)
            let wallColor = UIColor(white: 0.25, alpha: 1).cgColor
            let normalScale = scale * CGFloat(pxSize)
            let wallScale = normalScale * 0.2
            cgCtx.setFillColor(wallColor)
            for layer in layersCopy where layer.type == "wall" {
                let pixels = layer.decompressedPixels
                guard !pixels.isEmpty else { continue }
                var i = 0
                while i < pixels.count - 1 {
                    let x = CGFloat(pixels[i]) * scale + offsetX + normalScale * 0.4
                    let y = CGFloat(pixels[i + 1]) * scale + offsetY + normalScale * 0.4
                    cgCtx.fill(CGRect(x: x, y: y, width: wallScale, height: wallScale))
                    i += 2
                }
            }
        }

        await MainActor.run { [weak self] in
            self?.staticLayerImage = uiImage.cgImage
        }
    }
}
```

**5. Aufrufe von rebuildStaticLayerImage:**

In `loadMap()` und an allen Stellen wo `rebuildSegmentPixelSets()` aufgerufen wird, DANACH auch `rebuildStaticLayerImage(size: lastRenderSize)` aufrufen — ABER nur wenn `lastRenderSize` schon gesetzt ist (d.h. die View wurde schon einmal gezeichnet). Dazu:

Nach `rebuildSegmentPixelSets()` und `updateCachedSegmentInfos()`:
```swift
if lastRenderSize.width > 0 {
    rebuildStaticLayerImage(size: lastRenderSize)
}
```

**6. MapContentView: currentViewSize an ViewModel durchreichen:**

In MapView.swift in MapContentView, in `.onAppear` und `.onChange(of: geometry.size)` wo `currentViewSize = geometry.size` bzw. `currentViewSize = newSize` steht, ZUSAETZLICH:
```swift
viewModel.rebuildStaticLayerImage(size: geometry.size)
```
bzw.
```swift
viewModel.rebuildStaticLayerImage(size: newSize)
```

Das sorgt dafuer, dass das statische Image beim ersten Render und bei Groessenaenderungen (Rotation, Fullscreen-Toggle) neu gebaut wird.

WICHTIG: Die Segment-Farben im Pre-Render muessen EXAKT den Farben in InteractiveMapView.segmentColors entsprechen (gleiche RGB-Werte, gleiche Opacity 0.6). Sonst gibt es visuelle Artefakte.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "staticLayerImage\|rebuildStaticLayerImage\|UIGraphicsImageRenderer" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - MapViewModel hat `staticLayerImage: CGImage?` Property
    - `rebuildStaticLayerImage(size:)` nutzt `UIGraphicsImageRenderer` auf Background-Thread
    - Floor, Segments (mit Material-Textur) und Walls werden in das CGImage gerendert
    - CGImage wird bei Map-Update und View-Size-Aenderung neu gebaut
    - Segment-Farben und Wall-Rendering sind pixel-identisch mit der alten Canvas-Implementierung
  </acceptance_criteria>
  <done>MapViewModel rendert statische Map-Layer als CGImage auf Background-Thread</done>
</task>

<task type="auto">
  <name>Task 2: Canvas auf staticLayerImage umstellen (PERF-04)</name>
  <files>ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift, ValetudoApp/ValetudoApp/Views/MapView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift
    - ValetudoApp/ValetudoApp/Views/MapView.swift
    - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  </read_first>
  <action>
**1. Neues Property auf InteractiveMapView:**

```swift
var staticLayerImage: CGImage?
```

**2. Canvas body umbauen:**

Den Canvas-Body ersetzen. Statt der drei statischen Layer-Zeichenaufrufe (floor, segments, walls) wird das vorgerenderte Image gezeichnet:

```swift
Canvas { context, size in
    let pixelSize = map.pixelSize ?? 5
    guard let layers = map.layers, !layers.isEmpty else {
        let text = Text("No map layers")
        context.draw(text, at: CGPoint(x: size.width / 2, y: size.height / 2))
        return
    }

    let params = calculateMapParams(layers: layers, pixelSize: pixelSize, size: size)
    guard let p = params else {
        let text = Text("Map calculation failed")
        context.draw(text, at: CGPoint(x: size.width / 2, y: size.height / 2))
        return
    }

    // STATIC: Pre-rendered floor + segments + walls
    if let img = staticLayerImage {
        context.draw(
            Image(decorative: img, scale: UIScreen.main.scale),
            in: CGRect(origin: .zero, size: size)
        )
    } else {
        // Fallback: alte Pixel-fuer-Pixel Methode wenn CGImage noch nicht fertig
        drawLayersDecompressed(context: context, layers: layers, type: "floor", color: Color(white: 0.92), params: p, pixelSize: pixelSize)
        for layer in layers where layer.type == "segment" {
            let pixels = layer.decompressedPixels
            guard !pixels.isEmpty else { continue }
            let segmentId = layer.metaData?.segmentId
            let baseColor = segmentColor(segmentId: segmentId)
            let color = baseColor.opacity(0.6)
            let material = layer.metaData?.material
            drawPixelsWithMaterial(context: context, pixels: pixels, color: color, material: material, params: p, pixelSize: pixelSize)
        }
        drawWalls(context: context, layers: layers, color: Color(white: 0.25), params: p, pixelSize: pixelSize)
    }

    // DYNAMIC: Selection borders (nur fuer ausgewaehlte Segments)
    for layer in layers where layer.type == "segment" {
        let segmentId = layer.metaData?.segmentId
        let isSelected = segmentId.map { selectedSegmentIds.contains($0) } ?? false
        if isSelected {
            let pixels = layer.decompressedPixels
            guard !pixels.isEmpty else { continue }

            // Selected-Segment Overlay (dunklere Farbe ueber dem statischen Bild)
            let baseColor = segmentColor(segmentId: segmentId)
            let selectedColor = baseColor.opacity(0.9)
            drawPixels(context: context, pixels: pixels, color: selectedColor, params: p, pixelSize: pixelSize)
            drawSegmentBorder(context: context, pixels: pixels, params: p, pixelSize: pixelSize)
        }
    }

    // DYNAMIC: Entities (path, charger, robot)
    if let entities = map.entities {
        for entity in entities where entity.type == "path" || entity.type == "predicted_path" {
            drawPath(context: context, entity: entity, params: p, pixelSize: pixelSize)
        }
        for entity in entities where entity.type == "charger_location" {
            drawCharger(context: context, entity: entity, params: p, pixelSize: pixelSize)
        }
        for entity in entities where entity.type == "robot_position" {
            drawRobot(context: context, entity: entity, params: p, pixelSize: pixelSize)
        }
    }

    // DYNAMIC: Existing restrictions
    if let restrictions = existingRestrictions {
        for wall in restrictions.virtualWalls {
            drawVirtualWall(context: context, wall: wall, params: p, pixelSize: pixelSize, isNew: false)
        }
        for area in restrictions.restrictedZones {
            drawRestrictedZone(context: context, area: area, params: p, pixelSize: pixelSize, color: .red.opacity(0.3), isNew: false)
        }
        for area in restrictions.noMopZones {
            drawRestrictedZone(context: context, area: area, params: p, pixelSize: pixelSize, color: .blue.opacity(0.3), isNew: false)
        }
    }

    // DYNAMIC: New zones/restrictions + drawing preview (unveraendert)
    for zone in drawnZones {
        drawCleaningZone(context: context, zone: zone, params: p, pixelSize: pixelSize)
    }
    for area in drawnNoGoAreas {
        drawRestrictedZone(context: context, area: area, params: p, pixelSize: pixelSize, color: .red.opacity(0.4), isNew: true)
    }
    for area in drawnNoMopAreas {
        drawRestrictedZone(context: context, area: area, params: p, pixelSize: pixelSize, color: .blue.opacity(0.4), isNew: true)
    }
    for wall in drawnVirtualWalls {
        drawVirtualWall(context: context, wall: wall, params: p, pixelSize: pixelSize, isNew: true)
    }

    if let start = currentDrawStart, let end = currentDrawEnd, editMode != .goTo && editMode != .savePreset {
        drawCurrentDrawing(context: context, start: start, end: end, mode: editMode, size: size)
    }
}
```

WICHTIG:
- `import UIKit` am Anfang von MapInteractiveView.swift hinzufuegen (fuer UIScreen.main.scale)
- Das statische Image wird mit `scale: UIScreen.main.scale` gezeichnet damit es auf Retina-Displays nicht unscharf ist
- Selected-Segments werden UEBER dem statischen Bild gezeichnet (mit opacity 0.9 statt 0.6)
- Die alten Drawing-Helper-Methoden (`drawLayersDecompressed`, `drawPixels`, `drawPixelsWithMaterial`, `drawWalls`, `drawSegmentBorder`, `segmentColor`) BEHALTEN — sie werden als Fallback und fuer Selection-Borders weiterhin benoetigt
- Floor und unselected Segments werden NICHT mehr per Canvas gezeichnet wenn staticLayerImage vorhanden ist

**3. InteractiveMapView Instanziierung in MapView.swift aktualisieren:**

Ueberall wo `InteractiveMapView(` aufgerufen wird, den neuen Parameter hinzufuegen:
```swift
staticLayerImage: viewModel.staticLayerImage
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && echo "=== Uses context.draw for static image ===" && grep -c "Image(decorative:" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift && echo "=== Has staticLayerImage property ===" && grep -c "var staticLayerImage: CGImage" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift && echo "=== Passes staticLayerImage ===" && grep -c "staticLayerImage:" ValetudoApp/ValetudoApp/Views/MapView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Canvas zeichnet `staticLayerImage` mit `context.draw(Image(decorative:scale:in:))`
    - Bei fehlendem staticLayerImage gibt es einen Pixel-fuer-Pixel Fallback
    - Selected-Segments werden dynamisch ueber dem statischen Bild gezeichnet
    - Entities (path, robot, charger) werden weiterhin dynamisch gezeichnet
    - Restrictions und Drawing-Preview werden weiterhin dynamisch gezeichnet
    - InteractiveMapView-Instanziierungen in MapView.swift haben den neuen Parameter
  </acceptance_criteria>
  <done>Canvas nutzt vorgerendertes CGImage fuer statische Layer, zeichnet nur dynamische Elemente pro Frame</done>
</task>

</tasks>

<verification>
1. `grep "staticLayerImage" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — mindestens 5 Treffer
2. `grep "Image(decorative:" ValetudoApp/ValetudoApp/Views/MapInteractiveView.swift` — 1 Treffer
3. `grep "UIGraphicsImageRenderer" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — 1 Treffer
4. `grep "Task.detached" ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift` — 1 Treffer (Background-Thread)
5. Xcode Build erfolgreich
6. Karte wird korrekt angezeigt (visueller Check bei Ausfuehrung)
</verification>

<success_criteria>
- Floor, Walls und Segments als CGImage vorgerendert
- Canvas zeichnet nur dynamische Elemente (Selection, Entities, Restrictions) pro Frame
- Pre-Rendering auf Background-Thread — kein Main-Thread-Blocking
- Fallback auf Pixel-fuer-Pixel bei fehlendem CGImage
- Visuell identisch mit der alten Implementierung
</success_criteria>

<output>
After completion, create `.planning/phases/24-map-performance/24-03-SUMMARY.md`
</output>
