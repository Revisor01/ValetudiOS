---
phase: 15-ui-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/UpdateService.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Waehrend des Downloads zeigt die Update-Sektion eine ProgressView mit Prozentzahl"
    - "Bei einem Fehler erscheint ein rotes Banner mit Fehlermeldung und Retry-Button"
    - "Update-Check wird maximal 1x pro Stunde ausgefuehrt — mehrfaches View-oeffnen loest keinen erneuten Check aus"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      provides: "downloadProgress property, lastCheckDate throttle"
      contains: "downloadProgress"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "ProgressView with percentage, error banner section"
      contains: "ProgressView(value:"
  key_links:
    - from: "UpdateService.swift"
      to: "RobotDetailView.swift"
      via: "updateService.downloadProgress und updateService.phase == .error"
      pattern: "downloadProgress|case \\.error"
---

<objective>
Download-Fortschrittsanzeige, Error-Banner und Update-Check-Throttling in UpdateService und RobotDetailView verdrahten.

Purpose: UI-01/UI-02/UI-03 — User sieht jederzeit den exakten Update-Zustand, Fehlerdetails und unnoetige API-Calls werden vermieden.
Output: Funktionale Update-UI mit Fortschritt, Fehlerfeedback und Throttling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-ui-wiring/15-CONTEXT.md
@.planning/phases/14-apply-phase-hardening/14-01-SUMMARY.md

<interfaces>
<!-- UpdateService — aktuelle Public API -->
From ValetudoApp/ValetudoApp/Services/UpdateService.swift:
```swift
@MainActor
class UpdateService: ObservableObject {
    @Published private(set) var phase: UpdatePhase = .idle
    @Published private(set) var currentVersion: String?
    @Published private(set) var latestVersion: String?
    @Published private(set) var updateUrl: String?

    func loadVersionInfo() async
    func checkForUpdates() async
    func startDownload() async
    func startApply() async
    func reset()
}
```

From ValetudoApp/ValetudoApp/Models/RobotState.swift (Zeile 710-748):
```swift
struct UpdaterState: Codable {
    let metaData: UpdaterMetaData?

    struct UpdaterMetaData: Codable {
        let progress: UpdateProgress?

        struct UpdateProgress: Codable {
            let current: Int?
            let total: Int?
        }
    }
}
```

From ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift:
```swift
private(set) var updateService: UpdateService?

private func checkForUpdate() async {
    setupUpdateService()
    await updateService?.loadVersionInfo()
    await updateService?.checkForUpdates()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UpdateService um downloadProgress und Throttling erweitern</name>
  <files>ValetudoApp/ValetudoApp/Services/UpdateService.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Services/UpdateService.swift (komplett)
    - ValetudoApp/ValetudoApp/Models/RobotState.swift (Zeilen 710-748, UpdaterState/UpdaterMetaData)
  </read_first>
  <action>
Drei Aenderungen in UpdateService.swift:

**1. downloadProgress Property (UI-01):**
- Neue `@Published private(set) var downloadProgress: Double = 0.0` Property hinzufuegen (nach `updateUrl`).
- In `pollUntilReadyToApply()` nach jedem erfolgreichen `api.getUpdaterState()` den Progress extrahieren:
  ```swift
  if let meta = state.metaData?.progress,
     let current = meta.current, let total = meta.total, total > 0 {
      downloadProgress = Double(current) / Double(total)
  }
  ```
- In `setPhase()`: Wenn neue Phase NICHT `.downloading` ist, `downloadProgress = 0.0` zuruecksetzen.
- In `reset()`: `downloadProgress = 0.0` hinzufuegen.

**2. lastCheckDate Throttle (UI-03):**
- Neue `private var lastCheckDate: Date?` Property hinzufuegen (nach `backgroundTaskID`).
- Am Anfang von `checkForUpdates()`, VOR dem idle-Guard:
  ```swift
  if let last = lastCheckDate, Date().timeIntervalSince(last) < 3600 { return }
  ```
- Nach dem `setPhase(.checking)` Aufruf: `lastCheckDate = Date()` setzen.
- In `reset()`: `lastCheckDate = nil` hinzufuegen (damit nach Error ein neuer Check moeglich ist).

**3. Kein weiterer Umbau noetig** — die bestehende State Machine, pollUntilReadyToApply, setPhase-Wrapper und Background Task bleiben unveraendert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "downloadProgress" ValetudoApp/ValetudoApp/Services/UpdateService.swift liefert mindestens 4 Treffer (Deklaration, Zuweisung im Poll, Reset in setPhase, Reset in reset)
    - grep -c "lastCheckDate" ValetudoApp/ValetudoApp/Services/UpdateService.swift liefert mindestens 4 Treffer (Deklaration, Guard, Zuweisung, Reset)
    - grep "timeIntervalSince.*3600" ValetudoApp/ValetudoApp/Services/UpdateService.swift liefert 1 Treffer
  </acceptance_criteria>
  <done>UpdateService hat downloadProgress (0.0-1.0) das aus metaData.progress berechnet wird, und lastCheckDate das Checks auf 1x/Stunde drosselt. Build kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Download-Progress und Error-Banner in RobotDetailView verdrahten</name>
  <files>ValetudoApp/ValetudoApp/Views/RobotDetailView.swift</files>
  <read_first>
    - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (komplett)
    - ValetudoApp/ValetudoApp/Services/UpdateService.swift (nach Task 1 Aenderungen)
  </read_first>
  <action>
Zwei UI-Aenderungen in RobotDetailView.swift:

**1. Download-Sektion mit ProgressView ersetzen (UI-01):**
Die bestehende `.downloading` Section (circa Zeile 84-96) zeigt aktuell nur ein unbestimmtes `ProgressView()`. Ersetzen durch:
```swift
} else if case .downloading = viewModel.updateService?.phase {
    Section {
        VStack(spacing: 12) {
            ProgressView(value: viewModel.updateService?.downloadProgress ?? 0.0)
                .progressViewStyle(.linear)
                .tint(.orange)
            HStack {
                Text(String(localized: "update.downloading"))
                    .font(.subheadline)
                Spacer()
                Text("\(Int((viewModel.updateService?.downloadProgress ?? 0.0) * 100))%")
                    .font(.subheadline)
                    .fontWeight(.medium)
                    .monospacedDigit()
            }
            Text(String(localized: "update.do_not_disconnect"))
                .font(.caption)
                .foregroundStyle(.orange)
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 8)
    }
}
```

**2. Error-Banner Section hinzufuegen (UI-02):**
Nach der `.readyToApply` Section und VOR dem GitHub-Fallback-Block (`else if let currentVersion = ...`), eine neue Error-Section einfuegen:
```swift
} else if case .error(let message) = viewModel.updateService?.phase {
    Section {
        VStack(alignment: .leading, spacing: 8) {
            HStack {
                Image(systemName: "exclamationmark.triangle.fill")
                    .foregroundStyle(.red)
                VStack(alignment: .leading) {
                    Text(String(localized: "update.error"))
                        .font(.subheadline)
                        .fontWeight(.medium)
                    Text(message)
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
                Spacer()
            }

            Button {
                viewModel.updateService?.reset()
            } label: {
                HStack {
                    Image(systemName: "arrow.clockwise")
                    Text(String(localized: "update.retry"))
                }
                .frame(maxWidth: .infinity)
                .padding(.vertical, 8)
                .background(Color.red.opacity(0.15))
                .foregroundStyle(.red)
                .clipShape(RoundedRectangle(cornerRadius: 8))
            }
        }
    }
}
```

**Reihenfolge der if-else-Kette in der Update-Sektion muss sein:**
1. `updateInProgress` (checking/downloading/applying/rebooting Banner)
2. `.updateAvailable` (Install-Button)
3. `.downloading` (ProgressView mit Prozent)
4. `.readyToApply` (Apply-Button)
5. `.error(let message)` (Error-Banner mit Retry) — NEU
6. GitHub-Fallback (Version-Vergleich)

**Achtung:** Die `.downloading` Section wird sowohl durch den `updateInProgress`-Check (Zeile 27) als auch durch den expliziten `.downloading`-Check (Zeile 84) getroffen. Der `updateInProgress`-Check zeigt das generische "Update laeuft" Banner. Da `updateInProgress` `.downloading` einschliesst, wird die detaillierte ProgressView-Section (Zeile 84) nie erreicht. Daher: In der `updateInProgress` computed property `.downloading` aus der Liste ENTFERNEN, damit die detaillierte Progress-Section greift:

In RobotDetailView.swift, die bestehende `updateInProgress` Nutzung (Zeile 27) beruht auf `viewModel.updateInProgress` aus dem ViewModel. Pruefen ob `.downloading` dort enthalten ist — falls ja, NICHT aendern im ViewModel, sondern stattdessen die if-else-Kette in der View umstrukturieren: `.downloading` VOR `updateInProgress` pruefen.

Konkret: Die Reihenfolge der if-else-Kette aendern zu:
1. `.updateAvailable` (Install-Button)
2. `.downloading` (ProgressView mit Prozent) — VOR updateInProgress
3. `.readyToApply` (Apply-Button)
4. `updateInProgress` (generisches Banner fuer checking/applying/rebooting)
5. `.error(let message)` (Error-Banner mit Retry)
6. GitHub-Fallback (Version-Vergleich)

Damit wird `.downloading` explizit mit Progress angezeigt, und `updateInProgress` faengt nur noch checking/applying/rebooting.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild build -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "ProgressView(value:" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift liefert mindestens 1 Treffer
    - grep "downloadProgress" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift liefert mindestens 2 Treffer
    - grep "exclamationmark.triangle.fill" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift liefert mindestens 1 Treffer (Error-Banner)
    - grep "update.retry" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift liefert 1 Treffer
    - grep "case .error" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift liefert mindestens 1 Treffer
  </acceptance_criteria>
  <done>Download-Sektion zeigt lineare ProgressView mit Prozentzahl. Error-Section zeigt rotes Banner mit Fehlermeldung und Retry-Button. Build kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` kompiliert fehlerfrei
2. grep "downloadProgress" in UpdateService.swift liefert >= 4 Treffer
3. grep "lastCheckDate" in UpdateService.swift liefert >= 4 Treffer
4. grep "ProgressView(value:" in RobotDetailView.swift liefert >= 1 Treffer
5. grep "case .error" in RobotDetailView.swift liefert >= 1 Treffer
6. grep "update.retry" in RobotDetailView.swift liefert 1 Treffer
</verification>

<success_criteria>
- UI-01: ProgressView mit Prozentanzeige ist in der Download-Section sichtbar
- UI-02: Error-Banner mit Retry-Button erscheint bei UpdatePhase.error
- UI-03: checkForUpdates() gibt sofort zurueck wenn weniger als 1 Stunde seit letztem Check
- Build kompiliert fehlerfrei auf iOS Simulator
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-wiring/15-01-SUMMARY.md`
</output>
