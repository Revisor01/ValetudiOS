---
phase: 19-observable-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Models/RobotState.swift
  - ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift
  - ValetudoApp/ValetudoApp/Services/NotificationService.swift
  - ValetudoApp/ValetudoApp/Services/SupportManager.swift
  - ValetudoApp/ValetudoApp/Services/NWBrowserService.swift
  - ValetudoApp/ValetudoApp/Services/NetworkScanner.swift
  - ValetudoApp/ValetudoApp/Services/UpdateService.swift
  - ValetudoApp/ValetudoApp/Services/RobotManager.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]

must_haves:
  truths:
    - "Alle 11 ObservableObject-Klassen nutzen @Observable statt ObservableObject"
    - "Kein @Published verbleibt in den migrierten Klassen"
    - "Infrastruktur-Properties (logger, Task, API-Referenzen) sind @ObservationIgnored"
    - "private(set) Zugriffsmodifier bleiben erhalten"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "@Observable RobotDetailViewModel"
      contains: "@Observable"
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "@Observable RobotSettingsViewModel"
      contains: "@Observable"
    - path: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      provides: "@Observable MapViewModel"
      contains: "@Observable"
    - path: "ValetudoApp/ValetudoApp/Services/RobotManager.swift"
      provides: "@Observable RobotManager"
      contains: "@Observable"
    - path: "ValetudoApp/ValetudoApp/Services/UpdateService.swift"
      provides: "@Observable UpdateService"
      contains: "@Observable"
  key_links:
    - from: "ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift"
      to: "GoToPresetStore"
      via: "nested @Observable property"
      pattern: "var presetStore = GoToPresetStore"
---

<objective>
Alle 11 ObservableObject-Klassen auf @Observable Macro migrieren — Klassen-Definitionen aendern, @Published entfernen, @ObservationIgnored fuer Infrastruktur-Properties setzen.

Purpose: Modernisierung auf Swift Observation Framework (iOS 17+) fuer granulare View-Updates und sauberere API.
Output: 11 Klassen-Dateien mit @Observable statt ObservableObject, ohne @Published, mit @ObservationIgnored auf nicht-UI-Properties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-observable-migration/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leaf-Klassen migrieren (GoToPresetStore, ErrorRouter, NotificationService, SupportManager, NWBrowserService)</name>
  <files>
    ValetudoApp/ValetudoApp/Models/RobotState.swift,
    ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift,
    ValetudoApp/ValetudoApp/Services/NotificationService.swift,
    ValetudoApp/ValetudoApp/Services/SupportManager.swift,
    ValetudoApp/ValetudoApp/Services/NWBrowserService.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Models/RobotState.swift,
    ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift,
    ValetudoApp/ValetudoApp/Services/NotificationService.swift,
    ValetudoApp/ValetudoApp/Services/SupportManager.swift,
    ValetudoApp/ValetudoApp/Services/NWBrowserService.swift
  </read_first>
  <action>
    Migriere diese 5 Leaf-Klassen (keine Abhaengigkeiten untereinander) auf @Observable:

    **GoToPresetStore** (RobotState.swift, Zeile ~830):
    - `import Observation` am Dateianfang hinzufuegen (falls nicht vorhanden)
    - `class GoToPresetStore: ObservableObject` aendern zu `@Observable class GoToPresetStore`
    - `@Published var presets: [GoToPreset] = []` aendern zu `var presets: [GoToPreset] = []`
    - `private let saveKey` braucht kein @ObservationIgnored (ist let-Konstante)

    **ErrorRouter** (ErrorRouter.swift):
    - `import Observation` hinzufuegen
    - `final class ErrorRouter: ObservableObject` aendern zu `@Observable final class ErrorRouter`
    - `@Published var currentError: Error?` aendern zu `var currentError: Error?`
    - `var retryAction` bleibt unveraendert (kein @Published, kein Wrapper)
    - `@MainActor` bleibt erhalten

    **NotificationService** (NotificationService.swift):
    - `import Observation` hinzufuegen
    - `class NotificationService: ObservableObject` aendern zu `@Observable class NotificationService`
    - `@Published var isAuthorized = false` aendern zu `var isAuthorized = false`
    - `private let logger` — kein @ObservationIgnored noetig (let-Konstante)
    - `@AppStorage` Properties bleiben unveraendert (sind PropertyWrapper, nicht @Published)
    - `static weak var robotManagerRef: RobotManager?` — `@ObservationIgnored` hinzufuegen (static weak ref, kein UI-State)

    **SupportManager** (SupportManager.swift):
    - `import Observation` hinzufuegen
    - `class SupportManager: ObservableObject` aendern zu `@Observable class SupportManager`
    - Alle 4 `@Published` entfernen: `products`, `isLoading`, `purchaseError`, `showThankYou`
    - `@AppStorage` Properties bleiben unveraendert
    - `private let logger` — kein @ObservationIgnored (let-Konstante)

    **NWBrowserService** (NWBrowserService.swift):
    - `import Observation` hinzufuegen
    - `final class NWBrowserService: ObservableObject` aendern zu `@Observable final class NWBrowserService`
    - `@Published private(set) var discovered` aendern zu `private(set) var discovered` (private(set) bleibt!)
    - `@Published private(set) var isBrowsing` aendern zu `private(set) var isBrowsing`
    - `private var browser: NWBrowser?` — `@ObservationIgnored` hinzufuegen (Infrastruktur)
    - `private let logger` — kein @ObservationIgnored (let-Konstante)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "ObservableObject" ValetudoApp/ValetudoApp/Models/RobotState.swift ValetudoApp/ValetudoApp/Helpers/ErrorRouter.swift ValetudoApp/ValetudoApp/Services/NotificationService.swift ValetudoApp/ValetudoApp/Services/SupportManager.swift ValetudoApp/ValetudoApp/Services/NWBrowserService.swift | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep "@Observable" ErrorRouter.swift liefert 1 Treffer
    - grep "@Observable" NotificationService.swift liefert 1 Treffer
    - grep "@Observable" SupportManager.swift liefert 1 Treffer
    - grep "@Observable" NWBrowserService.swift liefert 1 Treffer
    - grep "@Observable" RobotState.swift liefert 1 Treffer (GoToPresetStore)
    - grep "ObservableObject" in allen 5 Dateien liefert 0 Treffer
    - grep "@Published" in allen 5 Dateien liefert 0 Treffer
    - grep "private(set) var discovered" NWBrowserService.swift liefert 1 Treffer
    - grep "private(set) var isBrowsing" NWBrowserService.swift liefert 1 Treffer
  </acceptance_criteria>
  <done>5 Leaf-Klassen nutzen @Observable, kein @Published/@ObservableObject verbleibt, private(set) erhalten</done>
</task>

<task type="auto">
  <name>Task 2: Service- und ViewModel-Klassen migrieren (NetworkScanner, UpdateService, RobotManager, 3 ViewModels)</name>
  <files>
    ValetudoApp/ValetudoApp/Services/NetworkScanner.swift,
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/Services/RobotManager.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Services/NetworkScanner.swift,
    ValetudoApp/ValetudoApp/Services/UpdateService.swift,
    ValetudoApp/ValetudoApp/Services/RobotManager.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift
  </read_first>
  <action>
    Migriere die 6 verbleibenden Klassen auf @Observable:

    **NetworkScanner** (NetworkScanner.swift):
    - `import Observation` hinzufuegen
    - `class NetworkScanner: ObservableObject` aendern zu `@Observable class NetworkScanner`
    - 3x `@Published` entfernen: `discoveredRobots`, `isScanning`, `progress`
    - `@ObservationIgnored` auf: `private var scanTask: Task<Void, Never>?`, `private let browserService = NWBrowserService()`
    - `private let logger` — kein @ObservationIgnored (let-Konstante)

    **UpdateService** (UpdateService.swift):
    - `import Observation` hinzufuegen
    - `class UpdateService: ObservableObject` aendern zu `@Observable class UpdateService`
    - 5x `@Published private(set)` entfernen, `private(set)` bleibt: `phase`, `currentVersion`, `latestVersion`, `updateUrl`, `downloadProgress`
    - `@ObservationIgnored` auf: `private var pollingTask: Task<Void, Never>?`, `private var backgroundTaskID: UIBackgroundTaskIdentifier`, `private var lastCheckDate: Date?`
    - `private let logger`, `private let api` — kein @ObservationIgnored (let-Konstanten)

    **RobotManager** (RobotManager.swift):
    - `import Observation` hinzufuegen
    - `class RobotManager: ObservableObject` aendern zu `@Observable class RobotManager`
    - 3x `@Published` entfernen: `robots`, `robotStates`, `robotUpdateAvailable`
    - `@ObservationIgnored` auf: `private var apis: [UUID: ValetudoAPI]`, `private var refreshTask: Task<Void, Never>?`, `private var previousStates: [UUID: RobotStatus]`, `private var lastConsumableCheck: [UUID: Date]`, `private let sseManager = SSEConnectionManager()`
    - `private let logger`, `private let storageKey`, `private let notificationService` — kein @ObservationIgnored (let-Konstanten)

    **RobotDetailViewModel** (RobotDetailViewModel.swift):
    - `import Observation` hinzufuegen
    - `final class RobotDetailViewModel: ObservableObject` aendern zu `@Observable final class RobotDetailViewModel`
    - ALLE `@Published` entfernen (ca. 17 Properties): segments, consumables, selectedSegments, selectedIterations, isLoading, fanSpeedPresets, waterUsagePresets, operationModePresets, hasManualControl, hasAutoEmptyTrigger, hasMopDockClean, hasMopDockDry, hasEvents, hasCleanRoute, hasObstacleImages, lastCleaningStats, totalStats, events, cleanRoutePresets, currentCleanRoute, obstacles, robotProperties, valetudoVersion, systemHostInfo
    - `@ObservationIgnored` auf: `private var statsPollingTask: Task<Void, Never>?`
    - `private let logger`, `private let robot`, `private let robotManager` — kein @ObservationIgnored (let-Konstanten)
    - Falls `let robot: RobotConfig` und `let robotManager: RobotManager` vorhanden: keine Aenderung (let)

    **RobotSettingsViewModel** (RobotSettingsViewModel.swift):
    - `import Observation` hinzufuegen
    - `final class RobotSettingsViewModel: ObservableObject` aendern zu `@Observable final class RobotSettingsViewModel`
    - ALLE `@Published` entfernen (ca. 45 Properties): volume, carpetMode, persistentMap, keyLock, obstacleAvoidance, petObstacleAvoidance, collisionAvoidance, mopDockAutoDrying, floorMaterialNavigation, carpetSensorMode, currentWashTemperature, hasVolumeControl, hasSpeakerTest, hasCarpetMode, hasPersistentMap, hasMappingPass, hasAutoEmptyDock, hasQuirks, hasWifiConfig, hasWifiScan, hasKeyLock, hasObstacleAvoidance, hasPetObstacleAvoidance, hasCarpetSensorMode, hasMapReset, hasCollisionAvoidance, hasMopDockAutoDrying, hasMopDockWashTemperature, hasMopDockDryingTime, hasFloorMaterialNavigation, hasMapSnapshots, hasPendingMapChange, hasVoicePack, hasAutoEmptyDockDuration, carpetSensorModePresets, mopDockWashTemperaturePresets, mopDockDryingTimePresets, currentMopDockDryingTime, autoEmptyDockDurationPresets, currentAutoEmptyDockDuration, voicePacks, currentVoicePackId, isSettingVoicePack, mapSnapshots, isRestoringSnapshot, pendingMapChangeEnabled, isHandlingMapChange, isLoading, isInitialLoad, volumeChanged
    - `private let logger`, `private let robotManager` — kein @ObservationIgnored (let-Konstanten)

    **MapViewModel** (MapViewModel.swift):
    - `import Observation` hinzufuegen
    - `final class MapViewModel: ObservableObject` aendern zu `@Observable final class MapViewModel`
    - ALLE `@Published` entfernen (ca. 35 Properties): map, segments, isLoading, mapRefreshId, loadError, hasZoneCleaning, hasVirtualRestrictions, hasGoTo, hasSegmentRename, hasSegmentEdit, editMode, drawnZones, drawnNoGoAreas, drawnNoMopAreas, drawnVirtualWalls, existingRestrictions, restrictionToDelete, showRenameSheet, renameSegmentId, renameNewName, splitSegmentId, selectedSegmentIds, goToMarkerPosition, goToApiCoords, showGoToConfirm, presetStore, showSavePresetSheet, pendingGoToX, pendingGoToY, newPresetName, showPresetsSheet, showPresetsOnMap, editingPreset, isCleaning, selectedIterations, showRoomLabels, errorMessage, isOffline
    - `@ObservationIgnored` auf: `private var refreshTask: Task<Void, Never>?`
    - `private let logger`, `private let robotManager` — kein @ObservationIgnored (let-Konstanten)
    - `var presetStore = GoToPresetStore()` bleibt als normales `var` — SwiftUI tracked nested @Observable automatisch
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && grep -c "ObservableObject" ValetudoApp/ValetudoApp/Services/NetworkScanner.swift ValetudoApp/ValetudoApp/Services/UpdateService.swift ValetudoApp/ValetudoApp/Services/RobotManager.swift ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift ValetudoApp/ValetudoApp/ViewModels/MapViewModel.swift | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep "ObservableObject" in allen 6 Dateien liefert jeweils 0 Treffer
    - grep "@Observable" in allen 6 Dateien liefert jeweils 1 Treffer
    - grep "@Published" in allen 6 Dateien liefert jeweils 0 Treffer
    - grep "private(set) var phase" UpdateService.swift liefert 1 Treffer
    - grep "private(set) var currentVersion" UpdateService.swift liefert 1 Treffer
    - grep "@ObservationIgnored" MapViewModel.swift liefert mindestens 1 Treffer (refreshTask)
    - grep "@ObservationIgnored" RobotManager.swift liefert mindestens 3 Treffer (apis, refreshTask, previousStates, lastConsumableCheck, sseManager)
    - grep "import Observation" in allen 6 Dateien liefert jeweils 1 Treffer
  </acceptance_criteria>
  <done>Alle 6 Service/ViewModel-Klassen nutzen @Observable, kein @Published verbleibt, private(set) erhalten, Infrastruktur-Properties mit @ObservationIgnored markiert</done>
</task>

</tasks>

<verification>
- `grep -rn "ObservableObject" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@Published" --include="*.swift" ValetudoApp/` liefert 0 Treffer
- `grep -rn "@Observable" --include="*.swift" ValetudoApp/` liefert 11 Treffer (eine pro Klasse)
- `grep -rn "import Observation" --include="*.swift" ValetudoApp/` liefert Treffer in allen migrierten Dateien
</verification>

<success_criteria>
Alle 11 ObservableObject-Klassen sind auf @Observable migriert. Kein @Published verbleibt. private(set) Zugriffsmodifier sind erhalten. Infrastruktur-Properties tragen @ObservationIgnored.
</success_criteria>

<output>
After completion, create `.planning/phases/19-observable-migration/19-01-SUMMARY.md`
</output>
