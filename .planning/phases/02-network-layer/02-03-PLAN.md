---
phase: 02-network-layer
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ValetudoApp/ValetudoApp/Models/RobotMap.swift
  - ValetudoApp/ValetudoApp/Views/MapView.swift
autonomous: true
requirements: [DEBT-03, NET-01]
must_haves:
  truths:
    - "Map-Rendering ist merklich flüssiger da Pixel-Dekompression gecacht wird"
    - "Map-Updates kommen via SSE statt 2s-Polling wenn SSE aktiv"
    - "Map-Polling bleibt als Fallback wenn SSE nicht verfuegbar"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Models/RobotMap.swift"
      provides: "MapLayerCache class + cached decompressedPixels"
      contains: "MapLayerCache"
    - path: "ValetudoApp/ValetudoApp/Views/MapView.swift"
      provides: "Map-SSE lifecycle in onAppear/onDisappear"
      contains: "streamMapLines\|mapSSE\|sseManager"
  key_links:
    - from: "MapLayer.decompressedPixels"
      to: "MapLayerCache.decompressedPixels"
      via: "computed property delegates to cache class"
      pattern: "cache\\.decompressedPixels"
    - from: "MapView.startLiveRefresh"
      to: "ValetudoAPI.streamMapLines"
      via: "SSE stream replaces 2s polling loop"
      pattern: "streamMapLines"
---

<objective>
Map-Pixel-Cache und Map-SSE-Lifecycle: MapLayer.decompressedPixels durch class-basierte Cache-Referenz ersetzen, MapView 2s-Polling durch Map-SSE ersetzen (mit Polling-Fallback).

Purpose: Map-Rendering wird flüssiger (kein Re-Decompress pro Frame), Map-Updates kommen in Echtzeit via SSE statt 2s-Polling.
Output: RobotMap.swift (refactored), MapView.swift (SSE-Lifecycle)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-network-layer/02-CONTEXT.md
@.planning/phases/02-network-layer/02-RESEARCH.md
@.planning/phases/02-network-layer/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 -->

From ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift (added in Plan 01):
```swift
actor ValetudoAPI {
    func streamMapLines() async throws -> URLSession.AsyncBytes
    // Uses sseSession with .infinity timeouts
}
```

From ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift (created in Plan 01):
```swift
actor SSEConnectionManager {
    func isSSEActive(for robotId: UUID) -> Bool
    func connect(robotId:api:onAttributesUpdate:onConnectionChange:)
    func disconnect(robotId: UUID)
}
```

From ValetudoApp/ValetudoApp/Models/RobotMap.swift (current):
```swift
struct MapLayer: Codable {
    let __class: String?
    let type: String?
    let pixels: [Int]?
    let compressedPixels: [Int]?
    let metaData: LayerMetaData?
    let dimensions: LayerDimensions?

    // CURRENT: computed property — recomputes every access
    var decompressedPixels: [Int] {
        if let pixels = pixels, !pixels.isEmpty { return pixels }
        guard let compressed = compressedPixels, !compressed.isEmpty else { return [] }
        // ... RLE decompression loop
    }
}
```

From ValetudoApp/ValetudoApp/Views/MapView.swift (current):
```swift
// Line 1877 — current 2s polling loop:
private func startLiveRefresh() {
    refreshTask?.cancel()
    refreshTask = Task {
        while !Task.isCancelled {
            try? await Task.sleep(for: .seconds(2))
            if !Task.isCancelled, let api = api {
                if let newMap = try? await api.getMap() {
                    await MainActor.run { map = newMap }
                }
            }
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MapLayerCache class-wrapper fuer Pixel-Dekompression</name>
  <files>
    ValetudoApp/ValetudoApp/Models/RobotMap.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Models/RobotMap.swift,
    .planning/phases/02-network-layer/02-RESEARCH.md
  </read_first>
  <action>
**MapLayer Pixel-Caching implementieren (per D-12, D-13):**

1. **MapLayerCache class erstellen** (in RobotMap.swift, vor MapLayer struct):
```swift
final class MapLayerCache {
    private var cachedPixels: [Int]?

    func decompressedPixels(from layer: MapLayer) -> [Int] {
        if let cached = cachedPixels { return cached }
        let result = layer.computeDecompressedPixels()
        cachedPixels = result
        return result
    }

    func invalidate() { cachedPixels = nil }
}
```

2. **MapLayer struct anpassen:**
   - `let cache = MapLayerCache()` als stored property hinzufuegen (NICHT in CodingKeys — wird nicht serialisiert, per D-13)
   - CodingKeys enum bleibt unveraendert (listet cache nicht auf → automatisch excluded)
   - Bestehende `var decompressedPixels: [Int]` computed property aendern zu: `cache.decompressedPixels(from: self)`
   - Die bisherige Dekompressions-Logik in eine neue `fileprivate func computeDecompressedPixels() -> [Int]` auslagern (wird nur vom Cache aufgerufen)

3. **Cache-Invalidierung:** Keine explizite Invalidierung noetig — wenn neue Map-Daten ankommen (via SSE oder Polling), wird ein neues RobotMap mit neuen MapLayer-Instanzen erstellt. Jede neue MapLayer hat ein frisches MapLayerCache-Objekt. Der Cache ist natuerlich an eine Map-Version gebunden.

**Warum class statt lazy var (D-13):** MapLayer ist ein Struct. SwiftUI uebergibt Structs als `let` (immutable) in Canvas-Closures. `lazy var` auf einem Struct erfordert `mutating` — kompiliert nicht in `let`-Kontext. Die class-basierte Referenz umgeht das Problem komplett.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "final class MapLayerCache" ValetudoApp/ValetudoApp/Models/RobotMap.swift
    - grep -q "cachedPixels" ValetudoApp/ValetudoApp/Models/RobotMap.swift
    - grep -q "let cache = MapLayerCache()" ValetudoApp/ValetudoApp/Models/RobotMap.swift
    - grep -q "cache\.decompressedPixels" ValetudoApp/ValetudoApp/Models/RobotMap.swift
    - grep -q "computeDecompressedPixels" ValetudoApp/ValetudoApp/Models/RobotMap.swift
    - grep -qv "cache" ValetudoApp/ValetudoApp/Models/RobotMap.swift | grep -q "CodingKeys" || echo "CodingKeys should NOT contain cache"
  </acceptance_criteria>
  <done>
    MapLayer.decompressedPixels ist gecacht via MapLayerCache class-wrapper. Erste Zugriff berechnet, folgende nutzen Cache. Neue Map-Daten erzeugen neue MapLayer mit frischem Cache (natuerliche Invalidierung). Build kompiliert.
  </done>
</task>

<task type="auto">
  <name>Task 2: MapView Map-SSE-Lifecycle statt 2s-Polling</name>
  <files>
    ValetudoApp/ValetudoApp/Views/MapView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/MapView.swift,
    ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift,
    ValetudoApp/ValetudoApp/Services/SSEConnectionManager.swift
  </read_first>
  <action>
**MapView 2s-Polling durch Map-SSE ersetzen (per D-01 — Map-SSE nur wenn MapView geoeffnet):**

1. **startLiveRefresh() umbauen** (Zeile 1877):
   - Zuerst versuchen, Map-SSE zu starten: `api.streamMapLines()` aufrufen
   - Wenn SSE erfolgreich: `for try await line in bytes.lines` iterieren, Zeilen die mit `data:` beginnen als JSON parsen, als `RobotMap` decoden, `map = newMap` auf MainActor setzen
   - Bei CancellationError: clean break (nicht swallowing)
   - Bei anderem Error: auf 2s-Polling-Fallback zurueckfallen (bestehende Logik beibehalten)

   Konkreter Ablauf:
   ```swift
   private func startLiveRefresh() {
       refreshTask?.cancel()
       refreshTask = Task {
           guard let api = api else { return }

           // Try SSE first
           do {
               let bytes = try await api.streamMapLines()
               for try await line in bytes.lines {
                   guard !Task.isCancelled else { break }
                   guard line.hasPrefix("data:") else { continue }
                   let json = String(line.dropFirst(5)).trimmingCharacters(in: .whitespaces)
                   if let data = json.data(using: .utf8),
                      let newMap = try? JSONDecoder().decode(RobotMap.self, from: data) {
                       await MainActor.run { self.map = newMap }
                   }
               }
           } catch is CancellationError {
               return  // Clean exit
           } catch {
               // SSE failed — fall back to polling
               await pollMapFallback(api: api)
           }
       }
   }

   private func pollMapFallback(api: ValetudoAPI) async {
       while !Task.isCancelled {
           try? await Task.sleep(for: .seconds(2))
           if !Task.isCancelled {
               if let newMap = try? await api.getMap() {
                   await MainActor.run { self.map = newMap }
               }
           }
       }
   }
   ```

2. **WICHTIG:** Es gibt ZWEI `startLiveRefresh()` Aufrufe in MapView (Zeilen 136 und 718 — in verschiedenen view body branches). Beide muessen die gleiche Methode nutzen. Pruefen ob die Methode bei Zeile 161 identisch mit der bei 1877 ist — falls ja, nur die eine bei 1877 aendern. Falls es zwei verschiedene sind, beide anpassen.

3. **onDisappear:** Bestehender `refreshTask?.cancel()` in onDisappear reicht aus — Task-Cancellation propagiert durch den SSE-Stream (CancellationError beendet die for-await-Schleife).

4. **Kein SSEConnectionManager fuer Map-SSE noetig:** Per D-01 laeuft Map-SSE nur wenn MapView offen ist, managed durch MapView's eigene Task-Lifecycle. Der SSEConnectionManager ist fuer den dauerhaften attributes-SSE, nicht fuer Map-SSE.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "streamMapLines" ValetudoApp/ValetudoApp/Views/MapView.swift
    - grep -q "pollMapFallback\|fallback\|poll.*Map" ValetudoApp/ValetudoApp/Views/MapView.swift
    - grep -q "data:" ValetudoApp/ValetudoApp/Views/MapView.swift
    - grep -q "CancellationError" ValetudoApp/ValetudoApp/Views/MapView.swift
    - grep -q "RobotMap.*from.*data\|decode.*RobotMap" ValetudoApp/ValetudoApp/Views/MapView.swift
  </acceptance_criteria>
  <done>
    MapView nutzt Map-SSE fuer Echtzeit-Map-Updates statt 2s-Polling. Bei SSE-Fehler automatischer Fallback auf Polling. SSE wird bei onDisappear sauber beendet via Task-Cancellation. Build kompiliert.
  </done>
</task>

</tasks>

<verification>
- Projekt kompiliert ohne Fehler
- MapLayerCache existiert als class-wrapper mit cachedPixels
- MapView startet Map-SSE bei onAppear, beendet bei onDisappear
- Polling-Fallback aktiv wenn SSE fehlschlaegt
</verification>

<success_criteria>
- Build succeeds
- decompressedPixels nur einmal pro MapLayer berechnet (gecacht)
- Map-Updates via SSE in Echtzeit statt 2s-Polling
- Saubere Fallback-Kette: SSE -> Polling
- Cache natuerlich invalidiert bei neuen Map-Daten
</success_criteria>

<output>
After completion, create `.planning/phases/02-network-layer/02-03-SUMMARY.md`
</output>
