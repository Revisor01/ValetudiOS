---
phase: 11-view-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  - ValetudoApp/project.yml
autonomous: true
requirements:
  - ORG-03

must_haves:
  truths:
    - "RobotSettingsView.swift kompiliert fehlerfrei nach dem Refactoring"
    - "Die eigenständigen Settings-Sub-Views (AutoEmptyDockSettingsView, QuirksView, WiFiSettingsView, MQTTSettingsView, NTPSettingsView, ValetudoInfoView, StationSettingsView) sind in RobotSettingsSections.swift"
    - "RobotSettingsView.swift bleibt als List-Container mit den inline Sections (Speaker, Cleaning, Carpet Sensor etc.)"
    - "xcodegen generate registriert die neue Datei im Xcode-Target"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift"
      provides: "Alle eigenständigen Section-Structs aus RobotSettingsView.swift"
      contains: "struct AutoEmptyDockSettingsView"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "Schlanker List-Container + inline Sections + private helper functions"
  key_links:
    - from: "RobotSettingsView.swift"
      to: "RobotSettingsSections.swift AutoEmptyDockSettingsView"
      via: "Direct struct reference in List body"
    - from: "RobotSettingsView.swift"
      to: "RobotSettingsSections.swift WiFiSettingsView"
      via: "Direct struct reference in NavigationLink"
---

<objective>
RobotSettingsView.swift (1801 Zeilen) in logische Sub-View-Dateien aufteilen.

Purpose: Die Datei enthält mehrere vollständige, eigenständige View-Structs die bereits mit MARK kommentiert sind. Diese werden in eine separate Datei extrahiert.
Output: RobotSettingsSections.swift + schlankes RobotSettingsView.swift (~500 Zeilen als List-Container)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-view-decomposition/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RobotSettingsView.swift vollständig lesen und Sub-View-Datei erstellen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift (ALLE 1801 Zeilen lesen — zwingend vor jeder Aktion)
  </read_first>
  <action>
    Lies RobotSettingsView.swift vollständig (1801 Zeilen). Identifiziere alle eigenständigen Structs mit MARK-Kommentaren.

    Die folgenden Blöcke sind bereits eigenständige `struct ... : View` mit eigenem State und eigenem `body` — sie stehen NACH dem schließenden `}` von `RobotSettingsView`:

    **Zeilen ~496–572:** `struct AutoEmptyDockSettingsView` (MARK: Auto Empty Dock Settings View)
    - Hat eigenen `@State`, eigenen `body`, eigene `api`-Computed-Property
    - Bindings: `let robot: RobotConfig`, `@EnvironmentObject var robotManager`

    **Zeilen ~573–683:** `struct QuirksView` (MARK: Quirks View)
    - Eigenständig mit `NavigationStack`, `List`, `Toggle`

    **Zeilen ~684–899:** `struct WiFiSettingsView` (MARK: WiFi Settings View)
    - Eigenständig mit SSH-ähnlichen Feldern, `@State`-Properties

    **Zeilen ~900–1075:** `struct MQTTSettingsView` (MARK: MQTT Settings View)
    - Eigenständig

    **Zeilen ~1076–1262:** `struct NTPSettingsView` (MARK: NTP Settings View)
    - Eigenständig

    **Zeilen ~1263–1487:** `struct ValetudoInfoView` (MARK: Valetudo Info View)
    - Eigenständig

    **Zeilen ~1488–1794:** `struct StationSettingsView` (MARK: Station Settings View)
    - Eigenständig — die längste der Sub-Views

    Erstelle `RobotSettingsSections.swift` mit EXAKT diesen Blöcken (vollständig kopiert):

    ```swift
    import SwiftUI
    import os

    private let sectionsLogger = Logger(subsystem: Bundle.main.bundleIdentifier ?? "com.valetudio", category: "RobotSettingsSections")

    // MARK: - Auto Empty Dock Settings View
    struct AutoEmptyDockSettingsView: View {
        // ... (vollständiger Code aus MapView.swift Zeilen ~496–572)
    }

    // MARK: - Quirks View
    struct QuirksView: View {
        // ... (~573–683)
    }

    // MARK: - WiFi Settings View
    struct WiFiSettingsView: View {
        // ... (~684–899)
    }

    // MARK: - MQTT Settings View
    struct MQTTSettingsView: View {
        // ... (~900–1075)
    }

    // MARK: - NTP Settings View
    struct NTPSettingsView: View {
        // ... (~1076–1262)
    }

    // MARK: - Valetudo Info View
    struct ValetudoInfoView: View {
        // ... (~1263–1487)
    }

    // MARK: - Station Settings View
    struct StationSettingsView: View {
        // ... (~1488–1794)
    }
    ```

    WICHTIG: Falls `settingsLogger` in den extrahierten Structs verwendet wird, ersetze es durch `sectionsLogger` oder füge die Logger-Property dem jeweiligen Struct hinzu.

    Das `#Preview` am Ende (Zeilen ~1795–1801) bleibt in RobotSettingsView.swift.
  </action>
  <verify>
    <automated>
      grep -c "struct.*View" /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - RobotSettingsSections.swift existiert
    - Enthält alle 7 Structs: AutoEmptyDockSettingsView, QuirksView, WiFiSettingsView, MQTTSettingsView, NTPSettingsView, ValetudoInfoView, StationSettingsView
    - `grep -c "struct.*View" RobotSettingsSections.swift` gibt 7 aus
    - Beginnt mit `import SwiftUI`
  </acceptance_criteria>
  <done>RobotSettingsSections.swift mit allen 7 eigenständigen Settings-Sub-Views erstellt</done>
</task>

<task type="auto">
  <name>Task 2: RobotSettingsView.swift bereinigen, xcodegen ausführen, Build prüfen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
    ValetudoApp/project.yml
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift (erneut lesen — aktuelle Version)
    ValetudoApp/ValetudoApp/Views/RobotSettingsSections.swift
    ValetudoApp/project.yml
  </read_first>
  <action>
    1. Aus RobotSettingsView.swift die in Task 1 extrahierten Blöcke entfernen:
       - `struct AutoEmptyDockSettingsView` (~Zeilen 496–572) entfernen
       - `struct QuirksView` (~573–683) entfernen
       - `struct WiFiSettingsView` (~684–899) entfernen
       - `struct MQTTSettingsView` (~900–1075) entfernen
       - `struct NTPSettingsView` (~1076–1262) entfernen
       - `struct ValetudoInfoView` (~1263–1487) entfernen
       - `struct StationSettingsView` (~1488–1794) entfernen

    2. Die private Helper-Funktionen am Ende von RobotSettingsView.swift (displayNameFor*, setAutoEmpty..., etc.) in Zeilen ~1653–1793 gehören zur `RobotSettingsView` Struct-Extension — diese BLEIBEN in RobotSettingsView.swift, da sie `viewModel` und `api` aus der View nutzen. Prüfe ob sie tatsächlich innerhalb des `}` von `RobotSettingsView` oder außerhalb liegen und handle entsprechend.

    3. Nach dem Entfernen soll RobotSettingsView.swift enthalten:
       - `import SwiftUI`, `import os`, `private let settingsLogger`
       - `struct RobotSettingsView: View` mit `body` (die List mit allen inline Sections)
       - Alle `@ViewBuilder private var` Computed Properties die inline Sections aufbauen
       - Alle private Helper-Funktionen (volumeIcon, displayName*, set*-Funktionen)
       - Das `#Preview` am Ende

    4. xcodegen ausführen: `cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodegen generate`

    5. Build prüfen: `cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`

    Typische Fehler und Fixes:
    - "use of unresolved identifier 'settingsLogger'" in RobotSettingsSections.swift → eigene Logger-Property in jedem Struct oder `sectionsLogger` aus File-Top-Level nutzen
    - "cannot find type 'X'" → X wurde zu früh gelöscht; in RobotSettingsSections.swift nachprüfen
    - "redeclaration of X" → X ist noch in RobotSettingsView.swift UND in RobotSettingsSections.swift → aus RobotSettingsView.swift entfernen
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... build` endet mit `BUILD SUCCEEDED`
    - `wc -l ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift` gibt <= 550 aus (war 1801)
    - `grep -rn "struct AutoEmptyDockSettingsView" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in RobotSettingsSections.swift)
    - Keine doppelten Struct-Definitionen im Views-Ordner
  </acceptance_criteria>
  <done>BUILD SUCCEEDED, RobotSettingsView.swift schlanker List-Container, RobotSettingsSections.swift mit allen 7 Sub-Views</done>
</task>

</tasks>

<verification>
- `xcodebuild ... build` → BUILD SUCCEEDED
- `wc -l ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift` → max. 550 Zeilen
- `grep -rn "struct AutoEmptyDockSettingsView" ValetudoApp/ValetudoApp/Views/` → 1 Treffer
- `grep -rn "struct WiFiSettingsView" ValetudoApp/ValetudoApp/Views/` → 1 Treffer
- `grep -rn "struct StationSettingsView" ValetudoApp/ValetudoApp/Views/` → 1 Treffer
</verification>

<success_criteria>
RobotSettingsView.swift ist ein schlanker List-Container (<=550 Zeilen). RobotSettingsSections.swift enthält alle 7 extrahierten Sub-Views. `xcodebuild` meldet BUILD SUCCEEDED.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/11-view-decomposition/11-02-SUMMARY.md` erstellen.
</output>
