---
phase: 11-view-decomposition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
  - ValetudoApp/project.yml
autonomous: true
requirements:
  - ORG-04

must_haves:
  truths:
    - "RobotDetailView.swift kompiliert fehlerfrei nach dem Refactoring"
    - "Die eigenständigen Helper-Structs (ControlButton, DockActionButton, PulseAnimationView) sind in RobotDetailSections.swift"
    - "Die großen @ViewBuilder Section-Properties bleiben in RobotDetailView.swift als private Extensions"
    - "xcodegen generate registriert die neue Datei im Xcode-Target"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift"
      provides: "ControlButton, DockActionButton, PulseAnimationView und View-Extension"
      contains: "struct ControlButton"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "RobotDetailView Container + alle @ViewBuilder Section-Properties"
  key_links:
    - from: "RobotDetailView.swift"
      to: "RobotDetailSections.swift ControlButton"
      via: "Direct struct reference in controlSection"
    - from: "RobotDetailView.swift"
      to: "RobotDetailSections.swift DockActionButton"
      via: "Direct struct reference in controlSection"
---

<objective>
RobotDetailView.swift (1253 Zeilen) in logische Sub-View-Dateien aufteilen.

Purpose: Die Datei enthält mehrere eigenständige Hilfs-Structs (ControlButton, DockActionButton, PulseAnimationView) sowie eine View-Extension, die unabhängig von RobotDetailView sind. Diese werden extrahiert.
Output: RobotDetailSections.swift + schlankes RobotDetailView.swift (ca. 1050 Zeilen — die @ViewBuilder Properties bleiben da sie `viewModel` direkt nutzen)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-view-decomposition/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RobotDetailView.swift vollständig lesen und Sub-View-Datei erstellen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (ALLE 1253 Zeilen lesen — zwingend vor jeder Aktion)
  </read_first>
  <action>
    Lies RobotDetailView.swift vollständig (1253 Zeilen).

    Identifiziere alle Blöcke die AUSSERHALB von `struct RobotDetailView` liegen (d.h. nach dem schließenden `}` der RobotDetailView-Struct):

    **Zeile ~614:** `// MARK: - Pulse Animation for Live Indicator`
    - `struct PulseAnimationView: View` — eigenständig, keine ViewModel-Abhängigkeit

    **Zeile ~1140:** `// MARK: - Control Button`
    - `struct ControlButton<MenuContent: View>: View` — generischer Hilfs-Button, eigenständig

    **Zeile ~1209:** `// Helper extension for conditional modifiers`
    - `extension View { func if(...) }` — allgemeine View-Utility

    **Zeile ~1221:** `// MARK: - Dock Action Button`
    - `struct DockActionButton: View` — eigenständig

    Die `@ViewBuilder private var` Computed Properties (compactStatusHeader, controlSection, consumablesSection, etc.) sind `private var` INNERHALB von `struct RobotDetailView` — sie nutzen `viewModel` direkt. Diese BLEIBEN in RobotDetailView.swift.

    Erstelle `RobotDetailSections.swift` mit EXAKT diesen Blöcken:

    ```swift
    import SwiftUI

    // MARK: - Pulse Animation for Live Indicator
    struct PulseAnimationView: View {
        // ... (vollständiger Code aus RobotDetailView.swift, Zeilen ~614–634)
    }

    // MARK: - Control Button
    struct ControlButton<MenuContent: View>: View {
        // ... (~1140–1219, inkl. beider init() Overloads)
    }

    // Helper extension for conditional modifiers
    extension View {
        @ViewBuilder
        func `if`<Content: View>(_ condition: Bool, transform: (Self) -> Content) -> some View {
            // ... (~1209–1219)
        }
    }

    // MARK: - Dock Action Button
    struct DockActionButton: View {
        // ... (~1221–1247)
    }
    ```

    Das `#Preview` (Zeilen ~1249–1253) bleibt in RobotDetailView.swift.

    WICHTIG: Die @ViewBuilder Properties wie `compactStatusHeader`, `controlSection`, `consumablesSection`, `statisticsSection`, `robotPropertiesSection`, `roomsSection`, `eventsSection`, `cleanRouteSection`, `obstaclesSection` sind PRIVATE Members der RobotDetailView-Struct — sie bleiben in RobotDetailView.swift. Nur die Top-Level Structs/Extensions werden extrahiert.
  </action>
  <verify>
    <automated>
      grep -l "struct ControlButton" /Users/simonluthe/Documents/valetudo-app/ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - RobotDetailSections.swift existiert
    - Enthält: `struct PulseAnimationView`, `struct ControlButton`, `struct DockActionButton`, `extension View`
    - `grep -c "struct.*View\|struct Control\|struct Dock\|extension View" RobotDetailSections.swift` gibt mindestens 4 aus
    - Beginnt mit `import SwiftUI`
    - Kein `import os` nötig falls kein Logger verwendet wird
  </acceptance_criteria>
  <done>RobotDetailSections.swift mit PulseAnimationView, ControlButton, DockActionButton und View-Extension erstellt</done>
</task>

<task type="auto">
  <name>Task 2: RobotDetailView.swift bereinigen, xcodegen ausführen, Build prüfen</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
    ValetudoApp/project.yml
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift (erneut lesen — aktuelle Version)
    ValetudoApp/ValetudoApp/Views/RobotDetailSections.swift
    ValetudoApp/project.yml
  </read_first>
  <action>
    1. Aus RobotDetailView.swift die in Task 1 extrahierten Top-Level-Blöcke entfernen:
       - `struct PulseAnimationView` (~Zeile 614) entfernen — ist jetzt in RobotDetailSections.swift
       - `struct ControlButton` (~Zeile 1140) mit ALLEN init()-Overloads entfernen — ist jetzt in RobotDetailSections.swift
       - `extension View { func if(...) }` (~Zeile 1209) entfernen — ist jetzt in RobotDetailSections.swift
       - `struct DockActionButton` (~Zeile 1221) entfernen — ist jetzt in RobotDetailSections.swift

    2. Die `@ViewBuilder private var` Computed Properties und alles innerhalb von `struct RobotDetailView { ... }` BLEIBEN unverändert in RobotDetailView.swift.

    3. Nach dem Entfernen soll RobotDetailView.swift enthalten:
       - `import SwiftUI`
       - `struct RobotDetailView: View` mit:
         - State Properties (@State, @StateObject)
         - `body` (der List mit allen Section-Aufrufen)
         - ALLE `@ViewBuilder private var` Sections (compactStatusHeader, controlSection, etc.)
         - Alle `// MARK: -` Sections die INNERHALB von RobotDetailView liegen
       - `#Preview` am Ende

    4. xcodegen ausführen: `cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodegen generate`

    5. Build prüfen: `cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`

    Typische Fehler und Fixes:
    - "redeclaration of 'ControlButton'" → noch in RobotDetailView.swift vorhanden → vollständig entfernen
    - "cannot find 'ControlButton'" → in RobotDetailSections.swift nicht korrekt definiert (z.B. `private` Modifier) → `private` entfernen
    - "ambiguous use of 'if'" → `extension View { func if }` doppelt definiert → aus RobotDetailView.swift entfernen
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/valetudo-app/ValetudoApp && xcodebuild -scheme ValetudoApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... build` endet mit `BUILD SUCCEEDED`
    - `wc -l ValetudoApp/ValetudoApp/Views/RobotDetailView.swift` gibt <= 1100 aus (war 1253; Reduktion ~150 Zeilen durch extrahierte Hilfs-Structs)
    - `grep -rn "struct ControlButton" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in RobotDetailSections.swift)
    - `grep -rn "struct DockActionButton" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in RobotDetailSections.swift)
    - `grep -rn "struct PulseAnimationView" ValetudoApp/ValetudoApp/Views/` → genau 1 Treffer (in RobotDetailSections.swift)
  </acceptance_criteria>
  <done>BUILD SUCCEEDED, RobotDetailView.swift ohne Top-Level-Hilfs-Structs, RobotDetailSections.swift mit allen extrahierten Komponenten</done>
</task>

</tasks>

<verification>
- `xcodebuild ... build` → BUILD SUCCEEDED
- `grep -rn "struct ControlButton" ValetudoApp/ValetudoApp/Views/` → 1 Treffer (RobotDetailSections.swift)
- `grep -rn "struct DockActionButton" ValetudoApp/ValetudoApp/Views/` → 1 Treffer (RobotDetailSections.swift)
- `grep -rn "struct PulseAnimationView" ValetudoApp/ValetudoApp/Views/` → 1 Treffer (RobotDetailSections.swift)
- Keine doppelten Struct-Definitionen im Projekt
</verification>

<success_criteria>
RobotDetailView.swift enthält nur noch die RobotDetailView-Struct mit all ihren privaten Section-Properties. RobotDetailSections.swift enthält die 3 extrahierten Hilfs-Structs und die View-Extension. `xcodebuild` meldet BUILD SUCCEEDED.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/11-view-decomposition/11-03-SUMMARY.md` erstellen.
</output>
