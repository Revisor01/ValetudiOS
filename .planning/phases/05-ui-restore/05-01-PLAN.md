---
phase: 05-ui-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
  - ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
autonomous: true
requirements: [UIR-01, UIR-02, UIR-05, UIR-06]

must_haves:
  truths:
    - "Events-Section zeigt chronologische Event-Liste mit Dismiss-Button (capability-gated)"
    - "CleanRoute-Picker zeigt verfuegbare Routen und erlaubt Auswahl (capability-gated)"
    - "Obstacle-Photos Section zeigt erkannte Hindernisse mit NavigationLink zu ObstaclePhotoView (capability-gated)"
    - "Notification-Actions GO_HOME und LOCATE funktionieren weiterhin (bereits verdrahtet)"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift"
      provides: "Events, CleanRoute, Obstacle properties + methods + capability flags"
      contains: "hasEvents"
    - path: "ValetudoApp/ValetudoApp/Views/RobotDetailView.swift"
      provides: "Events Section, CleanRoute Picker, Obstacles Section"
      contains: "eventsSection"
    - path: "ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift"
      provides: "dismissEvent and getCleanRoutePresets API methods"
      contains: "func dismissEvent"
  key_links:
    - from: "RobotDetailViewModel"
      to: "ValetudoAPI"
      via: "loadEvents(), loadCleanRoute(), dismissEvent()"
      pattern: "api\\.getEvents|api\\.getCleanRoute|api\\.dismissEvent"
    - from: "RobotDetailView"
      to: "RobotDetailViewModel"
      via: "viewModel.events, viewModel.cleanRoutePresets, viewModel.obstacles"
      pattern: "viewModel\\.events|viewModel\\.cleanRoutePresets"
    - from: "RobotDetailView"
      to: "ObstaclePhotoView"
      via: "NavigationLink"
      pattern: "ObstaclePhotoView"
---

<objective>
Events-Section, CleanRoute-Picker und Obstacle-Photos in RobotDetailViewModel und RobotDetailView verdrahten.

Purpose: Die in Phase 3 implementierten API-Methoden (getEvents, getCleanRoute, getObstacleImage) werden in den Phase-4 ViewModel/View-Layer integriert, sodass Benutzer diese Funktionen nutzen koennen.
Output: Funktionierende Events-Liste, CleanRoute-Auswahl und Obstacle-Foto-Anzeige in RobotDetailView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
@ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
@ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
@ValetudoApp/ValetudoApp/Models/RobotState.swift
@ValetudoApp/ValetudoApp/Models/RobotMap.swift

<interfaces>
<!-- Existing API methods in ValetudoAPI.swift -->
func getEvents() async throws -> [ValetudoEvent]
func getCleanRoute() async throws -> CleanRouteState
func setCleanRoute(route: String) async throws
func getObstacleImage(id: String) async throws -> Data

<!-- Existing models in RobotState.swift -->
struct ValetudoEvent: Codable, Identifiable {
    let __class: String; let id: String; let timestamp: String
    let processed: Bool; let type: String?; let subType: String?; let message: String?
    var displayName: String { ... }
    var iconName: String { ... }
}
struct CleanRouteState: Codable { let route: String }

<!-- ObstaclePhotoView.swift -->
struct ObstaclePhotoView: View {
    let obstacleId: String; let label: String?; let api: ValetudoAPI
}

<!-- MapEntity metaData for obstacles (RobotMap.swift) -->
struct EntityMetaData: Codable {
    let angle: Int?
    let id: String?       // UUID fuer ObstacleImagesCapability/img/{id}
    let label: String?    // z.B. "Pedestal (89%)"
}

<!-- Existing ViewModel capability pattern -->
hasManualControl = DebugConfig.showAllCapabilities || capabilities.contains("HighResolutionManualControlCapability")

<!-- Existing presets pattern (e.g. fan speed) -->
func getFanSpeedPresets() async throws -> [String] {
    try await request("/robot/capabilities/FanSpeedControlCapability/presets")
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-Erweiterungen und ViewModel-Properties fuer Events, CleanRoute, Obstacles</name>
  <files>
    ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/Models/RobotState.swift,
    ValetudoApp/ValetudoApp/Models/RobotMap.swift
  </read_first>
  <action>
    **ValetudoAPI.swift — 2 neue Methoden hinzufuegen:**

    1. `dismissEvent(id:)` nach dem bestehenden `getEvents()`:
       ```swift
       func dismissEvent(id: String) async throws {
           let body = try JSONEncoder().encode(["action": "dismiss"])
           try await requestVoid("/valetudo/events/\(id)", body: body)
       }
       ```
       Hinweis: Endpoint ist `/valetudo/events/{id}` (nicht `/robot/capabilities/`).

    2. `getCleanRoutePresets()` nach dem bestehenden `getCleanRoute()`:
       ```swift
       func getCleanRoutePresets() async throws -> [String] {
           try await request("/robot/capabilities/CleanRouteControlCapability/presets")
       }
       ```
       Folgt dem gleichen Pattern wie `getFanSpeedPresets()`.

    **RobotDetailViewModel.swift — Properties, Capability-Flags, Load-Methoden, Actions:**

    1. Neue @Published Properties (nach den bestehenden Statistics-Properties einfuegen):
       ```swift
       // Events
       @Published var events: [ValetudoEvent] = []
       // Clean Route
       @Published var cleanRoutePresets: [String] = []
       @Published var currentCleanRoute: String = ""
       // Obstacles (from map entities)
       @Published var obstacles: [(id: String, label: String?)] = []
       ```

    2. Neue Capability-Flags (nach den bestehenden has*-Flags):
       ```swift
       @Published var hasEvents = DebugConfig.showAllCapabilities
       @Published var hasCleanRoute = DebugConfig.showAllCapabilities
       @Published var hasObstacleImages = DebugConfig.showAllCapabilities
       ```

    3. Capabilities in `loadCapabilities()` erweitern — folgende Zeilen nach den bestehenden Capability-Checks hinzufuegen:
       ```swift
       hasEvents = true  // Events sind immer verfuegbar (kein Capability-Gate in Valetudo)
       hasCleanRoute = DebugConfig.showAllCapabilities || capabilities.contains("CleanRouteControlCapability")
       hasObstacleImages = DebugConfig.showAllCapabilities || capabilities.contains("ObstacleImagesCapability")
       ```

    4. Drei neue private Load-Methoden:
       ```swift
       private func loadEvents() async {
           guard let api = api else { return }
           do {
               events = try await api.getEvents()
           } catch {
               logger.error("Failed to load events: \(error, privacy: .public)")
               if !DebugConfig.showAllCapabilities { hasEvents = false }
           }
       }

       private func loadCleanRoute() async {
           guard let api = api else { return }
           do {
               let state = try await api.getCleanRoute()
               currentCleanRoute = state.route
               cleanRoutePresets = try await api.getCleanRoutePresets()
           } catch {
               logger.debug("Clean route not supported: \(error, privacy: .public)")
               if !DebugConfig.showAllCapabilities { hasCleanRoute = false }
           }
       }

       private func loadObstacles() async {
           // Obstacles kommen aus der Map-Daten (entities mit metaData.id)
           guard let status = status else { return }
           // Lese die letzte Map aus robotManager
           guard let api = api, let mapData = robotManager.mapData[robot.id] else { return }
           let obstacleEntities = (mapData.entities ?? []).filter {
               $0.metaData?.id != nil
           }
           obstacles = obstacleEntities.compactMap { entity in
               guard let id = entity.metaData?.id else { return nil }
               return (id: id, label: entity.metaData?.label)
           }
       }
       ```
       Hinweis: Pruefen ob `robotManager.mapData` existiert. Falls nicht, muss der Zugriff auf Map-Daten anders erfolgen — pruefen wie MapViewModel an die Map-Daten kommt und dasselbe Pattern verwenden. Alternativ: Die Map-Entities ueber die API laden falls kein direkter Zugriff auf mapData besteht.

    5. `loadData()` erweitern — die drei neuen Load-Methoden in das `async let`-Bundle aufnehmen:
       ```swift
       async let eventsTask: () = loadEvents()
       async let cleanRouteTask: () = loadCleanRoute()
       async let obstaclesTask: () = loadObstacles()
       ```
       Und im `await`-Tuple ergaenzen.

    6. Zwei neue Action-Methoden:
       ```swift
       func dismissEvent(_ event: ValetudoEvent) async {
           guard let api = api else { return }
           do {
               try await api.dismissEvent(id: event.id)
               events.removeAll { $0.id == event.id }
           } catch {
               logger.error("Failed to dismiss event: \(error, privacy: .public)")
           }
       }

       func setCleanRoute(_ route: String) async {
           guard let api = api else { return }
           do {
               try await api.setCleanRoute(route: route)
               currentCleanRoute = route
           } catch {
               logger.error("Failed to set clean route: \(error, privacy: .public)")
           }
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func dismissEvent" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift returns 1
    - grep -c "func getCleanRoutePresets" ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift returns 1
    - grep -c "hasEvents" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift returns >= 2
    - grep -c "hasCleanRoute" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift returns >= 2
    - grep -c "hasObstacleImages" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift returns >= 2
    - grep -c "func dismissEvent" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift returns 1
    - grep -c "func setCleanRoute" ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift returns 1
    - Build succeeds without errors
  </acceptance_criteria>
  <done>
    ValetudoAPI hat dismissEvent(id:) und getCleanRoutePresets(). RobotDetailViewModel hat events, cleanRoutePresets, currentCleanRoute, obstacles Properties, hasEvents/hasCleanRoute/hasObstacleImages Flags, loadEvents/loadCleanRoute/loadObstacles Methoden, und dismissEvent/setCleanRoute Actions. Build kompiliert fehlerfrei.
  </done>
</task>

<task type="auto">
  <name>Task 2: Events-Section, CleanRoute-Picker und Obstacles-Section in RobotDetailView</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotDetailView.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotDetailViewModel.swift,
    ValetudoApp/ValetudoApp/Views/ObstaclePhotoView.swift
  </read_first>
  <action>
    Drei neue Sections in RobotDetailView.swift hinzufuegen. Alle Sections sind capability-gated und folgen dem bestehenden Pattern der View.

    **1. Events Section** — nach der `consumablesPreviewSection` und vor `statisticsSection` einfuegen:
    ```swift
    // Events Section (capability-gated)
    if viewModel.hasEvents && !viewModel.events.isEmpty {
        Section {
            ForEach(viewModel.events) { event in
                HStack {
                    Image(systemName: event.iconName)
                        .foregroundStyle(.orange)
                        .frame(width: 24)
                    VStack(alignment: .leading, spacing: 2) {
                        Text(event.displayName)
                            .font(.subheadline)
                            .fontWeight(.medium)
                        if let message = event.message {
                            Text(message)
                                .font(.caption)
                                .foregroundStyle(.secondary)
                        }
                        Text(event.timestamp)
                            .font(.caption2)
                            .foregroundStyle(.tertiary)
                    }
                    Spacer()
                    if !event.processed {
                        Button {
                            Task { await viewModel.dismissEvent(event) }
                        } label: {
                            Image(systemName: "xmark.circle.fill")
                                .foregroundStyle(.secondary)
                        }
                        .buttonStyle(.plain)
                    }
                }
            }
        } header: {
            Text(String(localized: "events.title"))
        }
    }
    ```

    **2. CleanRoute Picker** — nach der `controlSection` und vor `roomsSection` einfuegen (oder als eigene Section direkt nach controlSection):
    ```swift
    // Clean Route Picker (capability-gated)
    if viewModel.hasCleanRoute && !viewModel.cleanRoutePresets.isEmpty {
        Section {
            Picker(String(localized: "clean_route.title"), selection: Binding(
                get: { viewModel.currentCleanRoute },
                set: { newValue in
                    Task { await viewModel.setCleanRoute(newValue) }
                }
            )) {
                ForEach(viewModel.cleanRoutePresets, id: \.self) { preset in
                    Text(preset.capitalized).tag(preset)
                }
            }
        } header: {
            Text(String(localized: "clean_route.section"))
        }
    }
    ```

    **3. Obstacles Section** — nach der Events Section einfuegen:
    ```swift
    // Obstacle Photos Section (capability-gated)
    if viewModel.hasObstacleImages && !viewModel.obstacles.isEmpty {
        Section {
            ForEach(viewModel.obstacles, id: \.id) { obstacle in
                NavigationLink {
                    if let api = viewModel.api {
                        ObstaclePhotoView(obstacleId: obstacle.id, label: obstacle.label, api: api)
                    }
                } label: {
                    HStack {
                        Image(systemName: "camera.viewfinder")
                            .foregroundStyle(.blue)
                            .frame(width: 24)
                        Text(obstacle.label ?? String(localized: "obstacle.unknown"))
                            .font(.subheadline)
                    }
                }
            }
        } header: {
            Text(String(localized: "obstacles.title"))
        }
    }
    ```

    **4. UIR-06 Verifikation:** Notification-Actions sind bereits vollstaendig verdrahtet:
    - AppDelegate in ValetudoApp.swift setzt sich als UNUserNotificationCenterDelegate
    - didReceive ruft NotificationService.shared.handleNotificationResponse(actionIdentifier:) auf
    - handleNotificationResponse handhabt "GO_HOME" (api.basicControl(action: .home)) und "LOCATE" (api.locate())
    - robotManagerRef wird in onAppear gesetzt
    - setupCategories() wird in RobotManager.init aufgerufen
    Keine Code-Aenderungen noetig fuer UIR-06. Build-Erfolg bestaetigt die Verdrahtung.

    **Localization:** Falls die Localization-Keys noch nicht in Localizable.xcstrings existieren, muessen folgende Keys hinzugefuegt werden:
    - "events.title" = "Events"
    - "clean_route.title" = "Reinigungsroute" / "Clean Route"
    - "clean_route.section" = "Reinigungsroute" / "Clean Route"
    - "obstacles.title" = "Erkannte Hindernisse" / "Detected Obstacles"
    - "obstacle.unknown" = "Unbekanntes Hindernis" / "Unknown Obstacle"
    Pruefen ob die Keys bereits existieren; falls ja, verwenden. Falls nein, in Localizable.xcstrings ergaenzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "eventsSection\|events\.title\|viewModel\.hasEvents" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift returns >= 1
    - grep -c "cleanRoutePresets\|clean_route\|hasCleanRoute" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift returns >= 1
    - grep -c "ObstaclePhotoView\|hasObstacleImages\|obstacles\.title" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift returns >= 1
    - grep -c "dismissEvent" ValetudoApp/ValetudoApp/Views/RobotDetailView.swift returns >= 1
    - Build succeeds without errors
  </acceptance_criteria>
  <done>
    RobotDetailView zeigt drei neue capability-gated Sections: Events-Liste mit Dismiss-Button, CleanRoute-Picker mit Presets, Obstacle-Photos mit NavigationLink zu ObstaclePhotoView. Notification-Actions (UIR-06) sind bestaetigt funktionsfaehig. Build kompiliert fehlerfrei.
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet` — Build erfolgreich
2. grep bestaetigt alle neuen Properties, Methoden und View-Sections existieren
3. Alle Sections sind capability-gated (hasEvents, hasCleanRoute, hasObstacleImages)
</verification>

<success_criteria>
- Events-Section in RobotDetailView zeigt Events chronologisch mit Dismiss-Button (UIR-01)
- CleanRoute-Picker in RobotDetailView erlaubt Routenauswahl aus Presets (UIR-02)
- Obstacle-Photos Section in RobotDetailView navigiert zu ObstaclePhotoView (UIR-05)
- Notification-Actions GO_HOME/LOCATE funktionieren (UIR-06 — bereits verdrahtet, Build bestaetigt)
- Alle Sections capability-gated
- Build kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-restore/05-01-SUMMARY.md`
</output>
