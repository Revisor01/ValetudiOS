---
phase: 05-ui-restore
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  - ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
autonomous: true
requirements: [UIR-03, UIR-04]

must_haves:
  truths:
    - "Map-Snapshots Section zeigt gespeicherte Snapshots und erlaubt Restore (capability-gated)"
    - "Pending-Map-Change Section zeigt Accept/Reject Buttons wenn Aenderung vorliegt (capability-gated)"
  artifacts:
    - path: "ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift"
      provides: "MapSnapshot und PendingMapChange Properties, Methoden und Capability-Flags"
      contains: "hasMapSnapshots"
    - path: "ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift"
      provides: "Map Snapshots Section und Pending Map Change Section"
      contains: "mapSnapshotsSection"
  key_links:
    - from: "RobotSettingsViewModel"
      to: "ValetudoAPI"
      via: "loadMapSnapshots(), restoreMapSnapshot(), loadPendingMapChange(), handlePendingMapChange()"
      pattern: "api\\.getMapSnapshots|api\\.restoreMapSnapshot|api\\.getPendingMapChange|api\\.handlePendingMapChange"
    - from: "RobotSettingsView"
      to: "RobotSettingsViewModel"
      via: "viewModel.mapSnapshots, viewModel.pendingMapChangeEnabled"
      pattern: "viewModel\\.mapSnapshots|viewModel\\.pendingMapChangeEnabled"
---

<objective>
Map-Snapshots und Pending-Map-Change Sections in RobotSettingsViewModel und RobotSettingsView verdrahten.

Purpose: Die in Phase 3 implementierten API-Methoden (getMapSnapshots, restoreMapSnapshot, getPendingMapChange, handlePendingMapChange) werden in den Phase-4 ViewModel/View-Layer integriert.
Output: Funktionierende Map-Snapshot-Liste mit Restore und Pending-Map-Change Accept/Reject in RobotSettingsView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift
@ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
@ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
@ValetudoApp/ValetudoApp/Models/RobotState.swift

<interfaces>
<!-- Existing API methods in ValetudoAPI.swift -->
func getMapSnapshots() async throws -> [MapSnapshot]
func restoreMapSnapshot(id: String) async throws
func getPendingMapChange() async throws -> PendingMapChangeState
func handlePendingMapChange(action: String) async throws

<!-- Existing models in RobotState.swift -->
struct MapSnapshot: Codable, Identifiable { let id: String }
struct PendingMapChangeState: Codable { let enabled: Bool }

<!-- Existing ViewModel capability pattern (RobotSettingsViewModel.swift) -->
hasMappingPass = DebugConfig.showAllCapabilities || capabilities.contains("MappingPassCapability")

<!-- Existing action pattern (RobotSettingsViewModel.swift) -->
func resetMap() async {
    guard let api = api else { return }
    isLoading = true; defer { isLoading = false }
    do { try await api.resetMap() }
    catch { logger.error("Failed to reset map: \(error, privacy: .public)") }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel-Properties und Methoden fuer MapSnapshots und PendingMapChange</name>
  <files>
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift,
    ValetudoApp/ValetudoApp/Services/ValetudoAPI.swift,
    ValetudoApp/ValetudoApp/Models/RobotState.swift
  </read_first>
  <action>
    **RobotSettingsViewModel.swift erweitern:**

    1. Neue @Published Properties (nach den bestehenden UI-State Properties `isLoading`, `isInitialLoad`, `volumeChanged`):
       ```swift
       // Map Snapshots
       @Published var mapSnapshots: [MapSnapshot] = []
       @Published var isRestoringSnapshot = false
       // Pending Map Change
       @Published var pendingMapChangeEnabled = false
       @Published var isHandlingMapChange = false
       ```

    2. Neue Capability-Flags (nach den bestehenden has*-Flags, z.B. nach `hasFloorMaterialNavigation`):
       ```swift
       @Published var hasMapSnapshots = DebugConfig.showAllCapabilities
       @Published var hasPendingMapChange = DebugConfig.showAllCapabilities
       ```

    3. Capabilities in `loadSettings()` erweitern — in den bestehenden `do { let capabilities = try await api.getCapabilities()` Block nach den vorhandenen Capability-Checks hinzufuegen:
       ```swift
       hasMapSnapshots = DebugConfig.showAllCapabilities || capabilities.contains("MapSnapshotCapability")
       hasPendingMapChange = DebugConfig.showAllCapabilities || capabilities.contains("PendingMapChangeHandlingCapability")
       ```

    4. Zwei neue Load-Methoden am Ende der `loadSettings()` Methode (vor `isInitialLoad = false`):
       ```swift
       // Load map snapshots
       if hasMapSnapshots {
           do {
               mapSnapshots = try await api.getMapSnapshots()
           } catch {
               if !DebugConfig.showAllCapabilities { hasMapSnapshots = false }
               logger.debug("Map snapshots not supported: \(error, privacy: .public)")
           }
       }

       // Load pending map change
       if hasPendingMapChange {
           do {
               let state = try await api.getPendingMapChange()
               pendingMapChangeEnabled = state.enabled
           } catch {
               if !DebugConfig.showAllCapabilities { hasPendingMapChange = false }
               logger.debug("Pending map change not supported: \(error, privacy: .public)")
           }
       }
       ```

    5. Drei neue Action-Methoden (nach den bestehenden Action-Methoden, z.B. nach `setMopDockWashTemperature`):
       ```swift
       // MARK: - Map Snapshots
       func restoreMapSnapshot(_ snapshot: MapSnapshot) async {
           guard let api = api else { return }
           isRestoringSnapshot = true
           defer { isRestoringSnapshot = false }
           do {
               try await api.restoreMapSnapshot(id: snapshot.id)
               logger.info("Restored map snapshot: \(snapshot.id, privacy: .public)")
           } catch {
               logger.error("Failed to restore snapshot: \(error, privacy: .public)")
           }
       }

       // MARK: - Pending Map Change
       func acceptPendingMapChange() async {
           guard let api = api else { return }
           isHandlingMapChange = true
           defer { isHandlingMapChange = false }
           do {
               try await api.handlePendingMapChange(action: "accept")
               pendingMapChangeEnabled = false
               logger.info("Accepted pending map change")
           } catch {
               logger.error("Failed to accept map change: \(error, privacy: .public)")
           }
       }

       func rejectPendingMapChange() async {
           guard let api = api else { return }
           isHandlingMapChange = true
           defer { isHandlingMapChange = false }
           do {
               try await api.handlePendingMapChange(action: "reject")
               pendingMapChangeEnabled = false
               logger.info("Rejected pending map change")
           } catch {
               logger.error("Failed to reject map change: \(error, privacy: .public)")
           }
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "hasMapSnapshots" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift returns >= 2
    - grep -c "hasPendingMapChange" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift returns >= 2
    - grep -c "func restoreMapSnapshot" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift returns 1
    - grep -c "func acceptPendingMapChange" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift returns 1
    - grep -c "func rejectPendingMapChange" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift returns 1
    - grep -c "mapSnapshots" ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift returns >= 3
    - Build succeeds without errors
  </acceptance_criteria>
  <done>
    RobotSettingsViewModel hat mapSnapshots, pendingMapChangeEnabled Properties, hasMapSnapshots/hasPendingMapChange Flags, Load-Logik in loadSettings(), und restoreMapSnapshot/acceptPendingMapChange/rejectPendingMapChange Actions. Build kompiliert fehlerfrei.
  </done>
</task>

<task type="auto">
  <name>Task 2: Map-Snapshots und Pending-Map-Change Sections in RobotSettingsView</name>
  <files>
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift
  </files>
  <read_first>
    ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift,
    ValetudoApp/ValetudoApp/ViewModels/RobotSettingsViewModel.swift
  </read_first>
  <action>
    Zwei neue Sections in RobotSettingsView.swift hinzufuegen. Platzierung: Nach der bestehenden "Map Management" Section (die resetMap und startMappingPass enthaelt) und vor der Quirks Section.

    **1. Map Snapshots Section** (capability-gated):
    ```swift
    // Map Snapshots Section
    if viewModel.hasMapSnapshots {
        Section {
            if viewModel.mapSnapshots.isEmpty {
                Text(String(localized: "snapshots.empty"))
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            } else {
                ForEach(viewModel.mapSnapshots) { snapshot in
                    HStack {
                        Image(systemName: "map")
                            .foregroundStyle(.blue)
                            .frame(width: 24)
                        Text(snapshot.id)
                            .font(.subheadline)
                            .lineLimit(1)
                        Spacer()
                        Button {
                            Task { await viewModel.restoreMapSnapshot(snapshot) }
                        } label: {
                            if viewModel.isRestoringSnapshot {
                                ProgressView()
                                    .controlSize(.small)
                            } else {
                                Text(String(localized: "snapshots.restore"))
                                    .font(.caption)
                            }
                        }
                        .disabled(viewModel.isRestoringSnapshot)
                    }
                }
            }
        } header: {
            Text(String(localized: "snapshots.title"))
        } footer: {
            Text(String(localized: "snapshots.footer"))
        }
    }
    ```

    **2. Pending Map Change Section** (capability-gated, nur wenn enabled):
    ```swift
    // Pending Map Change Section
    if viewModel.hasPendingMapChange && viewModel.pendingMapChangeEnabled {
        Section {
            VStack(alignment: .leading, spacing: 8) {
                HStack {
                    Image(systemName: "map.fill")
                        .foregroundStyle(.orange)
                    Text(String(localized: "pending_map.description"))
                        .font(.subheadline)
                }

                HStack(spacing: 12) {
                    Button {
                        Task { await viewModel.acceptPendingMapChange() }
                    } label: {
                        HStack {
                            Image(systemName: "checkmark")
                            Text(String(localized: "pending_map.accept"))
                        }
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, 8)
                        .background(Color.green)
                        .foregroundStyle(.white)
                        .clipShape(RoundedRectangle(cornerRadius: 8))
                    }

                    Button {
                        Task { await viewModel.rejectPendingMapChange() }
                    } label: {
                        HStack {
                            Image(systemName: "xmark")
                            Text(String(localized: "pending_map.reject"))
                        }
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, 8)
                        .background(Color.red)
                        .foregroundStyle(.white)
                        .clipShape(RoundedRectangle(cornerRadius: 8))
                    }
                }
                .disabled(viewModel.isHandlingMapChange)
            }
        } header: {
            Text(String(localized: "pending_map.title"))
        }
    }
    ```

    **Localization:** Falls die Keys noch nicht in Localizable.xcstrings existieren, muessen folgende Keys hinzugefuegt werden:
    - "snapshots.title" = "Karten-Snapshots" / "Map Snapshots"
    - "snapshots.empty" = "Keine Snapshots gespeichert" / "No snapshots saved"
    - "snapshots.restore" = "Wiederherstellen" / "Restore"
    - "snapshots.footer" = "Snapshots werden automatisch vom Roboter erstellt" / "Snapshots are created automatically by the robot"
    - "pending_map.title" = "Kartenanderung" / "Map Change"
    - "pending_map.description" = "Der Roboter hat Kartenanderungen erkannt" / "The robot detected map changes"
    - "pending_map.accept" = "Akzeptieren" / "Accept"
    - "pending_map.reject" = "Ablehnen" / "Reject"
    Pruefen ob die Keys bereits existieren; falls ja, verwenden. Falls nein, in Localizable.xcstrings ergaenzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/valetudo-app && xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "hasMapSnapshots\|snapshots\.title" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift returns >= 1
    - grep -c "hasPendingMapChange\|pending_map" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift returns >= 1
    - grep -c "restoreMapSnapshot" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift returns >= 1
    - grep -c "acceptPendingMapChange\|rejectPendingMapChange" ValetudoApp/ValetudoApp/Views/RobotSettingsView.swift returns >= 2
    - Build succeeds without errors
  </acceptance_criteria>
  <done>
    RobotSettingsView zeigt zwei neue capability-gated Sections: Map-Snapshots-Liste mit Restore-Button und Pending-Map-Change mit Accept/Reject-Buttons. Build kompiliert fehlerfrei.
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build -project ValetudoApp/ValetudoApp.xcodeproj -target ValetudoApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet` — Build erfolgreich
2. grep bestaetigt alle neuen Properties, Methoden und View-Sections existieren
3. Alle Sections sind capability-gated (hasMapSnapshots, hasPendingMapChange)
</verification>

<success_criteria>
- Map-Snapshots Section zeigt Snapshot-Liste mit Restore-Button (UIR-03)
- Pending-Map-Change Section zeigt Accept/Reject Buttons (UIR-04)
- Beide Sections sind capability-gated
- Build kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-restore/05-02-SUMMARY.md`
</output>
